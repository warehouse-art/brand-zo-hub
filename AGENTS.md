# AGENTS.md — دستور التشغيل للوكلاء (Claude · Codex)

<!-- identity:start -->
> ## ● أنت في **مستودع الشركة** — `warehouse-art/brand-zo-hub`
>
> النسخة المؤسسيّة، تُسلَّم لإدارة تقنية المعلومات. **قواعد إدارة تقنية المعلومات هي الحاكمة.**
>
> يقابله **المستودع الشخصيّ** `albarshi996/warehouse-system` (مشروع الاشتراكات، تطويرٌ وفق أفكارنا). مستودعان منفصلان، لكلٍّ مجلّدٌ وريموتٌ واحد؛ ولا مزامنة تلقائيّة — بل بأمرٍ صريح: `npm run sync`.
>
> **أوّل أمرٍ في كلّ جلسة: `npm run where`** — التفاصيل في [`WORKSPACE.md`](WORKSPACE.md).
<!-- identity:end -->

نظام مستودعات براندزو — بوابة عمليات حيّة (Astro 6 + React 18 + Tailwind 4 + Firebase Firestore) تُنشر تلقائيًّا من `main` إلى GitHub Pages. **الشراكة: محمد البرشي يقرّر · Claude وCodex ينفّذان.**

الإحاطة الكاملة: [`docs/إحاطة-كوديكس.md`](docs/إحاطة-كوديكس.md) · الذاكرة الحيّة: [`SESSION_HANDOFF.md`](SESSION_HANDOFF.md) · الخطة الأم: [`ROADMAP.md`](ROADMAP.md)

## طقس الجلسة
1. **قبل أي عمل:** `git pull` ثم **`npm run plan`** واقرأ [`docs/plan/استئناف-نسخة-العميل.md`](docs/plan/استئناف-نسخة-العميل.md) — هذه هي الجبهة الحالية. ثمّ راجع [`docs/plan/الاستئناف.md`](docs/plan/الاستئناف.md) لعوائق المرحلة الأولى المعلّقة على المالك، ورأس `SESSION_HANDOFF.md` عند الحاجة للسياق التاريخيّ فقط. **ومتتبّعٌ رابع قيد التنفيذ:** [`docs/plan/استئناف-التخزين-الموجّه.md`](docs/plan/استئناف-التخزين-الموجّه.md) (منظومة مواقع المخزون — `npm run plan:locations`) — **الدفعة الأولى م١ مكتملة 10/20** بأمر المالك «نفذ» 2026-08-16؛ التالي م٢ (استيراد أودو).
2. **بعد كل مهمة:** حدّث حالة المهمّة و`evidence.commit` في [`docs/plan/client-completion.json`](docs/plan/client-completion.json) (وفي `phase-one.json` أيضًا إن كانت المهمّة القديمة نفسها)، ثمّ `npm run plan`، ثمّ سطر في رأس `SESSION_HANDOFF.md` موسوم `[Codex]` أو `[Claude]` + التاريخ + معرّف المهمّة + المنجز والمتبقّي.
3. **الدفع لـ`main` بأمر المالك فقط** («ارفع»). عمل Codex في فروع `codex/*` وClaude في `claude/*`.
4. إن وجدت مدخلًا حديثًا لوكيل آخر عن عمل غير مرفوع — لا تلمس نطاقه.

## البوابة الخضراء (إلزامية قبل أي كومِت نهائي)
```
npm run build   # ١٠٣ صفحة، يشغّل prebuild (build-arch + build-org) تلقائيًّا
npm test        # ٢٤١٨ اختبارًا — كلها يجب أن تبقى خضراء
npm run audit   # تدقيق سلامة البوابة
npm run plan    # حارس المتتبّعات الستّة (المرحلة الأولى · نسخة العميل · مواءمة SAP · التخزين الموجّه · التنفيذ الميدانيّ · قطاع الأغذية) — يرفض إغلاق مهمّة بلا بيّنة، ويجدّد بطاقات الاستئناف
npm run lint    # نظيف: تحذيرٌ واحدٌ معلوم في `WarehouseMaps.jsx` (ملفّ مجمَّد بقرار LOC-O04) — أيّ زيادةٍ عليه رجعةٌ تُردّ
```

## قواعد غير قابلة للتفاوض
- **العربية** لغة التخاطب مع المالك (الكود والكوميتات والواجهات كما هي).
- **بلا إيموجي في الواجهات** (أيقونات SVG خطية) · **الأحمر للتحذير فقط** · أرقام لاتينية · بنية لوحات 3 طبقات (تدخّل الآن / إجراءات سريعة / فهرس كامل).
- **لا حذف ولا تغيير لأي URL قائم** · [`src/services/auth/navCatalog.js`](src/services/auth/navCatalog.js) هو مصدر التنقّل الوحيد · الربط قبل الإضافة.
- **المجموعات الملحقة-فقط مقدّسة** (`audit` · `scans` · `events` · `stock_moves` · `assignments` · `control` · `attachments`): لا تعديل ولا حذف على سجلاتها.
- **`firestore.rules`:** الوكلاء يعدّلونها في الريبو فقط — **المالك وحده ينشرها من Firebase Console**.
- **لا CDN إطلاقًا** — كل الأصول ذاتية الاستضافة (قاعدة الأوفلاين 100%)، الخط IBM Plex Sans Arabic محلي.
- **تحقّق قبل الثقة:** راجع git والكود الفعلي قبل الجزم بأي حالة — الوثائق قد تتقادم.
- Firebase على خطة **Spark المجانية**: لا Cloud Functions ولا Storage — الملفات base64 داخل Firestore بحدّ ~700 ك.ب بعد الضغط.
- المراسلات الرسمية بالتوقيعين المعتمدين PNG فقط (لا تلفيق توقيع) وبالرقم الإشاري BFP-SCM-PR.

## خريطة سريعة
```
src/pages/dashboard/   شاشات اللوحة (Astro يستضيف جزر React)
src/components/        المكوّنات (odoo/ = نظام تصميم أودو bz-light)
src/services/          المنطق الخالص + خدمات Firestore (معظم الاختبارات هنا)
src/data/              مصادر JSON الواحدة (org-structure.json · reference-crosswalk.json)
firestore.rules        قواعد الأمن · docs/ الخطط الحاكمة · brandzo-addons/ موديول أودو الحقيقي
```
