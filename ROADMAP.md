# خريطة الطريق والقواعد — Brandzo Warehouse System
# ROADMAP & RULES (master resume document)

> **الغرض:** وثيقة استئناف واحدة تُقرأ في أي جلسة جديدة — تختصر: أين نحن، وكل
> القواعد الحاكمة، وما تبقّى في الباك اند والواجهة والتكامل.
> **آخر تحديث: 2026-07-23 (الخميس)** — بعد تدقيق شامل للبوابة (§15).
> **الخطة الاستراتيجية (الركائز الثماني) في §8 · الجدول الزمني المعتمد في §9.**
>
> **ترتيب المراجع عند الشك:** هذه الوثيقة → [`DEVELOPER_GUIDE.md`](DEVELOPER_GUIDE.md)
> → [`SESSION_HANDOFF.md`](SESSION_HANDOFF.md) → [`docs/ODOO_BACKEND_DEVELOPMENT_PLAN.md`](docs/ODOO_BACKEND_DEVELOPMENT_PLAN.md)
> → README الموديول → **الكود الفعلي** (وهو الحكم الأخير).

---

## 0. هوية المشروع في سطور

- **الاسم:** Brandzo Warehouse System (Brandzo Hub 2026). المالك: محمد إبراهيم البرشي — أخصائي سلسلة إمداد، بنغازي.
- **المهمة:** تحويل الدورة المستندية اللوجستية (12 مرحلة) إلى: **(أ) قيود صارمة** داخل موديول Odoo 19 حقيقي، و**(ب) محاكي تدريبي عالي الدقة** يعلّمها، داخل **بوابة عمليات رسمية**.
- **تقسيم اللغات:** العقل المدبّر (Back-end) = **Python + XML** (موديول `brandzo_warehouse`) · الموقع (Front-end) = **JavaScript + Astro + Tailwind + React**.
- **القاعدة الحاكمة للتطوير:** **صفر حذف** — فقط إعادة صياغة، وإنشاء، وإعادة توجيه.
- **المستودع:** `warehouse-art/brand-zo-hub` · فرع `main` · Remote: `https://github.com/warehouse-art/brand-zo-hub.git` · يُنشر تلقائيًا إلى GitHub Pages: https://warehouse-art.github.io/brand-zo-hub/
- **🌐 النطاق الرسمي (أصل المشروع):** **`warehousehub.com.ly`** — مسجّل عبر **Libyan Spider** (2026-07-13، ينتهي 2027-06-30، 15 د.ل/سنة، فعّال). لوحة الإدارة عبر حساب Libyan Spider (الرابط محفوظ خاصًّا — لا يُنشر في مستودع عام) — **يُربَط بـ GitHub Pages** (انظر §8 ركيزة 4). حاليًّا Unlocked (يُنصَح بتفعيل Registrar Lock).
- **منهج الرفع:** نطوّر خلال الجلسة، وعند «ارفع»/«خلصنا»: مزامنة main → build+lint → commit → push مباشر → مراقبة النشر.

---

## 1. الحالة الحالية — ما أُنجِز ونُشِر ✅

### 1.1 الواجهة الأمامية (Astro/React — منشورة حيّة)

**المقاييس المُثبَتة (23.07.2026):** 40 صفحة مبنيّة · **252 اختبار وحدة** في 16 ملفًّا · `npm run audit` أخضر · خطّ أساس اللينت 122 مشكلة سابقة.

- **ثلاث منصّات بمصدر واحد:** موقع ويب · **PWA يُثبَّت ويعمل دون إنترنت** (156 ملفًّا محفوظًا مسبقًا، تحديث ذاتي ببصمة بناء حتميّة) · **برنامج تنصيب ويندوز** (Tauri، يُبنى آليًّا وينشر في Releases).
- **نظام الوصول:** 12 دورًا · حارس دخول · **مصدر واحد للصلاحيات** (`navCatalog.js`) يقرأه الحارس والقائمة والتدقيق · قواعد Firestore منشورة تفرض الإلزام على الخادم.
- **محرّك المستندات (F1 + F1.5):** GRN كامل بترقيم رسمي واعتماد بالدور وسجلّ تدقيق — **مُجرَّب حيًّا بمذكرة استلام حقيقية**. تعبئة البنود بالباركود من الماستر.
- **ماستر الأصناف:** استيراد بالإكسل بمعاينة · باركودات متعدّدة · أرصدة FEFO بتشغيلات وصلاحيات — **1017 صنفًا مستوردة فعليًّا**.
- **العمليات المخزنية:** مسح سحابيّ append-only · ماسح يعمل على آيفون · شاشة متابعة لحظية للمدير.
- **سلاسل الإمداد والحركة:** عمليات الأسطول (دورة Dispatch-to-POD بتوقيع إلكتروني وتصريح بوابة ورقابة وقود) · مركز الصيانة (أوامر شغل وجدولة وقائية وMTTR) · العُهد العينية · **لوحة القيادة التنفيذية** بثمانية مؤشرات لحظية · جرد المركبات (بذرة 23 مركبة مرفوعة).
- **الهيكل التنظيمي:** `org-structure.json` مصدرًا واحدًا يولّد التقريرين وكتالوج الوظائف · صفحة تفاعلية بإصدارات append-only.
- **الاجتماعات التحضيرية:** 7 اجتماعات و36 بندًا · وضع عرض · محضر حيّ · **محاضر وخطابات مرقّمة رسميًّا بالتوقيعين المعتمدين**.
- **التوظيف الذكي · محاكي «تعلم أودو» (12 مرحلة) · مركز التقارير · الأرشيف التأسيسي.**
- **واجهة البوابة (23.07):** شريط جانبي ذكي يتمدّد عند الحاجة · صدارة بالأنظمة الثمانية · بحث عربي مطبَّع — §14.
- **طبقة ربط أودو (جاهزة، غير مفعّلة):** `src/services/odoo/**` + `odoo-proxy/**` — انظر §5.

### 1.2 الباك اند (موديول `brandzo_warehouse` — على `main`)
- **المقاييس:** ~35 ملفًا · 10 كائنات (7 توسعة قياسية + 3 مخصّصة) · 7 مجموعات أمان · 42 حالة اختبار في 10 ملفات.
- **المُنجَز:** S1→S6 + S5a + S5b — أي **10 مراحل من 12** مع **6 حرّاس ذهبية**.
- **الأمر الأخير:** `ebf5c01` (S5b: الجرد الدوري + حارس التسويات).
- **الاعتماديات:** `purchase_stock, stock_account, purchase_requisition, product_expiry, account, barcodes`.

---

## 2. القواعد الذهبية الكاملة (الحُرّاس الستة) 🛡️

> كل قاعدة تُنفَّذ بطبقتين (دفاع في العمق): **(أ) تجاوز الزر/الدالة** لتجربة فورية، **(ب) `@api.constrains`** حارس صارم لا يُتجاوز حتى برمجيًّا — إلا حيث يُذكر خلاف ذلك.

| # | القاعدة | ما تمنعه | نقطة الربط (Odoo 19) | المرحلة | الحالة |
|---|---|---|---|---|---|
| 🥇 1 | **حوكمة الشراء** | لا أمر شراء (PO) دون طلب شراء (PR) معتمد من مدير المشتريات | `purchase.order.button_confirm` ← يفحص `bz_approval_state` على `purchase.requisition` | 01–02 | ✅ S1 |
| 🥈 2 | **بوابة الجودة (QC)** | لا اعتماد استلام «Done» قبل موافقة مفتّش الجودة | `stock.picking.button_validate` (وارد) + `@api.constrains` · anchor `stock_picking.py:1399` | 04 | ✅ S2 |
| 🥉 3 | **FEFO** | صرف أي دفعة أحدث قبل الأقرب انتهاءً | الصرف (صادر)؛ الترتيب `removal_date, in_date, id` من `product_expiry` | 06 | ✅ S3 |
| 🏅 4 | **تصريح البوابة (Gate Pass)** | خروج أي مركبة دون تصريح معتمد مربوط بإذن التسليم | `bz.gate.pass.state=='approved'` + constrains على إذن الشحن | 07 | ✅ S4 |
| ⚖️ 5 | **المطابقة الثلاثية (3-Way Match)** | ترحيل فاتورة مورّد قبل تطابق (أمر + استلام + فاتورة) | `account.move._post` · anchor `account_move.py:5526` | 11 | ✅ S6 |
| 🔒 6 | **حارس التسويات** | تعديل أي رصيد يدويًّا دون سند تسوية معتمد من المدير المالي | `stock.quant.action_apply_inventory` + `_apply_inventory` (مشروط بـ `_is_inventory_mode`) | 10 | ✅ S5b |

**حوكمة المرتجعات (S5a):** إشعار الإرجاع `bz_return_state` (مربوط بـ native `return_id` عند `stock_picking.py:566`) + الإتلاف `stock.scrap.bz_scrap_state` + **جسر الإشعار الدائن** (`in_refund` من فاتورة المورّد الأصلية للمرتجع). المرحلة 08. ✅

**⚠ فخّ R1 (حارس التسويات):** المستدعون البرمجيون الأربعة لـ `_apply_inventory` — اثنان منهم يمرّران `inventory_mode=True`، فالحجب المطلق يكسرهما. الاستثناءات المعتمدة: علَم `bz_adjustment_approved` (من جلسة الجرد) · `from_inverse_qty` (product.py:290) · `env.su` (تثبيت/استيراد) · مجموعة المدير المالي. المستدعيان stock_quant.py:369 (حذف الرصيد) و:554 (زر التصفير) يُحجبان عمدًا لغير المالي؛ product.py:1177 (بلا inventory_mode) لا يلمسه الحارس.

**مجموعات الأمان السبع:** `group_bz_warehouse_user` (الأساس) · `group_bz_purchase_manager` · `group_bz_qc_inspector` · `group_bz_gate_officer` · `group_bz_return_manager` · `group_bz_inventory_auditor` · `group_bz_finance_manager`.

---

## 3. الدورة المستندية (12 مرحلة) — حالة كل مرحلة

| # | المرحلة | الكائن / نقطة الربط | الحارس | الحالة |
|---|---|---|---|---|
| 01 | طلب الشراء PR | `purchase.requisition` | حوكمة الاعتماد | ✅ S1 |
| 02 | أمر الشراء PO | `purchase.order.button_confirm:625` | ربط بالاعتماد | ✅ S1 |
| 03 | إشعار الشحن ASN | `stock.picking` (وارد، قياسي) | — | 🟡 قياسي (تعميق اختياري) |
| 04 | الاستلام والجودة GRN+QC | `stock.picking.button_validate:1399` | 🥇 الجودة | ✅ S2 |
| 05 | التخزين الموجّه Putaway | `stock.putaway.rule` (قياسي) | — | 🟡 قياسي (Bin Card اختياري) |
| 06 | التحضير Picking | صادر + `product_expiry` | 🥈 FEFO | ✅ S3 |
| 07 | الشحن Dispatch | `bz.gate.pass` + `stock.picking` | 🥉 تصريح البوابة | ✅ S4 |
| 08 | المرتجعات والتالف | `stock.picking(return_id)` · `stock.scrap` · `account.move(in_refund)` | حوكمة الإرجاع | ✅ S5a |
| 09 | الجرد الدوري | `bz.cycle.count` يلفّ `stock.quant` | مصادقة العدّ الكامل | ✅ S5b |
| 10 | التسويات المخزنية | `stock.quant._apply_inventory:996` | 🔒 اعتماد المدير المالي | ✅ S5b |
| 11 | المطابقة الثلاثية | `account.move._post:5526` | ⚖️ 3-Way Match | ✅ S6 |
| **12** | **الإغلاق المالي** | `bz.period.close` + `res.company` lock dates · `account.move._post` | 🔐 قائمة إغلاق مشتقّة تمنع الإقفال ببنود مفتوحة + قفل الفترة | **✅ S12** |

---

## 4. الباك اند (العقل المدبّر) — مكتمل 12/12 ✅

### 4.1 S12 — الإغلاق المالي ✅ (اكتملت — المرحلة 12/12 الأخيرة)
- **أُنجز:** نموذج `bz.period.close` (فترة شهرية: open → closed) + قائمة تحقّق إغلاق **مشتقّة** من الحالة الفعلية للفترة (٦ بنود: فواتير مرحّلة · دفعات مسجّلة · إشعارات دائنة مرحّلة · جرد مُصادَق · تسوية مطبَّقة · مرتجعات مقفلة — تُقرأ كلّها بـ`sudo`) + قفل فترة على `res.company` **دفاعيًّا** (اكتشاف حقل القفل المتوفّر لأن أسماءه تغيّرت بين الإصدارات) + حارس `account.move._post` يمنع الترحيل بتاريخ داخل فترة مقفلة (يتسلسل مع حارس المطابقة الثلاثية عبر `super()`).
- **الحارس (السابع):** «لا تُغلق فترة وبنودها ناقصة» — نفس منطق `CloseView` في المحاكي، مشتقًّا من بيانات أودو الحيّة؛ والإغلاق حكرٌ على المدير المالي.
- **الاختبارات:** 10 حالات قبول (الحزمة صارت 52 في 11 ملفاً): القائمة ستّة بنود · الفترة الفارغة جاهزة · مسودّة/غير مدفوعة/مدفوعة تقلب البنود · **محجوب ببند ناقص → مسموح بعد رفعه** · الإغلاق للمدير المالي حصراً · **لا ترحيل في فترة مقفلة** · إعادة الفتح · قيد حدود الفترة. **مُتحقَّق ساكنًا** بـ`verify_module.py` (107 عناصر، صفر أخطاء) — يُشغَّل فعليًّا في بيئة أودو الحيّة (M2).

### 4.1.1 المتبقّي في الباك اند بعد S12
لم يبقَ في الدورة الأساسية شيء (12/12). المتبقّي **تعميقات اختيارية** (§4.2) + **تشغيل الاختبارات حيًّا** (§4.3، يتحقّق بـM2 ركيزة 3) + **S8 الربط بالواجهة** (§5.1).

### 4.2 تعميقات اختيارية (حسب الحاجة)
- **03 ASN:** نموذج إشعار شحن مسبق صريح قبل الاستلام (اليوم يعتمد على القياسي).
- **05 Bin Card:** بطاقة موقع مخصّصة فوق قواعد Putaway القياسية.
- **سجلّ تدقيق (Audit):** توثيق مَن اعتمد/عدّل ومتى لكل إجراء حسّاس.

### 4.3 مهام تشغيلية
- **تشغيل الاختبارات فعليًّا** في بيئة Odoo لديك: `odoo -d <db> -i brandzo_warehouse --test-enable --stop-after-init`. (محليًّا يتوفّر Python 3.14 للتحقّق **الساكن** عبر `brandzo-addons/brandzo_warehouse/verify_module.py` فقط — لا بيئة أودو حيّة بعد.)

---

## 5. المتبقّي في الواجهة والتكامل (الربط بأودو + نظام الوصول)

### 5.1 التكامل مع أودو الحقيقي (S8) — الطبقة جاهزة، ينقصها التفعيل
تفاصيل كاملة في [`docs/ODOO_INTEGRATION_HANDOFF.md`](docs/ODOO_INTEGRATION_HANDOFF.md). باختصار المتبقّي:
1. نشر الوسيط (Cloudflare Worker أو Firebase) بأسراره الأربعة (ODOO_URL/DB/USER/API_KEY).
2. توسيع `ALLOWLIST` للنماذج/الدوال التي تحتاجها الشاشات.
3. `PUBLIC_ODOO_MODE=production` + `PUBLIC_ODOO_PROXY_URL` ثم إعادة بناء ونشر.
4. تحويل الشاشات إلى أودو تدريجيًّا (المنتجات ثم الحركات).

### 5.2 نظام الوصول (من حاوية `/dashboard/access-control`) — النقاط القادمة
1. **تسجيل الدخول الفعلي** (Firebase Auth) — شاشة دخول عربية للبوابة. *(النقطة الأولى المقترحة للبدء)*
2. **جلب الأدوار من أودو** — قراءة مجموعات المستخدم عبر الوسيط وتخزين الدور في الجلسة.
3. **قائمة جانبية حسب الدور** — إخفاء/إظهار مجموعات القائمة تلقائيًّا حسب الصلاحية.
4. **صفحة إدارة المستخدمين** — إسناد الأدوار وتعطيل الحسابات من داخل البوابة.
5. **سجلّ التدقيق** — من فعل ماذا ومتى.

---

## 6. خطة OneDrive (بانتظار الدمج) 📌

> رابط الخطة: (محفوظ خاصًّا — رابط مشاركة OneDrive لا يُنشر في مستودع عام؛ محفوظ في الذاكرة المحلية ولدى المالك).
>
> **تعذّر الجلب الآلي** (صفحة OneNote تتطلّب تسجيل دخولك — HTTP 401 — ومتصفّح Chrome غير متصل بحساب Claude).
> **لدمجها:** الصق نص الخطة هنا (أو صدّرها من OneNote كـ PDF/نص وأرسلها)، وسأُدرجها في هذا القسم منظّمةً ومربوطةً بالمراحل أعلاه.

*(— يُملأ لاحقًا —)*

---

## 7. المراجع والملفات

```
warehouse-system/
├─ README.md                      # الواجهة الأولى للمستودع
├─ ROADMAP.md                     # ★ هذه الوثيقة (خريطة الطريق والقواعد)
├─ DEVELOPER_GUIDE.md             # ★ الملف الذي يُسلَّم للمطوّر (تشغيل وفهم في ساعة)
├─ SESSION_HANDOFF.md             # الذاكرة الحيّة — آخر ما جرى بالتفصيل
├─ firestore.rules                # الإلزام الحقيقي على الخادم (394 سطرًا · 30 قاعدة)
├─ docs/
│  ├─ ODOO_BACKEND_DEVELOPMENT_PLAN.md   # معمارية الباك اند
│  ├─ ODOO_INTEGRATION_HANDOFF.md        # ربط الواجهة بأودو (S8)
│  └─ archive/                           # وثائق تاريخية — لا يُعتمد عليها
├─ scripts/
│  ├─ audit-portal.mjs            # ★ npm run audit — حارس البنية
│  ├─ build-org.mjs               # يولّد التقارير من org-structure.json
│  ├─ build-items-template.mjs    # يولّد قالب الأصناف من المخطّط
│  └─ generate-icons.mjs          # أيقونات PWA (يدويّ)
├─ src/
│  ├─ layouts/DashboardLayout.astro       # القشرة + الحارس + القائمة
│  ├─ pages/dashboard/**                  # 38 صفحة
│  ├─ components/brandzo-erp/{access, documents, items, odoo-sim, ...}
│  ├─ data/{org-structure.json, meetings-seed.json, vehicles-seed.json}
│  └─ services/                           # ★ المنطق الخالص المُختبَر
│     ├─ auth/{navCatalog, navAccess, pageAccess, roles, authService, usersService}
│     ├─ documents/{schemas/, numbering, states, documentsService}
│     ├─ items/ · balances/ · excel/ · stock/
│     ├─ fleet/ · maintenance/ · custody/ · vehicles/
│     ├─ meetings/ · org/ · recruitment/ · hiring/
│     └─ odoo/                            # طبقة الربط (غير مفعّلة)
├─ src-tauri/                     # قشرة برنامج ويندوز
├─ odoo-proxy/                    # وسطاء Cloudflare/Firebase المرجعيون
└─ brandzo-addons/brandzo_warehouse/      # ★ موديول أودو الحقيقي
   ├─ README.md · models/ · security/ (7 مجموعات) · views/ · data/ · tests/ (42 حالة)
```

## 8. الخطة الاستراتيجية الأمامية — الركائز الثماني (خطة 2026) 🚀

> **ميثاق الشراكة (2026-07-13):** المالك يحلم ويختار ويتعلّم؛ القائد البرمجي يخطّط وينفّذ ويُلهم.
> **الهدف الأسمى:** نظام مستودعات **مثالي واحترافي — الأول من نوعه في ليبيا**، جاهز ليكون منتجًا.
> كل ركيزة تُعرَض بـ: **الهدف · ما لدينا · الخطوات · توصيتي · قرارك المطلوب.**

### ⬛ ركيزة 1 — نظام الأدوار والصلاحيات (من يرى ماذا · من يفعل ماذا)

**الهدف:** لكل موظف واجهة تُظهر ما يخصّه فقط، وصلاحيات تسمح بأفعاله فقط — بحيث تُفرَض الحرّاس الستة على مستوى الدور لا بالنيّة.
**ما لدينا:** 7 مجموعات أمان في الموديول + حاوية `/dashboard/access-control`. **✅ منجز ومنشور حيًّا (2026-07-13):** (أ) **M1:** Firebase Auth + شاشة دخول `/login` + حارس `AuthGate` + 10 أدوار + بطاقة المستخدم/الخروج + تمهيد أدمن بالبريد (المالك دخل كـ«👑 المدير العام»). (ب) **الإكمال (`d06bb98`):** تقييد القائمة حسب الدور (`navAccess.js` + `RoleNav`) + صفحة إدارة المستخدمين `/dashboard/users` (إسناد الأدوار الـ10 + تفعيل/تعطيل + إضافة بالـUID، أدمن فقط). **✅ قواعد أمان Firestore نُشرت (2026-07-15):** [`firestore.rules`](firestore.rules) — كانت القاعدة **مفتوحة للعالم** (ثبت بالاختبار: قراءة/كتابة/حذف بلا حساب)؛ الآن: لا وصول بلا دخول · الأدوار للأدمن فقط · سجلّ المسح غير قابل للتعديل/الحذف · منع افتراضي. أُعيد الاختبار = كل المحاولات مرفوضة. **يتبقّى:** جلب الأدوار من أودو + شاشة متابعة العمليات للمدير.

**مصفوفة الأدوار (المرجع المعتمد):**

| الدور | يرى (شاشات) | يفعل (أفعال محكومة) | المجموعة |
|---|---|---|---|
| **المدير العام / الأدمن** (رمز الدخول 7777) | كل شيء | كل شيء + إدارة المستخدمين وإسناد الأدوار | فوق المجموعات |
| **مدير المستودع** | كل شاشات المستودع + التقارير + لوحات KPI | يعتمد، يوزّع المهام، يراقب | `warehouse_user`+ |
| **أمين المخزن / المستلم** | الاستلام GRN · التخزين · الجرد | ينشئ GRN، يخزّن، يعدّ المخزون | `warehouse_user` |
| **مفتّش الجودة** | الاستلامات المعلّقة · تقرير الجودة | يوافق/يرفض الجودة 🥇 (يفكّ حجب Done) | `qc_inspector` |
| **ضابط البوابة / الأمن** | تصاريح الخروج · الشحنات الجاهزة | يصدر/يعتمد تصريح البوابة 🏅 | `gate_officer` |
| **موظف المشتريات** | طلبات وأوامر الشراء | ينشئ PR/PO (محكوم بحوكمة الاعتماد) | `purchase_manager` |
| **المحاسب / المدير المالي** | الفواتير · المطابقة الثلاثية · التسويات · الإغلاق | يرحّل الفواتير ⚖️، يعتمد التسويات 🔒، يغلق الفترة | `finance_manager` |
| **مسؤول المرتجعات والتالف** | المرتجعات · الإتلاف · الإشعارات الدائنة | ينشئ مرتجعًا/إتلافًا (محكوم) | `return_manager` |
| **مدقّق الجرد** | أوراق الجرد · الفوارق | يدقّق ويصادق العدّ | `inventory_auditor` |
| **المشاهد / المدقّق الخارجي** | تقارير للقراءة فقط | لا تعديل | قراءة فقط |

- **«يرى ماذا» ← الواجهة:** تُخفى/تُظهر مجموعات القائمة الجانبية (`navGroups`) تلقائيًّا حسب الدور.
- **«يفعل ماذا» ← الباك اند:** ACLs في الموديول + الحرّاس الستة `@api.constrains` = لا تجاوز حتى لو ظهر الزر.
- **منطق الأدمن 7777 (قرار المالك المؤجّل):** رمز دخول موحّد يمنح رؤية شاملة — يُدمَج كطبقة أدمن فوق المصفوفة.

**توصيتي:** نبني هذا فوق **Firebase Auth** (الدخول) + جدول أدوار، ثم لاحقًا نجلب الدور الحقيقي من مجموعات أودو عبر الوسيط.
**قرارك:** هل نبدأ بمنطق الأدمن 7777 المبسّط أولًا (سريع)، أم بالمصفوفة الكاملة مباشرة؟

### ⬛ ركيزة 2 — الباك اند (العقل المدبّر) — مكتمل 12/12 ✅

مفصّل في §4. باختصار: **(أ) ✅ S12 الإغلاق المالي أُنجز** (المرحلة 12/12 الأخيرة — `bz.period.close` + قائمة إغلاق مشتقّة + قفل الفترة + حارس الترحيل؛ مُتحقَّق ساكنًا بـ`verify_module.py`) · **(ب) تشغيل الاختبارات الـ52 فعليًّا** في بيئة أودو حقيقية (تتحقّق بركيزة 3 — M2) · **(ج) تعميقات اختيارية:** ASN صريح، Bin Card، سجلّ تدقيق كامل · **(د) S8** ربط الموديول بالواجهة.
**ملاحظة:** أُنجز S12 بالتحقّق الساكن (لا بيئة أودو حيّة بعد)؛ تشغيل الاختبارات الحيّ يبقى معلّقًا على M2 لإثباته فعليًّا لا ساكنًا فقط.

### ⬛ ركيزة 3 — قاعدة بيانات حيّة (أفضل الخيارات المجانية) 💾

**الهدف:** بيئتان حيّتان مجانيتان: (أ) قاعدة الواجهة/الدخول، (ب) أودو حقيقي يشغّل موديولنا المخصّص.
**الحقيقة الحاكمة:** موديول `brandzo_warehouse` **مخصّص** — لا يعمل على «Odoo Online» المجاني (لا يقبل موديولات مخصّصة). فنحتاج استضافة ذاتية.

| الخيار | يصلح لـ | المجانية | الحكم |
|---|---|---|---|
| **Oracle Cloud «Always Free»** (ARM حتى 4 أنوية/24GB) | أودو 19 + PostgreSQL (استضافة ذاتية Docker) | مجاني **للأبد** | ★ **توصيتي** — يشغّل موديولنا المخصّص بقوة إنتاجية |
| **Firebase (خطة Spark)** | دخول Auth + بيانات الواجهة | مجاني سخيّ | ★ **مدمج أصلًا** — نبدأ به فورًا |
| Supabase / Neon (Postgres) | بديل لبيانات الواجهة | 500MB مجاني | بديل جيد لو أردنا علائقيًّا |
| Odoo Online المجاني | تطبيق واحد فقط | مجاني | ✗ **لا يقبل موديولنا** |
| Google/AWS free | أودو | صغير/12 شهرًا | ✗ ضعيف أو مؤقّت |

**توصيتي (المعمارية المجانية الكاملة):**
`الواجهة + الدخول → Firebase` · `أودو الحقيقي + موديولنا → Oracle Always Free (Docker: Odoo19 + Postgres)` · `الجسر بينهما → odoo-proxy`.
**قرارك:** (أ) نبدأ بـ **Firebase فقط** كـ MVP سريع، أم (ب) نؤسّس **Oracle + أودو حيّ** من البداية للقوة الكاملة؟

### ⬛ ركيزة 4 — الواجهة الأمامية وتجربة المستخدم بالكامل 🎨

**الهدف:** بوابة عمليات فعلية يستخدمها الموظفون يوميًّا، لا عرضًا.
**خارطة العمل:**
1. **شاشة تسجيل دخول عربية RTL** (Firebase Auth) — نقطة البداية للنظام كله.
2. **قائمة وشاشات حسب الدور** (من ركيزة 1).
3. **صفحة إدارة المستخدمين** — إسناد/تعطيل الأدوار من داخل البوابة.
4. **ربط بيانات حيّة** — استبدال المحاكاة ببيانات Firebase/أودو تدريجيًّا. **✅ أُنجز لمنظومة الجرد (2026-07-15):** `/dashboard/stock-operations` تحفظ كل مسح سحابيًّا في سجلّ append-only (`operations/{opId}/scans`) بهوية ووقت، تعمل بلا إنترنت وترفع تلقائيًّا، ومؤشّر حالة يمنع «الفشل الصامت» — الخدمة `services/stock/operationsService.js`.
5. **محاكي Phase 2** — شاشة تقييم/نتيجة + إعادة لعب (جزء منها منجز).
6. **تطبيق جوّال PWA بقارئ باركود** — للعمل الميداني (ركيزة 8).
7. **لمسات احترافية:** وضع ليلي · استجابة للجوال · إشعارات · لوحات KPI عربية بالدينار الليبي.
8. **🌐 ربط النطاق الرسمي `warehousehub.com.ly` بـ GitHub Pages:**
   - **جانبي أنا (برمجي):** إضافة ملف `CNAME` يحوي `warehousehub.com.ly` + **تغيير `astro.config.mjs` من `base:'/brand-zo-hub'` إلى `base:'/'`** (فخّ مهم: مع النطاق المخصّص يُخدَم الموقع من الجذر لا من مسار فرعي، فتحديث المسار إلزامي وإلا تكسر الروابط).
   - **جانبك أنت (في Libyan Spider — لا أدخله):** سجلّات DNS للنطاق الجذر → **A records** إلى عناوين GitHub Pages: `185.199.108.153` · `185.199.109.153` · `185.199.110.153` · `185.199.111.153` (وإن دعم AAAA، عناوين IPv6 الأربعة لـ Pages).
   - **بعدها:** إعدادات المستودع → Pages → Custom domain = النطاق → تفعيل **Enforce HTTPS**.

### ⬛ ركيزة 5 — التحديث الدوري (كسر فخّ النسيان) 🧭

**الدرس المستفاد اليوم:** وقعنا في الفخّ فعلًا — `SESSION_HANDOFF` قال «غير مرفوع» وهو مرفوع، و`ROADMAP.md` «مفقود» وهو موجود غير ملتزَم. الحل بروتوكول صارم:

1. **مصدر حقيقة واحد = هذه الوثيقة `ROADMAP.md`.** كل شيء آخر يشير إليها.
2. **قاعدة «تحقّق قبل الثقة»:** لا يُذكر أي وضع من الذاكرة/الوثائق وحدها — يُطابَق مع Git/الكود أولًا. (هذه القاعدة أنقذتنا اليوم.)
3. **بداية كل جلسة → «تحديث أولي»:** قراءة الحالة + مطابقتها بالواقع + الإبلاغ بأي انحراف.
4. **نهاية كل مهمة → «احفظ» + «وثّق»:** تحديث `SESSION_HANDOFF` + التقارير الثلاثة.
5. **انضباط الرفع:** الوثائق الحاكمة (ROADMAP/HANDOFF) **تُلتزَم وتُرفع** فورًا — لا تُترك untracked (هو بالضبط ما أضاع ROADMAP).
6. **(مقترح) تدقيق اتساق أسبوعي مجدوَل** يقارن ادّعاءات ROADMAP بواقع Git ويُنبّه بالانحراف.

### ⬛ ركيزة 6 — ملف شرح المشروع للمطوّرين (لو أشركنا أحدًا) 👥

**الهدف:** أي مطوّر جديد يفهم المشروع ويشغّله خلال ساعة.
**ما لدينا:** [`DEVELOPER_GUIDE.md`](DEVELOPER_GUIDE.md) = دليل المطوّر (خلَف `PROJECT_MEMORY.md` المؤرشف) (هوية · معمارية · حالة · فخاخ · خريطة ملفات). قوية وكافية كأساس.
**المطلوب:** (أ) إبقاؤها **محدّثة مع كل مرحلة** (مربوطة بركيزة 5) · (ب) إضافة قسم «التشغيل السريع للمطوّر»: تنصيب الواجهة (`npm i && npm run dev`) · تشغيل الموديول في أودو · اصطلاحات الكود · الحرّاس الستة · كيفية الاختبار.
**✅ أُنجزت (23.07.2026):** أُلّف **[`DEVELOPER_GUIDE.md`](DEVELOPER_GUIDE.md)** — وثيقة المطوّر الواحدة: تشغيل في ثلاثة أوامر · المعمارية الثلاثية وقاعدتها الذهبية · نظام الوصول بطبقاته · محرّك المستندات · الحرّاس الستة · مصادر الحقيقة الواحدة ومولّداتها · **سبعة فخاخ موثّقة وقعنا فيها فعلًا** · خريطة الاختبارات وما لا تغطّيه. والجذر نُظّف إلى **أربعة ملفات** (README · ROADMAP · DEVELOPER_GUIDE · SESSION_HANDOFF) والباقي في `docs/` و`docs/archive/` بنقل لا حذف.

### ⬛ ركيزة 7 — ملف عمليات لشركة أودو + الربط الفعلي 🤝

جزءان:
- **(أ) ملف تعريفي احترافي لأودو (`ODOO_PARTNER_BRIEF.md`) — إنجليزي + عربي:** ملف شركة برند زو (ليبيا) · الدورة التشغيلية الـ12 · كيف يفرضها الموديول (الحرّاس الستة) · لقطات شاشة · القيمة التجارية · ملاءمة السوق الليبي. يُستخدم للتقديم كـ **Odoo Partner** أو عرض حالة نجاح.
- **(ب) الربط الفعلي = S8:** توصيل الموديول بأودو حيّ (Oracle من ركيزة 3) عبر `odoo-proxy` ثم `PUBLIC_ODOO_MODE=production`. تفاصيل في [`docs/ODOO_INTEGRATION_HANDOFF.md`](docs/ODOO_INTEGRATION_HANDOFF.md).
**توصيتي:** ننجز الملف التعريفي **بعد** أن يعمل الموديول على أودو حيّ (لنُرفق لقطات حقيقية) — أي بعد ركيزتي 3 و2.

### ⬛ ركيزة 8 — اقتراحاتي للتطوير (رؤية القائد) 💡

عشر أفكار تصنع الفارق «الأول في ليبيا»:
1. **📱 تطبيق جوّال PWA بقارئ باركود** — المسح الميداني offline-first: الميزة القاتلة لأي مستودع حقيقي.
2. **📊 لوحة تحليلات ذكية (KPIs)** — دوران المخزون · نسبة التزام FEFO · معدّل رفض الجودة · إنتاجية البوابة — بالعربية والدينار الليبي.
3. **🤖 مساعد ذكي بالعربية (Claude API)** — استعلام باللغة الطبيعية: «كم صنفًا قارب على الانتهاء؟» (يستفيد من مهارة `/claude-api`).
4. **🔔 إشعارات استباقية** — قرب انتهاء الصلاحية · نقص المخزون · جودة معلّقة · فواتير غير مرحّلة — عبر واتساب/بريد (لديك قناة واتساب أصلًا).
5. **🏢 تعدّد المستودعات والفروع** — التوسّع بنغازي → طرابلس → …
6. **🔏 سجلّ تدقيق كامل + توقيع إلكتروني** — مَن اعتمد ماذا ومتى: عنصر ثقة كبير لبيع المؤسسات الليبية.
7. **🇱🇾 توطين ليبي عميق** — دينار · عربية أولًا · موردون محليون · صمود دون إنترنت (offline-first) · تواريخ هجري/ميلادي.
8. **🎓 جسر تدريب↔إنتاج موحّد** — المحاكي يصبح تأهيل الموظف الجديد قبل النظام الحقيقي (تفرّد معماري لديك).
9. **🖨️ قوالب مطبوعة بهوية برند زو** — الـ21 نموذجًا كـ PDF موحّدة (لديك `public/forms/`).
10. **🌐 تحويله منتجًا (SaaS) + API موثّق** — ليخدم شركات ليبية أخرى: تحقيق طموح «الأول في ليبيا».

---

## 9. المسار الزمني المعتمد (المالك 2026-07-23) 🗺️

> **✅ قائمة P0 صُفّيت بالكامل** — لم يعد شيء معلّقًا على المالك: نُشرت قواعد الأمان
> (فدخلت ستة أنظمة الخدمة الفعلية) · جُرِّبت أول مذكرة استلام GRN حقيقية · استُورد كتالوج
> 1017 صنفًا · رُفعت بذرة المركبات · رُوجعت بنود الاجتماعات الـ36 والعقد التسع المقترحة ·
> صدر قرار التوظيف. **المؤجَّل الوحيد بقراره: سجلّات DNS للنطاق الرسمي.**

| متى | ماذا | المرجع |
|---|---|---|
| ~~**M1 — الأساس الحيّ**~~ ✅ | Firebase Auth + دخول عربي + أدوار — منشور 2026-07-13 | §8 ركيزة 1 |
| ~~**23.07**~~ ✅ | تدقيق شامل + إحكام الصلاحيات + بوابة جودة في النشر + إعادة هيكلة التوثيق | **§15** |
| **25–30 يوليو** | **دفعة ٣ الاجتماعات:** التقرير المجمّع «النظام الإداري التعاوني الموحّد» | §13 |
| **1–6 أغسطس** | **F2 سلسلة الشراء:** PR→PO→GRN→QC + تفعيل `links` = المطابقة الثلاثية | §11.5 |
| **8–13 أغسطس** | **إغلاق حلقة الأصناف:** I-هـ (فطام الجرد من localStorage) + I-د (الأصناف المعلّقة) | §12.5 |
| **15–20 أغسطس** | **F3 الصرف والخروج + F5** صندوق «بانتظار اعتمادي» — تكتمل دورة اليوم التشغيلي | §11.5 |
| **22 أغسطس – 3 سبتمبر** | **M2 بيئة أودو الحيّة** (Oracle Always Free) → تشغيل الاختبارات الـ52 فعليًّا (S12 أُنجز برمجيًّا سلفًا) → الباك اند 12/12 مُثبَت حيًّا | §8 ركائز 3+2 · §4.1 |
| **سبتمبر فما بعد** | S8 الربط · F4 المرتجعات والمالية · النطاق الرسمي · لوحة KPI · إشعارات واتساب · ملف شريك أودو | §5.1 · §8 ركائز 7+8 |
| **مستمرّ** | التوثيق الثلاثي بعد كل مهمة · `npm test` + `npm run audit` قبل كل دفع | §10 |

**منطق الترتيب:** ما يحرّر قيمة مبنيّة ← ما يخدم موعدًا حقيقيًّا (اجتماعات الإدارات السبع) ← ما يربط السلسلة ← ما يوسّعها.

---

## 10. بروتوكول مكافحة النسيان — سجلّ الاتساق (يُطبَّق كل جلسة) 🔒

> ملخّص تنفيذي لركيزة 5. القاعدة الذهبية السابعة (إجرائية): **«تحقّق قبل الثقة».**
> عند بداية أي جلسة: اقرأ هذه الوثيقة، طابِقها بـ Git، أبلِغ بأي انحراف قبل العمل.
> الوثائق الحاكمة تُرفع فورًا. لا وثيقة حاكمة تبقى untracked.

---

## 11. خطة النماذج الـ21 — من ورقٍ مطبوع إلى نظام مستندي حيّ 📑

> **الحالة (فحص فعلي 2026-07-15):** `public/forms/` فيه **22 نموذجًا** (1.2MB) + ملفان مشتركان
> (`brandzo-forms.css` · `brandzo-letterhead.js` — تستخدمهما **كل** النماذج ⇒ مَشْبَك مركزي جاهز).
> نموذج الاستلام وحده **1887 سطرًا و119 حقل إدخال**.

### 11.1 التشخيص — لماذا هي «ورق داخل متصفّح»
| العيب | الأثر التشغيلي |
|---|---|
| **صفر حفظ** (لا localStorage ولا قاعدة بيانات) | تملأ النموذج ثم تُغلق الصفحة ⇒ **ضاع كل شيء**. الوظيفة الوحيدة `window.print()`. |
| **أرقام مستندات مكتوبة في الكود** (`GRN-002` ثابت) | كل استلام مطبوع يحمل **نفس الرقم** ⇒ لا تسلسل ولا تتبّع ولا تدقيق. |
| **لا هوية ولا صلاحية** | لا يُعرف من ملأ المستند ولا من اعتمده — والاعتماد توقيع بالقلم. |
| **لا ترابط بين المستندات** | أمر الشراء والاستلام والفاتورة أوراق منفصلة ⇒ **المطابقة الثلاثية يدوية**. |
| **معزولة عمّا بنيناه** | لا تعرف الدخول ولا الأدوار ولا الجرد السحابي ولا الحرّاس الستة. |

### 11.2 المبدأ الحاكم: **محرّك واحد لا 21 تطبيقًا**
النماذج ليست 21 شاشة مستقلّة — بل **طبقة إدخال للدورة المستندية الـ12** التي يفرضها موديول أودو ويعلّمها المحاكي.
لذا نبني **محرّك مستندات واحدًا** + «مخطّط» (schema) لكل نموذج (حقوله · أعمدة جدوله · قواعده · دوره)،
ويتكفّل المحرّك بـ: مسودّة/حفظ · **ترقيم تسلسلي حقيقي** · هوية ووقت · اعتماد حسب الدور · **سجلّ تدقيق دائم** · طباعة.
**التصميم المطبوع الجميل يبقى كما هو** — لكن يُطبع من بيانات محفوظة برقم حقيقي وتوقيع معروف.

### 11.3 نموذج البيانات المقترح (يمتدّ من `operations` الذي نجح)
```
documents/{docId}
   type    : 'GRN' | 'PO' | 'GatePass' | …        ← أيّ النماذج الـ21
   number  : 'GRN-2026-0001'                       ← تسلسل حقيقي (لا يتكرّر)
   stage   : 04                                    ← ربطه بالدورة الـ12
   state   : draft → submitted → approved → done
   header  : {…}    lines: [{…}]                   ← حقول الرأس وبنود الجدول
   links   : { poId, grnId, … }                    ← سلسلة المستندات (أساس المطابقة الثلاثية)
   createdByUid/Name · createdAt · updatedAt
   └── audit/{id}                                  ← append-only: من فعل ماذا ومتى (نفس نمط scans)
```

### 11.4 الربط بما بنيناه فعلًا (القيمة الكبرى)
1. **الهوية والأدوار** (جاهزة): مفتّش الجودة يعتمد تقرير الجودة · ضابط البوابة يُصدر التصريح · المدير المالي يعتمد التسوية.
2. **🔥 الجرد الذكي ← نموذج الاستلام:** تمسح الأصناف بالباركود فتملأ **بنود GRN تلقائيًّا** بدل 119 حقلًا يدويًّا. (أقوى تكامل ممكن — والبنية جاهزة.)
3. **الحرّاس الستة** تصير حقيقية في الواجهة: لا استلام «تمّ» قبل اعتماد الجودة · لا خروج بلا تصريح معتمد · لا ترحيل قبل المطابقة الثلاثية.
4. **`odooMapper.js`** (موجود): أسماء الحقول القياسية ⇒ المستندات تُرحّل لأودو لاحقًا بلا إعادة بناء.
5. **قواعد أمان Firestore**: نفس نمط `scans` — إضافة فقط، لا محو للتاريخ.

### 11.5 المراحل (واقعية — 21 نموذجًا لا تُبنى دفعة واحدة)
| # | المرحلة | المحتوى |
|---|---|---|
| **F1** | **الأساس + نموذج رائد** | محرّك المستندات + الترقيم التسلسلي + سجلّ التدقيق، ومخطّط **نموذج واحد كامل من الطرف للطرف**: ملء → حفظ مسودّة → إرسال → اعتماد بالدور → طباعة برقم حقيقي. **يُثبت النمط قبل التوسّع.** |
| **F2** | **سلسلة الشراء (1–4)** | طلب شراء → أمر شراء → استلام → تقرير جودة، **مع ترابط المستندات** + حارس الجودة. |
| **F3** | **الصرف والخروج (5–7)** | قائمة السحب → التعبئة → إذن التسليم → تصريح البوابة + حارسا FEFO والبوابة. |
| **F4** | **المرتجعات والجرد والمالية (8–11)** | إرجاع · تالف · جرد دوري · سند تسوية · إشعار دائن + حارسا التسوية والمطابقة الثلاثية. |
| **F5** | **صندوق الوارد والتقارير** | شاشة «مستنداتي / بانتظار اعتمادي» (على نمط شاشة متابعة العمليات) + تصدير. |
| — | **خارج الأولوية** | `Report21` · `DailyHuddle` · `WeeklyCheck` — إدارية يدوية، تبقى للطباعة. |

### 11.6 قرارات المالك — ✅ اتُّخذت (2026-07-15)
1. **النموذج الرائد:** **الاستلام (GRN)** — قلب الدورة، ويستفيد فورًا من الجرد بالباركود.
2. **النطاق:** **الـ12 نموذجًا** داخل الدورة المحكومة. (Report21 · DailyHuddle · WeeklyCheck تبقى للطباعة.)
3. **الترقيم:** **`GRN-2026-0001`** — النوع · السنة · تسلسل من 4 خانات يبدأ 0001 ويُصفَّر مع كل سنة.

### 11.7 F1 — منجَز (2026-07-15) ✅

**ما بُني:** محرّك مستندات واحد يقرأ المخطّط ويرسمه — إضافة نموذج = ملف مخطّط لا شاشة.

| الملف | الدور |
|---|---|
| `services/documents/numberFormat.js` | صيغة الرقم (منطق خالص، مُختبَر وحده) |
| `services/documents/numbering.js` | حجز الرقم بمعاملة Firestore — لا تصادم ولا تكرار |
| `services/documents/states.js` | آلة الحالات + من يملك كل نقلة |
| `services/documents/documentsService.js` | حفظ · نقلات · سجلّ تدقيق append-only · مستمعات |
| `services/documents/schemaUtils.js` | قراءة المخطّطات (دوال خالصة) |
| `services/documents/schemas/grn.js` | مخطّط GRN + حدود CCP1 |
| `services/documents/schemas/index.js` | السجلّ + خارطة الـ12 (`ready` تُشتقّ من الواقع) |
| `components/brandzo-erp/documents/*` | 8 مكوّنات: المحرّك · الحقول · الجدول · قائمة الفحص · شريط الحالة · سجلّ التدقيق · عرض الطباعة · صندوق المستندات |
| `pages/dashboard/documents.astro` · `document.astro` | صندوق المستندات · شاشة المستند الواحد |

**العيوب الخمسة في §11.1 — أين صارت:**

| العيب | الحال بعد F1 |
|---|---|
| صفر حفظ | ✅ `documents/{docId}` + متابعة لحظيّة + مسودّات |
| رقم ثابت `GRN-002` | ✅ تسلسل حقيقي بمعاملة؛ القواعد تمنع تصفير العدّاد أو قفزه |
| لا هوية ولا صلاحية | ✅ المُنشئ والمعتمِد من Firebase Auth؛ الاعتماد بالدور؛ سجلّ تدقيق دائم |
| لا ترابط | 🟡 حقل `links` جاهز في النموذج — يُفعَّل في F2 مع سلسلة الشراء |
| معزولة | ✅ تعرف الدخول والأدوار وقواعد الأمان. **الربط بالجرد بالباركود لم يُنفَّذ بعد** |

**تصحيحات أثناء النقل** (كشفها فحص الورق): «رقم GRN» صار رقم النظام · حرارات CCP1 صارت أرقامًا **تُفحص فعلًا** (الورق كتب الحدّ ولم يفحصه) · الإجماليات الثلاثة **محسوبة** من البنود · «مشرف الاستلام» و«فاحص الجودة» من **الهوية** لا من القلم · 4 حقول «مقبول/مرفوض» صارت قوائم · الصفوف الثمانية الجامدة صارت بنودًا تُضاف وتُحذف · علامة «يوجد مرفق مدبس» التي طلبها الورق ولم يوفّرها — أُضيفت.

**🥇 حارس الجودة صار حقيقيًّا في الواجهة:** لا طريق من «مُرسَل» إلى «منجَز» — لا بدّ من اعتماد مفتّش الجودة. مفروض في ثلاث طبقات (واجهة · خدمة · **قواعد Firestore على الخادم**).

**التحقّق:** بناء أخضر (33 صفحة) · لينت نظيف (صفر مشكلة جديدة؛ الـ154 القائمة سابقة على main) · **30/30 اختبار وحدة** (`npm test`) تغطّي الترقيم وآلة الحالات والإجماليات وحدود CCP1 — منها اختبار يقارن أدوار المخطّط بـ`firestore.rules` فيمنع انحراف الطبقتين · الحارس يحوّل الصفحتين الجديدتين لغير المسجّل.

**✅ نُشرت القواعد وجُرِّب المسار حيًّا (23.07.2026):** أنشأ المالك **أول مذكرة استلام حقيقية** ومرّت الدورة كاملةً — ملء ← إرسال برقم رسمي ← اعتماد الجودة ← طباعة. لا معلّقات على هذا المسار.

**التالي في F1.5 (مرشّح):** ربط «الجرد الذكي» بالبنود — تمسح الباركود فتُملأ بنود GRN تلقائيًّا بدل 119 حقلًا يدويًّا (`getScans(opId)` جاهزة). هذا أقوى تكامل ممكن، وأُجّل عمدًا ليُثبت النمط أولًا.

---

---

## 12. ماستر الأصناف — مصدر الحقيقة الواحد 📦

> **الطلب (المالك 2026-07-15):** «مفهوم ومنطق للايتم مستر داتا ليكون مصدرًا يُعمل
> به في كافة البوابة · يُرفع عبر استيراد شيت إكسيل · وكل المستندات المخزنية
> تتعرّف على الأصناف بالباركود.»

### 12.1 التشخيص — المشكلة ازدواج لا نقص

الفحص (2026-07-15) وجد **نظامَي أصناف متوازيين لا يعرف أحدهما الآخر**:

| | `Items_Master` | `inventoryDB` |
|---|---|---|
| الهوية | SKU | **الباركود** |
| المخزن | Firestore | **localStorage فقط** |
| من يستخدمه | شاشة الأصناف وحدها | **كل مسح حقيقي** |
| باركود | **لا يوجد الحقل أصلًا** | المفتاح |

وثلاث طبقات مبنيّة بالكامل بلا مستدعٍ: `src/services/excel/` · `odooMapper.js` ·
`inventoryService.js`. وREADME يعترف: «يتبقّى ربط الاستيراد بكتابات حقيقية».

### 12.2 قرارات المالك (2026-07-15) ✅
1. **الباركود قد يتعدّد** للصنف الواحد ⇒ المعرّف = **SKU**، والباركود **فهرس** (`barcodes[]`).
2. **الرصيد مصدره الشيت** الآن؛ تحريكه بالمستندات خطوة لاحقة مستقلّة.
3. **الباركود المجهول** يُسجَّل **معلّقًا للمراجعة** — لا يوقف العمل ولا يلوّث الماستر.
4. **الاستيراد** للمدير العام ومدير المستودع (يطابق `isManager` المنشورة).

### 12.3 المفهوم: الصنف يُعرَّف مرّة، ويُستدعى بالباركود
```
Items_Master/{SKU}        ← SKU = default_code في أودو (فلا يُعاد بناء S8)
   barcodes: ['8059…']   ← فهرس الاستدعاء = barcode في أودو
   nameAr · nameEn · shade · category · subcategory
   unit · unitPrice · balance · minStock · notes · archived · odooId
```
ثلاث طبقات: **استيراد** (شيت ← تحقّق ← معاينة ← كتابة دفعية) ← **`lookupByBarcode()`
نقطة استدعاء واحدة** ← **يستهلكها الجميع** (الجرد · بنود GRN · كل مستند مخزني).

### 12.4 المُنجَز (`8474f9c`)
- `excelSchema.js`: أعمدة عملك الحقيقية (من قالب الجرد الحيّ) + `normalizeBarcode` + `splitMulti` + `detectHeaderRow` + `resolveHeaderCell`.
- `excelImport.js`: اكتشاف صفّ العناوين في أول 10 صفوف · `raw:false` (وإلا وصل الباركود `8.05969e+12`) · **تكرار SKU = دمج باركودات لا خطأ**.
- `itemService.js` (وُسّع في مكانه): `barcodes[]` · **`lookupByBarcode()`** · `getItem` · `upsertItems` دفعية بـ`merge` (تُضمّ الباركودات ولا تُمحى) · `normalizeUnit` («قطعة» ⇒ `piece`).
- `services/items/itemsImportService.js`: تحليل ← **معاينة الفروق** ← كتابة.

**🚨 خللان صامتان أُصلحا:** (أ) «كود» **مُحتواة داخل «الباركود»**، فمطابقة الاحتواء كانت تُسند عمود الباركود إلى SKU ⇒ كل الباركودات تُستورد أكوادًا، والأصناف بلا باركود، والماسح لا يطابق شيئًا. الحل: **حدود الكلمات + أطول مرادف**. (ب) `createItem` كان **يدهس الصنف الموجود ويُصفّر رصيده** رغم ادّعاء تعليقه العكس.

**التحقّق:** بناء أخضر · لينت نظيف · **46/46 اختبار**.

### 12.6 القالب القياسي — المسودّة المعتمدة (`35c133a`→`55ee5e5`)

**الملف:** `public/templates/Brandzo-Items-Template.xlsx` — **مولَّد من المخطّط**
(`scripts/build-items-template.mjs`) فلا ينحرف الملف عمّا يقرأه النظام.
**ملف واحد · ثلاث أوراق** (قرار المالك: الفصل يمنع دهس الأرصدة عند تصحيح التعريف):

| الورقة | الأعمدة | تتغيّر |
|---|---|---|
| **Items** (15) | Item Code (حاوية أودو، فارغة) · Item Description★ · Bar Code · Bar Code-Code · Purchase/Sell Price · UoM Code/Name · Department→Section→Family→Sub-Family · المورد · **Min Stock** · **Status** | نادرًا |
| **Balances** (10) | Bar Code/Code · Description · **Warehouse★** · Location · **Batch** · **Expiry** · Qty★ · **Unit Cost** · Count Date | يوميًّا |
| **تعليمات** | شرح كل عمود + 7 قواعد | — |

**قرارات تصميم محورية:**
- **الكود ليس المعرّف:** «حاوية تُملأ من أودو» ⇒ لو كان المعرّف لانكسر كل مستند يوم وصلت أكواد أودو. المعرّف داخلي ثابت، والباركود مفتاح العمل، والحارس: **لا كود ولا باركود ⇒ رفض**.
- **رصيد الصفر مقبول** (خلافًا للسجلّات): يعني «نفد»، وحذفه يُبقي رصيدًا كاذبًا.
- **الحالة الفارغة = نشط** (وكل مجهول): الإيقاف قرار صريح لا استنتاج — وإلا أوقف شيتٌ ناقص العمود الكتالوجَ كلّه.
- **Unit Cost في الأرصدة لا التعريف:** تكلفة التشغيلة ≠ سعر الشراء اليوم — أساس تقييم المخزون و S12.
- **قبول `Purchese Price` بخطئها:** ملفات المالك الحقيقية تحملها.

### 12.5 المتبقّي
| # | البند |
|---|---|
| **I-أ** | **واجهة الاستيراد** (اختيار ملف ← أعمدة مكتشفة ← معاينة وأخطاء ← تأكيد) + عمود الباركود والبحث به في شاشة الأصناف + **إزالة الوعد الكاذب** «يتحدث تلقائياً عبر سندات الاستلام/الصرف» من `ItemForm.jsx` |
| **I-ب** | **ربط بنود GRN بالباركود** — مسح/كتابة ⇒ `lookupByBarcode` ⇒ تعبئة تلقائية (طلب المالك الأساسي) |
| **I-ج** | **ماسح موحّد**: `BarcodeDetector` أولًا و`html5-qrcode` احتياطًا. **⚠️ اليوم شاشة الجرد لا تعمل على آيفون/سفاري إطلاقًا** |
| **I-د** | **الأصناف المعلّقة** — مجموعة + شاشة مراجعة يعتمدها المدير فتدخل الماستر |
| **I-هـ** | ترحيل شاشة الجرد لتقرأ الماستر السحابي بدل `localStorage` |
| **I-و** | قواعد أمان: `Items_Master` تبقى `isManager` · إضافة `Items_Pending` |

---

---

## 13. خطة الهيكل والتوظيف — أربعة محاور (المالك 2026-07-16) 🧑‍💼

> **المصدران الرسميان (على جهاز المالك، خارج المستودع):**
> `BrandZo HUB/تقرير-هيكل-الوظائف-والادوار (1).html` (هيكل 18 وظيفة — بيانات JS منظّمة)
> · `BrandZo HUB/توثيق_تشغيلي___مرجع_تنفيذي_رسمي.html` (248KB — الدورة الـ12 والنماذج الـ21)

**قرارات المالك:** السيرة داخل قاعدة البيانات ≤700KB (بلا ترقية Storage) · الوصول: المدير العام + مدير المستودع · الكتالوج ثابت مولَّد من ملف الهيكل · العملة دينار ليبي.

| المحور | المحتوى | الحالة |
|---|---|---|
| **1 — التوظيف الذكي** | كتالوج 18 وظيفة مولَّد آليًّا (`build-jobs-catalog.mjs` — أعد تشغيله عند تحديث ملف الهيكل) · `candidatesService` (خط أنابيب جديد←فرز←مقابلة←قبول/رفض بسبب إلزامي + تدقيق append-only + السيرة في مستند فرعي كي لا تسحبها القائمة) · شاشة `/dashboard/recruitment` · شريط على الصفحة القديمة · قواعد `candidates` (قراءةً وكتابةً للمديرَين — حتى القراءة) | ✅ **حيّ** — نُشرت قواعد `candidates` وصدر قرار المالك بشروط المرشحين (23.07) |
| **2 — مطابقة المسارات** | ✅ (16.07) تدقيق كشف انحرافات حقيقية وصُحّحت: الدورة كانت **8 مراحل لا 12** (المالية غائبة) · **GRN كان مسمًّى «إذن التسليم»** خطأً · «إدارة الحوكمة» لا وجود لها في الهيكل (الصحيح حينها: التوثيق والاعتماد J01 — وسُمّي منذ 01.08 «قسم الحوكمة المستندية») · أدوار خارج الهيكل في erp-workflows وُحّدت على الكتالوج · «19 نموذجًا» صُحّحت 21 · أُضيف جدول الدورة الكاملة والحُرّاس الستة وروابط متبادلة | ✅ مبني |
| **3 — ضمّ المرجعَين** | ✅ (16.07) `المرجع-التشغيلي-الرسمي.html` + `تقرير-هيكل-الوظائف-والادوار.html` في `public/` + بطاقتان في مركز التقارير + رابطان في القائمة | ✅ مبني |
| **4 — تحقّق وتوثيق** | تجربة المالك ← وثّق ← احفظ ← 7 | 🔴 بعد الرفع |

**⚠ ملاحظة ترقيم للمراجعة:** المرجع التشغيلي يرقّم 09–11 (مطابقة ← فاتورة ← قيد يومية) بينما §3 هنا يرقّم (جرد ← تسويات ← مطابقة). اعتُمد ترقيم المرجع في الصفحات (بطلب المالك «طابِق المرجع»)، وأُدرج الجرد والتسويات عمليتَي رقابة مستمرة — لا تناقض مع الحُرّاس، لكن توحيد الترقيمين رسميًّا قرار مالك معلّق.

**⚠ فخّ التبعية في ملف الهيكل:** الوظائف التشغيلية تحمل `reportingTo` والمساندة تحمل حقلين (`functionalReporting`/`operationalReporting`) — المولّد يركّبهما؛ لا تفترض حقلًا واحدًا.

---

## 14. واجهة البوابة التفاعلية (2026-07-23) 🎛️

بطلب المالك — مرحلة تحسين تجربة المستخدم، منشورة حيًّا (`70ad109`).

| المحور | ما أُنجز |
|---|---|
| **الشريط الجانبي (سطح المكتب)** | وضع افتراضي **مصغّر 76px** (أيقونات) يتمدّد إلى 256px عند تمرير المؤشّر (يطفو فوق المحتوى) أو **بزرّ ☰** الذي يثبّته فيدفع المحتوى. الحالة تُحفظ في `localStorage` وتُقرأ **قبل الرسم** فلا وميض. الموبايل لم يُمسّ (كل القواعد خلف `min-width:768px`). |
| **بحث القائمة** | تطبيع عربي (أ/إ/آ→ا · ة→ه · ى→ي · حذف التشكيل) فيصيب «الاصناف» هدفَ «الأصناف» · بحث بأسماء المجموعات · إبراز المطابَق · عدّاد نتائج · زرّ مسح · Escape يمسح قبل أن يُغلق. |
| **صدارة لوحة التحكم** | قسم «🚀 الأنظمة الرئيسية» بثماني بطاقات (توظيف · مستندات · أصناف · متابعة عمليات · سلاسل إمداد · اجتماعات · مهام · هيكل)، تحمل `data-item-roles` **فترث إخفاء `RoleNav` حسب الدور تلقائيًّا**. كل الأقسام القديمة باقية أسفلها (صفر حذف). |
| **لوحة سلاسل الإمداد** | أزرار دخول مباشر: الأسطول · الصيانة · العُهد · المركبات. |
| **التوقيعان المعتمدان** | رُفعا إلى `public/identity/signatures/` (قرار المالك)، و`sigBlock` يُدرج صورة الموقّع تلقائيًّا في المحضر والخطاب عند مطابقة الاسم. |

---

## 15. نتيجة التدقيق الشامل (2026-07-23) 🔍

**السبب:** طلب المالك يقينًا أن البوابة تعمل بالكامل وأن لا شيء سقط عبر عشرات الجلسات. الفحص كان **فعليًّا على الكود** لا على الوثائق.

### الفجوات الخمس المكتشفة — وما جرى لها

| # | الفجوة | الحال |
|---|---|---|
| 1 | **خط النشر لا يُشغّل اختبارًا ولا لينت** — يبني وينشر فقط، فانحدارٌ يصل للموقع الحيّ | ✅ **سُدّت:** وظيفة `Quality Gate` تسبق البناء — `npm test` و`npm run audit` حاجزان صلبان، واللينت تقريريّ (لخطّ أساسه القائم) |
| 2 | **عشرة أدوار تفتح أي صفحة بكتابة رابطها** (القائمة تُخفي الرابط لا أكثر) | ✅ **سُدّت:** مصدر واحد للصلاحيات + حارس معمَّم — §5.2 |
| 3 | **ادّعاء توثيقيّ كاذب:** «16/16 اختبارًا لتقييد القائمة» ولا ملف اختبار موجود | ✅ **سُدّ:** `navAccess.test.js` مؤلَّف فعلًا (14 حالة) |
| 4 | **صفحة يتيمة:** `تقرير-الدورة-المستندية-الكامل` مبنيّة وغير مربوطة بأي قائمة | ✅ **رُبطت** في مركز التقارير، وسكربت التدقيق يمنع تكرار الحالة |
| 5 | **الوثائق متأخّرة 7–15 يومًا** عن الكود | ✅ **أُعيد هيكلتها:** أربعة ملفات في الجذر + `docs/` + `docs/archive/` |

### ما ثبت سليمًا
كل روابط تقارير `public/` تعمل · **`firestore.rules` تغطّي كل مجموعة يستخدمها الكود** بما فيها الفرعية (30 قاعدة) · 252 اختبارًا يمرّ · بناء 40 صفحة ينجح · الصفحات الحسّاسة الـ11 محصورة بأصحابها عبر الأدوار الـ12.

### الحارس الدائم — `npm run audit`
[`scripts/audit-portal.mjs`](scripts/audit-portal.mjs) يفحص في كل دفعة: صفحات يتيمة · روابط مكسورة · ملفات `public/` مفقودة · **مجموعات Firestore بلا قاعدة أمان** · تسريب صلاحيات. يُنهي بخطأ عند أي فشل، فيوقف النشر. (اختُبر بزرع عطبين متعمّدين فالتقطهما، ثم عاد نظيفًا بعد الاستعادة.)

### الدَّين التقني المعروف
- **لينت:** 122 مشكلة سابقة (54 خطأ + 68 تحذيرًا) يغلب عليها `no-undef` لمكتبات عالمية داخل `<script is:inline>`. أكبر المصادر: `stock-operations.astro` · `assets-inventory.astro` · `public/formUtils.js`.
- **منطق داخل صفحات Astro:** الصفحات الثلاث أعلاه تحمل منطقًا ضخمًا مضمّنًا لا يمكن اختباره — يُستخرج تدريجيًّا إلى `services/`.
- **سجلّ تدقيق موحّد** عابر للأنظمة غير منجَز (لكل نظام سجلّه اليوم).

---

*عند الشك، ثق بالترتيب: هذه الوثيقة → [`DEVELOPER_GUIDE.md`](DEVELOPER_GUIDE.md) → README الموديول → **الكود الفعلي**.*
