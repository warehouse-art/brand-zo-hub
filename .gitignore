# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# environment variables
.env
.env.production
.env.local
.env.*.local

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp

# screenshots that were previously committed at the repo root
/dashboard.png
/guide.png
/form_po_filled.png

# Claude Code local state (machine-specific permission allowlist; not shared)
.claude/settings.local.json

# Obsidian vault (personal notes; not part of the app)
.obsidian/
/*.canvas
/*.base

# agent scratch dirs (contain a nested git clone; must never be committed)
.codex/
.codex-publish-scenarios-*/

# local scratch & generated artefacts
tmp/
output/

# pasted screenshots dropped at the repo root
/Pasted image *.png
