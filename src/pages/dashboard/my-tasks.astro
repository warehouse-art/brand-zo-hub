---
/**
 * «مهامي» — شاشة تنفيذ التخزين والسحب على جهاز العامل ‹LOC-402›.
 *
 * كانت مدفونةً داخل لوحة عمالة الشحن والتفريغ رغم أنّها **مصمّمةٌ للهاتف
 * أوّلًا**. فأُخرجت إلى رابطها: العامل يفتحها مباشرةً، ويثبّتها على شاشة
 * هاتفه (البوّابة PWA)، فتصير تطبيقًا بضغطةٍ واحدة.
 */
import DashboardLayout from '../../layouts/DashboardLayout.astro';
// ⚠️ لا تُحذف: قواعد الثيم كلُّها تحت `.o_theme`، والغلافُ على جذر المكوّن
// نفسه — فبلا الملفّين أو بلا الغلاف تسقط الأنماط. يحرسهما `npm run audit` (٦).
import '../../styles/tokens.css';
import '../../styles/odoo.css';
import MyTasks from '../../components/brandzo-erp/labor/MyTasks.jsx';
---

<DashboardLayout title="مهامي" showGuide={false} light>
  <MyTasks client:load />
</DashboardLayout>
