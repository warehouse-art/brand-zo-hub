---
/**
 * طلب الفرع ‹FNB-302 · FNB-303› — ثاني شاشات قطاع الأغذية.
 *
 * المقترحُ يُبنى من حركات الفرع ورصيده وما في الطريق، والفرع يعدّله بسببٍ
 * مقيَّد، ثمّ يصير **طلب نقلٍ (TR)** — نفس مستند الطلب القائم لا مستندٌ ثانٍ.
 */
import DashboardLayout from '../../layouts/DashboardLayout.astro';
// ⚠️ لا تُحذف: المكوّن يرسم داخل `.o_theme`، وبلا هذين الملفّين تسقط الأنماط.
import '../../styles/tokens.css';
import '../../styles/odoo.css';
import BranchOrder from '../../components/brandzo-erp/fnb/BranchOrder.jsx';
---

<DashboardLayout title="طلب الفرع" light>
  <BranchOrder client:load />
</DashboardLayout>
