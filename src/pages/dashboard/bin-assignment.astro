---
/**
 * إسناد الأصناف إلى مواقعها ‹LOC› — قالبٌ يُرفع فيُعرف أين كلّ صنف.
 *
 * تقرأ ورقة الأرصدة بالمستورد القائم نفسه — لا مخزنَ ثانٍ ولا منطقَ حفظٍ
 * جديد — لكن **بعين الموقع**: رفٌّ لا وجود له يُوقف الاعتماد قبل أن يصير
 * رصيدًا لا يجده أحد.
 */
import DashboardLayout from '../../layouts/DashboardLayout.astro';
// ⚠️ لا تُحذف: قواعد الثيم كلُّها تحت `.o_theme`، والغلافُ على جذر المكوّن
// نفسه — فبلا الملفّين أو بلا الغلاف تسقط الأنماط. يحرسهما `npm run audit` (٦).
import '../../styles/tokens.css';
import '../../styles/odoo.css';
import BinAssignment from '../../components/brandzo-erp/locations/BinAssignment.jsx';
---

<DashboardLayout title="إسناد الأصناف إلى المواقع" light>
  <BinAssignment client:load />
</DashboardLayout>
