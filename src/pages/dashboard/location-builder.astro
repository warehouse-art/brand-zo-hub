---
/**
 * بانية مواقع التخزين — «نظام إضافة أماكن أوتوماتيك» (طلب المالك 2026-08-24).
 *
 * تصف المدى مرّةً فيُولَّد الكاملُ دفعةً: مستودعٌ بعشرة ممرّاتٍ وخمسة رفوفٍ
 * وأربعة مستوياتٍ واثني عشر صندوقًا = ٢٤٠٠ موقعٍ كانت تُكتب بالقلم.
 */
import DashboardLayout from '../../layouts/DashboardLayout.astro';
// ⚠️ لا تُحذف: قواعد الثيم كلُّها تحت `.o_theme`، والغلافُ على جذر المكوّن
// نفسه — فبلا الملفّين أو بلا الغلاف تسقط الأنماط. يحرسهما `npm run audit` (٦).
import '../../styles/tokens.css';
import '../../styles/odoo.css';
import LocationBuilder from '../../components/brandzo-erp/locations/LocationBuilder.jsx';
---

<DashboardLayout title="بانية مواقع التخزين" light>
  <LocationBuilder client:load />
</DashboardLayout>
