---
/**
 * دورة الإنتاج ‹FNB-502 · FNB-503 · FNB-504› — ثالث شاشات قطاع الأغذية.
 *
 * الوصفة تقول ما يُصرف، وما صُرف يقول ما كان يُنتَج، والمنتَج يُقسَّم على
 * الفروع بالتناسب. ثلاثةُ مقاطعَ لدورةٍ واحدة — لا ثلاث شاشات.
 */
import DashboardLayout from '../../layouts/DashboardLayout.astro';
// ⚠️ لا تُحذف: المكوّن يرسم داخل `.o_theme`، وبلا هذين الملفّين تسقط الأنماط.
import '../../styles/tokens.css';
import '../../styles/odoo.css';
import ProductionCockpit from '../../components/brandzo-erp/fnb/ProductionCockpit.jsx';
---

<DashboardLayout title="دورة الإنتاج" light>
  <ProductionCockpit client:load />
</DashboardLayout>
