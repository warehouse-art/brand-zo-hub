---
/**
 * برج القطاع ‹FNB-801…804 · FNB-703 · FNB-704› — رابعُ شاشات قطاع الأغذية.
 *
 * نزولٌ خماسيّ في الاستثناءات، وطلبٌ مجمَّعٌ بستّة أوجه، وإعادةُ توازنٍ
 * بقيودها الثلاثة، وإغلاقٌ يوميٌّ بعناصره العشرة — وحدُّ البوابة عند المال.
 */
import DashboardLayout from '../../layouts/DashboardLayout.astro';
// ⚠️ لا تُحذف: قواعد الثيم كلُّها تحت `.o_theme`، والغلافُ على جذر المكوّن
// نفسه — فبلا الملفّين أو بلا الغلاف تسقط الأنماط. يحرسهما `npm run audit` (٦).
import '../../styles/tokens.css';
import '../../styles/odoo.css';
import SectorTower from '../../components/brandzo-erp/fnb/SectorTower.jsx';
---

<DashboardLayout title="برج القطاع" light>
  <SectorTower client:load />
</DashboardLayout>
