---
/**
 * خطّة السحب — «نرفق الأكواد فيظهر مكانه» (طلب المالك 2026-08-24).
 *
 * توصل `pickPlan` المبنيّ المهجور: تخصيصٌ بـFEFO، وموقعٌ لكلّ بند، ومسارٌ
 * مرتَّبٌ بالمشي الفعليّ متى توفّرت شبكة المسافات — وبالكود حين لا تتوفّر.
 */
import DashboardLayout from '../../layouts/DashboardLayout.astro';
// ⚠️ لا تُحذف: المكوّن يرسم داخل `.o_theme`، وبلا هذين الملفّين تسقط الأنماط.
import '../../styles/tokens.css';
import '../../styles/odoo.css';
import PickPlanScreen from '../../components/brandzo-erp/locations/PickPlanScreen.jsx';
---

<DashboardLayout title="خطّة السحب" light>
  <PickPlanScreen client:load />
</DashboardLayout>
