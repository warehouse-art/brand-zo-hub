---
/**
 * أقسام شاشات غرفة القرار — المحتوى وحده بلا غلاف.
 *
 * فُصل عن `ReviewPage` كي يُستهلك مرتين: الشاشة المفردة (غلاف + أقسام
 * شاشتها)، و«العرض الكامل» `/executive-review/present` الذي يجمع أقسام
 * المحطات التسع كلها شرائحَ في عرض واحد (ملاحظة المالك 2026-08-07:
 * الوصول لكل محتويات العرض دون الخروج منه).
 */
import { STATUS_BOARD, KPI_CARDS, ACHIEVEMENTS, OPEN_FILES, DECISIONS, AFTER_MEETING } from '../../data/executive-review/board.js';
import { UNIFIED_CHAIN, NO_DOC_NO_MOVE, HOLDING_ROLE, POLICIES, SUPPLIERS, EFFICIENCY, CYCLE_TEMPLATE } from '../../data/executive-review/model.js';
import { SITE_155, SITE_133 } from '../../data/executive-review/sites.js';
import { CATALOG, CATALOG_NOTE } from '../../data/executive-review/catalog.js';
import { SALES_CYCLE, SECTORS, SECTOR_QUESTIONS } from '../../data/executive-review/sectors.js';
import { GOVERNANCE_PRINCIPLE, OFFICE_RULES, OFFICES, EXCLUDED_OFFICES, GOVERNANCE_MATRIX_COLUMNS, GOVERNANCE_MATRIX } from '../../data/executive-review/governance.js';
import { DIGITAL_CAPABILITIES } from '../../data/executive-review/digitalCapabilities.js';
import MeetingDecisionBoard from './MeetingDecisionBoard.jsx';

const { section = '' } = Astro.props;
// العرض الكامل يطلب قسمًا واحدًا في كل محطة. إبقاء `overview` افتراضيًا مع
// وجود `section` كان يحقن أقسام المنظور التنفيذي في كل محطة (110 شرائح).
const view = Astro.props.view ?? (section ? '' : 'overview');
// `amber` هي حالة البيانات الفعلية في board.js — تُبقى `yellow` احتياطًا.
const stateLabel = { green: 'مستقر', amber: 'يحتاج متابعة', yellow: 'يحتاج متابعة', red: 'يتطلب تدخلًا', 'on-track': 'تقدم مستمر', 'at-risk': 'معرض للخطر', blocked: 'متوقف' };

// روابط وصور تسبقها `base` — البوابة تُنشر تحت `/brand-zo-hub`.
const base = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL.slice(0, -1)
  : import.meta.env.BASE_URL;
const img155 = (src) => `${base}/executive-review/155/${src}`;
const drawing133 = (src) => `${base}/executive-review/133/${src}`;
const restaurants = SECTORS.find((item) => item.key === 'restaurants');
---

{view === 'overview' && <>
    <section class="hero-grid">
      <article class="hero-copy"><p class="eyebrow">قرار الإدارة العامة</p><h2>سلاسل الإمداد منظومة تخطيط وتحكم وتنفيذ وقياس، وليست مجرد إدارة مخازن.</h2><p>يعرض هذا المسار ما أُنجز، ما يحتاج قرارًا، ومن يملك التنفيذ والمتابعة والإغلاق.</p></article>
      <article class="decision-summary"><span>القرارات المطلوب اعتمادها</span><strong>{DECISIONS.length}</strong><p>قرارًا محددًا بمالك الاعتماد والأثر عند التأجيل.</p><a href={`${base}/dashboard/executive-review/decisions`}>فتح سجل القرارات ←</a></article>
    </section>
    <section class="section"><div class="section-head"><div><p>لوحة الحالة</p><h2>الصورة الفورية</h2></div><button type="button" class="focus-toggle no-print" data-focus-toggle="overview-status">عرض بطاقة واحدة</button></div><div class="status-grid" data-focus-grid="overview-status">{STATUS_BOARD.map((item) => <article class:list={['status-card', item.status]}><i></i><h3>{item.label}</h3><p>{item.note}</p><small>{stateLabel[item.status]}</small></article>)}</div><div class="focus-controls no-print" data-focus-controls="overview-status" hidden><button type="button" data-focus-prev>السابق</button><b data-focus-counter>1 / {STATUS_BOARD.length}</b><button type="button" data-focus-next>التالي</button></div></section>
    <section class="kpi-grid">{KPI_CARDS.map((item) => <article><strong>{item.value}</strong><span>{item.label}</span><small>{item.hint}</small></article>)}</section>
    <section class="section two"><article class="panel"><p class="eyebrow">القاعدة الحاكمة</p><h2>لا مستند = لا حركة</h2><p>كل احتياج يمر بطلب ومراجعة واعتماد وتخطيط وتنفيذ وتوثيق ومطابقة وإغلاق قابل للقياس.</p><a href={`${base}/dashboard/executive-review/model`}>عرض النموذج التشغيلي ←</a></article><article class="panel accent"><p class="eyebrow">المواقع</p><h2>تحوير وتجهيز تشغيلي للمخازن</h2><p>ملف 155 يعرض السعة المتاحة وخطة التحوير والأولويات التنفيذية؛ لا يتضمن طلب ميزانية صيانة.</p><a href={`${base}/dashboard/executive-review/sites`}>عرض المواقع والمخازن ←</a></article></section>
  </>}

  {(view === 'status' || ['status', 'achievements', 'open-files'].includes(section)) && <>
    {section === 'status' && <section class="section"><div class="section-head"><p>المشهد التنفيذي</p><h2>الحالة الحالية والإنجازات</h2></div><div class="status-grid">{STATUS_BOARD.map((item) => <article class:list={['status-card', item.status]}><i></i><h3>{item.label}</h3><p>{item.note}</p><small>{stateLabel[item.status]}</small></article>)}</div></section>}
    {(view === 'status' || section === 'achievements') &&
    <section class="section"><div class="section-head"><p>الإنجازات</p><h2>ما تم بناؤه وتشغيله</h2></div><div class="card-grid">{ACHIEVEMENTS.map((item) => <article class="detail-card"><h3>{item.title}</h3><p>{item.done}</p><div><b>النتيجة:</b> {item.result}</div><div><b>القيمة:</b> {item.value}</div></article>)}</div></section>
    }
    {(view === 'status' || section === 'open-files') &&
    <section class="section"><div class="section-head"><p>الملفات المفتوحة</p><h2>الحالة والخطوة التالية</h2></div><div class="table-wrap"><table><thead><tr><th>الملف</th><th>الحالة</th><th>الوضع</th><th>الخطوة التالية</th></tr></thead><tbody>{OPEN_FILES.map((item) => <tr><td>{item.name}</td><td><span class:list={['pill', item.state]}>{item.stateLabel}</span></td><td>{item.detail}</td><td>{item.next}</td></tr>)}</tbody></table></div></section>
    }
  </>}

  {(view === 'model' || section.startsWith('model-')) && <>
    {(view === 'model' || section === 'model-chain') && <section class="section"><div class="section-head"><p>One Supply Chain Operating Model</p><h2>الدورة الموحدة</h2></div><ol class="chain">{UNIFIED_CHAIN.map((item, index) => <li><span>{index + 1}</span>{item}</li>)}</ol></section>}
    {(view === 'model' || section === 'model-operating') && <section class="section two"><div class="section-head"><p>الإطار الحاكم</p><h2>النموذج التشغيلي الموحد</h2></div><article class="panel"><p class="eyebrow">دور الشركة القابضة</p><h2 class="statement">{HOLDING_ROLE.statement}</h2><ul>{HOLDING_ROLE.functions.map((item) => <li><b>{item.name}:</b> {item.role}</li>)}</ul><small class="foot">لكل وظيفة: {HOLDING_ROLE.perFunction.join(' · ')}</small></article><article class="panel accent"><p class="eyebrow">ضابط البوابة</p><h2>لا مستند = لا حركة</h2><ul>{NO_DOC_NO_MOVE.map((item) => <li><b>{item.rule}</b><br />{item.how}</li>)}</ul></article></section>}
    {(view === 'model' || section === 'model-policies') && <section class="section"><div class="section-head"><p>السياسات</p><h2>خمس سياسات تشغيلية</h2></div><div class="card-grid">{POLICIES.map((item) => <article class="detail-card"><h3>{item.name}</h3><p>{item.scope}</p><p><b>الدورة:</b> {item.cycle.join(' ← ')}</p><small>{item.kpi}</small></article>)}</div></section>}
    {view === 'model' && <section class="section two"><article class="panel"><p class="eyebrow">الموردون وعمليات الشراء</p><h2 class="statement">{SUPPLIERS.role}</h2><div class="table-wrap"><table><thead><tr><th>الحلقة</th><th>المسؤول</th><th>المستند/المخرج</th></tr></thead><tbody>{SUPPLIERS.cycle.map((item) => <tr><td><b>{item.step}</b></td><td>{item.who}</td><td>{item.doc}</td></tr>)}</tbody></table></div><small class="foot">{SUPPLIERS.evaluation}</small></article><article class="panel"><p class="eyebrow">روافع الكفاءة</p><h2>تطوير قابل للقياس</h2><ul>{EFFICIENCY.map((item) => <li><b>{item.lever}:</b> {item.how}</li>)}</ul></article></section>}
    {(view === 'model' || section === 'model-template') && <section class="section"><div class="section-head"><p>قالب الدورة</p><h2>أي وظيفة تُبنى كدورة مكتملة</h2></div><div class="template-grid">{CYCLE_TEMPLATE.map((item) => <article><b>{item.field}</b><span>{item.hint}</span></article>)}</div></section>}
  </>}

  {(view === 'sites' || section.startsWith('sites-')) && <>
    {view === 'sites' && <section class="section two"><article class="site-card"><p class="eyebrow">{SITE_155.code}</p><h2>{SITE_155.name}</h2><p>{SITE_155.headline}</p><div class="identity">{SITE_155.identity.map((item) => <div><b>{item.k}</b><span>{item.v}</span></div>)}</div></article><article class="site-card"><p class="eyebrow">{SITE_133.code}</p><h2>{SITE_133.name}</h2><p>{SITE_133.headline}</p><div class="identity">{SITE_133.identity.map((item) => <div><b>{item.k}</b><span>{item.v}</span></div>)}</div></article></section>}
    {(view === 'sites' || section === 'sites-masterplan') && <section class="section"><div class="section-head"><p>موقع 155</p><h2>المخطط التنفيذي للتحوير والتجهيز</h2><span class="note">هذا عرض لأولويات التنفيذ والتخصيص، وليس طلب ميزانية صيانة.</span></div><p class="lead">{SITE_155.occupancySummary}</p><div class="occupancy">{SITE_155.occupancy.map((item) => <article><header><b>{item.id}</b><span>{item.area.toLocaleString()} م²</span></header><div class="bar"><i style={`width:${item.used}%`}></i></div><p>{item.used}% مشغول</p><small>{item.holder}</small></article>)}</div></section>}
    {(view === 'sites' || section === 'sites-gallery') && <section class="section"><div class="section-head"><p>المخططات والصور</p><h2>الموقع كما صممه المهندسون وكما هو اليوم</h2><span class="note">صور 155 من المخططات والجرد الميداني، ووثائق 133 هي ملفات DWG الأصلية المسلّمة.</span></div><div class="gallery">{SITE_155.images.map((item) => <figure><img src={img155(item.src)} alt={item.caption} loading="lazy" /><figcaption>{item.caption}</figcaption></figure>)}</div><div class="drawing-grid">{SITE_133.drawings.map((item) => <a href={drawing133(item.file)} download><span>DWG</span><b>{item.title}</b><small>{item.detail}</small><em>تنزيل الوثيقة الأصلية</em></a>)}</div></section>}
    {view === 'sites' && <section class="section two"><article class="panel"><p class="eyebrow">مبنى التبريد والتجميد</p><h2 class="statement">{SITE_155.cooling.summary}</h2><div class="rooms">{SITE_155.cooling.rooms.map((item) => <div><b>{item.room}</b><span>{item.spec}</span></div>)}</div></article><article class="panel"><p class="eyebrow">أنماط المخازن الجافة</p><h2>ثلاثة أنماط تخزين</h2><div class="rooms">{SITE_155.storeTypes.map((item) => <div><b>{item.type}</b><span>{item.count} · {item.unit}</span></div>)}</div><small class="foot">{SITE_155.reportStatus}</small></article></section>}
    {(view === 'sites' || section === 'sites-requirements') && <section class="section"><div class="section-head"><p>مراحل التنفيذ</p><h2>الأولويات التشغيلية للمخازن</h2></div><div class="card-grid">{SITE_155.requirements.map((item, index) => <article class="detail-card"><span class="number">{index + 1}</span><h3>{item.family}</h3><p>{item.items}</p></article>)}</div></section>}
    {view === 'sites' && <section class="section two"><article class="panel"><p class="eyebrow">التحديات المفتوحة — 155</p><ul>{SITE_155.challenges.map((item) => <li>{item}</li>)}</ul></article><article class="panel accent"><p class="eyebrow">قرارات مطلوبة — 155</p><ul>{SITE_155.decisions.map((item) => <li>{item}</li>)}</ul></article></section>}
    {view === 'sites' && <section class="section two"><article class="panel"><p class="eyebrow">الرحبة 133 — الوضع التشغيلي</p><h2>19 غرفة تخزين تشمل ثلاجة الرحبة</h2><ul>{SITE_133.operations.map((item) => <li>{item}</li>)}</ul></article><article class="panel accent"><p class="eyebrow">الرحبة 133 — التشغيل والمتابعة</p><ul>{SITE_133.needs.map((item) => <li>{item}</li>)}</ul><h3>قرارات مطلوبة</h3><ul>{SITE_133.decisions.map((item) => <li>{item}</li>)}</ul></article></section>}
  </>}

  {(view === 'catalog' || section === 'catalog') && <>
    <section class="section"><div class="section-head"><p>الاحتياجات</p><h2>كتالوج الاحتياجات التشغيلي</h2></div><p class="lead">{CATALOG_NOTE}</p>{CATALOG.map((group) => <article class="catalog"><header><h3>{group.section}</h3><p>{group.intro}</p></header><div class="table-wrap"><table><thead><tr><th>الاحتياج</th><th>الأولوية</th><th>الغرض التشغيلي</th><th>الكمية والموقع</th><th>الحالة</th></tr></thead><tbody>{group.items.map((item) => <tr><td><b>{item.need}</b><br /><small>{item.role}</small></td><td><span class:list={['priority', item.priority?.toLowerCase()]}>{item.priority}</span></td><td>{item.purpose}<br /><small>{item.reason}</small></td><td>{item.qty}<br /><small>{item.site}</small></td><td>{item.status}<br /><small>الاعتماد: {item.approval}</small><br /><small>التوفير: {item.supply}</small></td></tr>)}</tbody></table></div></article>)}</section>
  </>}

  {(view === 'sectors' || ['sectors-sales', 'restaurants-report'].includes(section)) && <>
    {section === 'restaurants-report' && <section class="section"><div class="section-head"><p>BFP-SCM-REQ-2026-006</p><h2>خطة المطاعم والمقاهي</h2></div><div class="restaurant-slide"><article class="panel accent"><span class:list={['pill', restaurants.status]}>{restaurants.statusLabel}</span><h3>{restaurants.name}</h3><p class="statement">{restaurants.model}</p><ul>{restaurants.facts.map((fact) => <li>{fact}</li>)}</ul></article><article class="panel"><p class="eyebrow">الدورة التشغيلية</p><ol class="mini-cycle">{restaurants.cycle.map((step) => <li>{step}</li>)}</ol><p><b>المؤشرات:</b> {restaurants.kpis}</p><small class="foot">{restaurants.pending}</small><a class="sector-link" href={`${base}/dashboard/executive-review/restaurants`}>فتح التقرير التفصيلي للمطاعم والمقاهي ←</a></article></div></section>}
    {(view === 'sectors' || section === 'sectors-sales') && <>
    <section class="section"><div class="section-head"><p>إدارة المبيعات · قطاع التوزيع · تنسيق العمليات</p><h2>{SALES_CYCLE.status}</h2></div><p class="lead">{SALES_CYCLE.goal}</p><ol class="chain">{SALES_CYCLE.chain.map((item) => <li title={item.role}><span>{item.code}</span>{item.name}</li>)}</ol><div class="table-wrap"><table><thead><tr><th>المستند</th><th>دوره في الدورة</th><th>مقابله في أودو</th></tr></thead><tbody>{SALES_CYCLE.chain.map((item) => <tr><td><b>{item.code}</b> · {item.name}</td><td>{item.role}</td><td>{item.odoo}</td></tr>)}</tbody></table></div></section>
    {view === 'sectors' && <>
    <section class="section"><div class="section-head"><p>العلاقة بين الأطراف</p><h2>من المبيعات حتى أودو — Workflow واحد</h2></div><div class="template-grid">{SALES_CYCLE.parties.map((item) => <article><b>{item.party}</b><span>{item.link}</span></article>)}</div></section>
    <section class="section two"><article class="panel"><p class="eyebrow">مكونات المنظومة الست</p><ul>{SALES_CYCLE.components.map((item) => <li>{item}</li>)}</ul></article><article class="panel accent"><p class="eyebrow">البعد الاستثماري</p><p class="statement">{SALES_CYCLE.investment}</p></article></section>
    <section class="section"><div class="section-head"><p>مراحل التنفيذ</p><h2>أربع مراحل بعد اعتماد الجدولة</h2></div><div class="card-grid four">{SALES_CYCLE.phases.map((item) => <article class="detail-card"><h3>{item.phase}</h3><p>{item.scope}</p><small>{item.out}</small></article>)}</div></section>
    <section class="section"><div class="section-head"><p>القطاعات</p><h2>خمسة قطاعات متصلة بسلاسل الإمداد</h2></div><div class="card-grid">{SECTORS.map((item) => <article class="detail-card"><span class:list={['pill', item.status]}>{item.statusLabel}</span><h3>{item.name}</h3><p>{item.model}</p><ul>{item.facts.map((fact) => <li>{fact}</li>)}</ul>{item.cycle.length > 0 && <ol class="mini-cycle">{item.cycle.map((step) => <li>{step}</li>)}</ol>}{item.kpis && <p><b>المؤشرات:</b> {item.kpis}</p>}<small>{item.pending}</small>{item.key === 'restaurants' && <a class="sector-link" href={`${base}/dashboard/executive-review/restaurants`}>فتح الخطة التفصيلية للمطاعم والمقاهي ←</a>}</article>)}</div></section>
    <section class="section"><div class="section-head"><p>أسئلة القرار</p><h2>قبل بناء أي دورة قطاعية</h2></div><div class="template-grid">{SECTOR_QUESTIONS.map((question, index) => <article><b>{index + 1}</b><span>{question}</span></article>)}</div></section></>}
    </>}
  </>}

  {(view === 'governance' || section.startsWith('governance-')) && <>
    {(view === 'governance' || section === 'governance-principle') && <section class="section"><div class="section-head"><p>المبدأ الحاكم</p><h2>{GOVERNANCE_PRINCIPLE.headline}</h2></div><div class="template-grid">{GOVERNANCE_PRINCIPLE.separations.map((item) => <article><b>{item.k}</b><span>{item.v}</span></article>)}</div></section>}
    {(view === 'governance' || section === 'governance-offices') && <section class="section"><div class="section-head"><p>المكاتب المساندة</p><h2>حدود الاختصاص والتوجيه</h2></div><div class="card-grid">{OFFICES.map((office) => <article class="detail-card"><h3>{office.name}</h3><p><b>التبعية:</b> {office.parent}</p><p>{office.scRole}</p><p><b>المهام:</b> {office.tasks.join(' · ')}</p><small>{office.conflictGuard}</small></article>)}</div><p class="exclusions"><b>مستثنى من نموذج المكاتب التشغيلية:</b> {EXCLUDED_OFFICES.join(' · ')}</p></section>}
    {(view === 'governance' || section === 'governance-matrix') && <section class="section"><div class="section-head"><p>مصفوفة الحوكمة</p><h2>من يطلب، من ينفذ، ومن يغلق</h2></div><div class="table-wrap"><table class="wide"><thead><tr>{GOVERNANCE_MATRIX_COLUMNS.map((column) => <th>{column}</th>)}</tr></thead><tbody>{GOVERNANCE_MATRIX.map((row) => <tr><td>{row.dept}</td><td>{row.parent}</td><td>{row.needFrom}</td><td>{row.requests}</td><td>{row.directs}</td><td>{row.executes}</td><td>{row.approves}</td><td>{row.escalates}</td><td>{row.audits}</td><td>{row.follows}</td><td>{row.closes}</td><td>{row.where}</td></tr>)}</tbody></table></div></section>}
    {(view === 'governance' || section === 'governance-rules') && <section class="section"><div class="section-head"><p>القواعد العامة</p><h2>ضوابط العمل داخل المواقع</h2></div><ol class="rules">{OFFICE_RULES.map((rule) => <li>{rule}</li>)}</ol></section>}
  </>}

  {(view === 'digital' || section.startsWith('digital-')) && <>
    {(view === 'digital' || section === 'digital-hero') && <section class="hero-grid"><article class="hero-copy"><p class="eyebrow">اعتماد تشغيلي</p><h2>{DIGITAL_CAPABILITIES.subtitle}</h2><p>{DIGITAL_CAPABILITIES.principle}</p></article><article class="decision-summary"><span>{DIGITAL_CAPABILITIES.classification}</span><strong>$400</strong><p>تكلفة تشغيلية رقمية ثابتة شهريًا للنموذجين.</p><a href="#digital-decision">عرض قرار الاعتماد ←</a></article></section>}
    {view === 'digital' && <section class="kpi-grid">{DIGITAL_CAPABILITIES.financials.map((item) => <article><strong>{item.value}</strong><span>{item.label}</span></article>)}</section>}
    {(view === 'digital' || section === 'digital-outcomes') && <section class="section"><div class="section-head"><p>القيمة الإنتاجية</p><h2>أين تعمل القدرات الرقمية</h2></div><div class="card-grid">{DIGITAL_CAPABILITIES.outcomes.map((item) => <article class="detail-card"><h3>{item.title}</h3><p>{item.detail}</p></article>)}</div></section>}
    {view === 'digital' && <section class="section two"><article class="panel"><p class="eyebrow">Claude</p><h2>{DIGITAL_CAPABILITIES.models[0].role}</h2><ul>{DIGITAL_CAPABILITIES.models[0].outputs.map((item) => <li>{item}</li>)}</ul></article><article class="panel accent"><p class="eyebrow">Codex</p><h2>{DIGITAL_CAPABILITIES.models[1].role}</h2><ul>{DIGITAL_CAPABILITIES.models[1].outputs.map((item) => <li>{item}</li>)}</ul></article></section>}
    {(view === 'digital' || section === 'digital-controls') && <section class="section"><div class="section-head"><p>ضوابط الاعتماد</p><h2>استمرارية دون تجاوز للصلاحيات</h2></div><ol class="rules">{DIGITAL_CAPABILITIES.controls.map((item) => <li>{item}</li>)}</ol></section>}
    {(view === 'digital' || section === 'digital-decision') && <section id="digital-decision" class="decision-card"><p>قرار مطلوب</p><h2>{DIGITAL_CAPABILITIES.decision.title}</h2><b>{DIGITAL_CAPABILITIES.decision.ask}</b><div><span><strong>عند الاعتماد:</strong> {DIGITAL_CAPABILITIES.decision.ifYes}</span><span><strong>عند التأجيل:</strong> {DIGITAL_CAPABILITIES.decision.ifNo}</span></div><small>مالك القرار: {DIGITAL_CAPABILITIES.decision.owner}</small></section>}
  </>}

  {(view === 'decisions' || section.startsWith('decisions-')) && <>
    {(view === 'decisions' || section === 'decisions-board') && <section class="section decision-session-slide"><MeetingDecisionBoard decisions={DECISIONS} client:load /></section>}
    {(view === 'decisions' || section === 'decisions-after') && <section class="section"><div class="section-head"><p>ما بعد الاجتماع</p><h2>التنفيذ والمتابعة والإغلاق</h2></div><div class="table-wrap"><table><thead><tr><th>المالك</th><th>الإجراء</th><th>الجهات</th><th>الموعد</th><th>المتابعة</th><th>مخرج الإغلاق</th></tr></thead><tbody>{AFTER_MEETING.map((item) => <tr><td>{item.owner}</td><td>{item.action}</td><td>{item.party}</td><td>{item.due}</td><td>{item.follow}</td><td>{item.close}</td></tr>)}</tbody></table></div></section>}
  </>}

<script>
  document.querySelectorAll('[data-focus-toggle]').forEach((toggle) => {
    const key = toggle.dataset.focusToggle;
    const grid = document.querySelector(`[data-focus-grid="${key}"]`);
    const controls = document.querySelector(`[data-focus-controls="${key}"]`);
    const cards = Array.from(grid?.children || []);
    let current = 0;

    function paint() {
      cards.forEach((card, index) => card.classList.toggle('focus-current', index === current));
      const counter = controls?.querySelector('[data-focus-counter]');
      if (counter) counter.textContent = `${current + 1} / ${cards.length}`;
    }

    function closeFocus() {
      grid?.classList.remove('focus-mode');
      controls?.setAttribute('hidden', '');
      toggle.textContent = 'عرض بطاقة واحدة';
      cards.forEach((card) => card.classList.remove('focus-current'));
    }

    toggle.addEventListener('click', () => {
      const opening = !grid?.classList.contains('focus-mode');
      if (!opening) return closeFocus();
      grid?.classList.add('focus-mode');
      controls?.removeAttribute('hidden');
      toggle.textContent = 'عرض كل البطاقات';
      current = 0;
      paint();
    });
    controls?.querySelector('[data-focus-prev]')?.addEventListener('click', () => { current = Math.max(0, current - 1); paint(); });
    controls?.querySelector('[data-focus-next]')?.addEventListener('click', () => { current = Math.min(cards.length - 1, current + 1); paint(); });
  });
</script>

<style>
  /* ═══ هوية البوابة الفاتحة حصرًا (ملاحظة المالك 2026-08-07): كل الألوان من
     توكِنز global.css — سطح ورقي + تمييز بنفسجي الهوية + دلالات الحالة فقط ═══ */
  .section{margin-top:26px}
  .section-head{display:flex;align-items:end;justify-content:space-between;gap:16px;flex-wrap:wrap;margin-bottom:14px}
  .section-head>div>p{margin-bottom:4px}
  .section-head p,.eyebrow{margin:0;color:var(--color-accent);font-size:12px;font-weight:900;letter-spacing:.02em}
  .section-head h2{margin:0;font-size:clamp(20px,2.2vw,30px);color:var(--color-ink)}
  .section-head .note{font-size:12px;color:var(--color-muted)}
  .hero-grid,.two{display:grid;grid-template-columns:1.6fr 1fr;gap:14px}
  .hero-copy,.decision-summary,.panel,.site-card,.detail-card,.status-card,.risk-card,.catalog,.decision-card,.decision-grid article{background:var(--color-surface);border:1px solid var(--color-line);border-radius:var(--radius-card);padding:20px;box-shadow:var(--shadow-card)}
  .hero-copy{background:var(--color-chip)}
  .hero-copy h2{margin:8px 0;font-size:clamp(22px,2.6vw,34px);line-height:1.45;color:var(--color-ink)}
  .hero-copy p:last-child{color:var(--color-ink-2);line-height:2}
  .decision-summary{display:flex;flex-direction:column;justify-content:center}
  .decision-summary span{font-size:13px;color:var(--color-muted);font-weight:800}
  .decision-summary strong{font-size:clamp(42px,6vw,74px);color:var(--color-accent);line-height:1.15}
  .decision-summary p{color:var(--color-ink-2);line-height:1.8}
  .decision-summary a,.panel a{color:var(--color-accent);font-weight:900;text-decoration:none}
  .status-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}
  .focus-toggle,.focus-controls button{border:1px solid var(--color-accent);background:var(--color-surface);color:var(--color-accent);border-radius:var(--radius-chip);padding:8px 13px;font:inherit;font-size:12px;font-weight:800;cursor:pointer}
  .focus-grid{min-height:0}
  [data-focus-grid].focus-mode{display:block}
  [data-focus-grid].focus-mode>article{display:none;min-height:260px;padding:clamp(22px,4vw,46px)}
  [data-focus-grid].focus-mode>article.focus-current{display:block;animation:focus-in .18s ease-out}
  [data-focus-grid].focus-mode>article h3{font-size:clamp(22px,3vw,34px)}
  [data-focus-grid].focus-mode>article p{font-size:clamp(15px,1.8vw,20px);line-height:2}
  .focus-controls{display:flex;align-items:center;justify-content:center;gap:14px;margin-top:12px}
  .focus-controls[hidden]{display:none}
  .focus-controls b{min-width:72px;text-align:center;color:var(--color-ink)}
  @keyframes focus-in{from{opacity:.35;transform:translateY(5px)}to{opacity:1;transform:none}}
  .status-card{position:relative;padding-top:17px}
  .status-card i{position:absolute;top:0;right:20px;width:48px;height:4px;border-radius:0 0 6px 6px;background:var(--color-muted)}
  .status-card.green i{background:var(--color-success)}
  .status-card.amber i,.status-card.yellow i{background:var(--color-warning)}
  .status-card.red i{background:var(--color-danger)}
  .status-card h3{margin:8px 0;font-size:15px;color:var(--color-ink)}
  .status-card p{min-height:50px;margin:0;color:var(--color-ink-2);font-size:13px;line-height:1.75}
  .status-card small{font-weight:900;color:var(--color-muted)}
  .kpi-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-top:18px}
  .kpi-grid article{background:var(--color-surface);border:1px solid var(--color-line);border-radius:var(--radius-card);box-shadow:var(--shadow-card);padding:16px}
  .kpi-grid strong{font-size:28px;display:block;color:var(--color-accent)}
  .kpi-grid span{font-size:13px;font-weight:800;display:block;margin-top:5px;color:var(--color-ink)}
  .kpi-grid small{display:block;color:var(--color-muted);margin-top:7px;font-size:11px}
  .panel h2{font-size:21px;margin:8px 0;line-height:1.5;color:var(--color-ink)}
  .panel p,.panel li{color:var(--color-ink-2);line-height:1.9}
  .panel h3{color:var(--color-ink);font-size:15px;margin:14px 0 6px}
  .panel ul,.detail-card ul{padding-right:20px}
  .accent{border-color:var(--color-accent);background:var(--color-chip)}
  .card-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}
  .card-grid.four{grid-template-columns:repeat(4,1fr)}
  .detail-card{position:relative}
  .detail-card h3,.risk-card h3{font-size:16px;margin:0 0 10px;color:var(--color-ink)}
  .detail-card p,.risk-card p{font-size:13px;line-height:1.8;color:var(--color-ink-2)}
  .detail-card div{border-top:1px solid var(--color-line-strong);padding-top:10px;font-size:12px;line-height:1.8;color:var(--color-ink-2)}
  .detail-card small,.risk-card small{display:block;color:var(--color-muted);font-size:11px;line-height:1.8}
  .risk-card{border-inline-start:4px solid var(--color-warning)}
  .risk-card.risk,.risk-card.blocker{border-inline-start-color:var(--color-danger)}
  .risk-card.overlap{border-inline-start-color:var(--color-warning)}
  .risk-card.support{border-inline-start-color:var(--color-muted)}
  .risk-card b{color:var(--color-ink)}
  .table-wrap{overflow:auto;border:1px solid var(--color-line-strong);border-radius:var(--radius-card);background:var(--color-surface)}
  table{width:100%;border-collapse:collapse;min-width:740px;font-size:12px}
  th{background:var(--color-surface-2);text-align:right;padding:12px;color:var(--color-ink);white-space:nowrap}
  td{padding:12px;vertical-align:top;border-top:1px solid var(--color-line-strong);color:var(--color-ink-2);line-height:1.75}
  .pill,.priority{display:inline-block;padding:4px 8px;border-radius:var(--radius-chip);background:var(--color-surface-2);font-size:11px;font-weight:900;color:var(--color-ink-2)}
  .pill.on-track,.pill.green,.pill.agreed,.pill.scheduled{color:var(--color-success);background:rgba(46,158,91,.10)}
  .pill.at-risk,.pill.yellow,.pill.follow-up,.pill.pending{color:#8a6410;background:rgba(224,168,60,.14)}
  .pill.blocked,.pill.red,.pill.decision{color:var(--color-danger);background:rgba(196,30,58,.09)}
  .priority.p1{color:var(--color-danger);background:rgba(196,30,58,.09)}
  .priority.p2{color:#8a6410;background:rgba(224,168,60,.14)}
  .priority.p3{color:var(--color-ink-2);background:var(--color-surface-2)}
  .chain{display:flex;flex-wrap:wrap;gap:10px;list-style:none;padding:0;margin:0 0 14px}
  .chain li{display:flex;align-items:center;gap:8px;background:var(--color-surface);border:1px solid var(--color-line-strong);padding:10px 13px;border-radius:10px;font-size:13px;font-weight:800;color:var(--color-ink)}
  .chain span{display:grid;place-items:center;background:var(--color-accent);color:#fff;min-width:22px;height:22px;border-radius:var(--radius-chip);font-size:10px;padding:0 5px}
  .template-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}
  .template-grid article{display:grid;gap:7px;background:var(--color-surface);border:1px solid var(--color-line);border-radius:var(--radius-card);box-shadow:var(--shadow-card);padding:14px}
  .template-grid b{color:var(--color-accent);font-size:13px}
  .template-grid span{color:var(--color-ink-2);font-size:12px;line-height:1.7}
  .site-card h2{margin:8px 0;font-size:24px;color:var(--color-ink)}
  .site-card>p:not(.eyebrow){color:var(--color-ink-2);line-height:1.9}
  .identity{display:grid;gap:8px;margin-top:14px}
  .identity div{display:grid;grid-template-columns:130px 1fr;gap:12px;border-top:1px solid var(--color-line-strong);padding-top:8px;font-size:12px}
  .identity b{color:var(--color-ink)}
  .identity span{color:var(--color-ink-2);line-height:1.8}
  .lead{color:var(--color-ink-2);line-height:2;background:var(--color-surface);border-right:4px solid var(--color-accent);padding:13px 16px;border-radius:10px;box-shadow:var(--shadow-card)}
  .occupancy{display:grid;grid-template-columns:repeat(5,1fr);gap:10px}
  .occupancy article{background:var(--color-surface);border:1px solid var(--color-line);border-radius:var(--radius-card);box-shadow:var(--shadow-card);padding:12px}
  .occupancy header{display:flex;justify-content:space-between;gap:6px;font-size:12px}
  .occupancy header b{color:var(--color-accent)}
  .occupancy header span{color:var(--color-ink-2)}
  .occupancy .bar{height:8px;background:var(--color-surface-2);border-radius:var(--radius-chip);margin:12px 0 7px;overflow:hidden}
  .occupancy i{display:block;height:100%;background:var(--color-warning)}
  .occupancy p{margin:0;font-size:11px;font-weight:900;color:var(--color-ink)}
  .occupancy small{display:block;color:var(--color-muted);font-size:10px;line-height:1.6;margin-top:6px}
  .number{float:left;font-size:20px;font-weight:900;color:var(--color-accent);opacity:.55}
  .catalog{margin-top:14px;padding:0;overflow:hidden}
  .catalog header{padding:16px 20px;background:var(--color-surface-2)}
  .catalog h3{margin:0;font-size:18px;color:var(--color-ink)}
  .catalog p{margin:7px 0 0;color:var(--color-ink-2);line-height:1.8;font-size:13px}
  .catalog .table-wrap{border:0;border-radius:0}
  .wide{min-width:1500px}
  .rules{display:grid;gap:8px;padding-right:22px}
  .rules li{background:var(--color-surface);border:1px solid var(--color-line);border-radius:10px;padding:11px 14px;color:var(--color-ink-2);line-height:1.8;font-size:13px;box-shadow:var(--shadow-card)}
  .exclusions{font-size:13px;color:var(--color-muted);margin-top:12px}
  .decision-card{background:var(--color-chip);border-color:var(--color-accent)}
  .decision-card>p{color:var(--color-accent);font-weight:900;font-size:12px;margin:0}
  .decision-card h2{margin:7px 0;font-size:24px;color:var(--color-ink)}
  .decision-card>b{color:var(--color-ink);line-height:1.9}
  .decision-card>div{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:16px 0}
  .decision-card span{padding:12px;border-radius:10px;background:var(--color-surface);border:1px solid var(--color-line);color:var(--color-ink-2);line-height:1.8;font-size:13px;display:block}
  .decision-card small{color:var(--color-muted)}
  .decision-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}
  .decision-grid article{position:relative}
  .decision-grid article>span{position:absolute;top:15px;left:15px;color:var(--color-accent);opacity:.55;font-size:22px;font-weight:900}
  .decision-grid h3{margin:0 0 12px 34px;font-size:16px;color:var(--color-ink)}
  .decision-grid p{font-size:12px;color:var(--color-ink-2);line-height:1.8}
  .decision-grid .yes{background:rgba(46,158,91,.08);padding:9px;border-radius:8px}
  .decision-grid .no{background:rgba(196,30,58,.07);padding:9px;border-radius:8px}
  .decision-grid small{color:var(--color-muted);line-height:1.8;font-size:11px}
  /* ─── مكوّنات مراجعة الدمج (بتوكِنز البوابة) ─── */
  .statement{font-size:15px !important;line-height:1.9 !important;font-weight:700}
  .foot{display:block;margin-top:12px;color:var(--color-muted);font-size:11px;line-height:1.8;border-top:1px solid var(--color-line-strong);padding-top:10px}
  .gallery{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}
  .gallery figure{margin:0;background:var(--color-surface);border:1px solid var(--color-line);border-radius:var(--radius-card);box-shadow:var(--shadow-card);overflow:hidden}
  .gallery img{display:block;width:100%;height:190px;object-fit:cover;background:var(--color-surface-2)}
  .gallery figcaption{padding:10px 13px;font-size:12px;font-weight:800;color:var(--color-ink-2);line-height:1.7}
  .drawing-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-top:14px}
  .drawing-grid a{display:grid;gap:7px;background:var(--color-surface);border:1px solid var(--color-line);border-radius:var(--radius-card);padding:18px;text-decoration:none;box-shadow:var(--shadow-card)}
  .drawing-grid span{width:max-content;padding:4px 9px;border-radius:var(--radius-chip);background:var(--color-chip);color:var(--color-accent);font-weight:900;font-size:11px}
  .drawing-grid b{color:var(--color-ink)}.drawing-grid small{color:var(--color-muted)}.drawing-grid em{color:var(--color-accent);font-style:normal;font-size:12px;font-weight:900}
  .sector-link{display:block;margin-top:12px;color:var(--color-accent);font-size:12px;font-weight:900;text-decoration:none}
  .restaurant-slide{display:grid;grid-template-columns:1fr 1.2fr;gap:14px}
  .rooms{display:grid;gap:7px;margin-top:12px}
  .rooms div{display:flex;justify-content:space-between;gap:10px;border-top:1px solid var(--color-line-strong);padding-top:7px;font-size:12px}
  .rooms b{color:var(--color-ink);white-space:nowrap}.rooms span{color:var(--color-ink-2);text-align:left}
  .mini-cycle{padding-right:18px;margin:9px 0}.mini-cycle li{font-size:12px;color:var(--color-ink-2);line-height:1.8}
  .panel .table-wrap{margin-top:10px}.panel table{min-width:0;font-size:11px}
  @media(max-width:1000px){.status-grid,.card-grid,.decision-grid,.card-grid.four{grid-template-columns:repeat(2,1fr)}.occupancy,.gallery{grid-template-columns:repeat(3,1fr)}}
  @media(max-width:680px){.hero-grid,.two,.status-grid,.kpi-grid,.card-grid,.card-grid.four,.template-grid,.decision-grid,.decision-card>div,.gallery,.drawing-grid,.restaurant-slide{grid-template-columns:1fr}.kpi-grid{gap:8px}.occupancy{grid-template-columns:repeat(2,1fr)}.identity div{grid-template-columns:1fr}.section-head{align-items:start;flex-direction:column;gap:5px}}
</style>
