# brandzo_warehouse — تقرير الموقف التشغيلي والبرمجي

> موديول Odoo 19 يجسّد الدورة المستندية اللوجستية لـ Brandzo (12 مرحلة / 21 نموذجاً) وقواعدها
> الذهبية كحُرّاس صارمين. هذا الملف **تقرير موقف للمراجعة** — يوثّق ما أُنجز، وما تبقّى، وبروتوكول
> العمل، والاكتشافات المعمارية. الخطة المرجعية الكاملة في
> [`ODOO_BACKEND_DEVELOPMENT_PLAN.md`](../../ODOO_BACKEND_DEVELOPMENT_PLAN.md) بجذر المستودع.

**الحالة:** S1→S6 + S5a + S5b + **S12** — الدورة المستندية كاملة **12/12 مرحلة**: القواعد الذهبية + المرتجعات/الإتلاف/الإشعار الدائن + الجرد الدوري وحارس التسويات + **الإغلاق المالي وقفل الفترة**. المتبقي: التعميقات الاختيارية فقط (ASN صريح · Bin Card).
**المقاييس:** 37 ملفاً · 11 كائناً (توسعة 7 قياسية + 4 نماذج مخصصة) · 7 مجموعات أمان · 52 حالة اختبار في 11 ملفاً · سكربت تحقّق ساكن `verify_module.py` (107 عناصر).

---

## 1) بروتوكول العمل والرفع — «Brandzo: نطوّر ثم نرفع»

نعمل ونطوّر طوال الجلسة، وعند قول **«ارفع»** أو **«خلصنا»** تُنفَّذ الخطوات الخمس تلقائياً بالترتيب:

| # | الخطوة | الأمر / الإجراء |
|---|---|---|
| 1 | مزامنة الفرع | `git checkout main` ثم `git pull origin main` |
| 2 | حارس البناء (CI) | `npm run build` + `npm run lint` — **لا رفع لكود مكسور**؛ يُصلَح أو يُنبَّه |
| 3 | التزام حفظي | رسالة واضحة، مع **استثناء النظافة**: `.env` · `node_modules/` · `.claude/settings.local.json` |
| 4 | الدفع المباشر | `git push origin main` (مباشرة إلى الرئيسي — لا PR في السير المعتاد) |
| 5 | رقابة النشر | متابعة GitHub Pages والإبلاغ بالنتيجة + الرابط الحيّ |

**بيئة النشر ــ GitHub Pages:** الفرع `main` يُنشَر تلقائياً عبر `.github/workflows/astro.yml`.
- الموقع الحيّ: `https://warehouse-art.github.io/brand-zo-hub/`

**بيانات المستودع:**
- المستودع: **`warehouse-art/brand-zo-hub`** (المرجع المعتمد)
- Remote (HTTPS): `https://github.com/warehouse-art/brand-zo-hub.git`
- هوية الالتزام: `albarshi996 <albarshi.96@gmail.com>`

> ملاحظة: موديول Odoo (Python/XML) خارج نطاق `astro build`/`eslint`؛ حارس البناء يغطّي واجهة Astro
> فقط. أخطاء الـ lint الحالية (49) **سابقة** لعملنا في ملفات `.astro` لم نلمسها — لا يسببها الموديول.

---

## 2) خريطة المنجز (Done) — مربوطة بالمراحل الـ12 والنماذج الـ21

### المراحل الـ12 — حالة التنفيذ

| # | المرحلة | الكائن · نقطة الربط | الحارس | النماذج (21) | الحالة |
|---|---|---|---|---|---|
| 01 | طلب الشراء PR | `purchase.requisition` · `action_confirm` | حوكمة الاعتماد | 1 | ✅ S1 |
| 02 | أمر الشراء PO | `purchase.order` · `button_confirm:625` | ربط بالاعتماد | 2 | ✅ S1 |
| 03 | إشعار الشحن ASN | `stock.picking` (وارد، قياسي) | — | 3 | 🟡 قياسي |
| 04 | الاستلام والجودة GRN+QC | `stock.picking` · `button_validate:1399` | 🥇 **الجودة (QC)** | 4، 5 | ✅ S2 |
| 05 | التخزين الموجّه Putaway | `stock.putaway.rule` (قياسي) | — | 6، 7 | 🟡 قياسي |
| 06 | التحضير Picking | `stock.picking` (صادر) + `product_expiry` | 🥈 **FEFO** | 9 | ✅ S3 |
| 07 | الشحن Dispatch | `bz.gate.pass` + `stock.picking` | 🥉 **تصريح البوابة** | 11، 12 | ✅ S4 |
| 08 | المرتجعات والتالف | `stock.picking` (`return_id:566`) · `stock.scrap` · `account.move` (`in_refund`) | حوكمة الإرجاع/الإتلاف | 14، 15، 17 | ✅ S5a |
| 09 | الجرد الدوري | `bz.cycle.count` يلفّ `stock.quant` | مصادقة العدّ الكامل | 18 | ✅ S5b |
| 10 | التسويات المخزنية | `stock.quant` · `_apply_inventory:996` + `action_apply_inventory:433` | 🔒 **اعتماد المدير المالي** | 16 | ✅ S5b |
| 11 | المطابقة الثلاثية | `account.move` · `_post:5526` | 🏅 **3-Way Match** | — | ✅ S6 |
| 12 | الإغلاق المالي | `bz.period.close` + `res.company` lock dates · `account.move._post` | 🔐 **قفل الفترة** (قائمة تحقّق مشتقّة) | — | ✅ S12 |

**دلالة الرموز:** ✅ مبرمَج ومنشور/جاهز · 🟡 يعتمد على Odoo القياسي دون حارس مخصص · 🔴 لم يُبرمَج. **الدورة الأساسية مكتملة 12/12.**

### القواعد الذهبية الأربع — كلها مُنفَّذة (طبقتان: تفاعلية + `@api.constrains` صارمة)

| # | القاعدة | نقطة الربط المؤكَّدة | الأثر عند الخرق |
|---|---|---|---|
| 🥇 | لا Done قبل الجودة | `stock.picking.button_validate` + constrains | `UserError`/`ValidationError` |
| 🥈 | FEFO إلزامي عند الصرف | `_bz_check_fefo` فوق `product_expiry` (`removal_date`) | `UserError` يمنع التلاعب بالتواريخ |
| 🥉 | لا خروج بلا تصريح | `bz.gate.pass.state == approved` + constrains | `UserError`/`ValidationError` |
| 🏅 | المطابقة الثلاثية | `account.move._post` (PO ↔ GRN ↔ Bill) | `UserError` يمنع الترحيل |

> بالإضافة إلى **حارس حوكمة خامس** (اعتماد طلب الشراء قبل التأكيد)، و**حارس سادس — التسويات** (S5b): «لا يُعدَّل أي رصيد يدوياً بدون سند تسوية معتمد من المدير المالي» — طبقتان على `stock.quant`: `action_apply_inventory` (مدخل الواجهة) + `_apply_inventory` مشروطاً بـ`_is_inventory_mode()` مع استثناء المسارات البرمجية (`from_inverse_qty` · نداءات بلا `inventory_mode` · superuser للتثبيت/الاستيراد) — انظر توثيق فخّ R1 في `models/stock_quant.py`.
>
> و**حارس سابع — الإغلاق المالي** (S12): «لا تُغلق فترة وبنودها ناقصة» — قائمة تحقّق **مشتقّة** من ٦ بنود (فواتير مرحّلة · دفعات مسجّلة · إشعارات دائنة مرحّلة · جرد مُصادَق · تسوية مطبَّقة · مرتجعات مقفلة)، تُقرأ بـ`sudo` من الحالة الفعلية للفترة؛ وبعد الإغلاق يمنع `account.move._post` ترحيل أي قيد بتاريخ داخلها (يتسلسل مع حارس المطابقة الثلاثية عبر `super()`)، ويُطبَّق قفل فترة على `res.company` **دفاعيًّا** عبر اكتشاف حقل القفل المتوفّر (تغيّرت أسماؤه بين إصدارات أودو).

### النماذج الـ21 — التغطية الفعلية

| منجز فعلياً (حارس/حقول مخصصة) | يعتمد على Odoo القياسي | متبقٍّ | خارج النطاق (قياسي/يدوي) |
|---|---|---|---|
| 1 طلب الشراء · 2 أمر الشراء · 4 GRN · 5 تقرير الجودة · 9 قائمة السحب · 11 إذن التسليم · 12 تصريح البوابة · 14 إشعار الإرجاع · 15 بضاعة تالفة · 16 سند تسوية · 17 إشعار دائن · 18 ورقة جرد | 3 ASN · 6 أمر التخزين · 7 بطاقة الموقع · 8 طلب الصرف · 10 قائمة التعبئة · 13 إذن النقل | — (اكتملت نماذج المراحل المبرمَجة) | 19 بطاقة الصنف · 20 اجتماع يومي · 21 قائمة أسبوعية |

### البنية المُنجَزة
- **الكائنات (11):** توسعة `purchase.requisition` · `purchase.order` · `stock.picking.type` · `stock.picking` · `stock.scrap` · `stock.quant` · `account.move` + نماذج مخصصة `bz.gate.pass` · `bz.cycle.count` · `bz.cycle.count.line` · `bz.period.close`.
- **الأمان (7 مجموعات):** مستخدم المستودع · مدير المشتريات · مفتش الجودة · ضابط البوابة · مسؤول المرتجعات والتالف · مدقق الجرد · المدير المالي + `ir.model.access.csv` (الإغلاق: قراءة للجميع، وإنشاء/إغلاق للمدير المالي وحده).
- **الواجهات:** statusbar وشارات ملوّنة · تبويب الجودة · زر إحصائي وشريط تنبيه للتصريح · لوحة المطابقة الثلاثية · نموذج جلسة الجرد ببنود عدّ قابلة للتحرير وفوارق ملوّنة · **نموذج الإغلاق بقائمة تحقّق HTML حيّة وشارة الجاهزية** · قوائم Brandzo (شراء/شحن/مرتجعات/جرد/مالية+إغلاق).
- **البيانات:** تسلسلات `GP-%(year)s-####` · `CC-%(year)s-####` · `PC-%(year)s-####` (الإغلاق) · `post_init_hook` لترحيل السجلات التاريخية.
- **الاختبارات:** 52 حالة `TransactionCase`/`AccountTestInvoicingCommon` في 11 ملفاً تُثبّت كل حارس — منها **10 لـS12** (الإغلاق محجوب ببند ناقص → مسموح عند اكتماله → لا ترحيل في فترة مقفلة) — وتُثبّت أيضاً سلامة المسارات البرمجية الأربعة لـ`_apply_inventory` (فخّ R1).

---

## 3) خريطة المتبقي (Pending)

### 🟢 المراحل الأساسية مكتملة 12/12 — المتبقّي تعميقات اختيارية فقط
- **05 التخزين الموجّه (تعميق اختياري):** بطاقة موقع (Bin Card) مخصصة فوق قواعد Putaway القياسية.
- **03 ASN (تعميق اختياري):** نموذج إشعار شحن مسبق صريح قبل الاستلام.

### 🔧 مهام تشغيلية معلّقة
- **تشغيل الاختبارات فعلياً** في بيئة Odoo (لا مفسّر Python محلي — انظر المحور 4).
- **S8 — ربط الواجهة:** توصيل وسيط `odoo-proxy` بالنماذج الحقيقية بدل المحاكي.
- (اختياري) إصلاح دين الـ 49 lint السابق كمهمة نظافة منفصلة.

---

## 4) التقييم المعماري — اكتشافات Odoo 19 وكيف تغلّبنا عليها

| الاكتشاف (من فحص المصدر الفعلي) | الأثر | حلّنا البرمجي |
|---|---|---|
| **نسخة Community**: لا `quality`/`quality_control` (مؤسسي) | لا فحص جودة أصلي | بنينا QC كحقول (`bz_qc_state` وغيرها) + حارس مزدوج على `stock.picking` |
| لا `account_3way_match` (مؤسسي) | لا مطابقة ثلاثية أصلية | بنيناها فوق `is_purchase_matched` و`purchase_line_id` كحارس على `_post` |
| 🔴 **`stock.valuation.layer` أُزيل في v19** | تغيّر بنية التقييم | نستهدف التقييم على `stock.move` (`value`, `remaining_qty`) — مهم لتسويات S5 |
| `purchase_requisition` و`product_expiry` **موجودان** | لا داعي لإعادة الاختراع | بنينا PR وFEFO **فوقهما** (FEFO = ترتيب `removal_date, in_date, id`) |
| `qty_done` أُعيدت تسميته إلى **`quantity`** على `stock.move.line` | كسر توافق قديم | كل كودنا يستخدم `quantity` |
| توفّر Python 3.14 لكن **لا بيئة أودو حيّة** | تعذّر التثبيت/تشغيل الاختبارات فعلياً | **تحقّق ساكن** عبر `verify_module.py`: نحو كل ملفات `.py` (`compile`) · سلامة كل XML (`ElementTree`) · صحّة CSV وربطه بالنماذج · وجود كل ملفات `data` · **ترابط المراجع** (كل إجراء قائمة معرَّف · كل مجموعة عرض معرَّفة) · 19 فحصاً خاصاً بـS12 — **107 عناصر، صفر أخطاء** |

### أنماط هندسية اعتمدناها
- **حارس مزدوج الطبقة:** `button_validate`/`_post` (تفاعلي، رسالة واضحة) + `@api.constrains` (صارم لا يُتجاوز برمجياً).
- **`post_init_hook`:** يمنح السجلات التاريخية القيمة الآمنة (اعتماد/اجتياز) حتى لا تُحجَب بعد التثبيت.
- **تكامل بلا تكرار:** استهلاك عرض تواريخ الصلاحية من `product_expiry` والاكتفاء بشارة تلخيصية على مستوى المستند.
- **نقاط ربط مؤكَّدة:** `button_validate` (`stock_picking.py:1399`) · `_post` (`account_move.py:5526`) · `purchase_line_id` (`purchase/…/account_invoice.py:528`).

---

## التثبيت والاختبار (في بيئة Odoo)

```bash
# التثبيت النظيف (ضع المجلد الحاوي في addons_path)
./odoo-bin -c odoo.conf \
    --addons-path=/path/to/odoo/addons,/path/to/repo-clone/brandzo-addons \
    -d brandzo_test -i brandzo_warehouse --stop-after-init

# تشغيل اختبارات القبول (حُرّاس القواعد الذهبية)
./odoo-bin ... -u brandzo_warehouse --test-enable --stop-after-init
```

**التبعيات:** `purchase_stock` · `stock_account` · `purchase_requisition` · `product_expiry` · `account` · `barcodes` (كلها Odoo 19 Community).
