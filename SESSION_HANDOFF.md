# 🧭 ملف الجلسة الحيّ — دليل الاستئناف السريع

# SESSION HANDOFF — Live Resume File

> **ما هذا الملف؟** الذاكرة الخفيفة السريعة للمشروع. يخبرك في ثوانٍ:
> **أين توقّفنا · ماذا فعلنا · ما الخطوة التالية.** يُحدَّث مع *كل مهمة أو خطة جديدة*.
> عند اقتراب الجلسة من الامتلاء، هذا هو الملف الذي يُسلَّم لفتح جلسة جديدة.
>
> **للوثيقة المعمارية الكاملة** (القواعد الستة · المراحل الـ12 · المتبقّي التفصيلي):
> اقرأ [`ROADMAP.md`](ROADMAP.md). عند الشك: هذا الملف → ROADMAP → README الموديول → الكود.
>
> ⚠️ **هذا الملفّ تجاوز ٣٠٠ ك.ب — لا تقرأه كلّه.** للاستئناف في العمل الحالي:
> شغّل **`npm run plan`** واقرأ أولًا [`docs/plan/استئناف-نسخة-العميل.md`](docs/plan/استئناف-نسخة-العميل.md)، ثم [`docs/plan/الاستئناف.md`](docs/plan/الاستئناف.md) لعوائق المرحلة الأولى التاريخية.
> هذا الملفّ للسرد التاريخيّ؛ ونقطتا الاستئناف في [`docs/plan/client-completion.json`](docs/plan/client-completion.json) و[`docs/plan/phase-one.json`](docs/plan/phase-one.json).
> **ولقطاع الأغذية والمشروبات (F&B) خطّتُه:** `npm run plan:fnb` ← [`docs/plan/استئناف-قطاع-الأغذية.md`](docs/plan/استئناف-قطاع-الأغذية.md) · الوثيقة الحاكمة [`docs/خطة-قطاع-الأغذية-والمشروبات.md`](docs/خطة-قطاع-الأغذية-والمشروبات.md).
> الآليّة كاملةً: [`docs/plan/آلية-التنفيذ.md`](docs/plan/آلية-التنفيذ.md).
> **ولطبقة الالتقاط (صفحة الجرد) خطّتُها:** `npm run plan:capture` ← [`docs/plan/استئناف-طبقة-الالتقاط.md`](docs/plan/استئناف-طبقة-الالتقاط.md) · الوثيقة الحاكمة [`docs/خطة-طبقة-الالتقاط.md`](docs/خطة-طبقة-الالتقاط.md).

> **[Claude] 2026-08-24 — طبقة الالتقاط م١ اكتملت 7/7: الوحدة صارت جزءًا من القيد لا زينةً في العرض (CAP-101…CAP-107):** **★★ العطب الجذريّ:** القيد كان يُحفظ `{باركود · اسم · كمّيّة}` — رقمٌ بلا وحدة. فمسحُ كرتونٍ فيه ١٢ قطعة وكتابةُ «١» كانت تُخزَّن ١، وفارقُ ذلك **١١٠٠٪**. والقيد اليوم يحمل **ستّة حقول**: الباركود كما مُسح · الصنف المحلول · الكمّيّة بوحدتها · الوحدة · المعامل · والكمّيّة الأساس — و`baseQty` **تُحسب وقت الالتقاط لا وقت العرض**، فتصحيحُ معاملٍ غدًا لا يُعيد كتابة ما عُدّ أمس. **★★ وأغلب العمل كان وصلًا لا بناءً:** `unitForBarcode` مبنيٌّ ومختبَرٌ في `uomWiring.js` منذ SAP-3 ولم يستدعه تدفّق المسح قطّ. **وتبع الوحدةَ ما لا يصحّ بدونه:** المجموع من `baseQty` لا من `qty` (جمعُ «١ كرتون» و«٣ قطع» على أنّهما ٤ رقمٌ لا معنى له — صارا ١٥) · حارس الكسر بوحدة القيد والرسالة تسمّي ما كتبه العادّ · والتصحيح بوحدة الأساس بمعامل ١. **★★★ وأخطر ما أمسكه التنفيذ: `Number(null)` صفرٌ محدود** — فلو فُحصت `baseQty` بـ`isFinite` وحدها لابتلع المعاملُ المجهول الكمّيّةَ صفرًا صامتًا. تُقرأ خامّةً ويُوسم صفُّها «غير مضمون»، ولا يُصحَّح صفٌّ كذلك. **وثغرتان خارج النطاق المرسوم سُدَّتا بالجرد:** اللصق الجماعيّ كان يكتب قيودًا **بلا وحدة**، ولوحة المتابعة كانت تجمع `qty` خامًّا. **وقرارا المالك نُفّذا حرفًا:** ق-٢ — الصنف بلا وحدة **يُعدّ ويُوسم ولا يُمنع**، وغياب الرصيد لا يُنقص من الصفّ شيئًا (٩٦٧ صنفًا من ١٠٤١) · و«الباركود ليس مفتاحًا»: التصادم يُعرض خيارًا ويُختم `collision` مع `byUid` و`at`، فيُعرف **من** فصله و**متى**. **وCAP-101 نزعت الرصيد والفرق من النواة قبل الشاشة** — وحُذفت `aggregateSession` المزدوجة الميّتة. **والقاعدة التي حكمت المهامّ السبع كلّها: لا حارس يحجب العادّ على الرفّ** — كلّ حراسة وسمٌ أو قيدٌ على الإقفال والتصحيح. **البيّنة:** ٢٥١٨ اختبارًا خضراء · لِنت نظيف · بناءٌ ١٠٣ صفحة · تدقيقٌ نظيف · وحزمة `ScanFlow` بصفر ذكرٍ لـ«الدفتريّ» و«الفرق». **ولا تعديل على `firestore.rules`** — قاعدة `scans` لا تحصر الحقول فالجديدة تمرّ بلا مساس بالملحق-فقط. **⚠️ ولم تُجرَّب حيًّا** (تسجيل دخول). **التالي: م٢ — CAP-201 نطاق الجلسة.**

> **[Claude] 2026-08-23 — طبقة الالتقاط: صفحة الجرد تلتقط ولا تُحاسِب (CAP-000 · CAP-101):** المالك أعاد تعريف الشاشة معماريًّا: «**الالتقاط لا يُحاسِب** — تسجّل ما رآه الإنسان على الرفّ فقط، لا تعرف رصيدًا ولا تحسب فرقًا». فثُبّتت **الخطة السابعة** `npm run plan:capture` (٢٩ مهمّة · ٦ مراحل · ١٠ فجوات كلٌّ بمسار ملفٍّ وسطر). **★★ وأهمّ ما كشفه جرد الكود ليس فجوةً بل عكسها: طبقة المطابقة مبنيّةٌ ومختبَرةٌ وبلا مستدعٍ واحد** — `locations/reconcile.js` ← `toCountDraft` ← `schemas/cc.js` ← `chain.js:adjustmentVerdict`، فالعطب ليس نقصًا في الطبقات بل أنّ شاشة العدّ تؤدّي عمل المطابقة بطريقةٍ رديئة بينما المحرّك الصحيح معطَّل. وهذا يفسّر «رصيد ٤٧٥ يظهر صفرًا» تفسيرًا كاملًا: الشاشة **ادّعت معرفة** ما ليس من اختصاصها. **وستّة قرارات مالك حُسمت:** الـ١٣ عمليّة المفتوحة **تُلغى** (وسمًا لا محوًا — القاعدة تمنع الحذف) · الصنف بلا وحدة **يُعدّ موسومًا ولا يُمنع** ووجود رصيدٍ ليس شرطًا · النطاق **يُطلب ولا يُلزم** بقيدٍ حاكم «لا يقطع الفريق» · الرفّ على القيد **غير ضروريّ** · **الختم للمدير وحده** · و**تأجيل** المطابقة (٧٤ رصيدًا من ١٠٤١) فسُجّلت `deferred` لا محذوفة. **★ والتأجيل قلب ترتيب الخطة:** «تجريد الشاشة» كان آخر مهمّة بحجّة «لا تعمَ عن الفروقات» — وسقطت الحجّة فصعد أوّلًا. **CAP-101 نُفّذت في النواة قبل الشاشة:** `buildSessionRows` لم يعد يقرأ `item.balance` ولا يحمل الصفّ `bookQty` ولا `diff` · `sessionProgress` بلا عدّاد فروقات · `filterRows` بلا تبويبه · `exportRows` بلا عموديه ويصدّر «لم يُمسح» **«—» لا صفرًا** (الصفر يقول «عددتُ ولم أجد» وهو معنًى آخر — ف‑٩) · والجدول صار الصنف والمعدود والقيود. **وحُذفت `aggregateSession`** — نسخةٌ أقدم من `buildSessionRows` بلا مستدعٍ منذ أن حلّت محلّها، وكان بقاؤها يُبقي باب الرصيد مفتوحًا في النواة: نواةٌ واحدة تكبر لا نواتان تتباعدان. **البيّنة:** ٢٤٩٢ اختبارًا خضراء (٢٢/٢٢ في `scanFlow.test.js` وفيها حارسٌ جديد بالرقم ٤٧٥ نفسه) · لِنت نظيف على الملفّات الثلاثة · بناءٌ ١٠٣ صفحة · تدقيقٌ نظيف · وحزمة `ScanFlow` المبنيّة فيها **صفرُ ذكرٍ** لـ«الدفتريّ» و«الفرق» و«الفروقات». **⚠️ ولم تُجرَّب حيًّا** — الشاشة خلف تسجيل دخول. **التالي: CAP-102** — وصل `unitForBarcode` المهجور بـ`panelForScan` (الوحدة تُحلّ من الباركود لا تُكتب).

> **[Claude] 2026-08-21 — شاشة الجرد: رمزُ عمليةٍ من ستّة محارف، مُجرَّبٌ حيًّا بجهازين:** **العلّة:** «العمل الجماعيّ» كان مبنيًّا **وغيرَ صالحٍ للاستعمال** — الحقل والزرّ موجودان منذ زمن، لكنّ «الرمز» المطلوب هو **معرّف Firestore الخام** (عشرون محرفًا عشوائيًّا حسّاسًا لحالة الأحرف) معروضًا في حاشيةٍ بحجم عشر نقاط. لا لجنةَ جردٍ تُملي ذلك في رحبةٍ صاخبة. البناء تامٌّ والطريق مسدودٌ عند آخر متر. **الحلّ:** `H4K-9TM` — أبجديّة Crockford بلا `I`/`L`/`O`/`U`، و**تُقبل عند الإدخال وتُحوَّل** إلى ما تشبهه (كاتبُ `O` يقصد صفرًا)، ومعها الأرقام العربيّة-الهنديّة والحروف الصغيرة والشرطات، ويُهمَل ما لا يُفهَم بدل رفض السطر (لاصقُ رمزٍ من واتساب يجرّ محارف اتّجاهٍ غير مرئيّة). **والحسم منطقٌ خالصٌ مختبَر** لا استعلام (`resolveOperationByCode`): المفتوحة تسبق المُقفلة · **مفتوحتان بالرمز نفسه ⇒ توقّف** لا اختيارٌ عشوائيّ · والمُقفلة تُميَّز عن المعدومة. **★ ولا قاعدة Firestore جديدة، بالتصميم:** الرمز يُكتب **عند الإنشاء** (القاعدة تسمح بالإنشاء لكلّ فاعلٍ مخزنيّ وبالتعديل للمديرين — فلو أُجّل لَما استطاع أمينُ مخزنٍ أن يُنتج رمزًا لفريقه)، والبحث **بحقلٍ واحد** (`code` بلا `status`) فيكفيه الفهرس التلقائيّ ولا يُطلب من المالك فهرسٌ مركّب. **التجربة الحيّة على المنشور (جهازان في متصفّح المالك):** فُتحت عمليّةُ جردٍ حقيقيّة فتولّد `JWN-1MH` في بطاقةٍ ظاهرة بزرَّي نسخٍ وتغيير · التغيير بإدخالٍ فوضويّ `  h4k ٩t m  ` أنتج **`H4K-9TM`** (التطبيع مُثبَتٌ في الواقع لا في الاختبار) · جهازٌ ثانٍ بلا جلسةٍ محفوظة كتب `h4k-9tm` **فانضمّ** ووَرِث الوضع والدفتر · عدَّ صنفًا فصار الجهاز الأوّل «٢ من ١٬٠٤١» **بلا إعادة تحميل** · الإقفال أعاد الشاشة نظيفة · والرمز بعده ردَّ حرفيًّا: **«الرمز H4K-9TM صحيح، لكنّ عمليّته أُقفلت»**. وأُقفلت عمليّة التجربة ولم يُترك أثر. **وما لا يُكسر:** العمليّات المفتوحة قبل هذا لا رمز لها، فالانضمام يجرّب القصير ثمّ يقع على المعرّف الخام كما كان، والبطاقة تقول لصاحبها: «عمليةٌ فُتحت قبل الرموز — اضغط تغيير الرمز». **البوّابات:** بناء ١٠٣ صفحة · **٢٤٨٠/٢٤٨٠** · تدقيق نظيف (٧٣ قاعدة) · المتتبّعات ٦/٦ · ESLint تحذيرٌ واحدٌ معلوم. **وتأكيدٌ في الجولة نفسها:** المالك نشر `firestore.rules`، فخريطة بنغازي صارت تقرأ من السحابة بلا رسالة خطأ، والحفظ ردّ **«✓ حُفظ «وسط البلد» — يراه الجميع»** (وأُعيدت الملاحظة التي غيّرتُها للتجربة).

> **[Claude] 2026-08-21 — د٣ ود٥ بتصريح المالك بالمتصفّح: أربعة أعطابٍ حيّة أُمسكت، والمزامنة عادت تعمل وحدها:** **★★ العطب الأوّل — حارس المزامنة كان يسأل سؤالًا اتّجاهُه خاطئ:** «هل نسختنا تخالف الشقيق؟» — وهذا يصيح كلّما **تقدّم** الشقيق. فسقطت مزامنة الشركة الساعيّة، وليس عندها سطرٌ فريد؛ تحقّقتُ بجلب النسختين ومقارنتهما مطبَّعتين: الفرق **عملي أنا** لا عملُها. والسؤال الصحيح شرطان في `atRiskOfErasure`: نخالف **الأصل المشترك** *و*نخالف الشقيق. بأربعة اختبارات أوّلها يُعيد بناء مشهد السقوط بحرفه. ثمّ زُومنت الشركة يدويًّا بـ`--force` (فالأداة المعطوبة لا تجلب إصلاح نفسها)، **وعادت الساعيّة تعمل وحدها** — قِيس: الشركة تقدّمت إلى `ad85901` بلا تدخّل. **★★ الثاني — العودة إلى تبويبٍ في `erp-workflows` كانت تُدمّر مخطّطه:** الكود يمحو `data-processed` ويُعيد تشغيل mermaid، وmermaid يقرأ `textContent` — وهو بعد الرسم **ناتجُه هو** (تنسيق SVG) لا التعريف. قِيس حيًّا: ٩ عقد ← **عقدةٌ واحدة**، ونصّ العنصر يبدأ بـ`#mermaid-…`. والحفظ في `data-original-text` **كان مكتوبًا وينتظر من يستعمله** — فصار يُعاد قبل كلّ تشغيل. ويُصلح معه الطباعة (تقرأ الحفظ نفسه). **مُثبَتٌ بعد النشر:** تسع تنقّلاتٍ ذهابًا وإيابًا والمخطّط ٩ عقد و٨ أسهم كما بدأ. **★★ الثالث والرابع — في استيراد إكسل الأصول، كشفهما نقلُ المنطق إلى النواة:** (أ) **تاريخ ورقة الإدارة كان يُقرأ من عمود الملاحظات** — الورقة الرئيسة فيها «الإدارة» وأوراق الإدارات لا، فينزاح ما بعده؛ والكود يتبع الإزاحة في القسم والموقع والملاحظات **إلّا التاريخ**. (ب) **كلّ تاريخٍ مستورَدٍ يتأخّر يومًا**: `toISOString` يحوّل إلى UTC وليبيا على +2. ولا شيء يشتكي في الحالتين لأنّ الحقل نصٌّ يقبل أيّ نصّ. **والحارس الذي سبق النقل:** القسم ٧ في `npm run audit` — ميزانيّة منطقٍ داخل الوسوم تنزل ولا تصعد، و`INLINE_LOGIC_MAX` يمنع أن **تُولد** صفحةٌ سمينة. ثمّ **قلبَ نفسه عليّ**: إصلاح المخطّطات أضاف سطرين كودًا وعشرة شرحًا فأسقط التدقيق — **والعيب في المقياس**: مقياسٌ يعاقب على الشرح يُغري بحذفه. فصار يعدّ الكود ويتخطّى التعليق. **ودرسٌ في الطريق:** دفعتُ مرّةً بتدقيقٍ أحمر (خطأ ترتيب أوامر)، **فأوقفه حاجز النشر في `astro.yml` ولم يصل شيءٌ معطوب للموقع**. **والقياس صحّح الخطّة مرّتين:** ستٌّ من الصفحات «السمينة» موصولةٌ بالنواة أصلًا وما فيها رسمُ DOM؛ و**٥٤٪ من «منطق» `retail-hub` لم يكن منطقًا** بل ١٠١ حيٍّ نزلت إلى `src/data/benghazi-hub.js` بحدودٍ جغرافيّة تمنع حيًّا في البحر. **ما نزل إلى النواة:** `retail/hubModel` (١٩ اختبارًا · هندسة سداسيّة تُختبر بالعدد: أضلاعٌ متساوية وضلعٌ يساوي نصف القطر ونسبةُ الامتدادَين √3/2 · و**٧٠٧ رؤوسٍ على البذرة الحقيقيّة صفرٌ منها خارج بنغازي**) · `assets/assetsModel` (١٩ اختبارًا · خريطتا أعمدةٍ بالاسم لا بالإزاحة · ترقيمٌ يُقرأ من السجلّات فلا يتصادم بعد استيراد) · وتهريب HTML: **سبع نسخٍ متباينة** صارت مصدرًا واحدًا (`escape.Dc8NSuu9.js` قطعةٌ مشتركة في البناء). **والسحابة:** `retail_hub_overrides` — **مُثبَتٌ حيًّا**: النافذة تُفتح للمدير، والقراءة تسقط رشيقًا («تُعرض البذرة وحدها»)، والحفظ يرتدّ برسالةٍ **تسمّي السبب**: «قواعد retail_hub_overrides لم تُنشر بعد». **ود٥:** جذر `src/services` صار **صفر ملفٍّ سائب** (كان ١١) — تسعةٌ عادت لمجلّداتها واثنان كانا ميّتَين. وأفلت من مُصلِح الاستيرادات صنفان: مسارٌ نصّيّ في اختبار، و**عشرة مسارات في متتبّعات الخطط** منها ثلاثة `guardTest` كانت ستُسقط `npm run plan`. **البوّابات:** بناء ١٠٣ صفحة · **٢٤٦٦/٢٤٦٦** · تدقيق نظيف · المتتبّعات **٦/٦** (كانت ٤/٦ بلا حارس) · الهويّة سليمة · ESLint **تحذيرٌ واحد** (كان ٣٨ منها ٢٨ خطأً). **⚠️ المتبقّي وسببه:** **د٤ (توحيد قِشَر العروض) موقوفٌ بقرارٍ لا بعجز** — القِطَع **ليست متطابقة**: `DeckControls` أربع نسخٍ (٢٠٠·٢٢٤·١٨٦·٢٠٨) تتباعد في **بادئات CSS** (`nv-`·`mtg-`·`rs-`) بتطابق ٥٦–٦٤٪. فالتوحيد دمجُ أربعة سلوكيّاتٍ في عروضٍ تُقدَّم لعملاء، ويحتاج تمرير فضاء الأسماء وتحقّقًا بصريًّا للأربعة. **و`SESSION_HANDOFF.md` ما زال ٤٥٦ ك.ب.** **و٣٠ مرجع CDN** باقية (ماسح الباركود في ٢٤ نموذجًا · FontAwesome في ٤ وثائق أرشيف · Tailwind في تقرير التوظيف). **وقواعد `retail_hub_overrides` تنتظر نشر المالك.**

> **[Claude] 2026-08-21 — د٣ «المنطق يهبط إلى النواة»: البقعة العمياء صارت محروسة، وخريطة بنغازي هاجرت إلى السحابة:** **★★ حارسٌ قبل نقل:** `npm test` يمسح `*.test.js` تحت `src`، فكلّ سطرٍ داخل وسم `script` في صفحة `.astro` **خارج نطاق الاختبار بالبناء** — وحارس النقاء (القسم ٥) أعمى عنه لأنّه يعدّ الملفّات لا الوسوم. فأُضيف **القسم ٧** في `audit-portal.mjs`: ميزانيّةٌ تنزل ولا تصعد + `INLINE_LOGIC_MAX=40` يمنع أن **تُولد** صفحةٌ سمينة + دَينٌ مسمًّى صفحةً صفحة. وجُرّب بمسبارٍ من ٥٠ سطرًا فأسقط التدقيق بالسببين ثمّ عاد أخضر. **★ والقياس صحّح الخطّة قبل أن تُنفَّذ:** الظنّ أنّ الدَّين موزّعٌ على ١٥ صفحة، والواقع أنّ ستًّا منها موصولةٌ بالنواة أصلًا (`woSnapshot` · `mttrHours` · `custodySummary`…) وما فيها رسمُ DOM وهو مكانه — والدَّين الحقيقيّ في `retail-hub` و`assets-inventory` وحدهما. **التهريب:** `src/services/ui/escape.js` موجودٌ ومختبَرٌ منذ «الجراحية» ونصّه «لا نسخة محليّة»، ولم يكن يستورده إلّا **ملفٌّ واحد** بينما ستّ صفحاتٍ تُعرّف نسخها الخاصّة **وليست متطابقة**. فوُصلت الستّ بمناداةٍ تُبقي ١٥٥ موضع استدعاءٍ بلا مساس، وصار `escape.Dc8NSuu9.js` قطعةً مشتركةً واحدة في البناء. **وتصحيحٌ لظنّي:** خشيتُ ثغرةً لأنّ أربعًا لا تُهرّب المفردة، فقِستُ: **صفر سِمةٍ محاطةٍ بمفردة** تتغذّى من `esc()` — والمسار الخطر (`onclick`) يمرّ بـ`escJs` وحده. فالمكسب توحيدٌ لا سدُّ ثغرة. **★★ وأربعةٌ وخمسون بالمئة من «منطق» retail-hub لم تكن منطقًا:** ٧٣١ سطرًا هي ١٠١ حيٍّ ببياناتها، نزلت إلى `src/data/benghazi-hub.js` بستّة حرّاس — أهمّها **حدودٌ جغرافيّة**، لأنّ حيًّا بإحداثيّةٍ مقلوبة يُرسم في البحر بلا خطأٍ ولا تحذير: صورةٌ خاطئة تُقرأ حقيقةً. جُرّب بتبديل الطول بالعرض في «وسط البلد» فسقط الاختبار وسمّى الحيّ. **والمنطق الخالص** إلى `src/services/retail/hubModel.js` (١٩ اختبارًا): الوسوم · الدمج · الإحصاء · **الهندسة السداسيّة** — وهي أخطرها لأنّ خطأها يُرسم شكلًا معقولًا في غير موضعه، فتُختبر بالعدد: أضلاعٌ ستّة متساوية، وضلعٌ يساوي نصف القطر، ونسبة الامتدادين √3/2 تُثبت أنّ تصحيح خطّ الطول مطبَّقٌ حقًّا. **وحارسُ تكاملٍ على البذرة الحقيقيّة: ٧٠٧ رؤوسٍ سداسيّة، صفرٌ منها خارج بنغازي.** **والهجرة إلى السحابة (قرار المالك):** `retail_hub_overrides/{hoodId}` — معرّف الوثيقة هو معرّف الحيّ فالكتابة ساكنة، والبذرة تبقى في الكود فما في السحابة تصحيحٌ فوقها؛ ولو تعذّرت القراءة ظهرت الخريطة كاملةً لا شاشةً فارغة. **و`sanitizeOverride` يمنع `lat`/`lng` من المرور** فلا يُنقل حيٌّ إلى البحر بتحرير حقل. **وفخٌّ أُمسك:** قائمتي المسموحة كانت تُسقط `neighborhood` بينما النموذج القائم يسمح بتعديله — فكنتُ سأكسر ميزةً بصمت؛ فأُضيف واختبارٌ يشترط تطابق القائمة مع حقول النموذج. **وقرارٌ يخصّ المالك:** الكتابة صارت للمديرين (`isManager`) لأنّ تعديل المندوب كان يعيش في متصفّحه ولا يراه أحد، فلمّا صار مشتركًا وجب أن يُعرف كاتبه؛ و**زرّ «إعادة الضبط» تغيّر معناه** — كان يمسح تعديلاتي وصار يمسح تعديلات الجميع، فأُلزم بتأكيدٍ يقول ذلك حرفًا. **الأثر:** الميزانيّة ٥٣٣٨ ← ٤٥٧٦، و`retail-hub` ١٣٦٧ ← ٦١٥ فلم تعد الأثقل. **البوّابات:** بناء ١٠٣ صفحة · **2443/2443** · تدقيق نظيف (٧٣ قاعدة) · المتتبّعات ٦/٦ · ESLint تحذيرٌ واحدٌ معلوم. **⚠️ قواعد `retail_hub_overrides` تحتاج نشرًا من المالك** — وقبله تُقرأ البذرة ويرتدّ الحفظ برسالةٍ صريحة تقول السبب. **⚠️ ولم تُجرَّب بحسابٍ حقيقيّ خلف حارس الدخول؛ والتحقّق كان على الحزمة المبنيّة: `localStorage` اختفى منها تمامًا (صفر موضع) و`retail_hub_overrides` حاضر.**

> **[Claude] 2026-08-21 — ترتيب الكود، دفعتان بأمر المالك بعد فحصٍ شامل: البوّابة عادت صادقة، والميّت أُخلي:** سبق العملَ **جردٌ كامل** للشجرة (٧٤٠ ملفًّا · ١٢٩ ألف سطر) أخرج اثنتَي عشرة نتيجة، فاختار المالك «د١ ثمّ د٢» و«الكود الميّت يُزال — وgit يحفظ التاريخ». **★★ د١ — الجذر واحدٌ وأعراضه ثلاثة، وكلّها CRLF:** كان `identity.test.js` يسقط **أبدًا** على ويندوز، و`npm run identity` يُعلن كذبًا «تسرّبت هويّة الشقيق إلى موضعين»، و`git status` يُعلن **ثمانية ملفّاتٍ مولَّدة «معدَّلة»** في كلّ بناءٍ وكلّ `npm run plan`. **والبرهان أنّ الثلاثة عرَضٌ لا مرضٌ: بصمة git متطابقة** (`git hash-object f` = `git rev-parse :f`) — لا فرق بايتٍ واحد؛ فالمولّد يكتب `
` وgit يستخرج `
` مع `core.autocrlf=true`. **العلاج طبقتان:** (١) `.gitattributes` جديدٌ يثبّت `eol=lf` **للمولَّدات وحدها** (١١ نمطًا مُثبَتًا بـ`git check-attr`، منها أسماءٌ عربيّة) — **ولم تُطبَّع الشجرة كلّها (٩٣٠ ملفًّا) عمدًا**: مستودع الشركة يزامن كلّ ساعة بدمجٍ يرجّح الشقيق، فتطبيعٌ شاملٌ يجعل أوّل دمجٍ بعده تعارضًا في كلّ ملفّ. (٢) `lf()` كانت خاصّةً في `identity.js` تخدم حارس المزامنة وحده، فصارت مُصدَّرةً وتُستعمل في مقارنة الوثيقتين المولَّدتين — فالعطب ممتنعٌ حتّى على استنساخٍ بإعدادٍ آخر. **وثلاثة حرّاس:** اختبارٌ يُحاكي CRLF عمدًا، وآخرُ يسأل `git check-attr` نفسه (لا يبحث عن نصٍّ في الملفّ — فنمطٌ لا يُطابق يمرّ من فحص النصّ ويسقط من فحص الآليّة)، والاختبار الأصليّ صار يقارن بـ`lf`. **★ ولينت 38 ← 1:** والباقي تحذيرٌ واحدٌ في `WarehouseMaps.jsx` المجمَّد بقرار LOC-O04. ومن الطريق: `XLSX` مكتبةٌ عالميّةٌ من `public/lib` لا يراها المحلّل ⇒ أُعلنت مرّةً في `eslint.config.js` (١٦ خطأً كاذبًا)؛ و`erp-workflows.astro` كان **يسقط كلّه بـ«Parsing error»** فلا يُفحص منه سطرٌ من ١٢٢١ — لأنّ سهم mermaid `-->` يُغلق تعليق HTML في نظر محلّل Astro؛ فصارت الأسهم الـ٥٨ كياناتٍ `--&gt;` (**قِيس حيًّا: `textContent` يفكّها ٨·١٠·٩·١٤·١٧، والمخطّطات الخمسة تُرسم فعلًا بـ١٠·١٢·١١·١٦·١٨ عقدة من المكتبة المستضافة ذاتيًّا**). **★★ وخرقٌ لقاعدةٍ غير قابلة للتفاوض ظهر بالمصادفة:** `public/formUtils.js` كان يجلب SheetJS **من cdnjs** — فتصدير الإكسل في **٢٤ نموذجًا** يسقط بلا إنترنت والنسخة المحلّيّة بجانبه. صار يشتقّ `lib/` من عنوان وسمه نفسه (لا مسارًا نسبيًّا: الملفّ في `<base>/` والنماذج تستدعيه من `<base>/forms/`) — **تحقّق حيّ: 200 و951928 بايتًا**. **ويبقى ٣٠ مرجع CDN** (ماسح الباركود في النماذج · FontAwesome في ٤ وثائق أرشيف · Tailwind في تقرير التوظيف) — محجوبةٌ على قرار المالك. **د٢ — الإخلاء بعد تحقّقٍ عدائيّ (١٢ وكيلًا · ٤٠٤ عمليّة فحص، مهمّتهم نقضُ حكمي لا تأييده):** حُذف **عشرة ملفّات (٦٬٢٠٢ سطرًا)** و**٣٦ ملفّ وسائط (٣٥ م.ب)**. **★ والتحقّق صحّح لي رقمًا:** قلتُ «٢١ صورةً من ٢٨ بلا مرجع» والصحيح **٢٨ من ٢٨** — والسبب الأعمق أنّ المستهلك الوحيد في التاريخ (`gallery.astro`) كان **يمسح المجلّد ولا يسمّي ملفًّا**، فالبحث بالاسم عاجزٌ بنيويًّا. **ودليلان قاطعان على الموت:** `AssetsInventory.jsx`/`VehiclesInventory.jsx` **ليستا مكوّنَي React أصلًا** بل وثيقتا `<!DOCTYPE html>` بامتداد `.jsx` (لذا «Parsing error» عند 1:1) — استيرادُهما كان سيُسقط البناء؛ و`ColdStoragePlan.jsx` صفحتُه `cold-storage-plan.astro` حذفها المالك بنفسه في `c512fb9` «حذف ١٢ صفحة بأمر المالك». **والتمييز الذي وقفتُ عنده وسألت:** قرار LOC-O04 يسمّي **`WarehouseMaps.jsx` بعينه** («لا يُحيا ولا يُربط ولا يُحذف») ولا يذكر `ColdStoragePlan`؛ فأُبقي الأوّل بلا لمسة وحُذف الثاني بإذن المالك «افعل ما تراه مناسبًا». **وأُغلقت فجوةٌ في الحارس نفسه:** `npm run plan` كان يحرس **أربعة** متتبّعاتٍ من ستّة (`plan:exec` و`plan:fnb` خارجه) — فصار يحرس الستّة (كلاهما أخضر). وصُحّحت خمس حقائق في `AGENTS.md` كانت تكذب (٦٣ صفحة ← ١٠٣ · ٧٢٨ اختبارًا ← ٢٤١٨ · «خطّ اللينت أحمر أصلًا» ← نظيف · `src/config/navCatalog` ← `src/services/auth/navCatalog.js` · أربعة متتبّعات ← ستّة). **البوّابات:** بناء ١٠٣ صفحة · **2418/2418** (كانت 2415/2416) · تدقيق نظيف · المتتبّعات ٦/٦ · ESLint تحذيرٌ واحدٌ معلوم · الهويّة سليمة في ٢١ موضعًا · وشجرة العمل نظيفةٌ من ضجيج السطور. **⚠️ لم يُرفع — محجوبٌ على «ارفع». والاسترجاع إن نُدم على الوسائط: `git checkout e573e43 -- public/assets`.** **⚠️ ولم تُجرَّب الشاشات المعدَّلة بحسابٍ حقيقيّ خلف حارس الدخول** (`assets-inventory` مسّها تغييرٌ مكافئٌ برهانًا: `window.` صريحة و`hasOwnProperty.call` وكتلةُ صيدٍ مُسمّاة).

> **[Claude] 2026-08-19 — عرض اجتماع الموارد البشرية (`/dashboard/hr-meeting`): **شرائح دليل الإدارة نفسها**، خمس عشرة شريحة:** المحاور بنصّ المالك (التوظيف · الهيكل الوظيفي · سياسات التوظيف · التوثيق في البوابة · العقوبات · الترقية · الجوائز)، وكلّها موجودةٌ في `BFP-SCM-MAN-2026-053` (١٠٢ شريحة) فلم يُؤلَّف حرف. وبقرار المالك أثناء العمل — **«لا تجعلها صفحاتٍ كثيرة · ١٥ · الأساسيات فقط»** — قُصّت من ٣٠ إلى ١٥. **★ فخٌّ يستحقّ الحفظ: ترتيب الشرائح ليس أرقام ملفّاتها.** `slideN.xml` في هذا الدليل يبدأ من ١٠، والترتيب الحقيقيّ يحدّده `p:sldIdLst` (الفارق ٩ مواضع) — فخريطة الملفّ→الصفحة تُبنى من `presentation.xml.rels` لا من الأسماء. **المسار المنفَّذ:** PowerPoint COM رفض فتح الملفّ (ثلاث محاولات: مسارٌ قصير · رفع الحظر · نافذةٌ مرئيّة)، فأُثبِّت LibreOffice بإذن المالك عبر winget، ثمّ PPTX→PDF (١٠٢ صفحة · ١٫٤ م.ب) ثمّ **اقتصاص ١٥ صفحة بـpypdf → 416 ك.ب** في `public/hr-meeting/manual.pdf`. **درسان تقنيّان:** (١) قارئ PDF في المتصفّح **لا يستجيب لتغيير `#page` وحده** على المصدر نفسه — فالحلّ `key={current}` يُعيد تركيب الإطار (قِيس حيًّا: بقي على الصفحة ١ رغم `#page=10`). (٢) **رأس تعليقٍ لم يُغلق في CSS ابتلع القواعد الأساسية** — بعد تعميم `identity-meeting.css` إلى **`frame-deck.css`** (فضاء `fd-`) لعرضَي المستندات، كتبتُ رأسًا بلا `*/` فانهار الارتفاع إلى 286px؛ أمسكه القياس الحيّ لا العين. **التسجيل:** بندٌ في `navCatalog` ⇒ يظهر تلقائيًّا في **«عمليات البوابة»** (طلب المالك). **البوابات:** 2393/2394 · بناء ١٠٣ صفحة · تدقيق نظيف · ESLint نظيف. **⚠️ الاختبار الساقط ليس من هذا العمل:** `services/workspace/identity.test.js` يسقط على شجرةٍ نظيفة بلا تغييراتي — CRLF في نسخة ويندوز مقابل LF المرفوع؛ لم أمسّ `WORKSPACE.md` ولم أُصلح ملفَّ جلسةٍ أخرى بأثرٍ جانبيّ. **⚠️ ولم يُجرَّب بحسابٍ حقيقيّ خلف حارس الدخول.**

> **[Claude] 2026-08-19 — شاشة الأبعاد التنظيميّة صارت تقود بدل أن تمتحن (`/dashboard/org-dimensions`):** الشاشة كانت تسأل المستخدم عن أربعة أشياء دفعةً واحدة — **رمزٌ يخترعه بيده**، ومستوًى مجرَّد من منسدلة، وأبٌ يستدعيه من قائمة، واسم — وهو لا يعرف بعدُ ما الفرق بين قطاعٍ وبراند؛ فقال المالك «معقّدة». **الآن:** ① **شريط خطواتٍ أربع** (قطاع ثمّ براند ثمّ فرع ثمّ مركز تكلفة) دُمج مع صفّ العدّادات — لا صفٌّ يعدّ وصفٌّ يقود — والخطوة المحجوبة **تقول بم تُفتح** لا تصمت. ② **الأب يُنقر في موضعه من الشجرة لا يُستدعى من قائمة**: زرّ «+ أضف براند» على صفّ القطاع نفسه (كما يُدرَج موقعٌ تحت مستودعه)، فيتبع مكانَ النقر المستوى والأب والرمز — **ولا يبقى للمستخدم إلّا الاسم**. ③ **الرمز صار تلقائيًّا افتراضًا** لا حقلًا فارغًا: شارةٌ تعرض `BRD03` (يقفز فوق المشغول) وبابُ «أكتبه بنفسي» مفتوحٌ لمن له ترميزٌ قائم. **وشكلُه قرارُ المالك 2026-08-19: قصيرٌ مستقلّ لا يرث رمز أبيه** — فالنسب يُعرض مسارًا (`pathOf`) ولا يُحمَّل على الرمز، إذ لو حُمِّله لكذب أوّل نقلِ فرعٍ بين البراندات (والرمز لا يُغيَّر بعد أوّل حركة). ④ **ثغرتان صامتتان سُدّتا:** تحرير الرمز في التعديل كان **يشقّ الموقع نسختين** ويُيتّم أبناءه (setDoc على رمزٍ جديد) — فصار الرمز في التعديل شارةً ثابتة؛ وتصحيحُ اسم موقعٍ معطَّل كان **يُعيد تفعيله بصمت** — فصارت الحالة تُصان. **المنطق خالصٌ مختبَر** في `orgLocations.js` (`pathOf` · `childLevelOf` · `orgSetupSteps` · `newChildDraft`)، و`ready` مشتقٌّ من `parentChoices` نفسها فلا تقول خطوةٌ «جاهزة» ثمّ تُفتح على قائمةٍ فارغة. ⑤ **وقالبُ التعبئة صار يُنزَّل من الشاشة** (زرّ «نزّل قالب الإكسل»): مصنّفٌ بورقتين — **«الشجرة» تُملأ** بعناوين مخطّط المستورِد نفسه، و**«تعليمات»** فيها ثماني قواعدَ ومثالٌ عاملٌ من خمسة صفوف. **ولمَ ورقتان؟** لأنّ مثالًا داخل ورقة التعبئة يُستورد بيانًا حقيقيًّا إن نُسي حذفه — والمستورِد يقرأ الورقة الأولى، فتبقى نظيفةً **بالبناء لا بالتذكير**. والمحتوى **مشتقٌّ من `ORG_LEVELS`** فلو زيد مستوًى تبعه القالب. **★★ وحارسه الرحلةُ كاملةً** (`excelTemplate.test.js` — أوّل اختبارٍ يكتب xlsx حقيقيًّا ويقرؤه): قالبٌ ← ملفّ ← `importSheet` ← `planOrgImport` مقبولة، فيستحيل أن نُسلّم قالبًا يرفضه مستورِدُنا (وهو عطبٌ وارد: يكفي أن يُغيَّر `labelAr` لعمودٍ بلا مرادف). ⑥ **ورسالةُ النتيجة صارت تظهر حيث وقع الفعل** (`Flash` بوسم `at`) — كانت رسائل الاستيراد والبذرة تُعلَن في قسم «الإضافة» فوقها. **البوابات:** 2383/2384 (الساقط `WORKSPACE.md` سطورُ CRLF في هذه الشجرة العاملة لا مساسَ لها بالتغيير) · بناء ١٠٢ صفحة · تدقيق نظيف · ESLint نظيف · وتجربةٌ حيّة على `astro preview` بأثرٍ مؤقّتٍ أُزيل. **⚠️ لم يُرفع — محجوبٌ على «ارفع».**

> **[Claude] 2026-08-18 — المزامنة التلقائيّة كانت تسقط كلّ ساعة، والسبب «يعمل عندي» بحرفيّته:** انتبه المالك أنّ شيئًا لا يتزامن، فكان الورك-فلو قد فشل **خمس مرّاتٍ متتالية** وبقيت ثلاثة كوميتات (FNB-704 · الخطة 39/39 · هرم المواقع) عالقةً في الشخصيّ. **★★ الجذر:** `git` يُخرج اسم الملفّ غير اللاتينيّ **مُقتبَسًا ومهرَّبًا بالثمانيّ** (`"docs/\330\271\331\202\330\257…"`) ما لم يُطفأ `core.quotePath` — وهو **مطفأٌ على جهاز المالك ومشتعلٌ في مُشغّل GitHub**. فحارس المحو يقرأ الاسم المهرَّب، ولا يعرفه `git show`، فيحسب الملفّ مفقودًا ⇒ عملًا محلّيًّا سيُمحى ⇒ يوقف المزامنة. **والمفجِّر هو تصليبُ الأمس نفسه**: `docs/عقد-التكامل-مع-أودو.md` و`public/تقرير-النظام-التفصيلي.html` دخلا جدول الهويّة، فظهر عطبٌ كان كامنًا. **العلاج من الجذر:** قراءةُ المسارات صارت في دالّةٍ واحدة `changedPaths` تُمرّر `-z` دائمًا وتفصل بالمحرف الصفر — بلا اقتباسٍ ولا تهريب مهما كان الإعداد. **وثلاثة حرّاس تمنع العودة:** وجودُ أسماء عربيّة في الجدول صار مشروطًا (فالعطب يظهر أوّل مرّة لا بعد شهر) · واختبارٌ **يُشعل `core.quotePath` عمدًا** ويثبت أنّ `-z` تُعيد المسار خامًّا · واختبارٌ يشترط بقاء `--name-only` في موضعٍ واحدٍ ملازمًا لـ`-z`. **★ ودرسٌ في البنية:** الأداة المعطوبة **لا تستطيع جلب إصلاح نفسها** — الحارس يسبق الدمج، فبقي مستودع الشركة عالقًا حتّى شُغّلت المزامنة يدويًّا من الجهاز حيث تعمل. أيّ حارسٍ يسبق التحديث يحتاج طريقَ خروجٍ (هنا: `--force` أو التشغيل المحلّيّ). **البوابات:** 2377/2377 · تدقيق نظيف · بناء ١٠٢ صفحة · والجانبان مرفوعان (الشخصيّ `a02a1df` والشركة `a5ed66f`).

> **[Claude] 2026-08-18 — مستودع الشركة صار يلحق بالشخصيّ وحده: مزامنةٌ تلقائيّة كلّ ساعة بلا مفتاحٍ ولا سرّ:** بأمر المالك «لا أريد للشركة أن تتأخّر»، وشرطاه متحقّقان (المستودعان عامّان وصلاحيّته WRITE). **الورك-فلو `sync-from-sibling.yml` واحدٌ في المستودعَين** — يُزامَن معهما — **ولا يعمل إلّا حيث تأذن البطاقة** (`workspace.json.autoSync`: الشركة `true` والشخصيّ `false`)، فيستحيل أن يتقاذفا المزامنة بلا نهاية. يستيقظ كلّ ساعة (`cron 17 * * * *`) فيشغّل `sync-sibling.mjs` (جلب · دمجٌ يرجّح الشقيق · ختمُ الهويّة · توليد · بيّنة)، ثمّ **البوّابة الخضراء كاملةً — اختبارات وتدقيق وبناء — ولا يدفع إلّا إن مرّ كلّ شيء**. **★ ودرسٌ تقنيّ:** الدفع بـ`GITHUB_TOKEN` لا يُطلق خطوط العمل الأخرى (حمايةً من التسلسل اللانهائي)، فلولا `gh workflow run astro.yml` صريحةً لزُومن الكود وبقي الموقع قديمًا. وأيّ خلل — تعارضٌ لا يُحلّ · عملٌ محلّيٌّ سيُمحى · اختبارٌ ساقط — يوقفه ويصل المالكَ إشعارُ فشلٍ من GitHub بلا نشر. **و`npm run where` صار يقيس الفارق** بجلبٍ قصيرٍ (٦ ثوانٍ حدًّا) ويقوله **بحسب الدور**: المصدر يُسأل «هل بلغه ما عندي؟» فلا يُنبَّه بكوميتات دمج الشقيق، والمستقبِل يُسأل «هل عنده ما ليس عندي؟» — والعجزُ عن الاتّصال سكوتٌ لا خطأ. **البوابات:** 2365/2365 · تدقيق نظيف · ESLint وprettier نظيفان · وYAML مُثبَتُ الصحّة محلّيًّا.

> **[Claude] 2026-08-18 — تصليبُ آلة الهويّة بعد تحقّقٍ عدائيّ بسبعة فاحصين (٣٨ ملاحظة · ٣٤ نُقضت · صفر مانعِ رفع):** أوّلُ مزامنةٍ إلى مستودع الشركة نجحت وفرقُها ١٤ ملفَّ هويّةٍ لا غير، لكنّ التحقّق كشف **ثلاث فجواتٍ في الآلة نفسها** فسُدَّت قبل الرفع. **(١) جدول المواضع كان تسعةً فصار ٢١:** أخطرُ الزائد `odoo-proxy/cloudflare-worker/wrangler.toml.example` — قالبٌ يُنسخ عند النشر، فأصلٌ خاطئ فيه يجعل وكيل أودو يردّ الطلبات — ومعه `public/nova-meeting/plan.html`، **وثيقةُ اجتماع نوفا الحاكمة التي تُعرض لعميلٍ داخل iframe وفيها رابطان حيّان إلى نشر المستودع الآخر**، ثمّ `README` و`DEVELOPER_GUIDE` و`ROADMAP` وواجهةُ وحدة أودو وبيانُها وتقريرُ النظام المنشور وتعليقاتُ PwaHead وauthService. **(٢) الفحص كان أعمى عمّا خارج جدوله** فمرّ التسرّب من ٢٣٥٧ اختبارًا خضراء؛ فأُضيف **مسحٌ شاملٌ لكلّ ملفٍّ متعقَّب** بقائمة استثناءٍ **مكتوبةٍ بسببها** (`SWEEP_EXEMPT`: الأرشيفُ والسجلّ التاريخيّ · البطاقةُ والدستور وآلةُ الهويّة نفسها · ثوابتُ الاختبارات) — فكلّ ملفٍّ جديدٍ يذكر الشقيق يُسقط الاختبار ويُجبر على قرارٍ مكتوب. **(٣) ★★ حارس المحو كان قائمةَ أسماءٍ لا قائمةَ محتوى:** يكفي أن يكون الملفّ في جدول الهويّة ليمرّ، و`package.json` في الجدول — فلو أضافت إدارةُ تقنية المعلومات سكربتًا فيه لمُحي صامتًا بترجيح الشقيق. **وللخطر سابقةٌ فعليّة نجت بالمصادفة وحدها**: الشركة أضافت `where` في `package.json` ولم يضِع لأنّ الشخصيّ كان بناه أيضًا. فصار الحارس يسأل عن المضمون: **يختم نسخة الشقيق بهويّتنا ويشترط التطابق حرفًا بحرف** (`identityOnly`)، والدستور يُقاس خارج كتلة الهويّة وحدها. **وأُزيل خطرُ تطابقٍ جزئيّ:** المسار `/path/to/warehouse-system-main` في واجهة وحدة أودو كان سيتمزّق بختم المسار، فصار محايدًا. **البوابات:** 2363/2363 اختبارًا · تدقيق نظيف · ESLint وprettier نظيفان.

> **[Claude] 2026-08-18 — المستودعان صارا يتزامنان بأمرٍ واحد، والهويّة لا تتسرّب (`npm run sync` · `npm run identity`):** المستودع الشخصيّ سبق مستودعَ الشركة بـ**١٤٥ كوميتًا (٢٣٥ ملفًّا · +٤٤٫٥ ألف سطر)**، ولم يكن بينهما إلّا الانتقاء اليدويّ كوميتًا كوميتًا، ثمّ إصلاحٌ يدويّ لمواضع الهويّة التي يُعيدها النقل إلى صيغة المصدر — ونصفُه المنسيّ لا يظهر في المراجعة بل بعد النشر. **★★ الجرد كشف أنّ مواضع الهويّة تسعةٌ لا أربعة** كما كانت الذاكرة: يُضاف إلى (`astro.config.mjs` · `src-tauri/tauri.conf.json` · `odoo-proxy/firebase-function/index.js` · `package.json`) كلٌّ من `package-lock.json` و`odoo-proxy/cloudflare-worker/worker.js` وشاشتَي `executive-review` و`dashboard/archive.astro`. **البناء:** `workspace.json` صار المصدر الوحيد — تُشتقّ منه أربعة رموز (`slug`·`host`·`base`·`name`)، ويقول جدول `SPOTS` في `src/services/workspace/identity.js` أيّ رمزٍ يُختم في أيّ ملفّ؛ و`WORKSPACE.md` وكتلةُ الهويّة في `AGENTS.md` صارتا **مولَّدتين** فلا تتناقض النسختان. `npm run sync` يجلب من الشقيق بلا ريموتٍ دائم، ويدمج مُرجِّحًا إيّاه، ثمّ يختم ويولّد الخريطة ويُثبت أنّ الفرق صار مواضعَ الهويّة لا غير — **ولا يدفع**؛ وقبل الدمج **حارسُ محوٍ** يقف إن وجد هنا عملًا خارج جدول الهويّة ليس عند الشقيق. **★★ الحارس داخل `npm test`** (١٨ اختبارًا منها حارسُ انحرافٍ يقرأ المستودع الحقيقيّ ملفًّا ملفًّا)، و`npm test` حاجزٌ صلب في خطّ النشر — فنشرُ نسخةٍ تحمل هويّة المستودع الآخر صار ممتنعًا بالبناء لا بالتذكّر. **البوابات:** 2357/2357 اختبارًا · تدقيق نظيف · ESLint نظيف على الجديد. **⚠️ المتبقّي: دفعُ هذا إلى `main` الشخصيّ ثمّ `npm run sync` في مجلّد الشركة — الأمران محجوبان على «ارفع».**

> **[Claude] 2026-08-18 — «الهوية التشغيلية للمستودعات» أُعيد بناؤه بالكامل: **العرض صار التقرير نفسه** (`/dashboard/warehouse-identity-meeting`):** رفض المالك النسخة الأولى بنصٍّ لا يحتمل تأويلًا — «الذي يوجد في هذا الملفّ هذا هو العرض، لا تجوّد من عندكم، صمّم العرض به هو». وكان حقًّا: بنيتُ قراءتي للتقرير (جداول «من يملك ماذا» وتصنيفي للعائلات وربطي بترميز البوابة) **وحذفتُ ما صمّمه التسويق** — وهو جوهر العمل. **البديل المرفوع:** تقرير التسويق (٠٫٩ م.ب + ٤ أصول = 5.4 م.ب) مضمومٌ إلى `public/warehouse-identity/`، ويُعرض داخل إطارٍ من نفس الأصل **قسمًا في كلّ شريحة** (٢٧ شريحة: الغلاف + ٢٦ قسمًا)، برسومه الـ٧٦٨ SVG و**٤٤٢ بطاقة نموذج** كما صمّمها — والعرض لا يضيف إلا الإطار: تنقّلٌ وفهرسٌ وملء شاشة واختصارات. **★★ العزل كان أصعب ممّا يبدو (ثلاث محاولاتٍ فاشلة):** عناوين التقرير أقسامٌ مستقلّة والمحتوى يليها **شقيقًا**، فتُجمع العقد بين عنوانٍ وعنوان؛ وكتل الغلاف موزّعةٌ على **ثلاثة مستويات** (`main > header.hero` و`div.content > section.brief` و`article.report-body > header.report-head`) فلا يكفي تعدادها باسمها — تُؤخذ **أشقّاء الحاوية في كلّ مستوًى صعودًا**؛ وإخفاء الشريط الجانبي بـ`display:none` كان يُزيح `main` إلى عمود الشبكة الضيّق فينهار العرض إلى 253px (قِيس حيًّا) فوجب تحييد شبكة القشرة أوّلًا. **ودرسان تقنيّان:** حقنُ السكربت لا يُعلَّق على `load` وحده (الملفّ الثقيل يكتمل قبل تماسك React) فصار عديم الأثر عند التكرار مع مؤقّت؛ و**backtick داخل تعليقٍ عربيّ في نصٍّ قالبيّ** أسقط البناء. **الحارس الجديد لا يحرس محتوًى بل مطابقة:** يُعيد قراءة التقرير المنشور ويتحقّق أنّ فهرس العرض هو فهرسه نفسه (٢٦ قسمًا رقمًا وعنوانًا ومرساةً وترتيبًا) وأنّ الأصول منقولة وأنّ النماذج ما زالت فيه. **البوابات:** 2251/2251 · بناء ١٠٢ صفحة · تدقيق نظيف · ESLint نظيف. **⚠️ الحدّ الصادق: لم يُجرَّب بحسابٍ حقيقيّ خلف حارس الدخول، والاجتماع لم يُعقد. و5.4 م.ب دخلت المستودع (صورتان تُشكّلان 4.4 منها — قابلتان للضغط لاحقًا بلا مساسٍ بالرسوم).**

> **[Claude] 2026-08-18 — «الهوية التشغيلية للمستودعات» عرض الجلسة المشتركة: السلاسل × التسويق (`/dashboard/warehouse-identity-meeting`) — ٢٠ شريحة مبنيّةٌ ومحروسةٌ ومُعايَنة:** محورها **تقرير التسويق البصريّ** (٢٦ قسمًا · ٢٣٣ نموذجًا · ١٢ عائلة تصميم · ٤ مواقع منها اثنان عاجلان) — قُرئ كاملًا وحُوّل إلى قراءةٍ تشغيليّة لا نسخةٍ موازية. **★★ الفكرة التي بُني عليها العرض:** التقرير يطلب ترميز الموقع على **ستّة مستويات**، و`locations/locationCode.js` **يولّد الترميز نفسه بستّة مقاطع** (`MAIN-A01-R01-B09-LF-P01`) ومعه مختصرٌ للعامل (`R01-09-F`) **يُشتقّ ولا يُكتب**. فالسؤال في الجلسة ليس «كيف نرمّز؟» بل «كيف نطبع ما هو مرمَّزٌ أصلًا، ومن يعتمد كلّ طبقة» — وهو نصّ معيار القبول ١٣ في التقرير: توحيد المسمّيات بين اللوحات والنظام والبوابة. **المحاور الستّة:** الملخّص والقاعدة الحاكمة و**من يملك ماذا** (٦ جهات: التسويق · السلاسل · IT · الجودة والسلامة · المكتب الهندسي · الموارد البشرية) والأهداف ١١ · الترميز والحالات الستّ وأنواع التخزين السبعة والمناطق ٢٢ وبوابة المسح · عائلات التصميم ١٢ · الأولوية العاجلة (الرحبة وطرابلس) ومخرجات التسويق ٨ والمطالب ٨ · **بوابة الخطوات الستّ** والمراحل ٤ ومعايير القبول ١٥ · ثماني نقاط قرارٍ تُحسم حيًّا وتُنسخ محضرًا. **الحارس (١٣ اختبارًا) يُثبت الدعوى لا يدّعيها:** مستويات الترميز المعروضة = `CODE_SEGMENTS` ترتيبًا ومعرّفًا، والمثال يُمرَّر على `isValidLocationCode`/`parseLocationCode` فيُقبل ويُفكَّك إلى ستّة، والحالات والأنواع ومفتاح الخريطة تُقرأ من `locationsModel`/`mapGrid`. **بُني على `meeting-deck.css` المشترك** — رابع عرضٍ بلا نسخةٍ رابعة من الهيكل. **قرارُ نطاقٍ من المالك أثناء العمل:** «عرضٌ تقديميّ فقط» — فأُلغي ضمُّ تقرير التسويق (١ م.ب) إلى `public/` وحُذف أثره، والعرض يُحيل إلى التقرير ولا يستنسخه. **حادثةٌ وإصلاحها:** سكربت أُضيف به بطاقةُ مركز التقارير فشل في منتصفه بعد فتح الملفّ للكتابة فقطع `reportsData.js` — استُرجع من git كاملًا فورًا وأُعيدت الإضافة بأداة تحريرٍ آمنة (الدرس: لا `open(w)` قبل بناء النصّ كاملًا). **البوابات:** 2193/2193 اختبارًا · بناء ١٠٢ صفحة · تدقيق نظيف · ESLint نظيف · ٢٠ شريحة بلا فيضٍ ولا قصّ (قياسٌ آليّ لكلّ عنصر). **مزامنة:** الفرع كان متأخّرًا ٢١ كوميتًا فزُومن إلى `ea4f471` وأُعيد تشغيل كلّ الحرّاس. **⚠️ الحدّ الصادق: لم يُجرَّب بحسابٍ حقيقيّ خلف حارس الدخول، والاجتماع لم يُعقد.**


> **[Claude] 2026-08-18 — «الدورة المستندية — السلاسل والمشتريات» عرضُ اجتماع المالية (`/dashboard/finance-procurement-meeting`) — مبنيّ ومحروسٌ ومُعايَنٌ ومرفوع:** المشتريات تتبع المالية، فمحور الجلسة **حدٌّ يُكتب لا صلاحيةٌ تُنتزع**. ٢٩ شريحة على لوحة 1280×720: (١) القاعدة الحاكمة · **من يملك ماذا** (١٠ خطوات: المالك ومن يُستشار والمستند) · **نقاط التسليم الأربع** (PR · PO · GRN+QC · حكم المطابقة) لكلٍّ مستندُها ومخاطرةُ غيابها · (٢) الماستر الثلاثيّ · (٣) دورة الشراء حلقةً حلقة `PURCHASE_CHAIN` بشريط موضعٍ في السلسلة و**حقول كلّ مستندٍ من مخطّطه الحقيقيّ**، ثمّ **المطابقة الثلاثية** بأحكامها الستّة · (٤) الأثر الماليّ والمشتريات الداخلية · (٥) **اثنا عشر سيناريو سيّئًا** (٥ عالية · ٧ متوسّطة) لكلٍّ الأثرُ والحارسُ المبنيّ، وثلاثةٌ مُشرَّحة · (٦) المؤشّرات الأربعة وبطاقة المورّد و٦ تقارير مشتركة و**٨ بنودٍ تُعتمد** و٨ نقاط قرارٍ تُحسم حيًّا وتُنسخ محضرًا. **★★ الحارس (١٥ اختبارًا) لا يقارن نصًّا بنصّ:** أحكام المطابقة **تُولَّد بتشغيل `threeWayMatch` نفسها** على ستّ حالاتٍ حقيقيّة (فحكمٌ يُحذف أو يُضاف يُسقط العرض قبل القاعة)، وحدّ التسامح من `DEFAULT_TOLERANCE` لا رقمٌ مكتوب، والمراحل = `PURCHASE_CHAIN` والداخلية = `INTERNAL_PROCUREMENT_CHAIN` حرفيًّا، وأسماء المؤشّرات دوالٌّ مصدَّرة، والأوزان من `vendor-scorecard.json`، والعناوين من سجلّ التقارير. **استخراجٌ ينهي ازدواج العروض:** الهيكل المشترك صار `src/styles/meeting-deck.css` (فضاء `mtg-`) بدل نسخةٍ ثالثة — ونوفا والمطاعم لم تُمسّا (مُسلَّمتان ومحروستان؛ تُرحَّلان حين يُفتح أيٌّ منهما). **عيبٌ أُصلح أثناء المعاينة:** نصوصٌ تحمل `**` كانت تظهر نجمتين على الشاشة — ٧ مواضع منها **واحدٌ في عرض المطاعم المرفوع**، وأُضيف حارسٌ للعرضين يمنع عودتها. **البوابات:** 2077/2077 اختبارًا · بناء ١٠١ صفحة · تدقيق نظيف · ESLint نظيف · ٢٩ شريحة بلا فيضٍ ولا قصّ (قياسٌ آليّ لكلّ عنصر). **مزامنة:** الفرع كان متأخّرًا ٨١ كوميتًا فزُومن إلى `016f523` وأُعيد تشغيل كلّ الحرّاس على القاعدة الجديدة — لا انحراف. **⚠️ الحدّ الصادق: لم يُجرَّب بحسابٍ حقيقيّ خلف حارس الدخول، والاجتماع لم يُعقد.**

> **[Claude] 2026-08-16 — 🏁 الدفعة الأولى «الأساس الحاكم» (م١) منفَّذة كاملةً — **ف‑١٨ أُغلقت** — 10/20 (٥٠٪) — لم تُرفع:** بأمر المالك «نفذ · اعتمدت الخطة» رُفع قيد «بعد إغلاق CC-402/CC-501» ونُفّذت المهامّ التسع بترتيب اعتمادياتها. **`LOC-101` كيان الموقع** (`1e24a93`): `bin_locations` بمعرّفٍ حتميّ هو الكود، ونحوٌ خالص بستّة مقاطع قابلة للاختصار (`MAIN-A01` منطقةٌ كاملة موقعٌ صالح، وفجوةٌ في الوسط تُرفض لأنّ الترتيب هو المعنى)، والمختصر `R01-09-F` **يُشتقّ ولا يُخزَّن**. وحارس التصادم مع مواقع النظام و`VAN:`/`CUST:` **يسبق كلّ فحصٍ آخر عمدًا** — ولو تأخّر خلف عدد المقاطع لَما أُطلق أبدًا، واختبارٌ يحرس **ترتيبه** لا وجودَه. **`LOC-103` `rebuildBalances()`** (`2f7b21d`): الدعوى «الدفتر هو الحقيقة» كانت بلا تنفيذ منذ اليوم الأوّل — وهي العائق الحقيقيّ لا المفتاح. والأهمّ فيها **طبقةٌ تصمد عبر تغيير المفتاح**: حين يُقلب تتغيّر كلّ المعرّفات فتبدو الأرصدة منحرفةً وهي سليمة، فالمقارنة بالصنف والمستودع (`aggregates`) هي بوّابة الترحيل — تقسيمٌ أدقّ لا يخلق كميّةً ولا يُفنيها؛ و`migrationVerdict` يمنع القلب على رصيدٍ يتيم؛ و`applyRebuild` **لا تمسّ اليتيم عمدًا** (قد يكون بضاعةً على الرفّ دخلت من إكسل بلا حركات). **`LOC-107` حالة المخزون** (`fb3045a`): 🔒★★ اختبارٌ يمنع أيّ قيمةٍ تحمل اسم موقع نظام — وإلّا صار للحقيقة مصدران (صنفٌ في `QUARANTINE` بحالة `OK`: أيّهما يُصدَّق؟)؛ والقيم الثلاث المقترَحة **معطَّلة** حتى يعتمدها المالك. **`LOC-104` خانة الموقع** (`73e2c8b`): مرجعيّة ومسحيّة مركزيًّا في `LineItemsTable` لا في ٣٨ مخطّطًا، بـ`datalist` لا `select` (آلاف المواقع)، وثلاثة حدودٍ ★★ للحارس: الفراغ ليس خطأً · بلا سيّدٍ مبنيّ لا حكم · والتنبيه يُنبّه ولا يمنع. **`LOC-106` توحيد الحقلين** (`1a46f22`). **`LOC-105` الموقع طرفٌ لا صفة** (`dc77e1d`) — وهو **الشرط الحرفيّ** لفتح ف‑١٨: `binSide:'to'` للتخزين و`'from'` للسحب، والحركة تحمل `fromBin`/`toBin`، فما دخل الرفّ يخرج منه **بالمفتاح نفسه**؛ و★★ اختبارٌ يثبت أنّ TR/TRN/TRC وDN لم تتأثّر. **`LOC-108` قلب المفتاح** (`7fe60b9`): صار `الصنف × المخزن × التشغيلة × الموقع × الصلاحية × الحالة`، و★★ **الترحيل صفرُ الأثر**: بلا موقعٍ ولا صلاحيةٍ وبحالة `OK` ⇒ المفتاح القديم **حرفيًّا**، فلا يتيتم رصيدٌ ولا يحتاج المستودع «يوم ترحيل». و`batchExpiryConflicts` يرفع تعارض الصلاحية ولا يدمج صامتًا — بحارسٍ ★★ يمنعه من الصراخ على ما ليس تعارضًا. **`LOC-109` القواعد** (`f3aa051`) و**`LOC-102` الشاشة** (`57f2594`) تبويبًا في `/dashboard/warehouses` (لا صفحة فوق صفحة) بملصق CODE128 من المكتبة المستضافة ذاتيًّا، والإشغال من **الأرصدة الحيّة** لا من حقلٍ يدويّ. **⚠️ ثلاثة تصحيحات لادّعاءاتٍ سجّلتُها في خطة أمسٍ ولم تصمد للتحقّق:** ① «حارس `qty>=0` معطَّلٌ مع `increment()`» **استنتاجٌ لا أستطيع إثباته** (يحتاج المحاكي وJava غير مثبَّتة) — فلم أغيّر الحارس، وتغييرُ حارسٍ أمنيّ على ظنٍّ أسوأ من إبقائه ② «`BALANCES_ROLES` يناقض القواعد» **ليس تناقضًا** بل فعلان: القاعدة تحرس **القيد** (١٢ دورًا) وهذه تحرس **الاستيراد اليدويّ** الذي يدهس أرصدةً بملفّ ③ «`bin` لا ينتقل في `QC>PUTAWAY`» صحيحٌ وصفًا لا علاجًا — QC بلا موقعٍ على مستوى السطر أصلًا؛ والفقدُ الفعليّ في **`CC>ADJ`** (الجرد يعرف الرفّ والتسوية بلا عمود موقع) وقد عولج. **وعقدان انقلبا عمدًا واختباراهما حُدّثا لا حُذفا:** «الموقع عرضًا لا مفتاحًا» و«آخر واردٍ يكسب». **البوابات:** **1648/1648 اختبارًا** (+٥٨) · بناء **٩٩ صفحة** · تدقيق نظيف · المتتبّعات الأربعة سليمة · ESLint نظيف على كلّ المتغيّر. **قِيس بنيويًّا:** صفحة معاينةٍ مؤقّتة غير محميّة أثبتت تصيير الشجرة كاملةً (٦ حالات · ٧ أنواع تخزين · السعة · الخلط · البحث · الحالة الفارغة) وسقوط الاشتراكات بأمان — ثمّ **حُذفت** وأُعيد البناء ٩٩ صفحة. **⚠️ الحدّ الصادق:** لم يُرفع شيء · القواعد **لم تُنشر** (LOC-O06) و`npm run test:rules` **لم يُشغَّل** (Java غير مثبَّتة) · لا حسابات أدوار فلا قبولَ حيّ (LOC-O07) · ولم تُلتقط لقطة شاشة (لوحة المتصفّح غير معروضة). **الاستئناف: `npm run plan:locations` ← LOC-201 (م٢ استيراد أودو).**

> **[Claude] 2026-08-16 — `LOC-000`: «التخزين والسحب الموجّه» خطّةً مثبَّتةً ومتتبّعًا رابعًا — وثائق فقط، بلا سطر كود، لم يُرفع:** طلب المالك منظومة مواقع مخزونٍ كاملة (استيراد أودو ← اقتراح موقع ← مهمّة عامل ← مسح ← سحب FEFO ← مطابقة). فُحص الكود قراءةً فقط في ثلاثة محاور، فتبيّن أنّ **هذه المنظومة ليست فكرةً جديدة بل هي بعينها شرط فتح ف‑١٨ المؤجَّلة** في متتبّع مواءمة SAP — القرار المكتوب هناك يقول حرفيًّا إنّ إدخال الموقع في مفتاح الرصيد «مؤجَّلٌ عمدًا **حتى تحمل أسطر السحب مواقعها**، وإلا تباعدت الأرصدة ورفض حارس السالب سحبًا صحيحًا». **وثلاث حقائق غيّرت شكل الخطة:** ① الضرر **قائمٌ اليوم لا مستقبليّ** — دفعةٌ واحدة بصلاحيتين تُدهس صلاحيتُها السابقة بلا إنذار (`ledgerService.js:180`)، والموقع «آخر تخزينٍ يكسب» (`movements.js:244`)، وحقلان لمعنًى واحد (`location` من إكسل · `bin` من القيد) بلا توحيد ② **العائق الحقيقيّ ليس المفتاح بل غياب `rebuildBalances()`** — الكود يدّعي «الدفتر هو الحقيقة والأرصدة ذاكرةٌ مسرَّعة» (`ledgerService.js:9`) بلا تنفيذ، فلا أداة ترحيلٍ ولا سبيل لإثبات أنّ قلب المفتاح لم يُفسد شيئًا؛ لذلك صارت **أوّل مهمّة** وقلبُ المفتاح **آخرها ومحجوبٌ بصفريّة فرقها** ③ قرار المالك «إكسيل يدويّ فقط» **أصحّ ممّا يبدو**: `INTEGRATION_DIRECTION='pull'` مختوم وكلّ دوال الدفع ترمي خطأً، و`PULL_SCOPES` **لا تشمل** `stock.quant` ولا `stock.location` ولا `stock.lot` — فإكسيل ليس احتياطيًّا بل الطريق الوحيد. **قرارات المالك الأربعة:** خطّةٌ موثّقة الآن والتنفيذ بعد إغلاق CC-402/CC-501 · الأساس الحاكم دفعةً أولى · إكسيل مصدرًا وحيدًا · **CODE128** بمكتبة `public/lib/JsBarcode.all.min.js` القائمة (لا مكتبة QR في المشروع، وجلبها يخرق «لا CDN»). **المخرَج:** [`docs/خطة-التخزين-والسحب-الموجّه.md`](docs/خطة-التخزين-والسحب-الموجّه.md) الحاكمة + متتبّع [`warehouse-locations.json`](docs/plan/warehouse-locations.json) (**٧ مراحل · ٢٠ مهمّة · ٢٠ فجوة كلّها بمسار ملفٍّ حقيقيّ · ٧ قرارات مالك لكلٍّ سلوكٌ افتراضيّ معلَن**) + `npm run plan:locations` موصولًا في `plan` فصارت المتتبّعات **أربعة**. **وثلاث ثغرات قواعد اكتُشفت بالفحص وسُجّلت للسدّ (ل‑١٧):** `batch`/`expiry`/`bin` غير محميّة من التعديل في `stock_moves` · `qtyReserved` بلا حارس · وحارس `qty>=0` على `balances` **معطَّلٌ عمليًّا** لأنّ `increment()` لا يضع رقمًا حرفيًّا في `request.resource.data`. **وتناقضٌ قائم (ل‑١٨):** `BALANCES_ROLES` دوران مقابل ١٢ دورًا في القواعد. **قرارات تصميميّة:** صفحةٌ جديدة **واحدة** (`/dashboard/directed-storage`) والباقي توسعةٌ للقائم عملًا بقاعدة «لا صفحة فوق صفحة» — وشجرة المواقع تبويبٌ في `/dashboard/warehouses`، والخريطة **إحياءٌ لـ`WarehouseMaps.jsx`** (٢٨٥٣ سطرًا **يتيمة** غير مستورَدة من أيّ صفحة) · **لا دور `governance`** فالتسكين على الستّة عشر القائمة · و`Status` يبقى حقلًا لا مفتاحًا حتى يعتمد المالك قيمًا **منفصلة** عمّا يُشفَّر بالموقع (`QUARANTINE`/`RETURNS`/`MAINTENANCE`) وإلّا صار للحقيقة مصدران. **البوابات (مقيسة لا مفترضة):** **1590/1590 اختبارًا** · بناء **٩٩ صفحة** · تدقيق نظيف (٨١ صفحة مربوطة · ٦٦ قاعدة · لا مجموعة بلا قاعدة) · **المتتبّعات الأربعة سليمة** · ESLint **٣٧ مشكلة كلّها خطّ الأساس** والتغييرات `.md`/`.json` لا يلمسها اللينت فالجديد **صفر**. **⚠️ حدودٌ صادقة:** لم يُكتب سطر كودٍ واحد، ولم تُنشر قواعد، ولم يُرفع شيء — وأرقام `AGENTS.md` (~٧٢٨ اختبارًا) و`DEVELOPER_GUIDE.md` (٢٥٢ اختبارًا · ٤٠ صفحة) **متقادمة** ولم أصلحها لأنّها خارج نطاق هذه المهمّة. **الاستئناف: `npm run plan:locations` ثمّ LOC-101 — ولا تُبدأ قبل إغلاق CC-402/CC-501 بأمر المالك.**

> **[Claude] 2026-08-16 — «سلاسل الإمداد — المطاعم» تقريرٌ مستقلٌّ في مركز التقارير (`/dashboard/restaurants-supply-chain`) — مبنيّ ومختبَر ومُعايَن، لم يُرفع (طلب المالك: صمّم وأعطني معاينة):** عرض قطاع المطاعم كان شريحةً داخل «عروض أنظمة القطاعات»؛ أُخرج ليقف **تقريرًا يُدار به اجتماع القطاع**: **٣٥ شريحة** على لوحةٍ ثابتة 1280×720 بوضع عرضٍ وملء شاشة وفهرسٍ واختصارات لوحة مفاتيح (هندسة عرض نوفا نفسها، بفضاء أسماءٍ مستقلّ `rs-` كي لا يُعاد فتح عرضٍ مُسلَّم). **المحاور الستّة:** (١) الأبعاد التنظيمية + **توحيد المفاهيم** (١٢ مصطلحًا بثلاثة أعمدة: التعريف المعتمد والخلط الممنوع) + قاعدة الملكية · (٢) البيانات المرجعية الأربع (الأصناف ووحدات القياس · الموردون · العملاء · المستودعات) · (٣) **الدورات الخمس** التي طلبها المالك من شاشة المستندات (الوارد · النقل · المرتجعات · التالف · المبيعات والصرف) + شبكة إنشاءٍ بـ٢٧ رمزًا · (٤) تزويد الفرع بثماني مراحل TR→TRN→TRANSIT→TRC · (٥) الرقابة على الطلبات · الدفتر · تحليل المخزون · التقارير الـ١٩ · الميدان والبيع من المركبة · (٦) **الطلب الموحَّد لشركة تنفيذ أودو** — جدول ١١ بندًا بعمودَين (ما هو مبنيٌّ عندنا ↔ المطلوب تنفيذه هناك) يُسلَّم موقَّعًا، ثمّ ثماني نقاط قرارٍ تُحسم حيًّا وتُنسخ محضرًا (`decisionSession` بمفتاحٍ مستقلّ + `buildDecisionMinutes`). **لا نسخة ثانية:** محتوى دورة التزويد **يُستدعى** من `sector-presentations.js` نفسه بتسميات شرائحه (`sourceSlide`) — فعرض القطاع القائم باقٍ كما هو ولا يفترق النصّان. **الحارس (`src/data/restaurants-supply-chain.test.js`، ١٤ اختبارًا):** كلّ مسار اختصارٍ صفحةٌ في كتالوج القائمة · كلّ رمز مستندٍ مخطّطٌ مبنيّ · **عقد الدورات مطابقةٌ حرفيًّا لسلاسل `documents/chain.js`** · وحدات القياس هي وحدات `itemService` · مستويات الشجرة هي `ORG_LEVELS` · تسميات شرائح المصدر قائمة. **التسجيل:** بطاقة في `reportsData.js` («التقارير التشغيلية») وبندٌ في `navCatalog` للمديرَين. **البوابات:** 1587/1587 اختبارًا · بناء ٩٩ صفحة · تدقيق نظيف · ESLint نظيف على الجديد. **قِيس بصريًّا على البناء:** ٣٥ شريحة بلا فيضٍ ولا قصٍّ (فُحصت آليًّا بقياس كلّ عنصر)، ولُقطت ٣٥ لقطة + لوحة تجميعيّة عبر Puppeteer من صفحة معاينةٍ مؤقّتة غير محميّة **حُذفت بعد التصوير**. **دُفع إلى `main` بأمر المالك «ارفع» بعد مزامنةٍ مع `e8e6ccb`.** **⚠️ الحدّ الصادق: لم يُجرَّب بحسابٍ حقيقيّ خلف حارس الدخول.** **⚠️ تداخلٌ يحتاج قرار المالك:** الكوميت الأسبق `e8e6ccb` أضاف «توحيد المفاهيم + الطلب الموثّق لمنفّذ أودو» **فصلًا ثانيًا داخل عرض نوفا** (٤٠ شريحة) وبطاقةً له في مركز التقارير — فصار في المركز بطاقتان تلتقيان في المخرج نفسه (الطلب لمنفّذ أودو) وتختلفان في الجمهور (نوفا عميلٌ خارجيّ · المطاعم قطاعٌ داخليّ). أُبقيتا معًا ولم يُحذف شيء، والدمج في مرجعٍ واحدٍ بجمهورين قرارُ المالك.

> **[Codex] 2026-08-12 — تسليم إصدار CC-203 المقبول للدفع إلى `main` وتسليم القواعد الكاملة للمالك:** بناءً على قبول المالك وأمره الصريح «ارفع»، جُهّز هذا الإصدار من الحالة الملتزمة `c4068b5` في worktree نظيف ومعزول؛ `origin/main` كان `47017aa` وهو سلف مباشر، لذلك مسار الدفع fast-forward بلا استبدال لتاريخ أحد. يشمل الإصدار CC-101→CC-203 كاملة، ولا يشمل أي ملف من مسودة كود CC-204 غير الملتزمة ولا قاعدة `document_flow_allocations`. **القواعد المسلّمة:** ملف الجذر `firestore.rules` كامل (القديم + التحديثات)، 1093 سطرًا، SHA-256=`9624C2997F60E4EAE76D8871E561F1E6CEDF41B1CE5D26AFABB1E2F3CF5EA7CC`؛ يتضمن قاعدة `document_links` الملحقة-فقط، وتوسيع كتابة `audit` للأدوار التشغيلية المعتمدة. **بوابات worktree الإصدار:** 1276/1276 اختبارًا؛ build=94 صفحة؛ audit نظيف؛ حارسا الخطتين سليمان؛ ESLint المستهدف نظيف لـ20 ملفًا؛ lint العام بقي على خط الأساس 126 (53 خطأ/73 تحذيرًا) بلا زيادة. **حد التحقق:** `npm run test:rules` لم يعمل لأن Java غير مثبتة على الجهاز؛ لم تُنشر القواعد إلى Firebase، فالمالك وحده يلصق الملف الكامل في Firebase Console وينشره ثم يجري تحققًا حيًا. **أين نتوقف بعد هذا الإصدار:** CC-204 ما زالت قيد التنفيذ كما في المدخل التالي؛ يُستأنف عزل سبب تعليق البناء من مسودتها خارج هذا الإصدار، ولا تُدمج قبل بوابة خضراء كاملة.
> **[Claude] 2026-08-16 — الفصل الثاني: عرض نوفا صار مرجعَ مفاهيمٍ وطلبًا موثّقًا لمنفّذ أودو (٤٠ شريحة) — مبنيّ ومختبَر:** بطلب المالك أُضيف فوق خطة الجرد **فصلٌ ثانٍ بجمهورٍ ثانٍ**: الشركة المنفّذة لأودو. المنطق الحاكم في ترتيبه **دورة حياة البيانات لا ترتيب القائمة الجانبية**: مرجعيّاتٌ تُعرَّف مرّة (المستودعات · الأصناف ووحدات القياس · الموردون والعملاء) ← حركةٌ تُقيَّد يوميًّا (أبواب المستندات الخمسة · النقل · الميدان) ← رقابةٌ تمسك المتعثّر (الرقابة على الطلبيات · دفتر الحركات) ← تحليلٌ يقرأ الأثر (التقارير الـ١٩ · تحليل المخزون) ← الطلب الموثّق. **وشريحةُ فاصلٍ صريحة** تُعلن تبدّل الفصل والجمهور، فلا تُقرأ الشاشات تفاصيلَ في خطة جرد. **المحتوى من الكود لا من الوصف:** أبواب العمليات بأسماء مجموعات «بدء مستند جديد» الحقيقيّة، ووحدات القياس من `uomModel.js` (أربع عائلات · معاملٌ ثابت داخل العائلة وخاصٌّ بالصنف عبرها · حارس الكسر)، وأعمدة الدفتر وأرصدته من `StockLedger`، والتقارير الـ١٩ بعناوينها من السجلّ. **حارسان جديدان:** اختبارٌ يطابق عائلات التقارير في العرض بسجلّ `services/reports` عددًا وعنوانًا (فتسمية تقريرٍ تُغيَّر تكسر العرض قبل النشر)، وآخر يتحقّق أن كل رمزٍ في سلاسل الأبواب الخمسة مخطّطٌ مبنيّ. **الظهور:** «عمليات البوابة» يعرضه أصلًا (يُولَّد من الكتالوج)، وأُضيفت بطاقته يدويًّا إلى **مركز التقارير** (`reportsData.js`). **البوابات:** 1576/1576 · بناء ٩٨ صفحة · تدقيق نظيف · ESLint نظيف. **قِيس حيًّا:** ٤٠ شريحة بلا قصٍّ، و٥٤ رابطًا كلّها بالـ`base` الصحيح، وترتيب الفهرس مطابقٌ لعناوين الشرائح واحدةً واحدة. **⚠️ الحدّ الصادق باقٍ: لم يُجرَّب بحسابٍ حقيقيّ خلف الحارس، والاجتماع لم يُعقد.**

> **[Claude] 2026-08-16 — خطة نوفا صارت اجتماعًا حيًّا في البوابة (`/dashboard/nova-inventory-meeting`) — مبنيّ ومختبَر، لم يُرفع:** الوثيقة `NOV-OPS-001` (خطة جرد فرعَي بنغازي وطرابلس وتفعيل دورات النقل والبيع وتحميل المندوبين) تحوّلت من ملفّ HTML على سطح المكتب إلى **عرضٍ يُدار به الاجتماع**: ٢٧ شريحة على لوحةٍ ثابتة 1280×720 بوضع عرضٍ وملء شاشة وفهرسٍ واختصارات لوحة مفاتيح (نفس محرّك `EngineeringMeetingDeck` المُجرَّب). **الإضافة الحقيقيّة فوق الوثيقة: ١٧ بطاقة اختصار** — لكلّ خطوةٍ من السبع (وكلّ دورةٍ من الأربع) اسمُ الشاشة ومسارها والنقرات داخلها **بنصّ أزرارها الحقيقيّ** والدليل الذي تُخرجه، وزرٌّ يفتحها في تبويبٍ مستقلّ (٤١ رابطًا: ١٧ نوع مستندٍ عبر `?type=` + شاشات الأصناف والجرد والدفتر والتقارير والنقل والمركبة والاجتماعات والأرشيف). **وشريحة حسمٍ حيّ** للنقاط العشر المفتوحة: الحالة والمسؤول والموعد والملاحظة تُسجَّل أمام الحضور وتُحفظ محليًّا (`decisionSession.js` المُختبَر بمفتاحٍ مستقلّ)، وتُنسخ محضرًا نصيًّا بدالّة خالصة `buildDecisionMinutes` — والفشل مُعلَن في الزرّ لا صامت. **الحارس الجديد (`src/data/nova-meeting.test.js`، ٨ اختبارات):** كلّ مسار اختصارٍ **صفحةٌ في كتالوج القائمة**، وكلّ رمز مستندٍ **مخطّطٌ مبنيّ في المحرّك** — فلا يَعِد العرض أمام العميل بشاشةٍ أو دورةٍ لا وجود لها. **الأنماط في `src/styles/nova-meeting.css`** لا في `<style is:global>`: الصفحة محميّةٌ بحارس الدخول، والتحقّق البصريّ احتاج صفحةً غير محميّة تستورد **نفس** الملفّ (حُذفت بعد المعاينة). **البوابات:** 1573/1573 اختبارًا · بناء ٩٨ صفحة · تدقيق نظيف · ESLint نظيف على الجديد. **قِيس حيًّا على البناء:** ٢٧ شريحة بلا قصٍّ (٣ شرائح كانت تفيض ٣٨–٥٠px فضُبطت)، و٤١ رابطًا كلّها بالـ`base` الصحيح، والوثيقة المصدر تُحمَّل في الإطار. **⚠️ الحدّ الصادق: لم يُجرَّب بحسابٍ حقيقيّ خلف الحارس، ولم تُلتقط لقطة شاشة (لوحة المتصفّح غير معروضة) — التحقّق كان بنيويًّا وسلوكيًّا لا بصريًّا.**

> **[Claude] 2026-08-13 — ثلاث مهامّ في جلسةٍ واحدة: CC-301+302+401 مبنيّةً ومدفوعةً ومنشورة (`220fb3e → 5e69450` · نشر #223 نجح) — المتتبّع 13/22 (٥٩٪):** بدأت بمسحٍ عدائيّ (٤ محاور + تحقّق) وجد **١٢ فجوة** وخلاصته: المنطق الخالص ممتاز والعطب في **ثلاث وصلات مقطوعة**. وُصلت كلّها: **(١) الدفتر يعرف الرحلة** — كلّ حركة مخزون تحمل `tripRef`/`repName` (كانت تُنسب لضاغط الزرّ لا صاحب العهدة)، وسلسلة `VAN_CHAIN` مسجّلة (التحميل يتفرّع بيعًا وإرجاعًا والبيع نهائيّ)، والهوية تُورَّث ويحرسها `vanIdentityProblem` (الفارغ لا يحجب — توافق القديم)، و`tripRef` صار إلزاميًّا في VLD/VSI. **(٢) النقد له دفتر** — `rep_cash_moves` ملحقة-فقط (نظير `partner_ledger`)، قيد RCV صار بتاريخ `collectionDate` (كان فارغًا فيتقدّم السداد على فاتورته)، و`invoiceOutstanding` أوّل قارئ لـ`allocations` (المفتوح فاتورةً فاتورة في شاشة الذمم + عمود «قاصَّ»)، وحارس تجاوز التحصيل عند الإنجاز (فوق المفتوح يُرفض إلا بسبب `overCollectionReason`)، والمندوب يرى ذمم عملائه من مجموعته («ذمم عملائي وكشوفهم»). **(٣) الحكم يملك الفعل** — نافذة رحلةٍ حقيقيّة في لوحة المركبات (كانت تسوية عمرِ مركبة)، مستندٌ غير منجَز وميزانُ نقدٍ سالب **مانعا إقفالٍ** لا نصيحة، زرّ «إقفال الرحلة» يُنشئ محضر VSR معطَّلًا ما دام مانعٌ قائمًا، تبويب الأمانة أُحيي (المفتاح القديم لم يكتبه الدفتر قطّ) مع تدفّق `consignBack` مستقلّ، والجرد المعدود يحمل هويّته فلا يسقط صنفُ باركود، وحارس `vsrPostProblem` يمنع إنجاز تسويةٍ ومركبتُها محمّلة. **ثمّ CC-401:** سيّد المواقع (م٦-أ) كان **بصفر مستهلكين** — وُصل بشاشة `/dashboard/org-dimensions` (الشجرة بحرّاسها + التكلفة الصاعدة + غير المربوط يُحصى)، وحقل `costCenter` في رؤوس PO/SO/TR **يُورَّث عبر السلاسل كلّها** (يُدخل مرّةً ويسري، و`budgetCode` القديم يتوحّد إليه)، وحارس `internalBranchProblems` (فرعٌ سُجّل عميلًا يُكشف — النقل TR→TRN→TRC عبر TRANSIT)، و`dimensionsOf` يُكمل الأبعاد الأربعة من رمزٍ واحد. بلا أكواد مخترعة — الفارغ = سلوك اليوم، والأكواد قرار المالك (CC-O02). **لقطات الحرّاس:** انحراف الصلاحيات أمسك «اسمًا بلا باب» في sales_rep (أُصلح الجذر: المجموعة) · حارس الخطّة رفض إغلاقين حتى صُحّح مسارا اختبارين كُتبا قبل التنفيذ. **البوابات:** 1311/1311 (+20 اليوم) · بناء ٩٦ صفحة · تدقيق نظيف · ESLint نظيف على المتغيّر. **القواعد:** المالك نشر ثلاث نسخ وسيطة مؤكَّدة بالبصمة في يومين (١١٠٢ ثمّ ١١٤٤ ثمّ **١١٥٥ سطرًا** `B7423F8C…1148E1` شاملةً `rep_cash_moves`) — كلٌّ قبل دفع الكود المعتمد عليها، فلم تنكسر الدورة لحظة. **⚠️ الحدّ الصادق:** كلّ هذا **not tested حيًّا** — الشاشات محميّة بالدخول ولا حسابات أدوار (CC-602/و-٢ هي العنق). **الاستئناف: CC-402** (لوحة أودو والاعتمادية: idempotency·correlation·dead-letter) — `npm run plan:client`.

> **[Claude] 2026-08-12 — إنقاذ الشجرة المتسخة ثمّ إغلاق CC-204 (٣/٣ معايير) — مدفوع ومنشور `59c8c46 → 220fb3e`:** **السبب الجذريّ لفساد Git تبيّن غير ما شُخّص:** مرجع checkpoint من Codex طوله **٢٩٢ حرفًا > حدّ ويندوز MAX_PATH (٢٦٠)**، فيعجز Git عن قراءة ملفّه فيراه `0000000…` — لا فساد فيه أصلًا. `git config core.longpaths true` أعاد `show-ref` و`fsck` و`fetch` إلى الخروج 0 **بلا حذف أيّ مرجع**، وتبيّن أنّ «المرجع الفاسد» كائن tree سليم. وحذفه (كما اقتُرح) كان علاجًا للعرَض: المرجع يتغيّر UUID كلّ دورة عمل. **وتصحيح منهجيّ:** «الكائن موجود في مخزن Git» ≠ «العمل محفوظ» — بالاختبار الصحيح (`rev-list --all --objects`) تبيّن أنّ **١٥٣ ملفًّا (٥٠ م.ب) لا يبلغها أيّ كومِت**، لا ملفًّا واحدًا كما أوحى `cat-file -e`. **أُنقذ:** مسودة CC-204 (كانت في مجلّد جلسة مؤقّت) · صفحة «المراجعة التنفيذية لسلاسل الإمداد» (٨٠٩ أسطر بلا أثر في أيّ فرع) · ٤٤ كومِتًا معلّقًا بمراجع `refs/rescue/` · ٨ كومِتات بلا نسخة بعيدة بسبع حزم · ١٥٣ ملفًّا بنسخة محقَّقة البصمة (٠ اختلاف) في `أرشيف-الإنقاذ-2026-08-12`. **الأصل المتسخ لم يُلمس: ٣٥ عنصرًا و`18fd5b3` كما كان.** **ثمّ CC-204:** عائق البناء الذي أوقفها **غير قابل لإعادة الإنتاج** (٩٤ صفحة في ٥٢ ثانية على نسخةٍ نظيفة). كُسرت قاعدة `alreadyDerived` فصار **الرصيد المفتوح هو الحَكَم** (١٠٠←٦٠←٤٠ ولا يُغلق السطر إلا عند الصفر) عبر `PartialDerivationPanel`؛ ثمّ `createCombinedInChain` مسارٌ **واحد** لمصدرٍ واحد أو أكثر بقفل تخصيصٍ لكلّ مصدر داخل المعاملة نفسها — و`createNextInChain` صار غلافًا له، **واختبارٌ يثبت التكافؤ حرفًا بحرف** فلا تتغيّر الدورة القائمة. سياسة `multiSourceAllowed` صريحة (PR>PO · PO>GRN · PICK>PACK · TR>TRN) بحارسَي تطابق الرؤوس وجمع أرقام المراجع، و`fetchCombinableSources` تكتشف المرشّحين **بمحاولة دمجٍ حقيقيّة لا بتشابه حقول**. **البوابات:** 1291/1291 · بناء ٩٥ صفحة · تدقيق نظيف · ESLint نظيف على المتغيّر. **القواعد:** نشر المالك نسختين مطابقتين بالبصمة (١١٠٢ ثمّ ١١٤٤ سطرًا) قبل دفع الكود المعتمد عليهما — فلم تنكسر الدورة اليوميّة لحظة. **⚠️ الحدّ الصادق: لا شيء من هذا جُرّب حيًّا** — الشاشة محميّة بالدخول ولا حسابات أدوار (CC-602/و-٢)، ومحاكي القواعد يحتاج Java. **المتتبّع: نسخة العميل 10/22 (٤٥٪) والتالي CC-301.**


> **[Codex] 2026-08-12 — CC-204 بدأت ولم تُغلق؛ لا تثبّت مسودة كودها قبل إصلاح البناء:** أضيفت مسودة `documentFlow.js` واختباراتها (10 اختبارات) لسيناريو 100→60→40، ومنع التجاوز، واستقلال فرعي PUTAWAY/SRN، وقراءة الأطفال القدامى، ودمج مصدرين، وقفل تخصيص حتمي. عُدّل مسار `createNextInChain` تجريبيًا ليحسب المفتوح، ينشئ علاقات سطرية داخل المعاملة، ويحدّث `document_flow_allocations` بدل تعديل المستند المرحّل؛ وأضيفت قاعدة المجموعة. **المثبت حتى الآن:** الاختبارات الكاملة 1286/1286، audit نظيف و56 مجموعة محكومة، وESLint المستهدف نظيف. **المانع:** `npm run build` وAstro المباشر يبلغان `Building static entrypoints` ثم يدور Node باستهلاك CPU حتى المهلة؛ نُقلت `.astro` و`dist` ثم بُنيا نظيفًا فاستمر، وعُزلت واجهة اختيار الكميات فاستمر، وأزيل اعتماد documentRelationsService المباشر فاستمر. أُوقفت فقط عمليات البناء التي أنشأتها هذه المحاولات، وأُعيد آخر `dist/.astro` أخضر؛ آثار البناء الفاشل محفوظة خارج الريبو في `cc204-failed-build-artifacts`. **لا commit لكود CC-204 ولا ادعاء اكتمال.** الخطوة التالية: عزل استيرادات documentsService الجديدة واحدًا واحدًا، ثم إعادة واجهة الكميات وتوصيل مسار المصدرين حيًا واختبار القواعد. الأصل المتسخ لم يُلمس ولا دفع.

> **[Codex] 2026-08-12 — CC-203 أُنجزت في `bce28d7` وبدأت CC-204:** استُبدل عرض `ChainBar` الخطي بخريطة علاقات متعددة الاتجاهات تجمع `BASE/TARGET/REFERENCE/RETURN/REVERSAL/CORRECTION`، وتبقي عدة مصادر وعدة نتائج وطفلين من النوع نفسه، وتجمع الروابط السطرية بين المستندين مع الكمية/القيمة وعدد الأسطر وبيانات drilldown. كل عقدة غير الحالية تفتح URL المستند القائم، وتعرض النوع والرقم والحالة والتاريخ والمستخدم؛ الروابط القديمة تُقرأ توافقياً فقط وتُستبعد إذا وجدت علاقة مخزنة أغنى لنفس الزوج. عند تعذر `document_links` يظهر حدّ صريح بدل ادعاء اكتمال البيانات. **البوابات:** 19/19 مستهدفًا؛ 1276/1276 كاملًا؛ build=94 صفحة؛ audit نظيف؛ ESLint المستهدف نظيف؛ lint العام بقي 126 (53/73) بلا زيادة. **متصفح حقيقي مسجل على localhost:** ظهرت `PR → PO → GRN`، وفُتحت عقدة GRN في URL القائم، بلا أخطاء console؛ وعلى هاتف 390×844 تكدست البطاقات رأسيًا بلا overflow أفقي ولا إيموجي داخل الخريطة الجديدة. **الحد الصادق:** قواعد `document_links` لم ينشرها المالك، لذلك تحقق المتصفح من fallback القديم، أما تعدد العلاقات الجديدة والسطرية فمثبت آليًا لا حيًا. الأصل المتسخ لم يُلمس ولا دفع. **الآن CC-204:** السماح بالتنفيذ الجزئي المتعدد عبر الرصيد المفتوح مع منع التجاوز والتعارض المتزامن.

> **[Codex] 2026-08-12 — CC-202 أُنجزت في `f88121e` وبدأت CC-203:** رُكّب `DocumentNavigator` فعليًا داخل `DocumentEngine` فوق شريط الحالة، مع الأول/السابق/التالي/الأخير والبحث داخل النوع والجديد على URL القائم، وحماية برمجية للتغييرات غير المحفوظة. أضيفت ستة إجراءات مستقلة: العلاقات والمرفقات والسجل والموافقات والأثران المخزني والمالي، مع العدادات وحالات التعطيل قبل الحفظ ومرساة مستقلة لكل قسم؛ والأيقونات SVG خطية بلا إيموجي أو أحمر في المكوّن الجديد. **البوابات:** 13/13 مستهدفًا؛ 1270/1270 كاملًا؛ build=94 صفحة؛ audit نظيف؛ ESLint المستهدف نظيف؛ lint العام بقي 126 (53/73) بلا زيادة. **اختبار متصفح حقيقي بحساب قائم على Chrome/localhost:** بحث PO أعاد 10 سجلات؛ فتح الأول = 1/10 ثم التالي = 2/10 ثم الأخير = 10/10 مع تعطيل الحدود الصحيح؛ فُحص تمرير المرفقات وملخص الأثر المخزني بصورة مستقلة؛ لا أخطاء console. **الحدود الصادقة:** الهاتف لم يُختبر. محاولة فحص تنبيه المغادرة بعد تغيير محلي غير محفوظ استدعت حوارًا أصليًا عطّل قناة التحكم ولم يمكن التقاطه/إغلاقه منها؛ لم يُحفظ الحقل ولم تقع كتابة خادم. الأصل المتسخ لم يُلمس، ولا دفع. **الآن CC-203:** تحويل العرض الخطي إلى خريطة علاقات متعددة قابلة للفتح اعتمادًا على `document_links` مع توافق `links` القديمة.

> **[Codex] 2026-08-12 — CC-201 أُنجزت في `2fc5bac` وبدأت CC-202:** أضيفت `documentNavigator.js` كخدمة خالصة منفصلة عن خريطة العلاقات. تفهرس النوع نفسه فقط بعد حارس صلاحية fail-closed، تزيل تكرار المعرّف، وترتب الأقدم→الأحدث بختم Firestore ثم الرقم الطبيعي ثم المعرّف. تعيد الأول/السابق/الحالي/التالي/الأخير والموضع والإجمالي، بلا التفاف عند الحدود، وتبحث في الرقم والمعرّف والحالة وحقول الرأس، وتبني URL الشاشة القائم للمستند أو الجديد دون إنشاء مسار آخر. **البوابات:** 9/9 مستهدفًا؛ 1266/1266 كاملًا؛ build=94 صفحة؛ audit نظيف؛ ESLint المستهدف نظيف؛ lint العام 126 (53/73) بلا زيادة. لا UI تغيرت بعد ولا قبول بصري مدعى. **الآن CC-202:** تركيب شريط التنقل والإجراءات في `DocumentEngine` وفق bz-light، ثم اختبار المكوّن وفحص المتصفح الحقيقي.

> **[Codex] 2026-08-12 — CC-103 أُنجزت في `2f0e766` وبدأت CC-201:** أضيف `documentLineProgress.js` كمجمّع خالص للكمية المرجعية بحسب معنى المرحلة (`qty`/`qtyReceived`/`qtyInspected`/`qtyPicked`/`qtyShipped`...) ولتنفيذ BASE/TARGET فقط. يعيد لكل سطر المطلوب والمنفذ والمقبول والمرفوض والمفتوح وغير المقبول والتجاوز والحالة، ومع كل إجمالي drilldown حرفي للعلاقة والمستند والسطر الهدف. يقرأ أطفال `links` القدامى قراءة فقط بهوية فهرس، يميّز تكرار SKU، ويمنع مضاعفة fallback عندما توجد علاقة سطرية جديدة. الاختبارات تثبت 100→60 جزئيًا ثم +40 مكتملًا، والتجاوز 101، وتصنيف QC، وفروع PUTAWAY/SRN، وعدم احتساب الروابط المرجعية/الإرجاع/العكس/التصحيح إنجازًا تلقائيًا. **البوابات:** 10/10 مستهدفًا؛ 1257/1257 كاملًا؛ build=94 صفحة؛ audit نظيف؛ ESLint المستهدف نظيف؛ lint العام 126 (53/73) بلا زيادة. لا UI تغيرت ولا قبول بصري مدعى. **الآن CC-201:** خدمة تنقل موحدة داخل نوع المستند: الأول/السابق/التالي/الأخير/البحث/الجديد، منفصلة عن خريطة العلاقات.

> **[Codex] 2026-08-12 — CC-102 أُنجزت في `095cef6` وبدأت CC-103:** أضيفت `documentRelationsService.js` لكتابة `document_links` بالهوية الحتمية وقراءة الاتجاهين، وقاعدة Firestore صارمة ملحقة-فقط: إنشاء للفاعلين، تحقق من الطرفين والنوع والحقول وهوية الكاتب ووقت الخادم، ولا update/delete. تحوّل `createNextInChain` من أربع كتابات منفصلة إلى `runTransaction` واحدة تقرأ المصدر الحي ثم تنشئ الطفل وعلاقة BASE وقيدي التدقيق معًا؛ فلا يظهر طفل مشتق بلا رابط أو بلا أثر تدقيق عند فشل جزئي. لا ترحيل ولا تعديل للمستندات القديمة، و`firestore.rules` في الريبو فقط ولم تُنشر. **التحقق:** 13/13 مستهدفًا؛ 1247/1247 كاملًا؛ build=94 صفحة؛ audit نظيف و55 مجموعة محكومة؛ ESLint المستهدف نظيف؛ lint العام بقي 126 (53/73)، صفر جديد. أضيفت 3 اختبارات إيميولاتور للعلاقة والمعاملة والرفض، لكن تشغيلها المحلي **غير مثبت** لأن Firebase CLI توقف قبل تجميع القواعد: Java غير موجودة في PATH؛ لم أصف القواعد بأنها مجرّبة حيًا. **الآن CC-103:** مجمّع تقدم السطر للمطلوب/المنفذ/المقبول/المرفوض/المفتوح مع drilldown وتوافق القديم.

> **[Codex] 2026-08-12 — CC-101 أُنجزت في `ef22d89` وبدأت CC-102:** أضيف نموذج خالص `documentRelations.js` و10 اختبارات: الأنواع `BASE/TARGET/REFERENCE/RETURN/REVERSAL/CORRECTION`؛ هوية حتمية لا تشمل الوقت/الرقم/الكمية؛ طفلان من النوع نفسه؛ هدف من مصدرين؛ سطر قديم بهوية فهرس قراءة بلا mutation؛ replay=`noop` وتغير الكمية تحت الهوية نفسها تعارض؛ كمية بلا سطري مصدر/هدف مرفوضة؛ وتحويل `links` القديمة إلى BASE/REFERENCE دون ترحيل، مع فوز السجل الجديد عند التطابق. **البوابات:** build 94 صفحة؛ 1244/1244؛ audit نظيف؛ ESLint الملفين نظيف؛ lint العام بقي 126 (53/73)، صفر جديد. لا UI تغيرت ولا قبول بصري مدعى. **الآن CC-102:** خدمة Firestore + قواعد append-only وربط الكتابة بمعاملة ترحيل واحدة؛ القواعد في الريبو فقط ولا نشر.

> **[Codex] 2026-08-12 — CC-003 أُنجزت في `b703143` وبدأت CC-101:** تقرير [`docs/تقرير-جرد-الشجرة-المتسخة-2026-08-12.md`](docs/تقرير-جرد-الشجرة-المتسخة-2026-08-12.md) يصنف الأصل بلا تعديل: الفرع القديم متقدم بكومِتين محليين ومتأخر 71 عن `origin/main`؛ 8 ملفات تظهر معدلة (6 فروق محتوى + 2 metadata-only)؛ 145 ملفًا غير متتبعًا بحجم 49,186,847 بايت؛ 23 worktree مسجلة، 7 نظيفة، و`prune --dry-run` لا يجد stale؛ ومرجع checkpoint صفري يجعل `show-ref=128` و`fsck=2`. ثبتت نسختان متطابقتان فقط بالهاش، لكن الأدلة والتقارير والصور والكود غير المرفوع محفوظة ولا تُحذف. الإصلاح المقترح مرحلي وبعد موافقة منفصلة على لمس `.git`؛ لا `reset/clean` ولا حذف worktree كمجلد. **فُحصت مهمة Claude `019fed0a…` مباشرة:** توقفت بعد تشخيص الفجوات نفسها ولم تسجل تنفيذ الطلب الأخير. **الآن CC-101:** نموذج علاقات خالص متعدد وعلى مستوى السطر مع توافق `links` القديمة، والاختبارات أولًا.

> **[Codex] 2026-08-12 — CC-002 أُنجزت في `e955a0d` وبدأت CC-003:** استُعيد ملفا Claude [`docs/سيناريو-الاختبار-الشامل-للبوابة.md`](docs/سيناريو-الاختبار-الشامل-للبوابة.md) و[`docs/خطة-مركز-التقارير.md`](docs/خطة-مركز-التقارير.md) من `.claude/worktrees/brave-payne-40411e` عبر `apply_patch`، ونجحت مقارنة SHA-256 حرفيًا (`043520…155763` و`C316DF…4B7395`). أُغلقت `و-٣` و`و-٤` التاريخيتان ببيّنة، وأصبح `م٨-ب` غير محجوب وثائقيًا؛ لا يعني ذلك أن التقارير الـ21 نُفذت، بل صارت مواصفتها الحاكمة موجودة. **الآن CC-003:** جرد الشجرة الأصلية المتسخة والمرجع Git المعطوب وتصنيف كل شيء قبل اقتراح تنظيف عكسي؛ لا حذف ولا `reset` ولا `clean`.

> **[Codex] 2026-08-12 — CC-001 أُغلقت في `6d152fa` وبدأت CC-002 على فرع `codex/client-completion-20260812` دون دفع:** أُنشئت نسخة نظيفة مستقلة عند `origin/main` = `47017aa` لأن الشجرة الأصلية متسخة ومرجعًا داخليًا مشتركًا معطوبًا منع `git pull` في worktree مرتبط؛ لم يُحذف أو يُنظف شيء من الأصل. ثُبّتت [`docs/خطة-اكتمال-نسخة-العميل-المشتركة.md`](docs/خطة-اكتمال-نسخة-العميل-المشتركة.md) وكل متطلبات المالك في متتبّع مستقل: 13 فجوة و22 مهمة، مع فصل متصفح النوع عن خريطة العلاقات، وروابط سطرية متعددة، وتنفيذ جزئي، وتحميل VLD، وRCV/لوحة المندوب، والأبعاد، وأودو، و21 تقريرًا متبقيًا، والذكاء التشغيلي. صار `npm run plan` يحرس الخطتين ويولد بطاقتي الاستئناف؛ المرحلة الأولى بقيت تاريخيًا 22/30 (73٪). **خط الأساس المثبت:** بناء 94 صفحة، 1234/1234 اختبارًا، audit نظيف، وlint أساسه 126 مشكلة قديمة (المعيار صفر جديد). **الآن CC-002:** استعادة وثيقتي Claude المفقودتين من worktree القديم مع تطابق SHA-256 حرفي، ثم تحديث المتتبّعين. الصورة المشار إليها في مرفق المالك لم تكن ملفًا مرفقًا؛ النص محفوظ كاملًا والخطوة البصرية تبقى معلقة إن كانت الصورة مرجعًا ملزمًا.

> **[Claude] 2026-08-11 — 🏁🏁 اكتملت المرحلة الأولى كلُّها بما لا ينتظر المالك — ٢٢ من ٣٠ (٧٣٪) و«الجاهز = صفر»، والفجوات الاثنتا عشرة ف‑١…ف‑١١ سُدّت جميعًا:** م‑١ التصليحات وحدّ المال والإعدادات · م‑٢ نزاهة التاريخ · م‑٣ هويّة الصنف · م‑٤ وحدة الذمم · م‑٨أ محرّك التقارير و١٢ تقريرًا · م‑٥ الميدان الماليّ وأنواع الزيارة · م‑٦ المواقع التنظيميّة · م‑٧ لوحة التكامل والجسر المحكوم بها · م‑٩ الذكاء التشغيليّ. **من ٩٠٠ اختبار إلى ١٢٣٤** · من ٨٨ صفحة إلى ٩٤ · من ٣٥ نوع مستند إلى ٣٨ (RCP · SPV · RCV) · **٤٢ كوميتًا على فرع `claude/phase-one-repairs-plan-07f596`، لم يُرفع منها شيء.** **الخيط الجامع: كلّ ميزةٍ تُفعَّل بالبيانات لا بالنشر** — `typeOf`⇒sale · `hasUomDefinition` · لا قائمة⇒كتابة يدويّة · لا سقف⇒لا منع · لا خريطة⇒سلوك اليوم · زيارةٌ بلا نوع⇒الأوسع · سياسةُ تكاملٍ غائبة⇒سلوك اليوم. **وستّة عشر اختبارًا موسومًا `★★` تحرس هذا الاتّجاه وحده** لأنّ حارسًا يمنع ما يجب أن يمرّ أسوأ من الفجوة التي يسدّها. **🚨 الحرّاس أمسكت أعطابي إحدى عشرة مرّة** (انحراف التشكيل · اللينت مرّتين · انحراف الصلاحيات **أربع مرّات** · انحراف السلاسل · أربعة سجلّاتٍ نسيتُ RCP فيها · وعطبٌ في تصنيفي أنا كشفه حارسي · وفئةُ الأصناف في حارس الاتّجاه). **⚠️ الثمانية الباقية كلُّها على المالك:** ① **نشر `firestore.rules`** — تغيّرت كثيرًا: `settings` · حارس التاريخ على `documents` · `price_lists` · `partner_ledger` · `org_locations` · `integration_policy` · أدوار RCP/SPV/RCV ② **م٢-د إبلاغ المستخدمين** قبل تفعيل حارس التاريخ ③ ٦ حسابات اختبار ④ الوثيقتان المفقودتان (و‑٣ يحجب سيناريو Codex وم١-أ · و‑٤ يحجب م‑٨ الكاملة) ⑤ خمس حزم بيانات (تصنيف · معاملات · أسعار · أرصدة · سقوف) — **ولا واحدة منها تُعطّل شيئًا**، لكلٍّ سلوكٌ افتراضيّ معلَن ⑥ قرارا `PICK` والصنفَين الزائدَين. **الاستئناف: `npm run plan`.**

> **[Claude] 2026-08-11 — 🏁 ✅✅ اكتمل المسار الأساسيّ للمرحلة الأولى (١٦ من ٣٠ مهمّة · ٥٣٪) — سُدّت ست فجوات: ف‑١ ف‑٢ ف‑٣ ف‑٤ ف‑٤ب ف‑٧ ف‑٨ ف‑٩:** **م‑١** حدّ المال + سياسات التشغيل · **م‑٢** نزاهة التاريخ (تصنيف ٦٦ حقلًا + حارس بـ`request.time` + تقرير النمط) · **م‑٣** هويّة الصنف (النوع · الوحدات · الأسعار) · **م‑٤** وحدة الذمم (RCP · SPV · الدفتر · حدّ الائتمان · الكشوف) · **م‑٨أ** محرّك التقارير و١٢ تقريرًا. **من ٩٠٠ اختبار إلى ١١٥٨** (+٢٥٨) · من ٨٨ صفحة إلى ٩٣ · من ٣٥ نوع مستند إلى ٣٧. **الخيط الجامع لكلّ المراحل: الترحيل صفرُ الأثر** — كلّ ميزةٍ تُفعَّل **بالبيانات لا بالنشر** (`typeOf`⇒sale · `hasUomDefinition` · لا قائمة⇒كتابة يدويّة · لا سقف⇒لا منع · لا خريطة⇒سلوك اليوم)، وعشرة اختبارات موسومة `★★` تحرس هذا الاتّجاه وحده لأنّ **حارسًا يمنع ما يجب أن يمرّ أسوأ من الفجوة التي يسدّها**. **🚨 الحرّاس أمسكت أعطابي أنا تسع مرّات:** انحراف التشكيل (عمود استيراد مفقود) · اللينت (اشتراكٌ لم يُركَّب فكان الحارس معطَّلًا · حرف BOM خفيّ) · **انحراف الصلاحيات أربع مرّات** (صفحاتٌ في مجموعاتٍ لا يراها أصحابها — اسمٌ بلا باب) · انحراف السلاسل (RCP في سلسلةٍ خطّيّة وهو يُقاصّ فاتورةً أو أكثر) · وأربعة سجلّاتٍ نسيتُ تسجيل RCP فيها. **⚠️ ما ينتظر المالك (١٠ بنود في `npm run plan`):** ① **نشر `firestore.rules`** — تغيّرت كثيرًا: `settings` · حارس التاريخ على `documents` · `price_lists` · `partner_ledger` · أدوار RCP/SPV ② **م٢-د إبلاغ المستخدمين** قبل تفعيل حارس التاريخ ③ قرارا `PICK` والصنفَين الزائدَين ④ بيانات: تصنيف الأصناف · معاملات الوحدات · قوائم الأسعار · الأرصدة الافتتاحيّة · سقوف الائتمان ⑤ الوثيقتان المفقودتان (و‑٣ لسيناريو Codex · و‑٤ لـم‑٨ الكاملة). **لم يُرفع شيءٌ إلى main.**

> **[Claude] 2026-08-11 — ✅ `م‑٣` هويّة الصنف اكتملت (م٣-أ `4c1cb0f` · م٣-ب `85cfe26` · م٣-ج) — تسدّ ف‑٣ وف‑٤ وف‑٤ب:** أخطر مراحل الخطة لأنّها تمسّ **كلّ كمّيّة وكلّ سعر**. **الثلاثة بترحيلٍ صفرِ الأثر، وهو شرط القبول لا ميزة:** ① **النوع** — `typeOf` يُعيد `sale` لكلّ صنفٍ بلا نوع، وغيابُ خريطة الأنواع = سلوك اليوم: كلّ البنود تُقيَّد ② **الوحدات** — مفتاح `hasUomDefinition`: التحويل **لصنفٍ عرّف المالك وحداته وحده**، و`unit` النصّيّ القديم **لا يُعدّ تعريفًا** وإلّا انقلبت الميزة على كلّ الأصناف في لحظةٍ فرُفض كلّ استلامٍ ببندٍ وحدتُه «صندوق» ③ **الأسعار** — لا قائمة ⇒ الكتابة اليدوية تمرّ بلا وسمٍ ولا تنبيه ولا سعرٍ يُدهس. **سبعة اختبارات موسومة `★★` تحرس هذا الاتّجاه وحده.** **قرارات تصميميّة:** «يُخرج ملكيّة» غير «يُخرج مخزونًا» (النقل بين مستودعَينا يُخرج مخزونًا ولا يبيع، فالداخليّ يمرّ فيه) · الخدمة تُباع ولا تُحمَّل (VSI يمرّ وVLD يُمنع) · داخل عائلة الوحدات المعامل ثابتٌ للجميع ولا يملك صنفٌ أن يخالفه، وعبر العائلات خاصٌّ بالصنف · `null` من `factorToBase` تعني «لا أعرف» لا «صفر» · معاملٌ صفرٌ **أخطر من غيابه** · الدفتر بوحدة الأساس ويحفظ `entryQty`/`entryUom` · تقرير الانحراف يقرأ **الوسم** لا يقارن بأثرٍ رجعيّ (القائمة تتغيّر والوسم لا) ويرتّب **بالأثر بالدينار لا بالنسبة**. **🚨 ثلاثة أعطاب أمسكتها الحرّاس لا أنا:** ① حارس انحراف التشكيل: `itemType` بلا عمود استيراد — وتصنيفُ مئات الأصناف بيدٍ عملٌ لا يُنجَز، فأُضيف العمود بمرادفاته العربيّة ② اللينت: اشتراك ماستر الأصناف لم يُركَّب فكان الحارس **معطَّلًا تمامًا** ③ اختبار انحراف الصلاحيات **مرّتين**: صفحتان أُدرجتا في مجموعتَين لا يراهما أصحابهما — اسمٌ بلا باب. **١٠٥٩ اختبارًا** · ٩١ صفحة. ⚠️ قاعدة `price_lists` تحتاج نشرًا · لم يُرفع.

> **[Claude] 2026-08-11 — `م٢-ب` منجزة: حارس نزاهة التاريخ (`a010da4`) — يسدّ ف‑٨:** **ثلاث طبقات والحكم في الثالثة:** ① `datingGuard.js` منطقٌ خالص **لا يعرف اسم حقلٍ واحد** (الأصناف من م٢-أ والمدى من م١-ج) ② `documentsService` يمنع الحفظ ويكتب الوسم بهويّة الخادم ويقيّد في التدقيق ③ **`firestore.rules` هي الحكم بـ`request.time`** — فمن قدّم ساعة حاسوبه يجتاز فحص الواجهة **ويرتدّ من الخادم**. **🔑 نصف الشغل كان فيما لا يُحرَس عمدًا:** صلاحيّة الدفعة وموعد السداد والتسليم المطلوب **تقبل المستقبل**؛ حارسٌ يمنعها يرفض كلّ استلامٍ لبضاعةٍ صالحة — وهو فشلٌ أسوأ من الفجوة، فله اختباران مخصّصان (`★★`). المسوّدة تُفتح بختم اليوم لا بفراغ · القيد يُرى **وهو يقع** لا عند الحفظ (لوحة + مربّع سبب) · الوسم دائمٌ ظاهر (كم يومًا · لماذا · بيد من) والقاعدة تمنع محوه أو قلبه. **٩٧٠ اختبارًا** (+٢٢) · الحزمة تحمل الحارس والطرفيّة نظيفة. ⚠️ **قواعد `documents` تغيّرت — تحتاج نشرًا** · و**`م٢-د` (إبلاغ المستخدمين) يسبق التفعيل الحيّ**. ⚠️ لم يُرفع. **التالي: `م٢-ج`** تقرير التواريخ المعدّلة.

> **[Claude] 2026-08-11 — `م٢-أ` منجزة: تصنيف الحقول الزمنيّة (`b085e5b`) — تمهيدًا لسدّ ف‑٨:** مسحُ الـ٣٥ أخرج **٦٢ حقلًا زمنيًّا**: ٣٤ ختمَ واقعة · ١٨ سمةَ بيانات · ٩ مخطّطة · ١ منقول. **صنفان زائدان على الثلاثة في الخطة اقتضاهما الواقع:** ① **سمة بيانات** = صلاحيّة الدفعة (١٨ من ٦٢) — لو عُدّت ختمَ واقعةٍ لرفض حارسُ «لا واقعة في المستقبل» **كلَّ استلامٍ لبضاعةٍ صالحة**، أي لتوقّف المستودع أوّل يوم ② **تاريخ منقول** = `PICK.orderDate` يخصّ أمر البيع لا السحب، وختمُه بزمن السحب يعرض رقمًا كاذبًا تحت تسميةٍ صادقة. **🚨 عطبٌ كشفه الحارس على تصنيفي نفسه:** `PICK` **لا ختم واقعة له إطلاقًا** — يُنشأ صباحًا ويُنفَّذ مساءً ولا حقل يقول متى سُحبت البضاعة؛ سُجّل استثناءً مُعلَنًا بسببه وبديله (`createdAt`) وعلاجه (`pickedAt`) **ولم تُضعَّف القاعدة**. الحارس **ثنائيّ الاتّجاه**: يفشل عند حقلٍ جديد بلا تصنيف وعند تصنيفٍ لحقلٍ محذوف، ومعه اختبارٌ يثبت أنّه يُمسك فعلًا (مخطّطٌ مصطنَع) — فحارسٌ غير مُختبَرٍ ادّعاء. `npm run timefields` يولّد جدول المراجعة. **٩٤٨ اختبارًا** (+١٤). ⚠️ لم يُرفع. **التالي: `م٢-ب`** ختم الخادم والحرّاس.

> **[Claude] 2026-08-10 — `م١-ج` منجزة: سياسات التشغيل (`a5fd42a`) — القرارات ٤–٧ إعداداتٍ لا شيفرة:** المالك فوّض القرار، فقُدّمت على م‑٢/م‑٣/م‑٤ لأنّ الثلاثة تقرأ منها — وبناؤها متأخّرةً يعني حرّاسًا بقيمٍ مخبوزة تُعاد كتابتها ثلاثًا. **القرار التصميميّ:** الحارس **لا يقرأ حقلًا بل يسأل سؤالًا** — `creditVerdict` و`backdateVerdict` و`internalItemInSaleVerdict` و`manualPriceVerdict` — فلو تغيّر شكل المستند تغيّر موضعٌ واحد لا خمسة حرّاس. **ثلاث دقائق حَرِجة:** ① `creditVerdict` يفحص الرصيد **بعد الإضافة** (العطب الشائع فحصه قبلها فيمرّ بيعٌ يتجاوز السقف بعده) ② `backdateVerdict` **لا يقرأ ساعة النظام** — يُمرَّر اليوم صراحةً، وإلّا انحرف الحارس عن ختم الخادم وصار الاختبار متقلّبًا ③ قيمةٌ فاسدة **تسقط إلى افتراضها وتُعرض** في الشاشة بدل أن تختفي بصمت. **القاعدة:** قراءة للمصادَق (من يُمنع يجب أن يعرف بأيّ قاعدةٍ مُنع)، وكتابة **للأدمن وحده** — مدير المستودع مستثنًى عمدًا لأنّه نفسه `overrideRole` في قرارين فلو كتبها لوسّع صلاحيته. الشاشة `/dashboard/policy-settings` تعرض **أثر المسوّدة قبل الحفظ**. **التحقّق:** ٩٣٤ اختبارًا (+٢٣) · تدقيق نظيف · بناء ٨٩ صفحة · الجزيرة تُصيَّر. ⚠️ **القاعدة تحتاج نشرًا** + لم يُرفع. **التالي: `م٢-أ`** جرد الحقول الزمنيّة في المستندات الـ٣٥.

> **[Claude] 2026-08-10 — `م١-ب` منجزة: حدّ المال (`32f0b37`) — تسدّ ف‑٩:** السياسة «اسحب المالي ولا ترفعه» كانت مخروقة في **ثلاثة** مخطِّطات لا اثنين — `poMapper` و`vanSalesMapper` و**`docCrosswalk`** (`price_unit` في المسار العامّ، لم تكن مرصودة في الخطة). **القرار التصميميّ:** الحدّ **بأنماطٍ لا بقائمة أسماء** (`moneyFields.js`) — قائمةٌ ثابتة تحرس ما نعرفه اليوم وحده، والأنماط تلتقط ما لم يُخترع بعد؛ ولا تخلط الكمّيّة بالمال (`x_total_received` و`x_stock_variance` و`x_payment_mode` و`x_is_free` و`currency_id` تمرّ). و**الحارس في نقطة الاختناق `pushOnce`** لا في المخطِّطات وحدها، فيغطّي أيّ مخطِّطٍ يُكتب بعده. وفُصل العرض عن الدفع: `poDocTotal`·`vanDocTotal`·`settlementCashVariance` تحسب محلّيًّا ولا تُرسَل. والسحب مصون (`purchaseOrderToPoSummary` يقرأ `amount_total` من أودو — تلك هي المرآة). **⚠️ أثرٌ مقصود ينتظر إقرار المالك:** محضر التسوية `VSR` صار في أودو واقعةَ إقفالٍ تشغيليّة (فرق عدٍّ وزيارات) **بلا الفرق النقديّ** — يعود في م‑٤ وم‑٥ ويُقرَّر اتّجاهه في م٧-ب. **التحقّق:** ٩١١ اختبارًا (+١١) · الحارس اختُبر عدائيًّا بحقلٍ ماليٍّ **جديد** لم يُذكر في أيّ قائمة (`amount_tax`) فأسقط الاختبار · تدقيق نظيف · بناء ٨٨ صفحة. ⚠️ لم يُرفع. **التالي: `م١-ج`** (مستند الإعدادات — ينتظر إقرار المالك).

> **[Claude] 2026-08-10 — آليّة تنفيذ المرحلة الأولى (م‑٠):** نُقلت الخطة المعتمدة من سطح المكتب إلى [`docs/خطة-إصلاحات-المرحلة-الأولى.md`](docs/خطة-إصلاحات-المرحلة-الأولى.md) بنصّها حرفيًّا. أُنشئ **المتتبّع** `docs/plan/phase-one.json` (٣٠ مهمّة · ١٢ فجوة · ٥ مدخلات من المالك) و**الحارس** `scripts/plan-check.mjs` (`npm run plan`) الذي يرفض: جبهتين في آن · `done` بلا كوميت أو بلا اختبار حارس على القرص · حجبًا بلا سبب · فجوةً بلا مهمّة تسدّها — واختُبر عدائيًّا بمتتبّع مكسور عمدًا فأمسك الخروق الخمسة وأنهى بـ1. **التحقّق من الكود لا من الوثيقة:** ف‑١ وف‑٢ وف‑٣ وف‑٤ وف‑٤ب وف‑٩ كلّها قائمة فعلًا. **رُصد ناقصان:** وثيقتا «سيناريو الاختبار الشامل» و«خطة مركز التقارير» محال إليهما في القسم ٩ **وغير موجودتين** (`و‑٣` و`و‑٤` محجوبتان)، و`settings/current` أُضيف كمهمّة `م١-ج` لأنّ م‑٢ وم‑٣ وم‑٤ تقرأ منه. **الجاهز للبدء بلا انتظار أحد: `م١-ب`** إخراج حقول المال من مخططات أودو. ⚠️ لم يُرفع بعد.

> **[Claude] 2026-08-10 — تكامل أودو: المرحلة ٦ — وبها اكتملت مواصفة SFA ١٠/١٠ (مرفوعة لـmain `9809960` ومنشورة حيًّا):** البند العاشر: «تكامل كامل مع أودو **دون ازدواجية بيانات**». **🚨 العطب الذي كشفه المسح وأُصلح:** الجسر كان يستدعي `odoo.create` **بلا فحص** ثمّ يكتب مرآته بمعرّفٍ حتميّ و`merge:true` — فالدفع مرّتين يُنشئ **سجلَّين في أودو ومرآةً واحدة**: ازدواجٌ صامتٌ لا يُكتشف إلّا في تقريرٍ ماليٍّ بعد شهور. وليست الحالة نادرة (شبكةٌ تنقطع بعد الإنشاء وقبل المرآة · ضغطةٌ مزدوجة · إعادة مزامنةٍ بعد فشلٍ ظاهر). **الحلّ `pushOnce` بثلاث طبقات:** ① المرآة تحمل `odooId` ⇒ تحديث · ② لا مرآة لكنّ أودو يحمل سجلًّا بالأثر الراجع `x_source_number` ⇒ **تبنٍّ** (تعافٍ من ضياع المرآة — الحقيقة عند أودو لا عند مرآتنا) · ③ وإلّا إنشاء. والنسخ الزائدة **تُبلَّغ ولا تُحذف** (الحذف قرارٌ بشريّ). **حُصِّنت المسارات الخمسة كلّها:** `pushPurchaseOrder` · `pushGoodsReceipt` · `pushItem` (بـ`default_code` لا بالأثر الراجع) · الاستلام التلقائيّ من اعتماد PO · والمسار العامّ. لم يبقَ `odoo.create` مباشرًا إلّا داخل الحارس نفسه وفي `syncLedgerMoves` المحروس أصلًا. **`vanSalesMapper.js`**: الأنواع الثمانية إلى نموذجَين — `sale.order` لما يُخرج الملكيّة (`VSI`·`VCS`) و`stock.picking` لما يُحرّك بين مواقعنا (`VLD`·`VRT`·`CRN`·`VCD`·`VCR`) و`x_van.settlement` للتسوية. **أسماء المواقع تطابق دفترنا حرفيًّا** (`VAN/‹لوحة›` · `CUST/‹رمز›`) فتُطابَق الأرصدة بلا جدول ترجمةٍ ثالثٍ ينحرف. والخصم يُحوَّل نسبةً لأودو مع حراسة القسمة على صفر (المجّانيّ سعره صفر). واختبار انحرافٍ يقارن نموذج الموزّع بجدول العبور فلا يفترق الجسر عن الكروسووك. الواجهة لم تحتج تعديلًا — تشتقّ الأنواع من المستندات نفسها. **التحقق: ٩٠٠ اختبارًا · بناء ٨٨ صفحة · تدقيق نظيف · لينت نظيف.** ⚠️ يتبقّى: نشر القواعد + التجربة الحيّة للدورة كاملةً.
>
> **[Claude] 2026-08-10 — المستهدفات والإنجاز: المرحلة ٥ (مرفوعة لـmain `9809960`):** البند الثالث من مواصفة المالك (أهدافٌ حسب مندوب/مدينة/منطقة/سيارة/عميل/مجموعة/صنف/علامة بكميّةٍ أو قيمةٍ أو زيارات). **ثلاثة قرارات معماريّة:** ① **هدفٌ واحد بشكلٍ واحد** — بدل أحد عشر نموذجًا، الهدف *مرشِّحٌ ومقياسٌ ومدّةٌ ورقم*: `DIMENSIONS` (٩ أبعاد) × `METRICS` (٦ مقاييس) × مدّة. فبُعدٌ جديد سطرٌ في جدولٍ لا نموذجٌ جديد. ② **الإنجاز يُحسب من المستندات المنجَزة لا من دفتر الحركات** — لأنّ الدفتر لا يحمل أبعاد الإسناد عمدًا (يحمل `postedByUid` وحده، لا عميلًا ولا مركبةً ولا خطّ سير)، والأبعاد تعيش في رؤوس المستندات. ولا حقل `achieved` مخزَّن: ما يُخزَّن ينحرف، وما يُحسب لا ينحرف — ولا يستطيع أحدٌ تحسين رقمه بتعديل صفّ. ③ **الإيقاع لا النسبة وحدها** — «٤٠٪» ناقصة: أفي اليوم الثالث (تفوّق) أم الثامن والعشرين (كارثة)؟ فيُحسب المتوقّع حتى اليوم والمطلوب يوميًّا والإسقاط. **ثلاثة حرّاس دقيقة:** `INV` **ليست** في أنواع المبيعات (`VSI·POD·VCS` فقط) وإلّا تضاعف الإيراد لأنّ الفاتورة مرآةٌ ماليّة لإذن التسليم · **المجّانيّ يُحتسب كميّةً لا قيمة** ويُعرَض على حدة (إخفاؤه يُظهر المندوب أقلّ إنتاجًا، وحسابُه في القيمة يُظهر إيرادًا لم يُحصَّل) · **مقياس زياراتٍ على بُعد صنفٍ يُمنع** عند الحفظ — لولاه لظهر الإنجاز صفرًا أبدًا فيظنّ المندوب أنّه مقصّر وهو لم يُكلَّف بما يُقاس. و«نسبة النجاح» تُستثنى من الإيقاع لأنّها حالةٌ لحظيّة لا رصيدٌ يتراكم. `src/services/targets/` (`targetModel.js` + `achievement.js`، ٢٨ اختبارًا) + `/dashboard/targets` (شريط تقدّم بعلامة الإيقاع؛ **المندوب يرى مستهدفاته وحدها**) + قاعدة `targets`. **التحقق: ٨٨٠ اختبارًا · بناء ٨٨ صفحة · تدقيق نظيف · لينت نظيف.** ⚠️ يتبقّى نشر القواعد. **المرحلة ٦ (مخطِّطات أودو) لم تبدأ.**
>
> **[Claude] 2026-08-10 — البضاعة المحميّة والصلاحية عند العميل: المرحلة ٤ (مرفوعة لـmain `9809960`):** **تفعيل `CUST:` الذي مُهِّد في المرحلة ٠** — كان موجودًا في `locations.js` ومعزولًا عن القيد، فصار موقعًا حقيقيًّا في الدفتر. الرمز `CUSTOMER = '@customer'` في `postingRules.js` + محلّه في `movements.js` (يقرأ `header.customerCode`)، وثلاث قواعد قيد: `VCD` إيداع (مركبة→عميل) · `VCS` تحقّق بيع (عميل→خارج) · `VCR` استرداد (عميل→**مركبة** لا مستودع). **والمكسب الأكبر مجّانيّ:** حارس الرصيد السالب صار يحمي الأمانة بلا سطرٍ إضافيّ — لا يُستردّ ولا يُحتسب مبيعًا إلّا ما أُودع فعلًا. ثلاثة مخطّطات جديدة (**٣٢→٣٥**) بأدوارها وصفوف أودو وقواعد الخادم. **محرّك السياسات** `src/services/protection/protectionModel.js` (١٧ اختبارًا) بستّ سياسات (لا إرجاع · إرجاع كامل · المنتهي فقط · استبدال · حماية بمدّة · حماية حتى الانتهاء) و**التمييز الحاكم: الأهليّة غير المصير** — السياسة تُجيب «هل يُقبل الاسترداد؟» والحالُ يُجيب «إلى أين يذهب؟». فصنفٌ سياسته «إرجاع كامل» وعاد تالفًا **يُقبل استرداده ويُتلَف**؛ ومن يخلط بينهما يُعيد التالف إلى رفّ البيع لأنّ «للعميل حقّ الإرجاع». و`customerStockAlerts` يكشف المنتهي والمقارب ونوافذ الحماية التي توشك على الانقضاء — وهو التقرير الذي يُنقذ المال فعلًا. الشاشة `/dashboard/customer-stock` (٣ تبويبات: حسب العميل · كلّ البنود · محرّر السياسات للمشرف) + قاعدة `protection_policies` (مستندٌ واحد كنمط `access_control/matrix`). **التحقق: ٨٥٢ اختبارًا · بناء ٨٧ صفحة · تدقيق نظيف · لينت نظيف.** ⚠️ يتبقّى: نشر القواعد (٤ مواضع جديدة: `protection_policies` + VCD/VCS/VCR في approve/completeRoles). **المرحلتان ٥ و٦ لم تبدآ** (المستهدفات · مخطِّطات أودو).
>
> **[Claude] 2026-08-10 — محرّك العروض الترويجيّة: المرحلة ٣ (مرفوعة لـmain `e3480de` ومنشورة حيًّا):** تنفيذ بند «عروض وتحميل صنف على صنف» من مواصفة SFA. **القرار المعماريّ الحاكم: المجّانيّ بضاعةٌ لا خصم** — أشيع خطأٍ في أنظمة التوزيع معالجة «اشترِ ١٠ خذ ١» بخصمٍ قيمته كرتونة، فتخرج الكرتونة الحادية عشرة من المركبة بلا قيدٍ ويظهر عجزٌ في تسوية الرحلة يُنسب للمندوب سرقةً وهو بريء. هنا يخرج المجّانيّ **بندًا كامل الأهليّة** (`isFree:true` · `unitPrice:0` · `unitCost` حقيقيّة) يمرّ في الدفتر ويُخصم من رصيد المركبة ويدخل التسوية. الطبقة الخالصة (`src/services/promotions/`، ٢٨ اختبارًا): `promotionModel.js` (٥ أنواع: اشترِ X خذ Y · تحميل صنف على صنف · خصم متدرّج · حزمة · اخلط واختر — مع `promotionVerdict` يمنع حفظ عرضٍ معطوب لأنّه **لا يُخطئ بصوتٍ عالٍ بل لا ينطبق أبدًا**) · `promotionEngine.js` (`evaluateOrder` **حتميّ**: ترتيبٌ بالأولويّة ثمّ الرمز لا بترتيب قاعدة البيانات · حصريّة تحتكر بنودها · **سقفٌ يمنع الخصم من تجاوز قيمة البند** مهما تراكمت العروض · `skipped` بأسبابٍ ظاهرة لا مبتلعة · **`nudges` تلميحات بيعٍ إضافيّ** «أضف ٢ ليستحقّ العميل العرض»). `promotionsService.js` يقيّد الاستهلاك بـ`increment` ذرّيًّا (مندوبان يبيعان معًا لا يضيع عدّ أحدهما). **حارس تسريب الترويجات في `VSI`**: بندٌ بسعر صفر أو بخصمٍ بلا `promoCode` يُوسَم — لا يُمنع (قد يكون عيّنةً معتمدة) لكن لا يمرّ بلا أثر. الشاشة `/dashboard/promotions` (للمشرف والمديرَين — **المندوب لا يراها**) بمحرّرٍ لكلّ نوع و**محاكي طلبٍ حيّ** يُظهر الأثر قبل الإطلاق. قاعدة `promotions`: التعريف للمشرف والمديرَين، و`usage` وحده يكتبه المندوب بـ`affectedKeys().hasOnly` فلا يمدّد سقفًا. **التحقق: ٨١٤ اختبارًا · بناء ٨٦ صفحة · تدقيق نظيف · لينت نظيف.** **✅ وُصل المحرّك بالفاتورة:** لوحة `PromotionsPanel` داخل `DocumentEngine` (لـ`VSI` و`SO`) تُقيّم لحظيًّا وتُطبّق بضغطة، ومنطقها الخالص في `services/promotions/invoiceApply.js` (+٩ اختبارات). **ضمانتان:** التقييم على **المدفوع وحده** فلا يُنتج المجّانيّ مجّانيًّا (وإلّا توالدت الهدايا)، والتطبيق **إيديمبوتنت** فالضغط مرّتين كالضغط مرّة. و`isInSync` يُنبّه إن أُضيف بندٌ بعد التطبيق. ⚠️ يتبقّى: نشر القواعد. **المراحل ٤–٦ لم تبدأ.**
>
> **[Claude] 2026-08-10 — طبقة الميدان: المرحلة ٢ (مرفوعة لـmain `80f79d6` ومنشورة حيًّا):** تنفيذ بندَي «تتبّع المندوب» و«خطّة الزيارات» من مواصفة SFA. **أوّل استعمال لـ`navigator.geolocation` في المشروع كلّه.** الطبقة الخالصة (٢٦ اختبارًا، `src/services/field/`): `geo.js` (هافرساين · `fenceVerdict` · `routeDistanceMeters` · `centroid`) · `journeyPlan.js` (PJP بدوريّات يوميّ/أسبوعيّ/كلّ أسبوعين/شهريّ تُحسب من `startDate` · `visitsDueOn` · `planCompliance` · `coverageGaps`) · `visitModel.js` (آلة حالات `planned→checked_in→checked_out|skipped` · `visitVerdict` · `invoiceGuard` · `summarizeVisits`). **ثلاثة قرارات تصميميّة:** ① **السياج ثلاث نتائج لا اثنتان** — `inside`/`outside`/`unverified`، و«غير مُتحقَّق» ليست «خارج»: قراءةٌ دقّتها أسوأ من نصف القطر لا تحسم شيئًا، فادّعاء الانضباط بها أسوأ من الاعتراف بالجهل · ② **المنع للمؤكَّد خارجًا فقط** — لو منعنا الفاتورة عند ضعف الإرسال لباع المندوب خارج النظام · ③ **الزيارة الموسومة تُسجَّل ولا تُمحى** (قصيرة/خارج السياج/بلا نتيجة) — حذف ما لا يعجبنا يجعل التقرير جميلًا وكاذبًا. الطبقة غير الخالصة: `locationCapture.js` (يراقب حتى تتحسّن الدقّة ويُعيد **أفضل** قراءة لا آخرها) · `fieldService.js` (مجموعتا `journey_plans`+`visits` بمعرّف حتميّ `يوم__مندوب__عميل` + أحداث ملحق-فقط). **الشاشة** `/dashboard/field-operations` (جوّال أوّلًا): يوم المندوب + خريطة Leaflet المستضافة محلّيًّا + تبويبا المشرف (محرّر الخطط `JourneyPlanner` + فجوات التغطية). **ربط السياج بالفاتورة:** حقلا `fenceStatus`/`fenceDistanceM` في مخطّط `VSI` + تحذيرات. **قواعد Firestore الجديدة:** `journey_plans` (المشرف والمديران يكتبون — **المندوب لا يكتب خطّته**: خطّةٌ يعدّلها من ينفّذها تبريرٌ لاحق لا خطّة) · `visits` (المندوب يكتب زياراته وحدها عبر `repUid == uid`) · **وتوسعتان تحتاجان إقرارك:** (أ) `Customers_Master` صار المندوب يُحدّث فيه `geo`/`field` **فقط** بـ`affectedKeys().hasOnly` — لا الأسعار ولا شروط الدفع · (ب) `sales_supervisor` صار يقرأ `users` (لا يضع خطّةً لمندوبٍ لا يعرف معرّفه) — قراءةٌ فقط. **إصلاح عطبٍ كامن كتبتُه:** الخريطة كانت تقرأ `window.L` مرّةً واحدة فتستسلم أبدًا لو لم يُحمَّل Leaflet بعد — صارت تنتظر بمهلة ٤ ثوانٍ. **التحقق: ٧٨٦ اختبارًا · بناء ٨٥ صفحة · تدقيق نظيف (٤٦ قاعدة) · لينت نظيف · أصلا Leaflet يُخدَمان 200 بالمسار الصحيح.** ⚠️ **يتبقّى:** نشر `firestore.rules` (المجموعتان الجديدتان + التوسعتان) ثمّ تجربة حيّة (المندوب يرى شاشتين ويهبط على `field-operations`). **المراحل ٣–٦ لم تبدأ:** العروض · الحماية عند العميل · المستهدفات · مخطِّطات أودو.
>
> **[Claude] 2026-08-10 — طبقة البيع من المركبة: المرحلتان ٠+١ (مرفوعة لـmain):** بداية بناء منظومة SFA/Van-Sales التي طلبها المالك (١٠ بنود). **المسح كشف أنّ ٦٠٪ مبنيّ**: `VAN:‹لوحة›` موجود أصلًا في `locations.js` وDN/POD يمرّان به. **المرحلة ٠:** العميل صار موقع مخزون `CUST:‹رمز›` (`customerLocationCode`/`isCustomerLocation`/`atCustomerBalances` + حراسة البادئة في `isReservedCode`) — أساس البضاعة المحمية والصلاحية عند العميل (قرار المالك: موقع كامل بملكية) · دورا `sales_rep`+`sales_supervisor` في `roles.js`+`ROLE_NAV`. **المرحلة ١:** خمسة مستندات في المحرّك (**٢٧→٣٢**): `VLD` تحميل (مستودع→مركبة) · `VSI` بيع (مركبة→خارج) · `CRN` مرتجع ميدانيّ (خارج→**مركبة** لا مستودع) · `VRT` إرجاع المتبقّي (مركبة→مستودع) · `VSR` تسوية توثيقيّة **لا تُحرّك مخزونًا** (تقرأ ما قيّده غيرها) + قواعد قيدها في `postingRules.js` + صفوف أودو في `docCrosswalk.js`. **رياضيات التسوية** `services/vanSales/settlement.js` خالصة (+١٧ اختبارًا): التصنيف **بالاتّجاه نحو المركبة لا باسم المستند** فتدخل DN/POD القائمة تلقائيًّا؛ و**ثلاثة أرقام لا رقمان** (متوقّع/دفتريّ/معدود) ففرق النافذة `drift` غير العجز الحقيقيّ `variance`؛ وحارس `settlementVerdict` يمنع إقفال رحلةٍ ومركبتُها تحمل رصيدًا. **نموذج الرقابة:** المندوب يعتمد فاتورته (منعُها يوقف البيع فيُسجَّل خارج النظام) ولا يعتمد مرتجعه ولا تسويته، و`VRT` يُنجزه المستودع لا المندوب. **🚨 فخّ أُصلح:** المندوب كان خارج `isStockActor` وخارج بوّابة إنشاء المستندات وحجز الأرقام — أي أنّ الواجهة تسمح والخادم يرفض بصمت؛ أُضيف `isVanSalesWriter()` في `firestore.rules` ووُصل بأربعة مواضع (إنشاء المستند · `canReserveNumber` · `balances` · `stock_moves`) **بلا توسيع `isStockActor`** كي لا ينفتح للمندوب الجردُ وسجلّ المسح. **الشاشة:** `/dashboard/van-operations` (٣ طبقات، بلا إيموجي، الأحمر للتحذير فقط) تُعيد استخدام `listenBalances` القائمة و`listenVanMoves` الجديدة (استعلاما `from`/`to` مدمجان). **التحقق: ٧٦٠ اختبارًا · بناء ٧٦ صفحة · تدقيق نظيف · لينت الملفات الجديدة نظيف · الصفحة مبنيّة وحارس الدخول يعمل.** ✅ **المالك نشر `firestore.rules`** قبل الرفع. يتبقّى: تجربة حيّة بحساب `sales_rep` (تأكيد المطابقة الحرفيّة للمنشور لم يجرِ بعد). **المراحل ٢–٦ لم تبدأ:** الميدان (خطّة زيارات · GPS · سياج جغرافيّ · خريطة) · العروض · الحماية عند العميل · المستهدفات · مخطِّطات أودو للأنواع الخمسة. ملاحظة: مستندات البيع من المركبة **ليست في سلسلة اشتقاق** (`chain.js`) عمدًا — الفاتورة لا تُشتقّ من التحميل؛ وربط `CRN`←`VSI` تحسينٌ مؤجَّل.
>
> **[Codex] 2026-08-10 — عرض اجتماع المكتب الهندسي (مرفوع ومنشور):** أُنشئ عرض تفاعلي من 20 شريحة على `/dashboard/engineering-office-meeting` لاجتماع م. محمد الأوجلي، ويشمل المتطلبات والزيارة الميدانية كاملتين دون حذف، 9 صور ميدانية، و68 صفحة معاينة محلية للمخططات الأربعة، إضافة إلى آلية المتابعة ومشروعي الرحبة 133 ونقل مستودع فينسيا ومخرجات الإقفال. الأصول ذاتية الاستضافة، والرابط مسجل في `navCatalog` للأدمن ومدير المستودع. التحقق المحلي: مطابقة النص المرئي للمصدرين حرفيًا، مراجعة كل صفحات PDF بصريًا، معاينة سطح مكتب وهاتف، build = 83 صفحة، tests = 741/741، audit نظيف، وESLint لملفات المهمة نظيف. lint الشامل بقي على خط الأساس القديم 126 مشكلة (53 خطأ و73 تحذير) وكلها خارج ملفات المهمة. رُفع الالتزام `4caa1ee` إلى `main`، ونجحت Quality Gate والبناء والنشر، وأعادت صفحة العرض والمستندان وأحد المخططات HTTP 200؛ لا تعديل في `firestore.rules`.
>
> **[Codex] 2026-08-10 — عروض أنظمة القطاعات التفصيلية (مرفوعة ومنشورة):** أُنشئ `/dashboard/sector-presentations` كمركز لستة عروض مستقلة مبنية على الملف الأصلي دون اختلاق بيانات: المطاعم والكافيهات (18 شريحة)، الترفيه (6)، التجزئة (6)، البراندات (5)، المولات (5)، والمبيعات الخارجية (12). التوصيات مميّزة بصريًا عن المتطلبات، والقطاعات غير المحسومة موسومة «قيد المواءمة». أُضيف الرابط إلى `navCatalog.js` للأدمن ومدير المستودع. التحقق على نسخة نظيفة من أحدث `main`: بناء 82 صفحة، اختبارات 741/741، تدقيق البوابة نظيف، وESLint للملفات المتغيرة نظيف؛ فحص lint الشامل ما زال يعرض خط الأساس القديم خارج النطاق. رُفع الالتزام `81a3a4b` إلى `main`، ونجحت بوابة الجودة والبناء والنشر في GitHub Actions، وأعادت الروابط السبعة المنشورة HTTP 200. لا تغيير في `firestore.rules`.
>
> **[Claude] 2026-08-08 — إصلاح الأخطاء الخمسة من تقرير سيناريوهات Codex (فرع `claude/operational-scenarios-bugs`، لم يُرفع):** بعد مراجعةٍ عدائية طابقت كل ادّعاء على الكود، أُصلحت الخمسة: **(001)** ذرّية القيد — ختمُ «منجَز» صار **داخل معاملة `postDocument` نفسها** (خيار `markDone`) فلا مستند منجَز بلا أثر مخزنيّ؛ **(002)** التسوية الموجبة تُقيَّد بإعفاء موقع `ADJUSTMENT` الحسابيّ من حارس الرصيد السالب (الأرفف الحقيقية تبقى محروسة)؛ **(003)** توريث التشغيلة `GRN→QC→PUTAWAY` (عمود `batch` في مخطّطي GRN/QC + خرائط البنود) فتتطابق مفاتيح الرصيد؛ **(004)** فكّ حجز أمر البيع عند إنجاز `PICK` بمقدار المسحوب (تحرير جزئيّ إيديمبوتنت عبر `soReleasedByKey`/`soReleaseLog`) وفكّ الباقي عند الإنجاز؛ **(005)** اشتقاق `SRN` من `QC` لا `GRN` (حيث يعيش قرار الرفض ذو الأثر). المسّ: 9 ملفات خدمة/مخطّطات + 3 ملفات اختبار (+16 اختبارًا) + `firestore.rules` (حقلا التحرير في قائمة الختم). التحقق: **build 75 صفحة · tests 741/741 · audit نظيف · lint الملفات نظيف**. **يتبقّى للمالك:** نشر `firestore.rules`، ثم إعادة الاختبار الحيّ بحسابه وتوثيق قبل/بعد في تقرير السيناريوهات.
>
> **[Codex] 2026-08-08 — إكمال فجوتي الأسطول والعمالة:** أُلغيت خطة رحلة السائق 50 رسميًا مع سبب موثق، واعتمد نموذج التسليم اليومي للمركبة `8-638237` (عداد 128450، نصف خزان، جميع الفحوص سليمة)، ثم صدر تصريح `TRIP-2026-0002` وسُجل انطلاقها فأصبحت «في الطريق». أُغلقت مهمة العمالة `TEST-GRN-260807-01` بكمية 500، وأصبحت اللوحة تعرض لا مهام جارية. لوحة سلاسل الإمداد تحققت بعد اكتمال التحميل: 1 رحلة في الطريق، POD 100%، 5 أوامر صيانة مفتوحة وكلها عالية الخطورة، وMTTR 4.6 ساعة.
>
> **[Codex] 2026-08-08 — تدقيق عدم نسيان السيناريوهات:** فُحصت صفحات procurement/warehouses/customers/stock-operations/products/documents/tasks/suppliers/maintenance/custody/fleet/labor. صندوق المستندات يعرض 49 مستندًا و**0 مسودات** و4 بانتظار الاعتماد. أُنجز `SRN-2026-0001` كاملًا (20 وحدة، 3,000 د.ل) وربط بـ`GRN-2026-0001` مع توثيق فجوة أن رفض `QC-2026-0001` لا ينتقل آليًا إلى SRN. وُزعت حالات المهام: 51 منجزة، 50 تم الاطلاع، 49 قيد التنفيذ، والبقية مسندة. أُلغيت عينة أسطول رسميًا، وأُطلقت `TRIP-2026-0002` إلى «في الطريق»، وأُغلقت مهمة العمالة الجارية بكمية فعلية 500.
>
> **[Codex] 2026-08-08 — إظهار دقّة المخزون وسيناريو التالف:** أُنشئ وأُرسل واعتمد وأُنهي محضر الجرد المفاجئ `CC-2026-0002` للصنف `GUESS-085715323538` بنتيجة 475 دفتري = 475 عدّ أول = 475 عدّ ثانٍ، فانتقل مؤشر دقّة المخزون في لوحة القيادة من `—` إلى **100%**. وأُنشئ `DMG-2026-0001` رسميًا لكمية 2 وقيمة 300 د.ل بحالة «تحت الفحص/بانتظار الاعتماد»؛ عطّل النظام زر الاعتماد لغياب صورة التالف ومحضر الإتلاف الموقّع، فترك المستند مفتوحًا بدل تلفيق دليل. لا توجد مسودّة ناتجة عن هذه الجولة.
>
> **[Codex] 2026-08-08 — اختبار السيناريوهات التشغيلية الحية:** أُنشئت 50 عملية لكل مجموعة تشغيلية رئيسية، ثم وُزعت حالات الصيانة لتغذية المؤشرات (5 مفتوحة عالية الخطورة: 3 اعتماد مشرف + 1 جاري الإصلاح + 1 تم الإصلاح). اكتملت رحلة `TRIP-2026-0001` حتى POD والوقود والإقفال. أُنشئت دورة مرتجعات رسمية `RET-2026-0001` و`RET-2026-0002` مع `CN-2026-0001`. أُنجز `GRN-2026-0001` لكمية 500 واعتمد `QC-2026-0001` قبولًا مشروطًا 480/20. كشف الاختبار انفصال حالة `ADJ` و`PUTAWAY` عن نجاح القيد، وبقاء 500 في `RECEIVING` رغم إنجاز `PUTAWAY-2026-0002`؛ لذلك بقي `SO-2026-0001` بلا رصيد في `TST-WH-01`. التقرير الكامل: `docs/تقرير-اختبار-السيناريوهات-التشغيلية-2026-08-08.md`.
>
> **[Codex] 2026-08-07 — بطاقة المطاعم في عمليات البوابة والعرض (محلي، لم يُرفع):** تسجيل `/dashboard/executive-review/restaurants` في `navCatalog` باسم «خطة المطاعم والمقاهي» وبصلاحية المديرَين؛ إضافتها بطاقة مستقلة رقم **14** في العرض، فأصبح **23 محطة و23 شريحة**، شريحة واحدة لكل محطة. تحقق build 75 · tests 732/732 · audit نظيف (60 رابطًا، 30 صفحة حساسة محصورة) · lint الملفات نظيف.
>
> **[Codex] 2026-08-07 — إصلاح عاجل لعداد العرض بعد بلاغ المالك:** أصل الخلل كان إبقاء `view='overview'` افتراضيًا عند تمرير `section`، فحُقنت أقسام المنظور التنفيذي داخل كل محطة وصار العداد 110/110. أُلغي الافتراضي عند العرض المقسّم وتحقق تحليل HTML المبني: **22 محطة، 22 شريحة، شريحة واحدة لكل محطة، صفر تكرار**. build ناجح · 732/732 اختبار · audit نظيف · lint الملف نظيف. مرفوع إلى main بعد التحقق.
>
> **[Codex] 2026-08-07 — تعديلات العرض التنفيذي الجراحية (مرفوع إلى main: `6906ded`):** تحديث الرحبة 133 إلى «مستلم وجاهز للتخزين 100%» مع ثلاجة الرحبة وبيانات الغرفتين 1 و19 وفصل تقدم مكاتب الموظفين، وإدراج ملفات DWG الثلاثة الأصلية للتنزيل؛ إنشاء صفحة `/dashboard/executive-review/restaurants` من REQ-006 وربطها ببطاقة القطاع؛ ترتيب العرض في 22 شريحة حسب توجيه المالك، إضافة «إعادة بدء العرض»، وحذف شريحة المخاطر. التحقق: build = 75 صفحة، tests = 732/732، audit نظيف؛ نشر GitHub Pages ناجح وتشغيل الصفحة الجديدة وملفات DWG تحقق بحالة HTTP 200. lint بقي أحمر بخط الأساس القديم (53 خطأ/73 تحذير، لا بلاغ من ملفات المهمة). **المتبقي:** مراجعة المالك البصرية بالحساب الحي وإرسال التعديلات الجوهرية التالية.
>
> **آخر تحديث:** 2026-08-07 (الجمعة) · **آخر مهمة:** **✅ [Claude+Codex] «غرفة قرار سلاسل الإمداد» — المراجعة التنفيذية أمام المدير العام (worktree `claude/supply-chain-executive-review-f12534` — ينتظر مراجعة المالك البصرية ثم «ارفع»)**: تنفيذ تكليف المدير (34 محورًا) بتقسيم عمل الشراكة: **Claude** حلّل المصادر (مجلد 155: مخططات NAS-8155 + جرد E1–E10 + وثيقة المتطلبات BRZ-WH-155 الـ22 قسمًا · مجلد 133 DWG · تقريرا REQ-004/006 · حالة النظام) وبنى **6 ملفات بيانات** `src/data/executive-review/` (لوحة الحالة 12 بندًا · 8 KPI · 11 إنجازًا · 13 ملفًّا مفتوحًا · 10 تحديات · **9 قرارات** بأثر الاعتماد/التأخير · 12 صف ما-بعد-الاجتماع · النموذج الموحد 16 حلقة · «لا مستند=لا حركة» · 5 سياسات مالية · دورة الموردين 10 حلقات · 12 رافعة كفاءة · قالب الدورة 12 حقلًا · الموقعان · الكتالوج 5 أقسام · المبيعات 9 مستندات + القطاعات الخمسة · 5 مكاتب + مصفوفة حوكمة 12 عمودًا) + 10 صور مخططات مضغوطة في `public/executive-review/155/`؛ **Codex** سلّم حزمة الربط (مجلد التواصل) بمكوّنين + 9 صفحات `/dashboard/executive-review[/...]` + `digitalCapabilities.js` (القدرات الرقمية الدائمة: $400 شهريًا تكلفة تشغيلية ثابتة، لا رواتب)؛ **Claude راجع الدمج وأصلح 6 فجوات**: روابط بلا `base` · **وراثة صلاحية الأب للصفحات الفرعية في `pageAccess`** (+اختبار — الفرعيات الثماني للمديرَين فقط) · حالة `amber` بلا CSS · `[object Object]` في وظائف القابضة ودورة الموردين · معرض صور 155 وغرف التبريد وأنماط المخازن غائبة عن العرض · حقول محذوفة (التوفير·الأطراف·دورات القطاعات) + بند navCatalog واحد في «مركز التقارير». **بوابات خضراء: بناء 73 صفحة · 729 اختبارًا · تدقيق نظيف · ملفات الحزمة صفر لينت (src=120 تحت 122)**. ⚠️ يتبقى: مراجعة المالك البصرية للشاشات التسع بحسابه (بوابة الاعتماد 5 في سجل كوديكس) ثم «ارفع». **وقبلها:** **✅ [Claude] جاهزية جرد السبت — إغلاق حواجز كوديكس ١+٣+٤ (مرفوع)**: أول جرد ميداني حقيقي **السبت 2026-08-08** — قرار المالك: الأولوية القصوى لجاهزية `stock-operations`. أُغلق: (١) **استئناف العملية السحابية ذريًّا** — `resumeOperation()` جديد في `__bzCloud` يقرأ `bzCloudOpId` بعد اكتمال الهوية ولا يستأنف إلا عملية `open` ويمحو المعرّف اليتيم (كان يُحفظ ولا يُقرأ → انقسام الجرد بين عمليتين) + `getOperation()` في `operationsService` · (٤) `applyBulkScans()` يحفظ الجلسة فورًا · (٣) **قاعدة أمان جديدة**: إنشاء `operations/{opId}/scans` مشروط بـ`status=='open'` للرأس — ⚠️ **تنتظر نشر المالك للقواعد**. الحاجز ٢ (آلة الحالات الكاملة) مؤجَّل عمدًا لما بعد السبت. **بنود كوديكس (٥ CDN·٦ إيموجي·٧ تقييد الانضمام·٨ رمز مشاركة) مفعَّلة له** في مجلد التواصل `كوديكس -كلود-البرشي` (ملف التفعيل فيه خطة السبت الميدانية). المرجعية الكاملة: ملفات الردود الثلاثة في المجلد نفسه — **القرار الثلاثي المعتمد: لا نظام ثالث · الهاتف لا يعدّل الرصيد · معيار قبول سداسي**. **وقبلها:** **✅ [Codex] مركز متطلبات التكامل `/dashboard/requirements-command` (مرفوع)**: أوّل مهمة ينفّذها كوديكس في الشراكة الثلاثية — شاشة قرار مرجعيّة لحزمة المتطلبات BFP-SCM-REQ-2026: المشهد التنفيذيّ (٦ متطلبات) · خريطة الـ27 مستندًا تُستمدّ حيًّا من `DOC_ODOO_MAP` ببحث وتصفية · سجلّ ٥ قرارات معلّقة بحفظ محلّيّ (localStorage — لا كتابة Firestore/أودو) · مسار تسليم ٥ مراحل + طباعة؛ مقيّدة admin+warehouse_manager في navCatalog. **راجعها Claude وأغلقها**: ٧٢٨ اختبارًا · بناء ٦٤ صفحة · تدقيق نظيف · لينت src=118 (أقل من خط الأساس 122) · فحص بصريّ بعين المالك (سطح المكتب). **ومعها**: تحرير دستور الشراكة `AGENTS.md`+`docs/إحاطة-كوديكس.md` إلى main (كانا حبيسَي فرع `2d68acb`) · **فحص دوريّ شامل** أكّد سلامة البوابة الحيّة بالكامل (صفر أخطاء كونسول، كل النشرات خضراء) · **المالك نشر قواعد Firestore الـ51** بعد أن كشفت المطابقةُ غيابَ `odoo_sync`+`odoo_sync_events` من المنشور — أُغلق البند. ⚠️ ملاحظتان مفتوحتان من الفحص: `eslint .` يجرف worktrees والمجلدات الجديدة (95k إنذار كاذب — يحتاج سطرَي ignores في `eslint.config.js`) · ثغرات تبعيات npm (جولة صيانة غير عاجلة، الحرجة كلها بأدوات البناء). **وقبلها:** **✅ جسر المزامنة — الشمول الكامل (مرفوع)**: تعميم الجسر على **كلّ المستندات الـ27 + دفتر الحركات**: `docCrosswalk.js` (جدول عبورٍ تقنيّ نوع→نموذج أودو+حالة الاعتماد لكلّ الأنواع، يحرسه **اختبار انجرافٍ** يفشل إن أُضيف نوعٌ بلا تخطيط) · مخطِّط عامّ `docToOdooValues` · `pushAnyDocument`/`approveAnyInOdoo` · مسار «العمليات المخزنيّة» (`stock_moves`→`stock.move` تصل done لأنها وقائع مقيّدة) · الواجهة ٣ مسارات جامعة (الدورة كلّها بفلتر نوع · الأصناف · العمليات). **٧٢٨ اختبارًا · بناء ٦٣ · تدقيق نظيف · القواعد منشورة (المالك أكّد).** **وقبلها:** **✅ الحلقة الثانية: الاستلام GRN→stock.picking (مرفوع)**: مسار «الاستلام (GRN)» ثالثًا في الجسر — دفع مذكّرات الاستلام الحقيقيّة مسوّدةً + **اعتماد أمر الشراء يجدول استلامًا واردًا تلقائيًّا (WH/IN، assigned)** محاكاةً حرفيّة لسلوك أودو + «صدّق الاستلام» (⇒done). الجديد: `grnMapper.js` (+٧ اختبارات) · `pushGoodsReceipt`/`validateReceiptInOdoo`/توليد تلقائيّ في `approveInOdoo` · بذرة `stock.picking` · لا قواعد جديدة (نفس `odoo_sync*`). **٧٢١ اختبارًا · بناء ٦٣ · تدقيق نظيف.** **وقبلها (مرفوع `1c9feaf`):** **✅ جسر المزامنة الحيّ مع أودو — `/dashboard/odoo-sync`**: مرآةٌ مبنيّة على الوقائع تقرأ أوامر الشراء (`documents` type=PO) وماستر الأصناف الحقيقيّة وتدفعها إلى أودو **مسوّدةً حتى الاعتماد** (draft→purchase) عبر **نفس واجهة عميل الإنتاج** (`odoo.create/write/searchRead` — قلبها للإنتاج مفتاح بيئة واحد)، مع سحبٍ عكسيّ للأصناف (odooId يُكتب على الماستر) وتزامنٍ مستمرّ (onSnapshot) وإشعارات (جرس+خلاصة من سجلّ ملحق-فقط). الجديد: `poMapper.js` (+٨ اختبارات، الفجوة الوحيدة التي كانت ناقصة) · `odooSyncService.js` · `odoo-sync/OdooSyncBridge.jsx` (٣ أعمدة bz-light بلا إيموجي) · مجموعتان `odoo_sync`+`odoo_sync_events` · بذرة `purchase.order` في العميل المحاكى · بند navCatalog (مديران) · ربط متبادل مع تعلم-أودو. **٧١٤ اختبارًا · بناء ٦٣ صفحة · تدقيق نظيف · لينت الجديد نظيف.** **⚠️ يتبقّى على المالك: نشر `firestore.rules` (القاعدتان الجديدتان) + تجربة حيّة بحساب مدير + «ارفع».** **وقبلها:** **✅ رُفِع لـmain: المحور الأول من خطة تطوير البوابة — تكامل الهيكل مع التوظيف (أ‑١+أ‑٢، `push origin HEAD:main`)** — حقل «المتطلبات الوظيفيّة» + الأدوار الـ36 كاملةً + الربط المتبادل بالـhash + القراءة الحيّة `jobsResolver`. ٦٧٨ اختبارًا · بناء ٦٠ صفحة · **لا قواعد جديدة من هذا العمل**. **✅✅ خطة تطوير البوابة مكتملة ومرفوعة بالكامل (المحوران) — 2026-08-05.** المحور الثاني (لوحة عمالة الشحن/التفريغ ب‑١…ب‑٦): دور `labor_supervisor` · نموذج خالص `laborModel` (حالات/مدّة/إنتاجيّة/تخطيط) · مجموعتا `crews`+`labor_tasks` (تتبّع زمنيّ بـserverTimestamp، pausedMs تراكميّ) · مستند الحاوية `CTR` (النموذج صار ٢٧) · لوحة `/dashboard/labor-operations` (٣ طبقات: تدخّل/إجراءات/فهرس + تخطيط موارد). ٧٠٦ اختبارًا · بناء ٦١ · لينت نظيف · قواعد 123/123. **⚠️ نشر `firestore.rules` معلّق** (قواعد `assignments`+`crews`+`labor_tasks`+CTR في approve/completeRoles). **⏭️ لا مزيد من الخطة — تنتظر: نشر القواعد + تجربة حيّة بحساب.** **[المحور الأول:]** **✅ مكتمل ومرفوع (أ‑١…أ‑٤).** أ‑٤: الملف التعريفيّ للموظف المُسكَّن (`employeeProfileHtml`+`getCandidate`) + CRUD الأدوار من الواجهة (`addJob`/`updateJob`/`removeJob`/`nextJobId` + نموذج + حفظ سحابيّ). ٦٩٩ اختبارًا. **⏭️ التالي: المحور الثاني — لوحة عمالة الشحن/التفريغ (ب‑١…ب‑٦).** ⚠️ نشر `firestore.rules` (قاعدة `assignments`) معلّق.** الخطة [`docs/خطة-تطوير-البوابة-الهيكل-والعمالة.md`](docs/خطة-تطوير-البوابة-الهيكل-والعمالة.md). **وقبلها (مرفوع main `5a74f9e`):** أداة المحاضر اليدويّة الموحّدة (`MeetingsApp`) + الأرشيف الدوريّ (`archive_documents` ⚠️ قاعدتها تحتاج نشرًا). **وقبلها:** **✅ التحسينات الجراحية للمستندات — المطالب السبعة كاملة (٣ دفعات)** على `/dashboard/documents`: ① مرفقات + ضغط صور بالمتصفّح، ② طبقة رقابة/مطابقة، ③ **رصيد المركبة لكل مركبة** (DN يحمّل `STAGING→VAN:‹لوحة›`، ومستند POD جديد بيد الحركة يخصم `VAN→خارج`) + **إشعار رفض الاستلام SRN** (توثيقيّ من مرفوض GRN) + قسم تعبئة النقل + نوع الجرد + أنواع المرتجعات. **٦٦٦ اختبارًا** · لينت نظيف · **مراجعتان عدائيّتان** (١٨+وكيلًا؛ الأولى ١٠ إصلاحات مطبَّقة، الثانية للدفتر/القواعد جارية). **٢٦ نموذجًا محكومًا.** ⚠️ لم يُرفع · يحتاج نشر `firestore.rules` (fleet في isStockActor + POD/SRN) + تجربة حيّة · حاجز بناء سابق غير متعلّق (`whisper.worker.js`). **[أرشيف الدفعتين ١+٢:]** على `/dashboard/documents`: مرفقات ملحق-فقط `documents/{id}/attachments` (**ضغط صور بالمتصفّح** + كاميرا حسب نوع الخانة + طباعة) + **طبقة رقابة/مطابقة** `documents/{id}/control` (تُطابق المستند الصادر بالنسخة الموقّعة، الفوترة أبرز مكان) + خانات في ٦ مخططات + الجرد نوعٌ (يومي/أسبوعي/مفاجئ) + المرتجعات أنواع (عميل/فرع/مورّد). قرارا المالك: **موقع لكل مركبة** · **طبقة رقابة مدمجة**. **مراجعة عدائية (١٨ وكيلًا) → ١٠ إصلاحات مؤكَّدة طُبِّقت** (أبرزها تكرار ضغط لانهائي + تقييد قاعدة `/control`). **١٥٩ اختبارًا** · لينت نظيف · القواعد متوازنة. ⚠️ لم يُرفع · **الدفعة ١ منشورة القواعد (المالك)**، والدفعة ٢ تحتاج نشر `firestore.rules` · حاجز بناء سابق غير متعلّق (`whisper.worker.js`). **وقبلها:** **✅ نظام إسناد المهام السحابيّ** — صفحة `/dashboard/tasks` تحوّلت من localStorage إلى Firestore: المدير يُسنِد مهمّة لمستخدمٍ فعليّ + **توثيق** (`events` ملحق-فقط) + **ردّ** + **تنفيذ** (حالات assigned→acknowledged→in_progress→done) + **معدّل استجابة** (لوحة لكل مستخدم). نموذج «المديران يُسنِدان للجميع» (قرار المالك، متوافق مع قواعد الخصوصيّة). ٨ ملفّات جديدة (`taskShape`/`taskMetrics`+٩ اختبارات/`tasksCloudService` · `CloudTaskManager`/`AssignTaskForm`/`TaskThread`/`ResponseRateBoard`) + قاعدة `tasks` في `firestore.rules` + الصفحة تشير للجديد (القديم localStorage باقٍ على القرص). بوّابة خضراء (59 صفحة · **625 اختبارًا** · تدقيق نظيف · لينت 124=124) + تحقّق ترطيب المكوّن (بلا انهيار). **⚠️ لم يُرفع + يحتاج نشر `firestore.rules` + تجربة حيّة بحساب مدير.** **وقبلها:** **✅ تفريغ صوتي داخل المتصفّح في «مساعد الاجتماعات»** — رفع ملفّ صوت (أي مدّة) → تفريغ كامل عبر Whisper (`@huggingface/transformers` v4، WebGPU) بديلًا مجانيًّا عن اشتراك uniscribe (قرار المالك: داخل المتصفّح بالكامل). ٣ ملفّات جديدة (`whisper.worker.js` عامل خيطيّ يقطّع أي مدّة · `useAudioTranscriber.js` طابور+فكّ ترميز 16kHz+٣ أنماط جودة · `AudioFileTranscriber.jsx` إفلات+تقدّم+بثّ حيّ) + الدمج في التفريغ الرئيسيّ (تلخيص/ترجمة/PDF تعمل معه) + **استثناء `.wasm` من تخزين SW المسبق** (ملفّ ONNX ~23م.ب). **✅ تحقّق E2E حقيقيّ في المتصفّح** (عيّنة JFK فُرّغت حرفيًّا، WebGPU مُكتشَف، استئناف بعد تقطّع شبكة نجح) · بوابة خضراء (59 صفحة · ٦١٦ اختبارًا · تدقيق نظيف · لينت الملفّات الجديدة نظيف). **⚠️ worktree على فرع `claude/audio-to-text-extraction-e4c290` لم يُدفع** (ينتظر «ارفع»). **وقبلها:** **✅ مطابقة المرجع الرسمي ↔ البوابة إلى ١٠٠٪ + ٣ تقارير + ٥ خدمات ناقصة (طلب المالك «العملية الاحترافية»)** — ① **سجلّ فجوات ثنائي الاتجاه** مصدرًا واحدًا (`src/data/reference-crosswalk.json` + `src/services/reference/crosswalkModel.js` +٦ اختبارات) · ② **محاذاة المرجع الرسمي**: أُضيف `treasury` (أمين الخزينة) لـ`org-structure.json` (→ ١٣ دورًا مولَّدًا) + مصفوفة نماذج ٢٤/٧ سلاسل (٩ نماذج إضافية ٢٢–٣٠) + جدول ٩ حرّاس (حَجب صارم مقابل تحذير بصريّ) + تصحيح إحصاءات الغلاف والعناوين والخاتمة · ③ **٥ خدمات ناقصة بُنيت** (بناء متوازٍ بوكلاء): `vendor-scorecard` · `hse-checklists` · `business-continuity` · `cold-chain` · `role-activity` · ④ **٣ تقارير داخل البوابة** (Astro/React، قرار المالك): `reference-guide` (شرح تفاعليّ خطوة بخطوة) · `acceptance-check` (UAT بـ٨٢ معيارًا + localStorage + تصدير CSV) · `portal-value` (لماذا البوابة تكامليّة لأودو) · ⑤ الثماني مربوطة في `navCatalog.js` بأدوارها. بوابة خضراء (**٥٨ صفحة · ٦١٠ اختبارًا · تدقيق نظيف · بناء ✓**) + تحقّق بصريّ حيّ للمرجع (كل التعديلات ظاهرة، صفّ أمين الخزينة اكتمل). **⚠️ محلّيّ في worktree لم يُدفع بعد (ينتظر «ارفع»).** **وقبلها:** **✅ «العملية الجراحية» — المحاور السبعة منجزة عمليًّا ومدفوعة لـmain** (١ الحوكمة المستندية · ٢ المرجع+مصفوفة الصلاحيات · ٣ عقد أودو · ٤ الأمن، ونشر المالك القواعد حيًّا · ٥ الأوفلاين باختبار قبول حيّ · ٦ الربط التسلسلي docref+الإنشاء المباشر · ٧ الترقيم Pager). المتبقّي بنودٌ موثّقة تحتاج تجربة المالك أو قراره. **وقبلها:** **دمج «النبض الحيّ» في اللوحة الرئيسية** — قسم «النبض الحيّ — لوحة القيادة التشغيلية» أُدرج أعلى `/dashboard/` (قبل «ابدأ من هنا») بإعادة استخدام مكوّن `OperationsCommand` نفسه (صفر نسخ، صفر حذف — صفحة `/dashboard/command-center` باقية كما هي مع رابط «الصفحة الكاملة»)؛ مقيَّد بأدوار `admin,warehouse_manager` أسوةً ببطاقة «لوحات القيادة»، وقابل للإخفاء من «تخصيص العرض» (`data-card-id="live-pulse"`). بناء ✓ · اختبارات 579/579 ✓ · تدقيق ✓ (في worktree، لم يُدفع بعد). **وقبلها:** **خطة «العملية الجراحية»** — وثيقة تنفيذ أسبوعية معتمدة على مسح ثلاثي موازٍ للكود (الهيكل/الأدوار · المستندات/الترقيم · الأمن/الأوفلاين/أودو) في [`docs/خطة-العملية-الجراحية.md`](docs/خطة-العملية-الجراحية.md): ٧ محاور — (١) «قسم الحوكمة المستندية» بدل «التوثيق والاعتماد» في كل المواضع + أتمتة `build-org.mjs` (لا يعمل تلقائيًّا اليوم وCI يتجاوز prebuild!) · (٢) مطابقة المرجع الرسمي مع البوابة بمناطق توليد جديدة (أدوار البوابة الـ12 الفعلية + فهرس 47 شاشة) + مصفوفة صلاحيات سحابية `access_control/matrix` قابلة للتنفيذ (صفحة access-control الحالية عرض توثيقي ميت بأدوار وهمية) + تفعيل `active:false` (غير مُنفَّذ اليوم) · (٣) عقد تكامل أودو (Firestore المصدر؛ Worker وسيطًا؛ مزامنة ٣ مراحل؛ سياسة ترقية الإصدار؛ pg_dump) · (٤) سدّ ١١ خرقًا أمنيًّا مكتشفًا (حرق العدّادات لأي مصادَق · XSS مساعد الاجتماعات مع API ترجمة خارجي · users مقروءة للجميع…) بدفعة قواعد موحّدة + اختبارات rules-unit-testing · (٥) أوفلاين 100%: استئصال 8 مكتبات CDN و14 موضع Google Fonts (العامل وpersistentLocalCache موجودان أصلًا) · (٦) الربط التسلسلي: حقول المرجع نص حر ميت اليوم (لا استعلام بالرقم في المستودع كله!) → نوع `docref` بربط تراكمي + إنشاء مباشر Modal برقم شرعي من العدّاد (١٩ نوعًا في ٦ سلاسل لا ١٤/٤) · (٧) ترقيم أودو Pager + `usePagedCollection` (صفر startAfter في المشروع؛ Items_Master وbalances مشتركان كاملين في ٤ شاشات). جدول سبت→جمعة + قائمة ما يلزم المالك. **وقبله:** **الاجتماعات الجماعية + ترقية مساعد الاجتماعات (خطة Firebase المصحّحة)**: منشئ اجتماعات جماعية متعدّدة في `/dashboard/meetings` (جزيرة `GroupMeetings.jsx`) بغرفةٍ فيها **تسجيل صوتي MediaRecorder + تفريغ عربي حيّ Web Speech مدمجان** + محضر جماعي رسمي مرقّم (MOM، تواقيع متعدّدة) — التخزين في `preparatory_meetings` بوسم `kind:'group'` **فلا قاعدة Firestore جديدة**؛ والصوت **تنزيل محلي webm فقط** (قرار المالك — Cloud Storage يتطلّب Blaze للمشاريع الجديدة، وWeb Speech لا تفرّغ ملفًّا مسجّلًا بل الميكروفون الحيّ فالتسجيل والتفريغ بالتوازي)؛ والتفريغ النصّي يُحفظ سحابيًّا مع المحضر. + المساعد المنفصل صار يسجّل صوتًا فعليًّا بزرّ تنزيل (خطّاف مشترك `useAudioRecorder`). بوابة خضراء (49 صفحة · **571 اختبارًا (+21)** · تدقيق نظيف · لينت 124=124) · محلّيّ لم يُرفع. **وقبله:** **المرحلة ٤ — دفعة ٥ شاشات مستقلّة رُقِّيت لأودو (توزيع متوازٍ + مراجعة عدائيّة = صفر انحدار)**: المستخدمون (`UsersAdmin`) · ضبط الطلبات (`OrderControlBoard`) · المستندات (`DocumentsInbox`) · الصلاحيات (`AccessControlHub`) · طلبات التوظيف (`HiringRequestsBoard`) — `ListView`/`o_input`/`Badge`/`Icon` داخل `.o_theme`، المنطق محفوظ ١٠٠٪، بلا إيموجي. بوابة خضراء (49·550·تدقيق·لينت 124) · محلّيّ لم يُرفع. **وقبله: المرحلة ٤ — دفعة ٤ شاشات تشغيليّة رُقِّيت لأودو دفعةً واحدة (توزيع متوازٍ + مراجعة عدائيّة = صفر انحدار)**: المستودعات (`WarehouseManager`) · دفتر الحركات (`StockLedger`) · التحويلات (`TransferBoard`) · مراقبة العمليّات (`OperationsMonitor`) — كلّها `ListView`/`o_input`/`Badge`/`Icon` داخل `.o_theme`، المنطق محفوظ ١٠٠٪، بلا إيموجي، أرقام لاتينيّة (+تلتين تاريخ العمليّات وإزالة إيموجي ترويستَي دفتر/تحويلات). بوابة خضراء (49·550·تدقيق·لينت 124) · محلّيّ لم يُرفع. **وقبله: المرحلة ٤ — ماستر الأصناف (٥ مكوّنات) رُقِّي لأودو عبر توزيع متوازٍ (workflow) + مراجعتين عدائيّتين (منطق+بنية = صفر انحدار)**: ItemMaster/ItemForm/ItemsImport/BalancesImport/PendingItems صارت `ListView`/`o_input`/`o_alert`/`Icon` داخل `.o_theme`، **المنطق (CRUD·استيراد·رصيد حيّ/FEFO) محفوظ ١٠٠٪**، بلا إيموجي/FA، أرقام لاتينيّة، `products.astro` استورد أنماط أودو. بوابة خضراء (49·550·تدقيق·لينت 124) · محلّيّ لم يُرفع. **وقبله: المرحلة ٤ — تُرقّى شاشات البيانات لأودو: (١) شاشة الموردين/العملاء (`PartnerMaster`) أُعيد كساؤها بمكوّنات أودو (`ListView` + `o_input` + `o_alert` تحت `.o_theme`) مع الحفاظ على ١٠٠٪ من منطق CRUD/الاستيراد — المدخلات صارت مقروءة، وتحتاج `tokens.css`+`odoo.css` في الصفحتين؛ (٢) أُصلحت أيقونات اللوحة (FontAwesome غير مُحمَّل على `DashboardLayout` → مكوّن `Icon` SVG). بوابة خضراء (49·550·تدقيق·لينت 124) · ✅ تحقّق حيّ بالقيم المحسوبة · محلّيّ لم يُرفع · التالي: المرحلة ٥ (قلب الافتراضي) أو مزيد شاشات أودو. وقبله: المرحلة ٤ انطلقت — لوحة اللوجستيات التنفيذيّة `/dashboard/logistics-dashboard` (للمديرَين) بمكوّنات أودو الحقيقيّة على بيانات حيّة** (نموذج خالص `logisticsModel.js` +٨ اختبارات · مكوّن `LogisticsDashboard.jsx` يشترك في ٨ مصادر · بطاقات KPI في `odoo.css` تحت `.o_theme` · بند navCatalog محصور · بوابة خضراء 49·550·تدقيق·لينت 124 · ✅ تحقّق حيّ بالقيم المحسوبة · محلّيّ لم يُرفع · التالي: ترقية شاشة بيانات لـ`ListView`/`FormSheet` · التفصيل في «أين توقّفنا» أدناه). **وقبله اليوم: نظام أودو — «الجرد الذكي» صار فاتحًا: اكتمل ترحيل البوابة التفاعليّة للفاتح بالكامل** — «الجرد الذكي» (`stock-operations.astro`) كان آخر صفحة داكنة؛ صفحة تشغيليّة مستقلّة بلا `DashboardLayout` تدير ثيمها عبر `:root` محلّيّ → **قلب `:root` الستّة لقيم `bz-light`** المطابقة للبوابة (مع إبقاء البراند والدلالات) + إصلاح يدويّ للألوان المرمّزة (`09156d0`+`8284438`، مدفوعة). **⚠️ لكنّ شاشة `#dashboardScreen` بقيت داكنة** (بلاغ المالك بالصور): مبنيّة على **أصناف Tailwind الأداتيّة من مقياس `dark`** (`bg-dark-*`/`text-dark-400`/`border-dark-600`) — لا يلتقطها بحث hex ولا يمسّها قلب `:root`، فبقيت الخلفيّات داكنةً ونصُّ `var(--text)` فوقها اختفى. **صُحِّح `b9a39fe`:** تجاوز أصناف `dark-*` مباشرةً بـ`!important` (خلفيّات فاتحة·نصّ مقروء·حدود) + متغيّرا شفافية مضمّنان + 4 `text-white` على أسطح فاتحة → حبر داكن + تغميق أرقام الذهبي/الأخضر. **⚠️ درسان محوريّان:** (1) **أصناف Tailwind الأداتيّة تُتجاوَز بالصنف مباشرةً** (قلب `:root` لا يكفي وبحث hex لا يكشفها)؛ (2) **`*/` داخل تعليق CSS يُغلقه مبكرًا فيبتلع القاعدة التالية** (كان `bg-dark-*/text` في تعليقي يُسقط `.bg-dark-900`!). **✅ هذه المرّة تحقّق حيّ حقيقيّ** (صفحة اختبار تربط الحزمة المصرَّفة + `getComputedStyle`، لا grep وحده): كلّ درجات `dark` فاتحة والنصوص مقروءة. الطباعة/الكاميرا سوداء عمدًا. بوابة خضراء (48·**542**·تدقيق·لينت 124=124). **المتبقّي للفاتح = صفر — بقيت `index.astro` الكحليّة عمدًا. التالي: المرحلة ٤.** [قبلها — **تلميع بصريّ + توحيد الدخول (2026-07-30)**: **(أ) علاج «القصّ واللصق» `0cf71c8`** (بلاغ المالك بالصور): السبب الجذريّ = أداة Tailwind `.shadow-card` تُجمَّد قيمتها وقت البناء من الثيم الداكن (ظلّ أسود 35%!) فلا يصلها قلب التوكِن → الحلّ: إعادة تعريف `--tw-shadow` تحت `bz-light` بظلّ أودو ناعم (طبقتان رماديّتان دافئتان) + شبكة أمان لـshadow-lg/xl/2xl · خلفية `#f5f5f7` دافئة · حدّ شبه معدوم 6% · تقويس 0.75rem · **الشريط الجانبي:** العنصر النشط كان `border-right` مستقيمًا يقطع الزاوية المستديرة (= «الاعوجاج») → صار مؤشّر `box-shadow inset -3px` يحترم التقويس. **(ب) تشوّه خانة البحث `23f77fe`:** الحاوية اللاصقة تحمل `bg-brand-navy` على العنصر نفسه فقاعدة `.sb-x .sticky` (سليل) لم تصبها → قاعدة تبيّض أيّ `bg-brand-navy` داخل الشريط. **(ج) توحيد الدخول `2179908`:** login.astro صار `bz-light` + بطاقة `LoginForm` من navy/glass إلى سطح أبيض بظلّ أودو + حذف رابط Cairo الخارجيّ (offline rule) + `text-green-200` للشبكة. الكلّ مُتحقَّق حيًّا بالقيم المحسوبة. **⚠️ درس مهمّ:** أيّ أداة ظلّ Tailwind لا تستجيب لقلب التوكِن — يجب تجاوز `--tw-shadow` صراحةً. **المتبقّي — نقطتان:** ① **stock-operations (الجرد الذكي)** آخر صفحة داكنة، 3653 سطرًا بها 63 لون hex داخل `innerHTML` و91 تلاعب DOM → **تحتاج جلسة يدويّة مخصوصة لا sed** · ② **index.astro** landing تسويقيّ بـhero كحليّ متعمَّد (يُترك داكنًا ما لم يطلب المالك). ثم المرحلة ٤ (شاشات بيانات بمكوّنات أودو + لوحة TailAdmin) ← ٥ قلب الافتراضي.] [قبلها: **المرحلة ٣ + حذف ١٢ صفحة**: **(أ) الحذف `c512fb9`** بأمر المالك (٨ صفحات لوحة + ٤ تقارير public: gallery·warehouse-155·global-warehouse·achievement·odoo-installation·cold-storage·warehouse-maps·الدورة-الكامل·Multi_Brand·تقرير التخزين·تحوير-155·المقارنة-العالمية) مع تنظيف كامل (navCatalog ١٢ بندًا·الرئيسية ٥·مركز التقارير ٦+فئة·الدراسات·workflows) — البناء 56→48 صفحة واللينت تحسّن 125→124. **(ب) دفعة ٣ `836ad83`**: **٤٤/٤٥ صفحة لوحة فاتحة** (٢٨ صفحة + ٤٩ ملف مكوّنات ~640 موضعًا)؛ استُثني عمدًا **stock-operations** (الجرد الذكي القديم — تمريرة مخصوصة) و**odoo-sim** (محاكي طبق الأصل لا يُمسّ)؛ + **شبكة أمان CSS** للكتل الكحلية الثابتة (نص متكيّف داخل bg-brand-navy يعود فاتحًا تلقائيًّا) وإصلاح بطاقات الدراسات المتدرجة. بوابة خضراء (48·542·تدقيق·لينت 124=124). **المتبقّي من المشروع:** stock-operations · login · الرئيسية العامة index · المرحلة ٤ (شاشات بيانات بمكوّنات أودو + لوحة TailAdmin) · ٥ (قلب الافتراضي وحذف الداكن). [قبلها: **دفعة ٢ `1448609`**: ٧ صفحات كثيفة الترميز (~400 موضع: erp-workflows·comparative-study·archive·global-doc-cycles·interview-readiness·target-operating-model·workflows) → **المجموع ١٧ صفحة فاتحة**؛ أُتقنت مزامنة مصفوفات classList في سكربتات التبويبات مع الأصناف الجديدة. بوابة خضراء (542·تدقيق·لينت 125=125). **متبقٍّ للدفعة ٣:** odoo-installation (32) · stock-operations (15) · studies (10) · ~25 صفحة خفيفة (موضعان لكلٍّ) · الرئيسية العامة index (37) · login — ثم المرحلة ٤ فقلب الافتراضي. [قبلها: **دفعة ١ `1481417`**: ترحيل ٧ صفحات تشغيلية (المستندات·النماذج·الموردون·العملاء·المؤشرات·تحليل المخزون·مركز التقارير → **المجموع ١٠ صفحات فاتحة**) بتحويل ١٢ مكوّنًا للتوكِنز الدلالية (ink/chip/line/surface/accent؛ النص الأبيض فوق الأزرار الملوّنة بقي أبيض؛ خيارات select وأسطح bg-black/20 → bg-surface؛ + قاعدة تعتيم درجات ألوان الحالة على الفاتح تخدم الدفعات المقبلة). بوابة خضراء (56 صفحة·542·تدقيق نظيف·لينت 125=125 صفر جديد). **المتبقّي للدفعة ٢:** الصفحات ذات الترميز الداكن الكثيف (erp-workflows 144 · comparative-study 63 · archive 49 · global-doc-cycles 43 …) ثم قلب الافتراضي. [قبلها: **المرحلة ١ مدفوعة `ade0fec`** (الإطار الفاتح `bz-light` + خط IBM Plex Sans Arabic مستضافًا ذاتيًّا + ترحيل الرئيسية والاستلام ودليل الأنماط؛ المرحلة ٠ مدفوعة `dff6751`). [سابقًا: **اعتماد نظام تصميم أودو — المرحلة ٠** (توكِنز `--o-*` + ٨ مكوّنات React + صفحة أنماط + شاشة استلام تجريبية، كلّها معزولة في `.o_theme`؛ معاينة حيّة artifact `60be27f9`؛ **معلّقة على قرار المالك للون** `--o-brand-primary`). [سابقًا: **الأفق الثاني §17 — تحليل المخزون (التقييم + الراكد + إعادة الطلب)** — صفحة `/dashboard/inventory-analytics`] (للمديرَين): خدمة خالصة `inventoryAnalytics.js` (+٨ اختبارات) تحسب من ماستر الأصناف (الرصيد/التكلفة/`minStock`) ودفتر الحركات (آخر خروجٍ للراكد) — لا بيانات جديدة ولا تغيير قواعد؛ + مكوّن حيّ `InventoryAnalytics.jsx` (٣ تقارير: تقييم بالفئات · إعادة طلب بالنقص وقيمة التجديد · راكد بنافذة ٩٠/١٨٠ يومًا). **٥٤٢ اختبارًا · بناء ٥٤ صفحة · تدقيق نظيف · لينت صفر جديد.** ⚠️ رُبِط على `dfcc262` (دفعة المالك «المعيار الدافئ») بحلّ تعارض `navCatalog`. [سابقًا مدفوع: **استعداد ديلويت اكتمل ١→٥** حتى `3b4e228`؛ دفعة المالك للواجهة `dfcc262`]. **السابق — اليوم ٥: حزمة الاستعداد للمقابلة (البروفة)** — صفحة `/dashboard/interview-readiness` (قمرةٌ جامعةٌ بستّة مكوّنات: بطاقة أرقام حرجة · مسار عرض ١٥ د · بنك أسئلة بأجوبة · ردّ على الاعتراضات · خارطة أدلّة · بطاقة رؤية) مصدرها الدراسة + `interview-readiness.json` + خدمة `readinessModel` بحارس `readinessVerdict` (كل رابط دليلٍ يُتحقَّق ضدّ `navCatalog`). **٥٣٤ اختبارًا · بناء ٥٣ صفحة · تدقيق نظيف · لينت صفر جديد** — يُرفع الآن بأمر «ارفع». + **منهجٌ دراسيٌّ منشورٌ artifact `f1db33a5`** (بروفة تفاعليّة) + **بروفة حيّة جرت** (المالك درّب، والتصحيح المحوريّ: النضج «٢٫٩ من ٥» لا «٢٫٥ من عشرة»). · **✅ اكتملت الشريحة الحرجة الأيام ١→٥.** · **✅ اليوم ٤ مدفوع لـmain `c24f3c3`.** · **✅ المالك نشر `firestore.rules`** (2026-07-29، النسخة الحاليّة بعد مزامنة مجلده) فماستر الموردين/العملاء صار كتابةً حيّة. [سابقًا: ماستر شركاء الأعمال §15.2/15.3 مدفوع `2b85ccc` · تحليل إنفاق الموردين `02d5b27` · لوحة المؤشرات + تصحيح ١٩/٦ `52a8018`] — محرّك موحّد (`partnerService` + الاستيراد + مجموعتا `excelSchema`) + **مكوّن `PartnerMaster` موحّد** + صفحتان `/dashboard/suppliers` و`/dashboard/customers` (قائمة + إضافة/تعديل + استيراد بمعاينة + أرشفة) + قاعدتان. **٥٠٧/٥٠٧ اختبارًا · بناء ٥١ صفحة · لينت صفر جديد** — ✅ **مدفوع لـmain `2b85ccc`** والنشر انطلق · ⚠️ **يبقى على المالك: نشر `firestore.rules`**. · **★ اعتُمد معيار حوكمة الواجهة** ([[feedback-dashboard-methodology]]: بلا إيموجي · الأحمر للتحذير فقط · الربط قبل الإضافة · لا حذف/تغيير URL · بنية ٣ طبقات) + تمريرة إزالة إيموجي على kpis/suppliers/customers. [سابقًا مدفوع main: تحليل إنفاق الموردين `02d5b27` · لوحة المؤشرات + تصحيح ١٩/٦ `52a8018`]

---

## ⏸️ أين توقّفنا الآن (نقطة الاستئناف)

- **[Codex] 2026-08-07 — العملية الجراحية الأولى لتفاعل غرفة القرار (فرع `codex/executive-review-interactivity`، غير مرفوع):** أُضيف خط تقدم حيّ للمحطات التسع مع حفظ الزيارة محليًّا، ومؤقت واسم محطة داخل وضع العرض، ووضع تركيز «بطاقة واحدة» للصورة الفورية. تحوّل سجل القرارات التسعة إلى لوحة اجتماع تفاعلية بأربع نتائج (اعتماد · اعتماد مشروط · تأجيل · إعادة للدراسة) مع مسؤول وموعد وملاحظة لكل قرار، ولوحة نتيجة فورية، وحفظ `localStorage` فقط، وطباعة ملخص؛ لا Firestore ولا قواعد جديدة. المنطق الخالص في `src/services/executiveReview/decisionSession.js` مع 3 اختبارات، والواجهة في `MeetingDecisionBoard.jsx`. **التحقق:** 732/732 اختبارًا · بناء إنتاج 74 صفحة · تدقيق نظيف · لينت الملفات المعدلة نظيف. **المتبقي:** قبول بصري/تفاعلي بحساب مدير؛ بناء `preview` المحلي تحوّل إلى `/login` لأن جلسة Firebase الحيّة لا تنتقل إلى `127.0.0.1`. لا دفع ولا كومِت نهائي حتى مراجعة المالك وأمره.
- **🏛️ [Claude+Codex] غرفة قرار سلاسل الإمداد — جاهزة للمراجعة البصرية (2026-08-07):** 9 شاشات تنفيذية على `/dashboard/executive-review` (المنظور · الحالة · النموذج · المواقع · الكتالوج · القطاعات · الحوكمة · القدرات الرقمية · القرارات) — admin+warehouse_manager عبر **وراثة الأب** الجديدة في `pageAccess.js`. البيانات مصدر واحد `src/data/executive-review/` (7 ملفات) والعرض `src/components/executive-review/` (Shell+ReviewPage). سجل اعتماد كوديكس في مجلد التواصل «حزمة الربط — القدرات التشغيلية الرقمية الدائمة» (سياسة 6 مراحل + بوابات إلزامية). **يتبقى:** مراجعة المالك البصرية (سطح مكتب + جوال) → «ارفع». ملاحظة محتوى حاكمة: ملف 155 = **مخطط تحوير وتجهيز تشغيلي لا طلب ميزانية صيانة**؛ والقدرات الرقمية = **تكلفة تشغيلية ثابتة $400/شهر لا رواتب**.

- **🤝 [Claude] الشراكة الثلاثية — انضمام Codex (2026-08-06):** المالك اشترك في Codex (OpenAI) شريكًا تنفيذيًّا ثانيًا. أُنشئ ملفّان: [`docs/إحاطة-كوديكس.md`](docs/إحاطة-كوديكس.md) (الإحاطة الكاملة التي تُرسل له: المشروع · المنفَّذ · المتبقّي · الخطط · المستودع · الأشخاص · بروتوكول توحيد الذاكرة) + **`AGENTS.md` في الجذر** (دستور تشغيل مضغوط يقرؤه Codex تلقائيًّا كل جلسة — نظير ذاكرة Claude). **بروتوكول الذاكرة الموحّدة:** الريبو هو الذاكرة المشتركة الوحيدة؛ كل وكيل يقرأ رأس هذا الملف أول الجلسة ويضيف مدخلًا موسومًا `[Codex]`/`[Claude]` آخرها؛ فروع `codex/*` و`claude/*`؛ الدفع لـmain بأمر المالك فقط؛ لا يعمل وكيلان على نطاق واحد بالتوازي. ⚠️ في worktree `claude/project-partnership-file-f16cdc` — ينتظر «ارفع».

- **📐 المحور الأول من خطة تطوير البوابة (الهيكل+التوظيف) — أ‑١+أ‑٢ ✅ مرفوعتان لـmain (2026-08-05):** الخطة المعتمدة [`docs/خطة-تطوير-البوابة-الهيكل-والعمالة.md`](docs/خطة-تطوير-البوابة-الهيكل-والعمالة.md) (٥ قرارات محسومة، المحور الأول ٤ دفعات ثم الثاني ٦). **أ‑١:** حقل `requirements` لكل وظيفة في `org-structure.json` (المصدر الواحد) → `build-org.mjs` → `jobsCatalog`+التقريرين · قسم «كل الأدوار الـ36» في `org-structure.astro` يعرض الوظائف كلّها بما فيها المساندة (`allJobsGridHtml` + `jobRequirementsHtml`) · عرض المتطلبات في `RecruitmentBoard`+`CandidatePrint`. **أ‑٢:** ربط متبادل — زرّ «توظيف لهذا المنصب» (الهيكل→`recruitment#job` عبر `hireForJob`+`prefillJobId`) وزرّ «الموقع في الهيكل» (التوظيف→`org-structure#job` عبر `applyJobDeepLink`) + **قراءة حيّة** خدمة خالصة جديدة `jobsResolver.js` (`resolveJobs`/`findJob`/`sortedJobOptions`: التوظيف يشترك في `org_structure/current` مع fallback للكتالوج المولَّد؛ `RecruitmentBoard`+`CandidateForm` يستهلكان الأدوار الممرّرة). البوّابة: **٦٧٨ اختبارًا · تدقيق نظيف · بناء ٦٠ صفحة · لينت نظيف** · rebase نظيف فوق `5a74f9e` بلا تعارض. **لا قواعد Firestore جديدة من هذا العمل.** ⚠️ التنقّل الحيّ بين الصفحتين يحتاج حساب المالك للتحقّق (كلاهما محميّ بالدخول).

- **📐 الدفعة أ‑٣ (التسكين) ✅ منجزة worktree — لم تُرفع (2026-08-05):** نموذج خالص `assignmentsModel.js` (`activeAssignmentsByJob`/`withLiveHolders` +٥ اختبارات — الشاغل الحاليّ = أحدث تسكين نشط، **دون لمس** `holder`/`occupied` المصدر) · خدمة `assignmentsService.js` (مجموعة `assignments` ملحق-فقط: `listenAssignments`/`assignToJob`/`endAssignment`) · **قاعدة `assignments`** في `firestore.rules` (read signedIn · create/update للمديرَين · لا حذف · ثبات jobId/candidateId/startedAt) · زرّ «＋ تسكين» في التوظيف للمرشح `accepted` (`placeCandidate`) · الهيكل يشترك في assignments ويدمج `withLiveHolders` في شبكة الأدوار وبطاقات العقدة فيظهر الشاغل الحيّ. بوّابة خضراء (**٦٩٤ اختبارًا · تدقيق نظيف · بناء ٦٠ صفحة · لينت نظيف · قواعد 116/116**). ⚠️ **يحتاج: رفع + نشر قاعدة `assignments`** (Console → Firestore → Rules → لصق `firestore.rules` كاملًا). **⏭️ التالي: أ‑٤ (الملف التعريفيّ للموظف المُسكَّن + CRUD الأدوار من الواجهة) — آخر دفعات المحور الأول.**

- **📎🔍 التحسينات الجراحية للمستندات — الدفعتان ١ (المرفقات) + ٢ (طبقة الرقابة) ✅ منجزتان · worktree لم يُدفع (2026-08-04):** مصدر الطلب [`تحسينات جراحية.md`](تحسينات جراحية.md) (٧ مطالب على `/dashboard/documents`). **قرارا المالك (سؤال صريح):** رصيد المركبة = **موقع لكل مركبة**؛ أداة المقارنة = **طبقة رقابة مدمجة**. **المالك نشر `firestore.rules` (الدفعة ١)** فالرفع يعمل حيًّا.
  - **الدفعة ١ — نظام المرفقات:** مجموعة فرعية `documents/{id}/attachments` ملحق-فقط (نمط `audit`/`scans`): `attachmentFile.js` (+٧ اختبارات، صور+PDF، حدّ ٧٠٠ك.ب بعد الضغط) · `attachmentsService.js` · `AttachmentsPanel.jsx` (**ضغط صور داخل المتصفّح canvas→JPEG** + كاميرا الهاتف حسب نوع الخانة + مصغّرات باسم الرافع/وقته) · مربوطة في `DocumentEngine` (تظهر لأي مستند محفوظ ولو بعد الإرسال) و`DocumentPrint` (شبكة صور + إدراج PDF نصًّا) + قاعدة `attachments`. خانات مقترحة في ٦ مخططات (GRN·DMG·RET·TRN·DN·INV). ومكسبا مخطط: CC نوعٌ (يومي/أسبوعي/مفاجئ، المطلب ٦) · RET أنواع (عميل/فرع/مورّد، المطلب ٥).
  - **الدفعة ٢ — طبقة الرقابة/المطابقة (المطالب ٣·٤·٥·٧):** مجموعة فرعية `documents/{id}/control` ملحق-فقط: `control.js` (منطق نقيّ +١٤ اختبارًا) · `controlService.js` (الاختلاف يُلزِم بسبب) · `ControlPanel.jsx` (يعرض قيم المستند الصادر + قائمة مطابقة + نتيجة مطابق/اختلاف موقّعة بالهوية) · ضبط `control` على ٥ مخططات (INV·GRN·RET·TRN·DMG) · ختم في الطباعة (لون حسب الحالة) · قاعدة `control`. **الفوترة صارت أبرز مكانٍ للأداة** (طلب المالك). **⚠️ قاعدة `control` الجديدة تحتاج نشرًا** (مع بقيّة `firestore.rules`).
  - **🔬 مراجعة عدائية موازية (ورشة، ١٨ وكيلًا):** ٦ أبعاد × تحقّق مضادّ → **١٠ ملاحظات مؤكَّدة أُصلحت**: (HIGH) تكرار `compressImage` لانهائي → البُعد يتناقص فعلًا · (MED) قاعدة `/control` مفتوحة → قُيّدت بالفاعل المخزنيّ/المشترياتيّ النشط + **فرض سبب الاختلاف على الخادم** · (MED) `capture` يفرض الكاميرا للـPDF → صار حسب نوع الخانة · (MED) طباعة الصور تقصّ الدليل → `object-fit: contain` · (LOW×4) كسر صفحات المرفقات · PDF لا يُطبع · لون ختم الطباعة · **تزوير `byName`** (قُبل كاتّساقٍ مع عُرف `audit`/`scans` في كل المستودع — `byUid` وحده مثبَّت؛ التقييد بالفاعل يخفّف الأثر). أُسقطت ملاحظتان خاطئتان بحقّ (ترتيب `serverTimestamp` سليم · لون الختم تجميليّ).
  - **التحقّق النهائيّ:** **١٥٩ اختبارًا يمرّ** (١٤٥+١٤) · لينت نظيف · `firestore.rules` متوازن (١١٢/١١٢ قوس · ٣٣٤/٣٣٤ قوس هلاليّ). **⚠️ حاجز بناء سابق غير متعلّق:** `whisper.worker.js` (مساعد الاجتماعات) — لم ألمسه.
  - **الدفعة ٣ — القطع الثلاث الأخيرة ✅ (2026-08-04، قرارات المالك الثلاثة الموصى بها):** ① **رصيد المركبة لكل مركبة (المطلب ٢):** موقع `VAN:‹لوحة›` لكل مركبة (`locations.js`: `vehicleLocationCode`/`isVehicleLocation`/`onVehicleBalances`، بادئة محجوزة) · **قيد DN تغيّر** من `STAGING→خارج` إلى `STAGING→@vehicle` (تحميل بالمركبة) · **مستند POD جديد** (`pod.js`) يخصم `@vehicle→خارج` عند الإنجاز (رمز `@vehicle` يُحلّ في `movements.js` من `header.vehiclePlate`) · **دور fleet أُضيف لـ`isStockActor`** ليُنجز POD (بوّابة completeRoles تحصره في POD) + POD/SRN في approve/completeRoles. ② **إشعار رفض الاستلام SRN (المطلب ١):** توثيقيّ بلا قيد مخزنيّ، يُشتقّ من بنود GRN المرفوضة وحدها (`LINE_FILTER` جديد في `chain.js`: `qtyRejected>0`) موقّعًا من مندوب المورّد. ③ **قسم تعبئة النقل (المطلب ٤):** قائمة تحقّق تعبئة في مخطّط TRN. سلسلتان مصغّرتان `DELIVERY_CHAIN=['DN','POD']` و`REJECTION_CHAIN=['GRN','SRN']` + أزرار الاشتقاق في ChainBar + مجموعتا الصندوق. **العدّ صار ٢٦ نموذجًا.** **مراجعة عدائية للدفتر/القواعد (٤ أبعاد) اكتملت: ٣ نظيفة + ملاحظة واحدة أُصلحت** — POD لم يكن يمرّر `unitPrice` فتُقيَّد حركة التسليم بصفر وتُصفَّر تكلفة رصيد المركبة عند التسليم الجزئيّ؛ أُضيف `unitPrice` لخريطة `DN>POD` (كـ`DN>INV`) + حارس اختبار. **التحقّق:** **٦٦٦ اختبارًا يمرّ** (+٢٠ جديدة) · لينت نظيف · القواعد متوازنة (١١٢/١١٢). **⚠️ تحتاج نشر `firestore.rules`** (fleet في isStockActor + POD/SRN roles). **بهذا نُفِّذت المطالب السبعة كلّها ومُرّت بمراجعتين عدائيّتين** — ينتظر فقط: نشر القواعد · تجربة حيّة بحساب.

- **🎙️ تفريغ ملفّ صوتي داخل المتصفّح — «مساعد الاجتماعات» ✅ مدفوع main (2026-08-03 · `38a14a4` + إصلاح `c1d4a45`):** ⚠️ **إصلاح حرِج بعد بلاغ المالك** (وقوف على «تحميل 100%»): كان نمط turbo بمُشفّر fp32 = ملفّ 2.5غ.ب → تنزيل ~2.9غ.ب، ومؤشّري يُثبّت السقف فيُظهر 100% كاذبًا. صُحِّح لصيغ q4/q4f16 حقيقيّة (turbo 560م.ب·small 300·base 140، تحقّقتُ من HF API)، الافتراضيّ صار «متوازن»، وتقدّم صادق + مرحلة «تهيئة النموذج». **تحقّق E2E حيّ لكل الأنماط الثلاثة.** طلب المالك ميزة uniscribe (رفع تسجيل بأي حجم/مدّة → تفريغ كامل موثوق) لإلغاء اشتراكه الشهريّ. **القرار (سؤال صريح):** تشغيل **Whisper داخل المتصفّح بالكامل** عبر `@huggingface/transformers` v4 (WebGPU مُفضَّل، يسقط لـWASM بخيط واحد) — لا خادم/مفتاح/اشتراك، والصوت لا يغادر الجهاز. **البنية:** `whisper.worker.js` (عامل خيطيّ، تقطيع داخليّ `chunk_length_s:30`/`stride:5` فيُفرّغ أي مدّة كاملةً · نموذج يُنزَّل مرّة ويُحفَظ في كاش المتصفّح · تقدّم إجماليّ تصاعديّ) · `useAudioTranscriber.js` (طابور تِباعيّ · فكّ ترميز إلى 16kHz أحاديّ عبر AudioContext/OfflineAudioContext · ٣ أنماط: `turbo=large-v3-turbo` افتراضيّ مع WebGPU · `balanced=small` · `fast=base`) · `AudioFileTranscriber.jsx` (إفلات + أشرطة تقدّم + بثّ نصّ حيّ) — نتيجة كل ملفّ تُضاف مقطعًا للتفريغ الرئيسيّ فتعمل معها كل الأدوات. **+ `pwa-sw.mjs`: استُثنيت `.wasm` من التخزين المسبق** (ملفّ ONNX ~23م.ب كان سيُثقِل تثبيت PWA لكل النظام — يُخزَّن عند أوّل استخدام). **التحقّق:** ✅ **E2E حقيقيّ في المتصفّح عبر صفحة تحقّق مؤقّتة** (حُذفت): عيّنة JFK فُرّغت حرفيًّا «And so my fellow Americans…»، WebGPU مُكتشَف، تقدّم التنزيل + استئناف بعد «network error» عابر نجحا، والنصّ أُضيف للتفريغ الرئيسيّ وتحدّث عدّاد الكلمات. بناء 59 صفحة ✓ · ٦١٦ اختبارًا ✓ · تدقيق نظيف ✓ · لينت الملفّات الجديدة نظيف ✓. **🟡 أمانةً:** لا تفريغ «100% بلا خطأ» حرفيًّا (ولا الخدمة المدفوعة)، لكنه يفرّغ كامل الملفّ ودقّته للعربية عالية؛ ملفّات متعدّدة الساعات على أجهزة ضعيفة الذاكرة قد تحتاج تقسيمًا. قرار رفع: `ارفع`. راجع [[project-meeting-transcription]].

- **🌐 «عمليات البوابة» — لوحة شاملة مولَّدة تلقائيًّا ✅ منجزة (2026-08-02 · worktree لم يُدفع):** طلب المالك بطاقة/صفحة تجمع **كل مقاصد البوابة** مرتّبةً بنظام بطاقات، **تظهر فيها أي شاشة جديدة تلقائيًّا مستقبلًا**. **الحلّ (صفر تكرار):** صفحة `/dashboard/portal-operations` تُولَّد من المصدر الواحد `navCatalog.js` — ٨ أقسام + «الوصول السريع» + بحث عربيّ مطبَّع؛ البطاقات تحمل نفس سمات الشريط الجانبي (`data-nav-group`/`data-group-key`/`data-item-roles`) فيرشّحها `RoleNav` بالدور تلقائيًّا **بلا منطق صلاحيات مكرَّر**. **الأتمتة المضمونة:** حارس `npm run audit` يرفض أي صفحة يتيمة (تُلزَم بالتسجيل في `navCatalog`)، فكل شاشة جديدة تظهر هنا دون لمس الصفحة. أُدرج العنصر أوّل مجموعة «العمليات اليومية» في الشريط الجانبي + بطاقة أولى في «ابدأ من هنا» بلوحة التحكم (أيقونة `globe`). **التحقّق:** بناء ٥٩ صفحة ✓ · ٦١٠ اختبارًا ✓ · تدقيق نظيف (٥٧ صفحة مربوطة) ✓ · لينت نظيف على الملفات المعدّلة ✓ · فحص الـHTML المبنيّ (٦٠ بطاقة · ٤٨ بقيد أدوار · ٥ خارجيّة). **⚠️ لم يُجرَّب حيًّا** (خلف حارس الدخول) — يحتاج جلسة المالك بحساب. قرار رفع: `ارفع`.

- **🔀 مطابقة المرجع ↔ البوابة + التقارير + الخدمات ✅ منجزة (2026-08-02 · worktree لم يُدفع):** طلب المالك «عملية احترافية»: مقارنة المرجع التشغيلي الرسمي بالبوابة إلى تطابقٍ ١٠٠٪ + ٣ تقارير + (بقراره) تطوير شامل للخدمات الناقصة. **ما أُنجز:** ① **المصدر الواحد** `reference-crosswalk.json` (نماذج ثنائية الاتجاه · أدوار · حرّاس · دورة · خدمات · سجلّ فجوات ٦ بنود) + `crosswalkModel.js` (+٦ اختبارات، حارس بنية). ② **محاذاة المرجع** `public/المرجع-التشغيلي-الرسمي.html`: أضفتُ `treasury` لـ`org-structure.json` وأعدتُ توليد `build-org.mjs` (١٣ دورًا + فهرس الشاشات ٥٦) · أضفتُ جدول النماذج الإضافي (٢٢–٣٠) والسلاسل السبع في فصل النماذج · جدول ٩ حرّاس (حَجب/تحذير) في القواعد الذهبية · ملاحظة الدورة (٧ سلاسل) · صحّحتُ الغلاف (٢٤ نموذجًا · ١٣ دورًا) والعناوين والخاتمة. ③ **٥ صفحات خدمة جديدة** (بناء متوازٍ بوكلاء، نمط `comparative-study` حرفيًّا): `vendor-scorecard` · `hse-checklists` (localStorage) · `business-continuity` · `cold-chain` · `role-activity`. ④ **٣ تقارير** داخل البوابة: `reference-guide` (رحلة ١٢ مرحلة تفاعليّة + ركائز) · `acceptance-check` (٨٢ معيار قبول UAT · localStorage · تصدير CSV · طباعة) · `portal-value`. ⑤ ربطتُ الثماني في `navCatalog.js` (تقارير + مستودعات) بأدوارها. **التحقّق:** بناء ٥٨ صفحة ✓ · ٦١٠ اختبارًا ✓ · تدقيق نظيف (٥٦ صفحة مربوطة · لا رابط مكسور · ١٣ دورًا) · تحقّق بصريّ حيّ للمرجع. **🟡 المتبقّي/التالي:** تجربة المالك للصفحات الثماني حيًّا بحساب مدير (محميّة بالدخول فلم تُجرَّب تفاعليًّا) · مراجعة المرجع المُحاذى قبل تسليمه لشركة الميزان · قرار رفع (`ارفع`). البيانات في صفحات الخدمات **بذرة تمثيليّة** (موثّق في كل صفحة) تنتظر الربط الحيّ.

- **🔗 المحور ٦ — تعميم docref مكتمل (2026-08-02، `0deb4aa`):** حوّلتُ بقية حقول المرجع (PO.prRef→PR · INV.deliveryRef→DN · TRN.transferReqRef→TR · TRC.transferNoteRef→TRN، اختيارية بلا إلزام جديد) — فكلّ السلاسل الستّ الآن تدعم التعرّف التلقائيّ والربط والإنشاء المباشر (597 اختبارًا). المتبقّي التقنيّ: Tailwind CDN (ملف تقرير واحد، JIT يصعب استضافته) + ترقيم خادميّ بمؤشّرات (منخفض الإلحاح) — كلاهما موثّق ولا يستحقّ المخاطرة بلا تجربة حيّة.

- **📄 العملية الجراحية — المحور ٧ (الترقيم) ✅ منجَز عمليًّا (2026-08-02):** طبقة نقيّة `src/services/ui/pagination.js` (+5 اختبارات) + مكوّن `Pager` بنمط أودو («١–٥٠ / ٢٤٣») مكتفٍ ذاتيًّا (أنماط سطريّة، لا يعتمد .o_theme). **طُبِّق على ٣ شاشات** بترقيم من طرف العميل فوق المصفوفة المحمّلة (يكشف الذيل بدل قصّه صامتًا، بلا لمس تدفّق البيانات): PartnerMaster · ItemMaster · StockLedger (كان يقصّ 60 صامتًا). ⚠️ لم يُجرَّب حيًّا (محميّ بالدخول). 🟡 المتبقّي: ترقيم **خادميّ بمؤشّرات** (startAfter+getCountFromServer) لصندوق المستندات والاشتراكات الكاملة (Items_Master/balances) — منخفض الإلحاح (البيانات ليست ضخمة). **بهذا اكتملت المحاور ١-٧ عمليًّا** (مع بنود متبقّية موثّقة تحتاج تجربة حيّة/قرار مالك). راجع [docs/خطة-العملية-الجراحية.md].

- **🔗 العملية الجراحية — المحور ٦ (الربط التسلسلي) 🟡 التعرّف التلقائيّ مُنجَز (2026-08-02):** حقول المرجع (poRef/grnRef…) كانت **نصًّا حرًّا ميّتًا**؛ صارت **تعرّفًا تلقائيًّا بالرقم**. **اكتشاف مبسِّط:** الرقم عالميّ الفرادة (`TYPE-YEAR-SEQ`) فيكفي `lookupByNumber` بحقل `number` وحده — **لا فهرس مركّب ولا تدخّل من المالك** (خلاف ما توقّعت الخطة). **المرحلة أ (`8882890`) — الأساس الخالص المُختبَر:** `lookupByNumber` · `mergeParentLink` (ربط تراكميّ يورّث أسلاف الأب كالاشتقاق → المطابقة الثلاثية وشريط السلسلة يعملان) · `parentApprovalProblem` (حارس «لا إنجاز قبل اعتماد الأب» مربوط في `transitionDocument` عند «منجَز») · الحقول الستة صارت `kind:'docref'`+`docType` (GRN.poRef→PO · QC/PUTAWAY.grnRef→GRN · GP.dnRef→DN · CN.returnRef→RET · ADJ.cycleCountRef→CC) · `primaryParentType`/`docrefFields` + 8 اختبارات. **المرحلة ب — الواجهة:** `DocRefField.jsx` (تكتب رقم الأب → Enter/مغادرة → بحث → شريحة «✓ مربوط» خضراء، أو «سلسلة غير معتمَدة» صفراء، أو «غير موجود»/«نوع خاطئ») موصول عبر `FieldInput` (فرع docref) و`DocumentEngine.resolveParent` (يدمج links + يثبّت الرقم). القواعد الحالية تسمح بكتابة links أثناء تحرير المسودّة (لا تعديل قواعد). بوابة خضراء (591 اختبارًا · لينت 124=124). **⚠️ لم يُجرَّب حيًّا** (محرّك المستندات محميّ بالدخول) — يحتاج تجربة المالك. **المتبقّي (المرحلة ب٢):** نافذة **الإنشاء المباشر** للأب المفقود (معالج مصغّر يُنشئ الأب ويقدّمه فيأخذ رقمًا شرعيًّا من العدّاد ثم يربط) + خيار «رقم ورقيّ/تاريخيّ» (`externalRef`) + تعميم بقية الأزواج (INV→DN · TRN/TRC · PO→PR · PACK→PICK · DN→PACK). ثم **المحور ٧ (الترقيم)**.

- **📴 العملية الجراحية — المحور ٥ (أوفلاين) ✅ شبه مكتمل (2026-08-02):** ✅ **مكتبات CDN لصفحات اللوحة استُؤصلت كلها** (`e0cb429`): vendored في `public/lib` (لا تبعيات npm — ملفات ثابتة كنمط html5-qrcode): xlsx · xlsx-js-style · jspdf · html2canvas · mermaid · leaflet/ · fontawesome/ (css/+webfonts). المواضع الحقيقية ٤ صفحات (assets-inventory·stock-operations·retail-hub·erp-workflows) — تبيّن أن AssetsInventory.jsx/VehiclesInventory.jsx **نسخ ميتة** وvehicles-inventory.astro تستورد xlsx من الحزمة. **تحقّق حيّ: كل الأصول تُخدَم من الموقع (200).** بلاطات الخريطة أونلاين بطبيعتها (تدهور رشيق موثّق). ⚠️ `npm i` نُفِّذ في المستودع الرئيسي لنسخ الملفات ثم يُعاد package.json (استضفنا بدل الاستيراد) — **لا تبعيات جديدة في الشجرة**. ✅ **تمريرة public اكتملت أيضًا**: ٣٢ ملفًّا (٢٢ نموذج + ١٠ تقارير) نُظّفت — خط محلي مشترك `public/lib/fonts/bz-fonts.css` (يُغني عن Google Fonts في ٣١ ملفًّا) + xlsx وFontAwesome وhtml2pdf محلية (`html5-qrcode` كان محليًّا أصلًا). **المتبقّي الوحيد: Tailwind CDN JIT في `تقرير-التوظيف-والجدول-الزمني.html`** (يصعب استضافته بلا البناء المستقل — موثّق). المرجع الرسمي يستعمل Tahoma النظاميّ فهو أوفلاين-آمن بلا تغيير. 🟡 يبقى: **جولة قبول أوفلاين موثّقة** (تحتاج جلسة المالك — الشاشات محميّة). **وقبله:** ✅ **الخطوط أُنجزت** (الأوسع أثرًا): بديل `@font-face` في [`src/styles/fonts.css`](src/styles/fonts.css) يجعل `Cairo`/`Tajawal` تشيران للخط الرسمي المستضاف ذاتيًّا (نطاق عربي، اللاتيني يسقط لنظام آمن) → حُذفت **١٤ رابط Google Fonts** من كل صفحات اللوحة العشر + `stock-operations` + `index.astro`. تحقّق البناء: بديل Cairo→woff2 مدموج، صفر googleapis في الصفحات الرئيسية، لينت 124=124. المتبقّي موثّق (نوافذ طباعة/وثائق مولّدة، أثر ضئيل). 🔴 **مكتبات CDN الثماني لم تُستأصل** — أعقد من الظنّ: xlsx/mermaid/html2pdf في node_modules (تُنسخ لـpublic/lib) لكن AssetsInventory/VehiclesInventory **وثائق HTML مؤطَّرة** تحتاج base كاملًا وتحقّقًا حيًّا؛ وleaflet/xlsx-js-style/FontAwesome **تحتاج npm i** (تنزيل — مؤجَّل). الخطة الدقيقة للجلسة القادمة في [docs/خطة-العملية-الجراحية.md] قسم «حالة التنفيذ». **FontAwesome في stock-operations** بديله استبدال fa-solid بأيقونات Icon SVG (٣٦٥٣ سطرًا — تحقّق حيّ لازم).

- **⚠️🔴 يلزم المالك: نشر `firestore.rules` من جديد** — دفعة المحور ٤ الأمنية جاهزة في الملف وغير منشورة بعد. حتى النشر البوابة تعمل بالقواعد القديمة (متوافقة رجعيًّا)، لكن الخوارق تبقى مفتوحة حتى النشر. **الصق الملف كاملًا (Ctrl+A) وتحقّق أن آخر سطر `}` وعدد الأسطر 579.**

- **🛡️ العملية الجراحية — المحور ٣ + ٤ ✅ منفّذان (2026-08-02):**
  - **المحور ٣ (أودو):** وثيقة عقد التكامل [`docs/عقد-التكامل-مع-أودو.md`](docs/عقد-التكامل-مع-أودو.md) (`0f1941c`) — Firestore المصدر · Worker وسيطًا (odoo-proxy جاهز غير منشور) · مزامنة ٣ مراحل (polling→outbox→webhooks) · سياسة ترقية الإصدار (المعرفة محصورة في odooMapper+الموديول) · pg_dump يومي + تمرين استعادة. **التنفيذ مشروط بحساب Cloudflare واستضافة أودو حقيقية — قرار مالك.**
  - **المحور ٤ (الأمن) — الجزء ١ XSS `9bac61d`:** مُهرِّب موحّد `src/services/ui/escape.js` (+4 اختبارات) + إغلاق ثغرات: MeetingAssistant (تفريغ/ملاحظات/عنوان/ملخص + **ردّ API الترجمة الخارجي** قبل html2pdf) · retail-hub (3 مواضع localStorage). (جدول المركبات ثوابت ساكنة لا ثغرة.)
  - **المحور ٤ (الأمن) — الجزء ٢ القواعد (يحتاج نشر):** `isActive()` جديدة مدموجة في كل الأدوار (الموقوف يسقط من كل كتابة حسّاسة — يعيد استعمال get() المخزّن فبلا كلفة، والمدير المُمهَّد بالبريد فوقها) · `canReserveNumber()` للعدّادات (كان أي مصادَق يحرق أرقامًا — أخطر خرق) · `users` read محصور بالذات أو المدير (كان الدليل كله مقروءًا) · تشديد create لـ operations/documents/Items_Pending/Inbound_Log/Outbound_Log إلى `isStockActor` (كان `signedIn`). الأقواس متوازنة 99/99 · 579 سطرًا. **+ أول اختبارات قواعد آلية** `test/firestore-rules.test.mjs` (10 سيناريوهات) + `firebase.json` + سكربت `npm run test:rules` (تحتاج إيميولاتور+Java، تُتخطّى بأمان بدونه وخارج `src/**` فلا تكسر `npm test`). **⚠️ التشديد لم يُختبر حيًّا** (لا إيميولاتور محليًّا) — راجَعتُه بالتفتيش: كل حاجزي الأرقام/الفاعل المخزني تُغطّي الأدوار التي تكتب فعلًا (تحقّقتُ بمسح `reserveNumber`/`listUsers`)، والمالك ينشر ثم يجرّب.

- **✅ مصفوفة الصلاحيات تعمل حيًّا (2026-08-02):** المالك نشر القواعد وأكّد الحفظ. سلسلة إصلاحات بعد النشر: رسالة الخطأ تعرض الرمز الفعلي (`5817407`) · **تحقق ذاتي** بعد الحفظ عبر `getDocFromServer` يؤكّد عدد التجاوزات + تأكيد قبل «تراجُع» (`6581f2f`) · **الجذر الحقيقي لشكوى «لا يوجد زر حفظ»**: الزرّان استعملا صنفَي `o_btn/o_btn-primary` غير المعرَّفين في `odoo.css` فظهرا نصًّا باهتًا بلا شكل زر — استُبدلا بزر مصمَّم ذاتيًّا بارز ومفعَّل دائمًا + شريط أدوات لاصق (`b7a4053`). **درس:** أي زر/تنبيه في `.o_theme` يجب أن يستعمل أصنافًا معرَّفة فعلًا في `odoo.css` (`o_input/o_ds_card/o_alert/o_badge` موجودة · `o_btn/o_btn-primary/o_alert-success` **غير موجودة**).

- **🔐 العملية الجراحية — المحور ٢-ب ✅ منفّذ (2026-08-02 فجرًا · يُدفع لـmain):** **نظام صلاحيات دقيق قابل للتنفيذ** فوق الكتالوج: ① نموذج خالص `accessMatrix.js` (+8 اختبارات): `{overrides: {role: {allow[], deny[]}}}` — deny يغلب · admin يستحيل قفله · تعذُّر الجلب = كتالوج وحده (سقوط آمن). ② خدمة `accessMatrixService.js`: وثيقة `access_control/matrix` + سجلّ نسخ ملحق-فقط. ③ `pageAccess.js` صار يقبل `matrix` اختيارية في `canOpenPath/allowedPathsFor/landingPathFor/isPathAllowed` (غيابها = السلوك الأصلي حرفيًّا). ④ `AuthGate`: يجلب المصفوفة مع الملف الشخصي + **تفعيل `active:false` أخيرًا** (خروج فوري وتحويل `/login?disabled=1` مع لافتة في login.astro — كان العلم يُكتب ولا يُنفَّذ!). ⑤ `RoleNav`: الحجب يُخفي الرابط والسماح الإضافي يُظهره (+إخفاء المجموعات المفرغة). ⑥ **`AccessControlHub` أُعيد بناؤه محررًا فعليًّا**: شبكة (11 دورًا × 44 شاشة) بتدوير افتراضي/سماح/حجب + عدّادات حية `allowedPathsFor` + معاينة دور + حفظ بنسخ — حلّ محلّ العرض الساكن ذي الأدوار الوهمية. ⑦ توحيد `MANAGER_ROLES` في `roles.js` (كان مكرَّرًا 4×). ⑧ **قاعدة `access_control` أُضيفت لملف `firestore.rules`** (قراءة لمصادَق · كتابة admin · versions ملحق-فقط) — **⚠️ على المالك نشر القواعد** (حتى النشر: الحفظ يرتدّ برسالة واضحة والنظام يعمل بالكتالوج وحده بأمان). بوابة خضراء (49 · **579 اختبارًا (+8)** · تدقيق نظيف **35 قاعدة** + فحص pages[] الجديد · لينت 124=124) + فحص الأثر المبني (chunk المحرر + الخدمة). **⚠️ لم يُجرَّب حيًّا بحساب مدير** (يحتاج جلسة المالك + نشر القاعدة). **التالي: المحور ٤ (الأمن)** حسب [docs/خطة-العملية-الجراحية.md](docs/خطة-العملية-الجراحية.md) — دفعة القواعد الموحدة (تشمل هذه القاعدة + تشديد counters/operations/logs/users + isActive) ثم XSS الثلاث واختبارات القواعد.

- **🩺 العملية الجراحية — المحور ٢-أ ✅ منفّذ (2026-08-02 فجرًا · يُدفع لـmain):** المرجع التشغيلي صار **مطابقًا للبوابة ومولَّدًا**: ① **فصل جديد `#s-portal` «أدوار البوابة الفعلية وفهرس الشاشات»** يولّده `build-org.mjs` **من كود التنفيذ نفسه** (roles.js + navCatalog + pageAccess): جدول الأدوار الـ12 (الموقع التنظيمي من `portalRoles` الجديدة في المصدر + مقابل أودو + **عدد الشاشات محسوبًا بـ`allowedPathsFor`**) + فقرة فجوات التعيين (`portalRoleGaps`: دور doc_controller المقترح — قرار مالك) + **فهرس كل شاشات البوابة** (48 بندًا بمن يفتحها بالضبط عبر `canOpenPath`) — تحقيق حرفي لطلب «لو في البوابة شيء غير مذكور نذكره». ② **فصل الوظائف `#s-jobs` صار مولَّدًا**: 36 بطاقة من المصدر بدل 12 يدوية قديمة (منطقة `ORG:JOBS`). ③ فكّ تكرار `id="s-raci"` (الملحق → `s-raci-annex`) + روابط تنقّل وفهرس جديدة. ④ رابطا `pages[]` الميتان صُحّحا (warehouse-maps وcold-storage-plan → warehouses) + **قسم فحص جديد في `audit-portal.mjs`** يمنع تكرارهما. ⑤ `workflows.astro` يقرأ عدد الوظائف من المصدر (كان «18» يدويًّا والحقيقة 36). التحقق: معاينة حية بالمتصفح (12 صفًا · 36 بطاقة · صفر تكرار) + توليد خامل + بوابة خضراء (49 · 571 · تدقيق نظيف بالقسم الجديد · لينت 124=124). **التالي: المحور ٢-ب** (مصفوفة `access_control/matrix` + إعادة بناء AccessControlHub محررًا فعليًّا + تفعيل active:false) حسب [docs/خطة-العملية-الجراحية.md](docs/خطة-العملية-الجراحية.md).

- **🩺 العملية الجراحية — المحور ١ ✅ منفّذ (2026-08-01 ليلًا · يُدفع لـmain):** «قسم التوثيق والاعتماد» صار **«قسم الحوكمة المستندية»** في كل المواضع: المصدر `org-structure.json` (العقدة sec-doc بـ`formerTitle` + الوظائف J01/J15/J20/J21 بأثر تاريخي + الكادر + RACI + odooRoles + الإنجليزية Document Governance) · مرساة `build-org.mjs:194` · المواضع اليدوية في المرجع التشغيلي (8 مواضع، وُحّدت «إدارة»→«قسم» حسب نص المالك) وتقرير الهيكل وworkflows.astro · ROADMAP بتوضيح تاريخي. **+ سدّ الثغرة الجذرية:** `build-org.mjs` صار يتولّد آليًّا (`npm run org` + ضُمّ لـ`prebuild` + **خطوة CI جديدة** في astro.yml تشغّل المولّدَين قبل البناء — كان CI يتجاوز prebuild كليًّا!) **+ توسعة المولّد:** مصفوفتا `raciMatrix` و`odooRoles` في تقرير الهيكل كانتا نسختين يدويتين تنجرفان → صارتا مولّدتين من المصدر (`replaceJsLiteral`). التوليد خامل (تشغيلتان متطابقتان). grep نهائي: صفر بقايا عدا `formerTitle` والسياق التاريخي. بوابة خضراء (49 صفحة · 571 اختبارًا · تدقيق نظيف · لينت 124=124). **التالي: المحور ٢-أ** (مناطق توليد ORG:JOBS/PORTAL-ROLES/PORTAL-PAGES في المرجع + إصلاح الرابطين الميتين وs-raci المكرر) حسب [docs/خطة-العملية-الجراحية.md](docs/خطة-العملية-الجراحية.md).

- **🤝 الاجتماعات الجماعية + ترقية مساعد الاجتماعات (2026-07-31 · ✅ مدفوع لـmain `e4ae4df` · لا تغيير في `firestore.rules`):** طلب المالك خيار «اجتماع جماعي» (إداراتنا معًا) في صفحة الاجتماعات + خطة ChatGPT لترقية المساعد على Firebase/Spark. **ثلاثة تصحيحات تقنية عُرضت على المالك وأقرّها:** ① Cloud Storage **يتطلّب Blaze** للمشاريع الجديدة (منذ أواخر 2024) حتى ضمن الحدّ المجاني → **قرار المالك: تنزيل محلي فقط، لا صوت سحابيًّا إطلاقًا**؛ ② Web Speech API **لا تفرّغ ملفًّا مسجّلًا** (ميكروفون حيّ فقط) → التسجيل والتفريغ **بالتوازي** على نفس الميكروفون؛ ③ الحماية موجودة أصلًا (`/dashboard/` محميّة) فلا دخول جديد. **قرارا المالك الآخران:** منشئ اجتماعات **متعدّدة** (لا قالب واحد) + التسجيل **مدمج داخل الغرفة**. **ما نُفّذ:** ① نموذج خالص `groupMeetingsModel.js` (+17 اختبارًا): بناء بلا بذرة · إدارات مشاركة (`ORG_DEPARTMENTS` التسع) · `groupMinutesVerdict` (موقّعان بلا شرط us/them) · يعيد استخدام محرّك `meetingsModel` (صُدّر `SETTLED`). ② خطّاف صوتي مشترك `useAudioRecorder.js` (MediaRecorder · timeslice 15s ضد تراكم الذاكرة · webm/opus وSafari→mp4 · تنزيل محلي) + `useMeetingRecorder.js` يجمعه مع التفريغ الحيّ. ③ خدمة `groupMeetingsService.js`: **تخزين في `preparatory_meetings` بوسم `kind:'group'` — صفر قواعد جديدة** (القاعدة المنشورة تجيز للمدير الإنشاء بأي معرّف وتحرس الرقم)؛ يشارك عدّاد MOM؛ أرشفة لا حذف؛ التفريغ النصّي يُحفظ في الوثيقة. ④ `groupMinutesHtml` في `meetingsView.js` (+4 اختبارات: إدارات مشاركة · تواقيع معتمدة PNG · تهريب حقن). ⑤ جزيرة `GroupMeetings.jsx` في `meetings.astro` (`client:only`): قائمة/إنشاء/غرفة (بيانات+بنود+مسجّل+حاضرون+إصدار/اعتماد/طباعة/أرشفة)، ترث أنماط الصفحة (`kcard`/`dlg`/`chips`) + أنماط `rec-*` جديدة؛ القراءة لكل مصادَق والتحرير للمديرَين. ⑥ المساعد المنفصل: تسجيل صوتي فعلي + زرّ «تنزيل التسجيل الصوتي» عبر الخطّاف المشترك (منطق التفريغ القائم لم يُمسّ). **✅ تحقّق:** بوابة خضراء (49 صفحة · **571/571 اختبارًا (+21)** · تدقيق نظيف «كل مجموعة لها قاعدة» · لينت 124=124 صفر جديد) + معاينة حيّة: الصفحة تُحمّل بصفر أخطاء console (AuthGate يحوّل للدخول كالمعتاد) + فحص الأثر المبنيّ: chunk `GroupMeetings` مربوط بالصفحة والأنماط السبعة حاضرة و`useAudioRecorder` حزمة مشتركة يستوردها المكوّنان. **⚠️ لم يُجرَّب حيًّا بميكروفون وحساب مدير** (يحتاج جلسة المالك). **المتبقّي المحتمل:** نقل أرشيف المساعد المنفصل من localStorage إلى Firestore (يحتاج قاعدة `meeting_notes` جديدة ينشرها المالك — أُجّل عمدًا لتفادي عبء النشر)؛ ورفع الصوت سحابيًّا إن فُعّلت Blaze يومًا. راجع [[project-preparatory-meetings]] · [[project-firebase-plan-spark]].

> **🔖 رمز الجلسة «1236»** (اتّفاق المالك 2026-07-31): نقطة استئنافٍ مُسمّاة. عند فتح جلسةٍ جديدة وذكر «1236» → **تابِع المرحلة ٤ من هنا:** المتبقّي **شاشتان فقط — `tasks` (TaskManager+TaskForm+TaskList+StatsBar) و`recruitment` (RecruitmentBoard+CandidatePrint)** — متعدّدتا الملفّات، فاستعمل **نمط منسّق-التوابع** (المنسّق وحده يلفّ `.o_theme`، توابعه المستورَدة منه حصرًا تُرسَم بداخله بلا لفّ — كما في ماستر الأصناف). ثمّ **المرحلة ٥** (قلب الافتراضي: `light` يصبح الأصل وحذف الثيم الداكن = **نهاية رحلة اعتماد أودو**). **الأسلوب المثبت (ultracode):** workflow توزيع متوازٍ (وكيل لكلّ ملفّ · مرجع `PartnerMaster` · قاعدة «المنطق مقدّس، العرض فقط») ثمّ workflow مراجعة عدائيّة ثانٍ يقارن كلّ ملفّ بـ`git show HEAD:` (صفر انحدار). **١٣ شاشة مُرقّاة لأودو حتى الآن؛ CRUD لم يُجرَّب حيًّا (يحتاج دخول مدير — على المالك تجربته).**

- **🔐 المرحلة ٤ — دفعة ٥ شاشات مستقلّة لأودو (2026-07-31 · محلّيّ، لم يُرفع بعد · لا تغيير في `firestore.rules`):** توزيع متوازٍ ثانٍ — ٥ شاشات ملفٌّ واحدٌ لكلٍّ: **المستخدمون** (`UsersAdmin`) · **ضبط الطلبات** (`OrderControlBoard`) · **المستندات** (`DocumentsInbox`) · **الصلاحيات** (`AccessControlHub`) · **طلبات التوظيف** (`HiringRequestsBoard`). ٥ وكلاء، كلٌّ مكوّنه **+ استيراد صفحته + إزالة إيموجي ترويستها**، بمرجع `PartnerMaster` وقاعدة «المنطق مقدّس». القوائم `ListView`، الحالات/الأدوار `Badge`/`o_badge` (مُعرَّف)، الإيموجي → `Icon`، الأرقام لاتينيّة. **✅ تحقّق مضاعف:** workflow مراجعة عدائيّة ثانٍ (٥ مقابل `git HEAD` + تحقّق لكلّ نتيجة) → **صفر انحدار سلوكيّ**؛ + بناء + لينت (صفر على الخمسة) + إزالة ✅ من لافتة `users.astro`. **بوابة خضراء: 49 صفحة · 550 اختبارًا · تدقيق نظيف · لينت 124=124 صفر جديد.** **⚠️ لم تُجرَّب CRUD حيًّا** (المنطق مُراجَعٌ عدائيًّا). **المتبقّي في المرحلة ٤: `tasks` + `recruitment` (متعدّدتا الملفّات — تحتاجان نمط منسّق-التوابع كالأصناف)**، ثمّ المرحلة ٥ (قلب الافتراضي). راجع [[project-odoo-design-phase-0-shipped]].

- **🏭 المرحلة ٤ — دفعة ٤ شاشات تشغيليّة لأودو دفعةً واحدة (2026-07-31 · محلّيّ، لم يُرفع بعد · لا تغيير في `firestore.rules`):** أكبر دفعة — ٤ شاشات في توزيعٍ متوازٍ واحد: **المستودعات** (`WarehouseManager`) · **دفتر الحركات** (`StockLedger`) · **التحويلات** (`TransferBoard`) · **مراقبة العمليّات** (`OperationsMonitor`). كلّ شاشة ملفٌّ مستقلّ (كـ`PartnerMaster`) فتوزّعت بأمان: ٤ وكلاء، كلٌّ يُعيد كساء مكوّنه **+ يستورد أودو في صفحته**، بمرجع `PartnerMaster` وقاعدة «المنطق مقدّس». صارت القوائم `ListView`، الحالات `Badge`، التبويبات `o_notebook` (التحويلات)، الملخّصات `o_kpi`، الإيموجي → `Icon`، الأرقام لاتينيّة. **✅ تحقّق مضاعف (ultracode):** workflow مراجعة عدائيّة ثانٍ (٤ ملفّات مقابل `git HEAD` + تحقّق عدائيّ لكلّ نتيجة) → **صفر انحدار سلوكيّ**؛ + بناء + لينت (صفر على الأربعة) + صفر إيموجي. **تلميعات يدويّة بعد المراجعة:** `fmtTime` في OperationsMonitor أُلتِن (R2)، وأُزيل إيموجي ترويستَي دفتر الحركات (📇) والتحويلات (🚚). **بوابة خضراء: 49 صفحة · 550 اختبارًا · تدقيق نظيف · لينت 124=124 صفر جديد.** **⚠️ لم تُجرَّب CRUD حيًّا** (المنطق مُراجَعٌ عدائيًّا). **المتبقّي في المرحلة ٤:** users/tasks/order-control/documents/access-control/recruitment/hiring-requests، أو المرحلة ٥ (قلب الافتراضي). راجع [[project-odoo-design-phase-0-shipped]].

- **📦 المرحلة ٤ — ماستر الأصناف (٥ مكوّنات) رُقِّي لأودو عبر توزيع متوازٍ (2026-07-31 · محلّيّ، لم يُرفع بعد · لا تغيير في `firestore.rules`):** الشاشة المركزيّة الثالثة في المرحلة ٤ (`/dashboard/products`). **الأسلوب (ultracode):** أُطلق **workflow توزيع متوازٍ** — ٥ وكلاء، كلٌّ يُعيد كساء ملفٍّ واحد (`ItemMaster` المنسّق · `ItemForm` · `ItemsImport` · `BalancesImport` · `PendingItems`) بمرجع `PartnerMaster` الذهبيّ وبقاعدة صارمة: **المنطق مقدّس (اشتراكات · CRUD · استيراد · منطق الرصيد الحيّ/FEFO/الصلاحية) لم يُمسّ، يُغيَّر العرض فقط**. صارت القوائم `ListView` أودو، والنماذج/المستوردات بطاقات `o_ds_card` بمدخلات `o_input`، والملخّصات `o_kpi`، والحالات `Badge`، والإيموجي (📥📊⏳✅🚫) → `Icon` SVG، والأرقام لاتينيّة عبر `format`. **`ItemMaster` وحده يُلَفّ بـ`.o_theme`** (التوابع تُرسَم بداخله لأنها تُستورَد منه فقط)، و`products.astro` استورد `tokens.css`+`odoo.css`. **✅ تحقّق مضاعف (ultracode):** بعد التوزيع أُطلق **workflow مراجعة عدائيّة ثانٍ** قارن كلّ ملفٍّ بأصله في `git HEAD` (٥ مراجعين + تحقّق عدائيّ لكلّ نتيجة) → **صفر انحدار سلوكيّ**؛ + بناء + لينت (صفر على الأصناف) + فحص بنيويّ (منطق الرصيد محفوظ) + صفر إيموجي/FA في المصدر. **بوابة خضراء: 49 صفحة · 550 اختبارًا · تدقيق نظيف · لينت 124=124 صفر جديد.** **⚠️ لم تُجرَّب CRUD حيًّا** (دخول مدير — المنطق محفوظ ومراجَع عدائيًّا مرّتين). **المتبقّي في المرحلة ٤:** شاشات أخرى (transfers/warehouses…) أو المرحلة ٥ (قلب الافتراضي). راجع [[project-item-master]] · [[project-odoo-design-phase-0-shipped]].

- **🎨 المرحلة ٤ — شاشات البيانات تُرقّى لأودو + إصلاح أيقونات اللوحة (2026-07-31 · محلّيّ، لم يُرفع بعد · لا تغيير في `firestore.rules`):** المسار الثاني من المرحلة ٤ (بعد لوحة اللوجستيات أدناه): **(أ) `PartnerMaster` (شاشة الموردين والعملاء التوأم) أُعيد كساؤها بمظهر أودو** — القائمة صارت `ListView` أودو (صفوف كثيفة · `decoration-muted` للمؤرشفين · أزرار إجراء في خليّة)، والنموذج والاستيراد بطاقات `.o_ds_card` بمدخلات `.o_input` (**كانت داكنة غير مقروءة** `.fld` — أُصلحت) وتنبيهات `.o_alert`، وشريط أدوات `.o_ds_toolbar` (بحث `.o_searchview` + مبدّل المؤرشفين)، كلّه داخل `<div className="o_theme">`. **⚠️ المبدأ الحاكم: المنطق لم يُمسّ إطلاقًا** (الاشتراكات · `createPartner`/`updatePartner`/`archivePartner`/`unarchivePartner` · `analyzePartnersFile`/`commitPartnersImport`) — غُيّر ما يُرسَم فقط، فصفر خطرٍ على بيانات CRUD. أُضيفت أنماط الشاشات القابلة للتحرير لـ`odoo.css` (`.o_input`·`.o_ds_card`·`.o_ds_toolbar`·`.o_alert`·`.btn-sm`·`.o_filedrop`·`.o_form_grid`) واستُورد `tokens.css`+`odoo.css` في `suppliers.astro`+`customers.astro`. الأرقام لاتينية (R2) عبر `format`. **(ب) إصلاح أيقونات لوحة اللوجستيات:** كانت `fa-solid` لكنّ **FontAwesome غير مُحمَّل على صفحات `DashboardLayout`** (فقط على stock-operations عبر CDN خاصّ) → استُبدلت كلّها بمكوّن `Icon` SVG (`icon-data.js`) + إصلاح قواعد لون الأيقونات في `odoo.css` (`svg` بدل `i` الميّت). **✅ تحقّق حيّ** (صفحة اختبار + getComputedStyle): مدخلات بيضاء بنصّ `#212529` مقروء · أزرار `#714B67` · تنبيهات `#e5f4e8`/`#008818` · صفر `fa-solid` في اللوحة المبنيّة. **بوابة خضراء: 49 صفحة · 550 اختبارًا · تدقيق نظيف · لينت 124=124 صفر جديد.** **⚠️ لم تُجرَّب CRUD حيًّا** (تحتاج دخول مدير + بيانات — المنطق محفوظ حرفيًّا والأنماط مُتحقَّقة). **درس دائم:** أيقونات صفحات `DashboardLayout` = مكوّن `Icon` SVG لا FontAwesome. **المتبقّي في المرحلة ٤:** مزيد شاشات (الأصناف…) أو الانتقال للمرحلة ٥ (قلب الافتراضي). راجع [[project-odoo-design-phase-0-shipped]].

- **📊 المرحلة ٤ انطلقت — لوحة اللوجستيات التنفيذيّة بمكوّنات أودو الحقيقيّة (2026-07-31 · محلّيّ، لم يُرفع بعد · لا تغيير في `firestore.rules`):** بعد اكتمال ترحيل البوابة للفاتح، أوّل عمل في المرحلة ٤: صفحة `/dashboard/logistics-dashboard` (محصورة بالمديرَين) — **أوّل شاشة تستعمل مكوّنات أودو الحقيقيّة** (`ListView`/`Badge` داخل `.o_theme`) على **بيانات حيّة** لا عيّنة. **البنية (نمط المشروع: منطق خالص + اختبارات + جزيرة):** ① **نموذج خالص** `src/services/dashboard/logisticsModel.js` (+`.test.js`، ٨ اختبارات) — `logisticsSummary(data, nowMs)` يجمّع فوق `inventoryAnalytics` المُختبَر: مؤشّرات (قيمة المخزون·عدد الأصناف·تحت حدّ الطلب·قيمة الراكد·العمليّات المفتوحة·الرحلات·الصيانة·الموردون/العملاء) + قوائم «تدخّل الآن» (إعادة طلب/راكد مقصوصة top) + نشاط أخير (عمليّات/حركات) + تقييم بالفئات؛ حتميّ (`nowMs` مُمرَّرة) وحصينٌ للفراغ. ② **مكوّن** `src/components/brandzo-erp/dashboard/LogisticsDashboard.jsx` — يشترك حيًّا في ٨ مصادر (أصناف·حركات·عمليّات·موردون·عملاء·رحلات·صيانة·عُهد) بتنظيفٍ كامل، ويعرض بطاقات KPI + `ListView` أودو للتنبيهات/النشاط + أشرطة فئات + روابط سريعة؛ محصورٌ بالدور داخليًّا (`MANAGER_ROLES`) طبقةَ دفاعٍ ثانية. ③ **أنماط** أُضيفت لـ`src/styles/odoo.css` تحت `.o_theme` (بطاقات KPI بأسلوب TailAdmin بتوكِنز أودو `--o-*`؛ **الأحمر للتنبيه فقط**؛ صفر لون هوية حرفيّ). ④ صفحة `logistics-dashboard.astro` (`DashboardLayout light` + `client:load`) + بند `navCatalog` بأيقونة `gauge` محصورٌ بـ`['admin','warehouse_manager']` (الحارس يُشتقّ تلقائيًّا). **✅ تحقّق حيّ قاطع** (البانل لا يُصوَّر + الصفحة محميّة — استُعيض بصفحة اختبار تربط CSS المصرَّف + `getComputedStyle`): خلفية اللوحة `#f9f9f9`·بطاقة `#fff`·أيقونة التمييز `#714B67`·**التنبيه `#d23f3a`**·التوكِنز محلولة — مظهر أودو الفاتح تمامًا. **بوابة خضراء: بناء 49 صفحة · 550 اختبارًا (+8) · تدقيق نظيف (البند مربوط ومحصور، روابط 43) · لينت 124=124 صفر جديد.** **⚠️ لم تُعايَن اللوحة كاملةً حيًّا** (تحتاج دخول مدير + بيانات؛ المنطق مُختبَر وحدويًّا والأنماط مُتحقَّقة). **المتبقّي في المرحلة ٤:** المسار الثاني — **ترقية شاشة بيانات حيّة** (الموردون/العملاء أو الأصناف) لـ`ListView`/`FormSheet`؛ ثمّ المرحلة ٥ (قلب الافتراضي). راجع [[project-odoo-design-adoption]] · [[project-odoo-design-phase-0-shipped]].

- **🎨 نظام أودو — «الجرد الذكي» صار فاتحًا · اكتمل ترحيل البوابة التفاعليّة (2026-07-31 · ✅ مدفوع لـmain `09156d0`+`b9a39fe` · لا تغيير في `firestore.rules`):** «الجرد الذكي» (`stock-operations.astro`) كان **آخر صفحة داكنة** — صفحة تشغيليّة حرجة (سجلّ مسح حيّ) **مستقلّة تمامًا**: لها `<html>`/`<head>`/`<body>` خاصّة و**لا تستعمل `DashboardLayout`**، فلا ينطبق عليها مفتاح `light`؛ تدير ثيمها عبر **`:root` محلّيّ**. **⚠️ التقدير السابق («3653 سطرًا · 63 لونًا · جلسة يدويّة مخصوصة») كان مبالغًا:** التصنيف كشف أنّ **معظم الألوان تتدفّق من متغيّرات `:root`** (قلبةٌ واحدة تعالجها)، والباقي إمّا **طباعة** (تبقى أسود على أبيض) أو **كاميرا** (تبقى سوداء) أو **دلالات سليمة**. **ما نُفّذ (٣ دفعات):** ① **قلب `:root` الستّة** لقيم `bz-light` المطابقة للبوابة (`--bg:#f5f5f7`·`--surface:#fff`·`--surface-2:#f6f6f8`·`--text:#1f2430`·`--text-muted:#5f676e`·`--border:#e5e5ea`) مع **إبقاء هوية البراند** (`--primary` أحمر `#c41e3a`·`--accent` ذهبي `#DAAA3C`) والدلالات (danger/success). ② **الأماكن المرمّزة الصريحة (يدويًّا):** `.import-bg` (تدرّج داكن→فاتح)·`.glass-card` (زجاج داكن→سطح أبيض بظلّ أودو الناعم)·التنبيهات الأربعة (خلفيّات مشبعة داكنة→مصبوغة فاتحة بنصّ داكن)·`.diff-*` (غُمّقت للتباين على الأبيض: `#b45309`/`#dc2626`/`#15803d`)·حدود الجداول ورؤوسها·ظلال ثقيلة→ناعمة·تبويبات. ③ **الأنماط السطريّة الداكنة:** طبقة حارس الدخول·زرّ الرجوع·شارة حالة السحابة (+كائن ألوانها في JS)·الشريط العائم·ظلّ بطاقات جدول الجوال. **✅ تحقّق قاطع من الأثر المشحون** (البانل غير معروض فتعذّرت اللقطة + AuthGate يحوّل بلا دخول — كسابقاتها استُعيض بفحص CSS المُجمّع الفعليّ): `dist/_astro/stock-operations.CZ3VgdnQ.css` → `:root` الفاتحة مبنيّة حرفيًّا · `.glass-card` أبيض بظلّ أودو · التنبيهات فاتحة · الفروقات مغمّقة · **صفر قيمة داكنة قديمة** (0 سطحيّة · 0 rgba) · الطباعة (4×`#000`)/الكاميرا (`#000`) سليمتان. **بوابة خضراء: بناء 48 صفحة · 542 اختبارًا · تدقيق نظيف · لينت 124=124 صفر جديد.** **⚠️ لم يُعايَن حيًّا بصريًّا** (قيد البانل). **⚠️ ملاحظة نطاق:** الصفحة ما زالت تحمّل FontAwesome + Cairo من CDN (السطران 28-29) — خارج نطاق الثيم؛ الخطّ مُوحَّد أصلًا عبر `!important` في global.css، لكن يُنصح لاحقًا باستضافة FA ذاتيًّا لقاعدة العمل دون اتصال. **المتبقّي للفاتح = صفر:** بقيت فقط `index.astro` (landing تسويقيّ بـhero كحليّ **متعمَّد**، خارج البوابة التفاعليّة). **التالي: المرحلة ٤** (شاشات بيانات بمكوّنات أودو ListView/FormSheet + لوحة لوجستيات TailAdmin) ← المرحلة ٥ (قلب الافتراضي وحذف الداكن). راجع [[project-odoo-design-adoption]] · [[project-stock-operations-live]].

- **🎨 نظام أودو — تلميع بصريّ + توحيد الدخول (2026-07-30 · ✅ مدفوع لـmain `2179908` · لا تغيير في `firestore.rules`):** بعد أن صارت البوابة فاتحة (المراحل ٠→٣)، أبلغ المالك بالصور عن تشوّهات: البطاقات «قصاصات ملصوقة»، الخلفية «ظلّ جامد»، الشريط الجانبي «فيه اعوجاج»، وخانة البحث بإطار كحليّ. **الأسباب الجذريّة والعلاج (كلّها في طبقة `bz-light` المركزيّة — `global.css` + كتلة `is:global` بالتخطيط):** ① **الظلّ الجامد:** أداة Tailwind `.shadow-card` تُجمَّد قيمتها وقت البناء من `@theme` بقيمة الثيم الداكن (`0 8px 24px` أسود 35%) فلا يصلها قلب التوكِن في runtime → أُعيد تعريف متغيّر Tailwind الداخليّ `--tw-shadow` تحت `html.bz-light` بظلّ أودو ناعم (طبقتان رماديّتان دافئتان) + شبكة أمان لـ`shadow-lg/xl/2xl`. ② خلفية الصفحة `#f1f1f4` الميّتة → `#f5f5f7` دافئة، والحدّ الصريح → شبه معدوم (6%)، والتقويس 0.75rem. ③ **«الاعوجاج»:** العنصر النشط في الشريط كان `border-right: 3px` مستقيمًا يقطع الزاوية المستديرة فيُحدث سنًّا مسنّنًا → استُبدل بمؤشّر `box-shadow: inset -3px 0 0 #714B67` يحترم التقويس تمامًا + روابط بتقويس/انتقال ناعمين. ④ **خانة البحث:** غلافها اللاصق يحمل `bg-brand-navy` على العنصر نفسه فقاعدة `.sb-x .sticky` (سليل) لم تصبه → قاعدة تبيّض أيّ `bg-brand-navy` داخل الشريط. ⑤ **توحيد الدخول:** كان داكنًا (تنافر «دخول داكن ← لوحة فاتحة») → `bz-light` على `<html>`، بطاقة `LoginForm` من `bg-brand-navy/70` زجاجيّ إلى `bg-surface` أبيض بظلّ أودو، وحذف رابط Cairo الخارجيّ (قاعدة العمل دون اتصال). **✅ تحقّق حيّ بالقيم المحسوبة** (تعذّرت اللقطة — بانل المتصفّح غير معروض؛ استُعيض بنسخة تحقّق تستعمل CSS المُجمّع الحقيقي + قراءة `getComputedStyle`): الظلّ صار `rgba(20,20,40,.04/.05)` لا الأسود الثقيل · مؤشّر النشط `inset -3px` بحدّ يمين صفر · الخلفية `#f5f5f7` · غلاف البحث `#fff` · بطاقة الدخول بيضاء بظلّ أودو. **بوابة خضراء: 48 صفحة · 542 اختبارًا · تدقيق نظيف · لينت 124=124 صفر جديد.** **⚠️ درس دائم:** أيّ أداة ظلّ Tailwind (`shadow-*`) لا تستجيب لقلب التوكِن لأنها تُجمَّد وقت البناء — يجب تجاوز `--tw-shadow` صراحةً تحت `bz-light`. **المتبقّي (نقطتان فقط لإكمال الفاتح):** ① **`stock-operations` (الجرد الذكي)** = آخر صفحة داكنة، **3653 سطرًا بها 63 لون hex داخل قوالب `innerHTML` و91 تلاعب DOM** — التحويل بـsed يكسرها، **تحتاج تمريرة يدويّة مخصوصة** (صفحة تشغيليّة حرجة بسجلّ مسح حيّ). ② **`index.astro`** (الرئيسية العامة الجذر) = landing تسويقيّ بـ`hero-gradient` كحليّ **متعمَّد** — ليس جزء البوابة التفاعليّة، يُترك داكنًا ما لم يطلب المالك خلافه. **ثم:** المرحلة ٤ (شاشات البيانات بمكوّنات أودو الحقيقية ListView/FormSheet + لوحة لوجستيات بمرجع TailAdmin) ← المرحلة ٥ (قلب الافتراضي: `light` يصبح الأصل وحذف الثيم الداكن). راجع [[project-odoo-design-phase-0-shipped]] و[[project-odoo-design-adoption]].

- **🎨 نظام أودو — المرحلة ١: الإطار الفاتح + الخط الرسمي + أول ٣ صفحات مرحَّلة (2026-07-30 · ✅ مدفوع لـmain `ade0fec` · لا تغيير في `firestore.rules`):** قرّر المالك (بعد اختبار خطوط مرجعيّ قدّمه): **الخط الرسمي = IBM Plex Sans Arabic** (لاتينيّته من نفس العائلة → المراجع `WH/IN/00042` والأرقام اللاتينية R2 تتجانس مع العربي) والبدء فورًا بالمرحلة ١، ومرجعاه البصريّان: شريط جانبي **Metronic layout-20** ولوحة لوجستيات **TailAdmin** (للمرحلة ٤). **ما نُفّذ:** **(1) الخط:** ٨ ملفات woff2 (عربي+لاتيني × 400/500/600/700، ~258KB) في `src/assets/fonts/` + `src/styles/fonts.css` (@font-face بـunicode-range) — **استضافة ذاتية** تحفظ شرط العمل دون اتصال، وتحميل مسبق لسماكتَي 400/700 في الرأس، و«دبي» احتياط؛ حُدّثت الأسلاك الثلاثة (قاعدة `*` في global.css · `--o-font-family` في tokens.css · body في التخطيط). **+ إصلاح علّة قديمة:** قاعدة الخط العامة `!important` كانت تقتل محارف FontAwesome (`*::before`) — استُثنيت أصناف `fa-*`. **(2) الثيم الفاتح `bz-light` (opt-in لكل صفحة):** خاصية `light` في `DashboardLayout` تضع `class="bz-light"` على `<html>` — **الصفحات غير المرحَّلة داكنة حرفيًّا كما كانت** (تشخيص مقاس: 144 بطاقة `bg-white` مقلوبة قسرًا بـ`!important` تعود بيضاء تلقائيًّا، لكن ~563 نصًّا فاتحًا في 33 صفحة يمنع القلب الشامل دفعة واحدة). في global.css: قلب قيم التوكِنز الدلالية تحت `html.bz-light` (surface أبيض · line رمادي أودو · accent صار `#714B67` لأن الذهبي لا يقرأ على الأبيض) + **3 توكِنز جديدة** `--color-ink`/`--color-ink-2`/`--color-chip` (حبر العناوين/الثانوي/شريحة خفيفة، تنقلب مع الثيم) + إعادة رسم أصناف الثيم القديم (glass-card/bz-kpi-card/bz-dashboard-table/bz-text-*) سطحًا ورقيًّا. **(3) الإطار الفاتح** (كتلة `is:global` في التخطيط، بأسلوب Metronic-20): شريط جانبي أبيض · العنصر النشط صبغة `rgba(113,75,103,.1)` بحدّ يمين `#714B67` (يغلب الذهبي المضمَّن) · رؤوس مجموعات رمادية · بحث فاتح · شريط موبايل أبيض · حارس دخول ودرج دليل فاتحان · شارة الحالة بألوان أعمق للتباين. **(4) ٣ صفحات مرحَّلة:** الرئيسية `light` مع استبدال الألوان المرمّزة بالتوكِنز (`text-white→text-ink` · `bg-white/5→bg-chip` …) ونسخ فاتحة لشارات التخصيص، + شاشة الاستلام + دليل الأنماط (إطارهما صار متجانسًا مع محتواهما `.o_theme` الفاتح). **✅ تحقّق:** بناء 56 صفحة · **542/542 اختبارًا** · تدقيق نظيف · **لينت 125 قبل التعديلات وبعدها سواء = صفر جديد** · فحص dist: `bz-light` على الصفحات الثلاث فقط + التحميل المسبق بمسارات مجزّأة + الأدوات `text-ink`/`bg-chip` مولَّدة + قاعدة الشريط الفاتح مجمّعة · الخط ثبت تحميله حيًّا في المعاينة (`document.fonts`). **⚠️ لم تُعايَن اللوحة بصريًّا** (AuthGate يحوّل لـ/login بلا دخول). **التالي — المرحلة ٢/٣:** كنس الصفحات دفعاتٍ (تحويل النصوص الفاتحة المرمّزة إلى توكِنز ink ثم تفعيل `light`)، ثم المرحلة ٤ (شاشات البيانات بمكوّنات أودو + لوحة بأسلوب TailAdmin logistics) فالمرحلة ٥ (قلب الافتراضي وحذف الداكن). راجع [[project-odoo-design-adoption]].

- **🎨 اعتماد نظام تصميم أودو — المرحلة ٠ (تأسيس + نموذج، معزول · اللون معتمَد · اجتاز تدقيقًا عدائيًّا) (2026-07-30 · ✅ **مدفوع لـmain `dff6751`** · لا تغيير في `firestore.rules`):** قرّر المالك اعتماد **مظهر أودو على البوابة كاملةً** (راحة الاستخدام الطويل) مع بقاء اسم/هوية براندزو، بتدرّج آمن **٦ مراحل** (تغيير قِيَم التوكِنز لا آلاف الأصناف؛ حاجزا audit+test قبل كل دفع). المرجع: `طلب-اعتماد-تصميم-اودو.md` + `odoo-design-system-ar.html`. **ما نُفّذ (المرحلة ٠ — إضافة صرفة، صفر حذف):** **(1)** `src/styles/tokens.css` (D1) كل رموز أودو `--o-*` من القسم ٦ (المصدر الوحيد للألوان R3). **(2)** `src/styles/odoo.css` أنماط المكوّنات بأصناف أودو نفسها **كلّها تحت `.o_theme`** (عزل تامّ عن الثيم الداكن)، **صفر `#` وصفر left/right** (R6). **(3)** ٨ مكوّنات React في `src/components/odoo/` (D2): `ControlPanel·ListView·FormSheet·StatusBar·StatBox·Notebook·KanbanCard·Badge` + `format.js` (أرقام لاتينية R2 عبر `Intl('ar-LY-u-nu-latn')`) + `BrandSwitch` (أداة قرار مؤقّتة). **(4)** `src/pages/dashboard/styleguide.astro` (D3) و`receipts.astro` (D4 — أوامر الاستلام قائمة+نموذج ببيانات عيّنة)، في `ALWAYS_ALLOWED` بـ`pageAccess.js` (بلا تغيير الشريط الجانبي). **✅ تحقّق:** **٥٤٢/٥٤٢ اختبارًا · تدقيق ٦/٠ (٥٤ صفحة) · بناء ناجح · لينت صفر جديد · `odoo.css` صفر `#` · الأرقام المعروضة لاتينية 100%** (المتبقّيان تعليقا سكربت القِشرة الداكنة). **معاينة حيّة (بديلة عن اللقطة المحجوبة بالدخول + اللوحة غير المعروضة): artifact `60be27f9`** يعرض الشاشة بالألوان الثلاثة قابلةً للتبديل الحيّ. **✅ اللون معتمَد: `--o-brand-primary = #714B67`** (أودو المؤسسي، قرار المالك) وحُذف `BrandSwitch` بالكامل (فصار المشروع خاليًا من أي `#` خارج tokens.css). **✅ تدقيق عدائيّ (workflow · ١١ وكيلًا · 699k توكِن)** فحص R1–R10 + §8 ضد الكود والمخرَج والمرجع: D1/R2/R3/R6/R5+R7/R1 مطابقة تمامًا؛ أُصلحت **٦ must-fix** — (R9) نصّ «أودو» مرئيّ في عناوين/تبويبات الصفحتين + «كثافة أودو» → محايدة (بقيت الثمانية في الشريط الجانبي الموروث، خارج النطاق)؛ (R10) لونان دون 4.5:1 → `--o-main-color-muted=#5f676e` (5.75/5.46) والأسهم القادمة `gray-700` (6.90)؛ (§8) `FormSheet` أُدرج في دليل الأنماط — + ثانويات الوصول (aria دفتر التبويبات ومبدّل العرض، لوحة مفاتيح كانبان/صفوف القائمة) والتجاوب (overflow-x للجداول والمراحل عند 360px). **إعادة تحقّق نهائيّ: بناء · تدقيق 6/0 · 542 اختبارًا · لينت 0 · صفر `#` خارج tokens · «أودو» في محتوى صفحتيَّ=0 · تباين ≥4.5 مُحقَّق حسابيًّا · أرقام لاتينية.** **معاينة حيّة معتمَدة: artifact `60be27f9`.** **ملاحظة R2:** `ar-LY-u-nu-latn` يعطي فاصل آلاف نقطة (`48.250`) — مطابق للبند حرفيًّا. المتبقّي بعد «ارفع»: **المرحلة ١** (قلب `DashboardLayout` للفاتح). راجع [[project-odoo-design-adoption]].

- **📦 الأفق الثاني §17 — تحليل المخزون: التقييم + الراكد + إعادة الطلب (2026-07-30 · يُرفع الآن بأمر «ارفع» · لا تغيير في `firestore.rules`):** أوّل بنود حزمة التقارير §17 غير المبنيّة (كان `salesReports` يُنتج «المعلّق/العاجز» فقط). **ما نُفّذ:** **(1)** خدمة خالصة `src/services/inventory/inventoryAnalytics.js` (+`.test.js`، ٨ اختبارات): `valuationReport` (Σ رصيد×تكلفة بالفئات + إحصاء غير المسعَّر) · `reorderReport` (رصيد ≤ `minStock`، نقصٌ وقيمة تجديد، وإحصاء «بلا حدّ» = فجوة تغطية) · `stagnantReport` (رصيدٌ+قيمةٌ بلا خروجٍ للخارج `to` فارغ منذ نافذة؛ «لم يُصرف قطّ» = daysIdle null) · `canonKey` يطابق الماستر بالدفتر كـ`balanceId`. `nowMs` مُمرَّر (حتميّ). **(2)** مكوّن حيّ `src/components/brandzo-erp/inventory/InventoryAnalytics.jsx` يشترك `subscribeItems` + `listenRecentMoves(2500)` ويعرض الثلاثة + بطاقات ملخّص + مبدّل نافذة ٩٠/١٨٠. **(3)** صفحة `src/pages/dashboard/inventory-analytics.astro` (جزيرة `client:load`) + تسجيل في `navCatalog` بالتقارير للمديرَين. **حوكمة الواجهة:** بلا إيموجي · الأحمر للتحذير فقط. **✅ تحقّق:** **٥٤٢/٥٤٢ اختبارًا (+٨)** · تدقيق نظيف (٥٢ صفحة) · بناء ٥٤ صفحة · لينت صفر جديد · بلا إيموجي · قشرة البناء بها ترويسة الصفحة والجزيرة. **حدودٌ موثّقة:** إعادة الطلب تحتاج `minStock` مضبوطًا (غيره يُحصى فجوةَ تغطية) · الراكد يقرأ آخر ٢٥٠٠ حركة · التقييم يستثني غير المسعَّر. **⚠️ لم يُعايَن حيًّا بصريًّا** (يحجب بلا دخول مدير + بانل المتصفّح غير معروض). **المتبقّي من §17:** مبيعات بالعميل/الصنف بالهامش · مخزون بالمورّد. راجع [[project-deloitte-readiness]].

- **🎓 استعداد ديلويت — اليوم ٥: حزمة الاستعداد للمقابلة + البروفة (2026-07-29 · يُرفع الآن بأمر «ارفع» · لا تغيير في `firestore.rules`):** آخر أيام الشريحة الحرجة — يجعل المدير جاهزًا لعرض الدليل المبنيّ (أيام ١→٤) بالأرقام. **ما نُفّذ:** **(1)** ملف مؤلَّف `src/data/interview-readiness.json` (مسار عرض ١٥ د · ١٠ أسئلة بأجوبة · ٥ اعتراضات · خارطة أدلّة · رؤية؛ كل رابط دليلٍ يشير لصفحةٍ موجودة). **(2)** خدمة `src/services/readiness/readinessModel.js` (+`.test.js`، ١١ اختبارًا) بحارس **`readinessVerdict`**: كل رابط دليلٍ يُتحقَّق ضدّ `internalPaths()` في navCatalog (لا يَعِد بشاشةٍ غير موجودة)، وكل اعتراضٍ يربط بُعد نضجٍ حقيقيًّا وأفقًا معتمدًا؛ والأرقام الحرجة تُشتقّ من الدراسة لا تُكتب. **(3)** صفحة `src/pages/dashboard/interview-readiness.astro` (٦ مكوّنات، خالصة Astro، طباعة + توقيعان). **(4)** مُسجَّلة في `navCatalog` بالتقارير للمديرَين. **حوكمة الواجهة:** بلا إيموجي · الأحمر للتحذير فقط · أيقونات خطّية. **إضافةً:** منهجٌ دراسيٌّ منشورٌ **artifact `f1db33a5`** (٦ وحدات + خطّة ٥ أيّام + بطاقة تقييم ذاتيّ، قابل للطباعة) + **بروفة حيّة** لعب فيها Claude دور المُقيّم؛ أبرز تصحيح: النضج **٢٫٩ من ٥** (لا ٢٫٥ من عشرة) وقاعدة «مفهومٌ ثمّ رقم» والحركات الأربع للاعتراضات. **✅ تحقّق:** **٥٣٤/٥٣٤ اختبارًا (+١١)** · تدقيق نظيف (٥١ صفحة) · بناء ٥٣ صفحة · لينت صفر جديد · فحص HTML: المكوّنات الستّة والأرقام المشتقّة وروابط الأدلّة كلّها حاضرة · بلا إيموجي. **⚠️ لم تُعايَن الصفحة حيًّا بصريًّا** (تحجب بلا دخول). **بهذا اكتملت الشريحة الحرجة (١→٥).** المتبقّي من الأفق الثاني: Pagination §14 · ماستر المستودعات §15.4 · بقيّة حزمة التقارير §17. راجع [[project-deloitte-readiness]] («***»).

- **🎯 استعداد ديلويت — اليوم ٤: الهيكل التشغيليّ المستهدف وخارطة التحوّل (2026-07-29 · ✅ **مدفوع لـmain `c24f3c3`** · لا تغيير في `firestore.rules`):** يُكمل مرحلتَي نموذج التشغيل المستهدف (TOM) الأخيرتين — **التصميم + خارطة الطريق** — فوق التشخيص (لوحة المؤشرات) والتقييم (الدراسة المقارنة) القائمين. **ما نُفّذ:** **(1)** ملف بيانات مؤلَّف `src/data/target-operating-model.json` (طبقة التصميم وحدها مؤلَّفة — الهيكل مزدوج الأفق + ٥ أبعاد مستهدفة + ٣ موجات بنوافذها؛ لا رقم يُلفَّق). **(2)** خدمة خالصة `src/services/tom/tomModel.js` (+`.test.js`، ١٦ اختبارًا) تشتقّ الموجات والتصميم بالربط من `comparative-study.json` المُتحقَّق، وفيها **حارس `tomVerdict`**: كل بُعد تصميم يربط نضجًا (من ١٠ أبعاد) وفجواتٍ (من ١٥) موجودةً فعلًا، وكل موجة تطابق أفقًا معتمدًا (`REC_HORIZONS`)، ولا توصية (من ١٤) بلا موجة. **(3)** صفحة `src/pages/dashboard/target-operating-model.astro` (خالصة Astro كـcomparative-study): ٣ طبقات — النضج مرتّبًا من الأضعف (إشارة الطلب 0.5 · الموردون 1.0 بالأحمر) · الهيكل مزدوج الأفق (Firebase الآن ← جسر المحوّل §16 ← Odoo وجهةً M2) + أبعاد التصميم + «الأساس المتين» · خارطة ٣ موجات (الآن–٩٠ يومًا / ٣–٦ أشهر / ٦–١٢ شهرًا) بالمبادرات ومؤشّرات نجاحها. طباعة + توقيعان. **(4)** مُسجَّلة في `navCatalog` بمجموعة التقارير للمديرَين. **حوكمة الواجهة:** بلا إيموجي · الأحمر للتحذير فقط · أيقونات خطّية · الربط قبل الإضافة. **✅ تحقّق:** **٥٢٣/٥٢٣ اختبارًا (+١٦)** · تدقيق نظيف (٥١ صفحة، الجديدة مربوطة ومحصورة بالمديرَين) · بناء ٥٢ صفحة · لينت **صفر مشكلة جديدة** على ملفّاتي · فحص HTML المولَّد: الطبقات الثلاث والأرقام المشتقّة (0.5/5 · 4.5/5) والنوافذ والمحرّكان (Firebase · Odoo 19) والتوقيعان حاضرة · بلا إيموجي زخرفيّ. **⚠️ لم يُعايَن حيًّا بصريًّا** (الصفحة تحجب بلا دخول Firebase). راجع [[project-deloitte-readiness]] («***»). **المتبقّي من الشريحة الحرجة:** اليوم ٥ (البروفة + حزمة الاستعداد الستّة).

- **🤝 ماستر شركاء الأعمال — الموردون والعملاء (§15.2/15.3) + معيار حوكمة الواجهة (2026-07-29 · ✅ **مدفوع لـmain `2b85ccc`** (الأساس + الواجهة + تمريرة الإيموجي) والحواجز خضراء · ⚠️ **يبقى: نشر المالك لـ`firestore.rules`** — الكتابة تفشل حتى يُنشر):** أعطى المالك نموذج «موردين v.xlsx» (٨ أعمدة: BP Code · BP Name · المفوّض · الهاتف · البريد · Account/Open Orders/Open Deliveries — **نموذج فارغ ٢٠٦ صفوف**) وطلب «العملاء بنفس الكيفية». فبُني **محرّك شركاء واحد (توأمان لا نسختان)** على إطار الأصناف نفسه: **(1)** مجموعتا `suppliers`/`customers` في `excelSchema.js` عبر `partnerColumns(kind)` — النموذج الأصليّ + حقول عالميّة مُثراة (الرقم الضريبي · التصنيف · شروط الدفع · العملة · حد الائتمان · العنوان · الحالة)، Odoo-Ready (`res.partner`). **(2)** `src/services/partners/partnerShape.js` (+test) تشكيلٌ خالص يشتقّ من المخطّط + **حارس توأمة** (نفس الحقول والأنواع للنوعين). **(3)** `src/services/partnerService.js` خدمة Firestore موحّدة بـ`kind`: اشتراك · إنشاء يرفض الرمز المكرّر · `upsertPartners` دفعيّ (`merge`) · تحديث · **أرشفة لا حذف** (`odooM2oId` جاهز). **(4)** `src/services/partners/partnersImportService.js` معاينة (جديد/محدَّث/بلا تغيير/بلا رمز) ثمّ كتابة بعد موافقة المدير. **(5)** قاعدتان `Suppliers_Master`/`Customers_Master`: قراءة مصادَق · كتابة مدير · **`delete: if false`**. الأرصدة الثلاثة **افتتاحيّة للعرض** (§19#3). **✅ تحقّق:** **٥٠٧/٥٠٧ اختبارًا (+٤)** · بناء ٤٩ صفحة · تدقيق نظيف (٣٤ قاعدة، المجموعتان مغطّاتان) · لينت صفر جديد. **✅ الواجهة أُنجزت (نفس الجلسة):** مكوّن `src/components/brandzo-erp/partners/PartnerMaster.jsx` **موحّد** (قائمة حيّة + بحث + إضافة/تعديل + استيراد Excel بمعاينة جديد/محدَّث/بلا رمز + أرشفة)، وصفحتان `/dashboard/suppliers` و`/dashboard/customers` (`kind`)، مُسجَّلتان في `navCatalog` بمجموعة العمليات اليومية للمديرَين — **بناء ٥١ صفحة، تدقيق ١٥ صفحة حسّاسة**. **المتبقّي:** **نشر المالك لـ`firestore.rules`** (الكتابة تفشل حيًّا حتى يُنشر) + قوالب محسّنة سُلّمت. **⚠️ لم يُجرَّب حيًّا** (يحتاج نشر القواعد + دخول مدير + بيانات). راجع [[project-deloitte-readiness]] («***»).

- **🏢 تحليل إنفاق الموردين وتركّز أكبر ٥ (استعداد ديلويت — اليوم ٣، الشريحة الأولى) (2026-07-29 · ✅ **مدفوع لـmain `02d5b27`** والنشر انطلق والحواجز خضراء · لا تغيير في `firestore.rules`):** حسم المالك قرارَي §19: **#٣ = رصيد افتتاحيّ للعرض** (بلا محرّك تسوية) · **#٤ = مؤجَّل** (يكفي إنفاق المشتريات؛ «مخزون بالمورّد» للأفق الثاني). فبُنيت **الشريحة الأولى من اليوم ٣ بلا كيان مورّد/استيراد/تغيير قواعد**: محرّك خالص جديد `src/services/kpi/supplierSpend.js` (+`.test.js`) يجمع **صافي أوامر الشراء المُصدَرة** (معتمَدة/منجَزة، عبر `netTotal` نفسها) بالمورّد (مفتاح الرمز ثمّ الاسم) → **عدد الموردين النشطين · إجمالي الإنفاق · تركّز أكبر ٥ (حصّتهم ÷ الإجمالي) · ترتيب الموردين بحصصهم**. أُضيف قسم «الموردون — الإنفاق والتركّز» إلى `ProcurementKpis.jsx` (بطاقتان + قائمة بأشرطة حصص، نافذة ٣٦٥ يومًا). **يحوّل مؤشر «تركّز الموردين» من أحمر إلى أخضر** بلا انتظار الماستر. **✅ تحقّق:** **٥٠٣/٥٠٣ اختبارًا (+٦)** · بناء ٤٩ صفحة · تدقيق نظيف · لينت صفر جديد. **⚠️ لم يُجرَّب حيًّا** (يحتاج أوامر شراء معتمَدة). **المتبقّي للماستر الكامل (§15.2):** ملفّ «موردين v.xlsx» من المالك + إضافة قاعدة `Suppliers_Master` في `firestore.rules` (ينشرها المالك) + صفحة إدارة/استيراد. راجع [[project-deloitte-readiness]] («***»).

- **📈 لوحة مؤشرات المشتريات + تصحيح الأرقام القديمة (استعداد ديلويت — اليومان ١+٢) (2026-07-29 · ✅ **مدفوع لـmain `52a8018`** والنشر انطلق والحواجز خضراء · لا تغيير في `firestore.rules`):** خطّة استعداد اختبار ديلويت (نموذج التشغيل المستهدف — [artifact `6d8ba212`]). أقرّ المالك القرار ٥ من §19: **Firebase مصدر الحقيقة حاليًّا**، فنُفّذ اليومان ١+٢ ليلًا (اليوم ٢ غير معطّل بأيّ قرار). **اليوم ٢ — لوحة المؤشرات (الكسب الأكبر):** محرّك خالص جديد `src/services/kpi/procurementKpis.js` (+`.test.js`، بلا Firestore، `nowMs` يُمرَّر) يحسب **٤ مؤشرات مشتريات من طوابع المستندات وروابطها** لا من انطباع: **مدة دورة أمر الشراء** (`PR.createdAt`→PO المُصدَر عبر `links.PR`) · **مهلة التوريد** (PO المُصدَر→GRN المُستلَم عبر `links.PO`) · **التزام الموردين OTD** (`requiredDelivery` مقابل الاستلام، مهلة يوم) · **نسبة نفاد المخزون** (أصنافٌ عاجزة÷مطلوبة من `soShortfall`) — كلٌّ **`null` بلا بيانات** ومقامُه ظاهر. صفحة `/dashboard/kpis` + `components/brandzo-erp/kpi/ProcurementKpis.jsx` (للمديرَين، مُسجَّلة في `navCatalog` بمجموعة التقارير، شريط صدق البتر: نافذة ٩٠ يومًا/حدّ ٨٠٠). **يكمّل لوحة القيادة لا يكرّرها، ويصحّح لبس OTIF: OTD يقيس الموردين لا الأسطول.** **اليوم ١ — تصحيح الأرقام (المصداقية):** الكود فيه **١٩ نموذجًا محكومًا و٦ سلاسل** لا «١٤ و٤». صُحّح في: تعليقَي `schemas/index.js` (١٢→١٤ صار **١٩**؛ «خارطة الـ12» صار «الـ19») · تعليق `chain.js` («الأربع»→«الستّ» + إضافة `SO` للصادر + تسمية «الجرد») · `comparative-study.json` (الملخّص التنفيذيّ · بطاقتا الإحصاء `14→19` و`4→6` · «أربع سلاسل (وارد·صادر·مرتجعات·جرد)»→«ست سلاسل (+فوترة+نقل)» · أدلّة ASN/الأمانة «الـ14»→«الـ19» تبقى صحيحة لأن `asn.js` لا يزال غائبًا ولا مستند أمانة). **✅ تحقّق:** **٤٩٧/٤٩٧ اختبارًا (+٧ لمؤشرات المشتريات)** · تدقيق نظيف (٤٧ صفحة، الجديدة مربوطة ومحصورة بالمديرَين) · بناء **٤٩ صفحة** · لينت: **صفر مشكلة جديدة** (ملفّاتي الثمانية خروج 0؛ فرق ١٢٢→١٢٥ سابقٌ في `BrandzoDashboard`/`StatsBar` غير المَلموسَين). **⚠️ لم يُجرَّب حيًّا** (اللوحة تحجب بلا دخول؛ الأرقام تحتاج بيانات PR/PO/GRN حقيقية لتظهر). **المتبقّي من الخطّة:** اليوم ٣ (ماستر الموردين §15.2 — معطّل جزئيًّا على قرارَي §19 رقم ٣ و٤) · اليوم ٤ (ورقة الرؤية) · اليوم ٥ (البروفة).

- **🔒 Arch Wiki صارت صفحة محميّة للمدير — لا كشف عامّ (2026-07-27 · ✅ **مدفوع لـmain** والحواجز خضراء · لا تغيير في `firestore.rules`):** استشار المالك في إظهارها بلوحة التحكم؛ كشف الفحص أنّ الصفحة كانت **ملفًّا ثابتًا في `public/arch-wiki/` يُخدَم بلا بوابة دخول** (أيّ شخص معه الرابط يرى بنية النظام: المجموعات·قواعد الصلاحيات·نماذج Odoo). القرار: صفحة محميّة حقيقيّة. **ما نُفّذ:** (1) `build-arch.mjs` صار يُخرج اللوحة إلى **`src/generated/arch-wiki.html`** (خارج `public/`، لا يُخدَم). (2) صفحة جديدة **`src/pages/dashboard/arch-wiki.astro`** تعرضها عبر **`<iframe srcdoc={archHtml}>`** (استيراد `?raw`) — عزل تامّ للأنماط عن `global.css` وحماية حقيقيّة. (3) بند **«خريطة النظام»** (`icon:'grid'`, `roles:['admin']`) في مجموعة `archive` بـ`navCatalog.js` — **هو ما يفرض الحماية** عبر `pageAccess` (المدير يفتح؛ غيره يُمنع بنيويًّا). (4) رابط «🗺️ خريطة النظام» في بطاقة `hub-access` («الصلاحيات والأدوار») بالصفحة الرئيسيّة. (5) **حُذف `public/arch-wiki/`** (الرابط العامّ القديم سيعطي 404 بعد النشر — مقصود). **✅ تحقّق:** بناء ٤٨ صفحة أخضر · `audit` نظيف («لا تسريب صلاحية» — محسوبة ضمن الصفحات الحسّاسة الـ١٢) · **٤٩٠/٤٩٠ اختبارًا** (اختبار انحراف pageAccess) · لينت نظيف · **إثبات حيّ للبوابة:** فتحها بلا دخول حوّل لصفحة تسجيل الدخول (لقطة DOM). **⚠️ لم يُختبَر العرض داخل الـiframe بحساب مدير** (لا بيانات دخول)؛ المحتوى نفس اللوحة المُتحقَّق منها وآليّة srcdoc قياسيّة. **المرجع (توسعة العوالم ٣-٤):** —

- **🗺️ Arch Wiki — توسعة العالمَين ٣ و٤ (Odoo + البنية التحتية) (2026-07-27 · ✅ **مدفوع لـmain `1eb7d5f`**):** بعد MVP (العوالم ١-٢)، وُسِّع الماسح ليغطّي الأربعة كاملةً. **(العالم ٣) Odoo:** `scanOdoo()` يقرأ `brandzo-addons/brandzo_warehouse` → الmanifest (اسم·إصدار·٦ تبعيّات) + **١٢ نموذجًا** (٤ جديدة·٨ امتدادات؛ لكلٍّ حقوله وأنواعها·حالات دورة الحياة·عدد الحرّاس·الوراثة) + **٧ مجموعات أمان** + **مصفوفة وصول** (CSV: مجموعة×نموذج→CRUD) + ١٦ view. إجمالي ٥١ حارسًا (أثقلها `stock.picking` بـ١٦). **(العالم ٤) البنية التحتية:** `scanInfra()` → **٦ عقد** عبر ٤ طبقات (عملاء: متصفّح+Tauri `ly.brandzo.hub` · استضافة: GitHub Pages/Astro+PWA · بيانات: Firebase Firestore+Auth · خلفية: وكيل Odoo نسختان→Odoo 19) + **٥ تدفّقات** + **٣ خطوط CI** بمحفّزاتها (نشر Pages·مثبّت سطح المكتب·مطوّر Gemini) + التبعيّات المصنّفة. **اللوحة:** تبويبان جديدان — Odoo (بطاقات نماذج بحالاتها وحقولها + مصفوفة وصول) والبنية التحتية (**مخطّط طوبولوجيا متدرّج** + تدفّق البيانات + خطوط CI). **✅ تحقّق:** بناء ٤٧ صفحة أخضر · **٤٩٠/٤٩٠ اختبارًا** · لينت `build-arch.mjs` نظيف (صفر) · JSON مضمَّن صالح (٦٣ ك.ب) · **إثبات بصريّ حيّ:** التبويبات الخمسة حاضرة (read_page) ومخطّط الطوبولوجيا يُرسم صحيحًا (عقدة Tauri بمعرّفها ورابطها). **المرجع الأوّل (MVP، العوالم ١-٢):** —

- **🗺️ Arch Wiki — الأساس (MVP، العوالم ١-٢) (2026-07-27 · ✅ **مدفوع لـmain `1b6cea0`**):** المشكلة (طرحها المالك): الكود يُكتب أسرع من التوثيق، ففهم المشروع كان يتطلّب اللفّ على ٢٤٠+ ملفًّا (هدر توكِنز + انحراف مؤكَّد — سُجّل سابقًا «الوثائق متقادمة 13.07»). **الحلّ:** [`scripts/build-arch.mjs`](scripts/build-arch.mjs) يمسح الكود (بفلسفة `build-org.mjs`: مولَّد لا يُحرَّر) ويُخرج ملفَّين: **(1)** `architecture.json` (جذر الريبو) = **المصدر الواحد للـAI** — يُقرأ أوّل كل جلسة بدل اللفّ؛ **(2)** `public/arch-wiki/index.html` = لوحة تفاعليّة للبشر (بحث فوريّ + ٣ تبويبات: موديولات/خدمات/صلاحيات)، مستقلّة تمامًا. **نطاق النسخة الأولى (MVP):** الموديولات (`src/pages/dashboard/*.astro`، الوصف من JSDoc الافتتاحيّ) + الخدمات (`src/services/*`، التصديرات + مجموعات Firestore بحلّ الثوابت) + RBAC (`firestore.rules`، محلّل يتتبّع عمق الأقواس ويربط كل `allow` بمجموعتها ويحلّ الأدوار). **الحصيلة:** ٤٥ موديول · ١٨ نطاق خدمة (١١١ ملف) · ٣٤ مجموعة · ١٢ دور. **التجديد التلقائيّ:** `prebuild` hook في `package.json` (`npm run build` يشغّل المسح أوّلًا → الخريطة دائمًا طازجة فيما يُلتقَط بالـcommit؛ فشل المسح يُفشل البناء = حارس). أمر يدويّ: `npm run arch`. **قرار «ارفع» الجديد (ثُبِّت في الذاكرة):** يعيد المسح تلقائيًّا + يحدّث `SESSION_HANDOFF.md` + يراجع/يحفظ الذاكرة، كلّه قبل الـcommit. **✅ تحقّق:** بناء ٤٧ صفحة أخضر (المسح سبقه) · **٤٩٠/٤٩٠ اختبارًا** · لينت `build-arch.mjs` نظيف (صفر) · الـJSON المضمَّن في اللوحة صالح · اللوحة عُرضت بصريًّا (RTL سليم). **⚠️ التفاعل (تبويبات/بحث) لم يُنقَر حيًّا** (المتصفّح عرضها كلقطة ثابتة لأنها خارج مجلّد الخادم؛ الـJS تفاعُل vanilla مُتحقَّق منطقيًّا). **المتبقّي (توسيع لاحق):** موديول Odoo (`brandzo-addons`) + البنية التحتية + طوبولوجيا الخدمات (العالمان المتبقّيان من الأربعة) · ربط الموديول↔الخدمة حاليًّا ضعيف (regex على مسار الاستيراد).

- **🗂️ إعادة ترتيب بطاقات لوحة التحكم إلى ٦ بوابات حاوية (2026-07-27 · ✅ **مدفوعة حيًّا لـmain `bcd8a44`** والحواجز خضراء · لا تغيير في `firestore.rules`):** طلب المالك جمع البطاقات الرئيسية في لوحة `/dashboard` تحت بوابات موضوعية. **ما نُفّذ في `src/pages/dashboard/index.astro`:** استُبدلت «الأنظمة الرئيسية» الثماني + الشريط الأحمر بـ**٥ بطاقات حاوية** يسرد كلٌّ منها روابطه الفرعية، + **بطاقة الاجتماعات مفردة كما هي**: **(1)** الهيكل التنظيمي للمستودعات (org-structure · workflows · recruitment) · **(2)** عمليات المخزنية (documents · products · tasks · stock-operations · warehouses) · **(3)** لوحات القيادة (command-center · supply-chain · operations · stock-ledger · transfers · order-control) · **(4)** التدريب والتطوير (learn-odoo · erp-workflows · odoo-installation · archive) · **(5)** الصلاحيات والأدوار (users · access-control) · **(6)** الاجتماعات. **صفر حذف:** أُزيل قسما «الدورات والمحاكاة» و«المرجعية والتطوير» السفليّان والشريط الأحمر لأن كل مقاصدها (workflows · erp-workflows · archive · learn-odoo · access-control) انتقلت إلى الحاويات — **لا مقصد فُقد** (تأكّده `audit`). أُبقيت الأقسام السفلية ذات المقاصد الفريدة (العمليات اليومية · المستودعات والجرد · التقارير والتحليل) + الإحصاءات + «قريباً» + منطق التخصيص. **التقييد بالأدوار عبر RoleNav (`li[data-item-roles]`):** حاوية لوحات القيادة + رابط التوظيف الفرعي = `admin,warehouse_manager` · حاوية الصلاحيات = `admin` · الاجتماعات = `admin,warehouse_manager`. **✅ تحقّق:** **481/481 اختبارًا** · بناء 47 صفحة · **تدقيق نظيف — لا رابط مكسور ولا تسريب صلاحية** · لينت الملف نظيف · فحص HTML المُولَّد: الـ21 مقصدًا حاضرة بروابطها الصحيحة، والأدوار الأربعة مقيَّدة، والأقسام القديمة اختفت. **⚠️ لم يُعايَن حيًّا بصريًّا** (اللوحة تحجب بلا دخول — اعتُمد فحص المخرَج الثابت). **التوثيق اليوميّ حُدّث:** صفٌّ «مكتملة» أُضيف لجدول W30 الإثنين 27.07 (docx+pdf).

- **📱☁️ العمليات المخزنية: راحة الهاتف + الدمج الحيّ (منع ازدواج العدّ) (2026-07-27 · ✅ **مُصلَح محليًّا والحواجز خضراء** · ⏳ **لم يُرفع بعد** — بانتظار «ارفع» · لا تغيير في `firestore.rules`):** طلب المالك رسميًّا مراجعة `stock-operations` + عدم ارتياحٍ على الهاتف + اختبار القراءة/المزامنة. **المراجعة كشفت:** (أ) الجدول 12 عمودًا **غير متجاوب** (تمرير أفقي وخطّ مجهري على الجوال)؛ (ب) القراءة **ليست حيّة** — `__bzMaster.fetchAll()` يشترك ثم يُلغي فورًا (لقطة واحدة)؛ (ج) **الأخطر:** `listenScans` مكتوبة في `operationsService.js` لكن **غير مفعّلة** — كل جهاز جزيرة معزولة، وموظّفان يجردان المستودع نفسه يحسبان الصنف مرّتين (المالك أكّد أنّ عدّة موظّفين يعملون معًا). **ما نُفّذ:** **(1) علاج الهاتف** (غير تدميري، CSS+data-label فقط): الجدول يصير **بطاقات** تحت 768px عبر تحويل `#mainTable` + `data-label` لكل خانة في `renderCurrentTable` · **debounce 180ms** للبحث · **Enter يؤكّد المسح** في حقلَي الكمية/السعر. **(2) الدمج الحيّ — وضع «العمل الجماعي» اختياريّ (opt-in، مطفأ افتراضيًّا فلا يمسّ التدفّق الفردي):** محرّك خالص جديد `src/services/stock/scanMerge.js` (`summarizeScans`+`applyLedgerToItems`، مفتاح `bzCanonCode` ليطابق `findInvIdx`) — **الدفتر السحابي مصدر الحقيقة: actualQty لكل صنف = مجموع قيوده**، فلا ازدواج؛ `__bzCloud.startLive/joinOperation/stopLive` يستمع `listenScans` ويوفّق `inventoryDB` لحظيًّا (يضيف أصناف مسحها الزملاء)؛ **الإلغاء يكتب قيدًا مضادًّا** (كمية سالبة — القواعد تمنع الحذف) و**تعديل الكمية يكتب الفرق** تحت الوضع الحيّ؛ مودال `#liveOpsModal` (زرّ «👥 جماعي» بشارة خضراء): يُظهر عمليتك ويسرد العمليات الجارية للانضمام (`listOpenOperations`) — فتتشارك الأجهزة الدفتر نفسه. **✅ تحقّق:** **481/481 اختبارًا (+11 لـscanMerge)** · بناء 47 صفحة · تدقيق نظيف · لينت: **صفر أخطاء جديدة** (+4 تحذيرات من نمط onclick القائم نفسه) · scanMerge نظيف · **إثبات بصريّ حيّ لعرض البطاقات عند 375px** (نسخة معزولة مطابقة — الصفحة نفسها محجوبة بـAuthGate الذي يحوّل لـ/login). **⚠️ لم يُجرَّب الدمج الحيّ داخل البوّابة حيًّا** (يحتاج دخولًا + جهازين). **الحدّ الموثَّق:** الوضع الجماعي يتطلّب أن **يفعّله/ينضمّ كلُّ جهاز** على العملية نفسها (تناظر)؛ التبديل بعد مسحٍ محلّي يُنبَّه عليه. **✅ رُفعت هذه الدفعة (الهاتف + الجماعي) لـmain `aa2f497` والنشر انطلق.**

- **☁️ الماستر الحيّ — الأرصدة الدفترية تبقى طازجة أثناء الجلسة (2026-07-27 · ✅ **مُصلَح محليًّا والحواجز خضراء** · ⏳ **لم يُرفع بعد** — بانتظار «ارفع» · لا تغيير في `firestore.rules`):** إكمالٌ للمراجعة — كان `__bzMaster.fetchAll()` يقرأ الماستر **لقطةً واحدة** عند البدء، فتعديلُ رصيدٍ/سعرٍ في الماستر أثناء الجرد يبقى غير مرئيّ. **ما نُفّذ:** وحدة خالصة جديدة `src/services/stock/masterSync.js` (`refreshMasterFields`: تحدّث **systemQty (balance) وunitPrice فقط** للأصناف المطابقة بمفتاح `bzCanonCode`، و**لا تمسّ** actualQty/scanned/notes — الكمية الفعلية مصدرها المسح لا الماستر؛ اليدويّ `isManual` يُتجاوَز) — **+9 اختبارات**. و`__bzMaster.subscribeLive/stopLive` اشتراكٌ **دائم** يُغذّي `__bzRefreshMaster`، يبدأ مرّة من `switchToDashboard`؛ **يعالج عودة الشبكة تلقائيًّا** لأنّ `onSnapshot` يعيد الإرسال عند الاتصال. أوّل لقطةٍ صامتة، والتغيّرات اللاحقة تُعلَن بـtoast «☁️ حُدّثت بيانات N صنفًا». **✅ تحقّق:** **490/490 اختبارًا (+9)** · بناء 47 صفحة · تدقيق نظيف · لينت بلا مشاكل جديدة · الربط في البناء. **⚠️ لم يُجرَّب حيًّا** (يحتاج دخولًا + تعديل الماستر). **✅ رُفع لـmain `2521d73`** (بعد دمج التزام زميلٍ منفصل `bcd8a44` بلا تعارض).

- **🧹 توحيد نسخة العمليات المخزنية — نسخة واحدة معتمدة (2026-07-27 · ✅ **مُصلَح محليًّا والحواجز خضراء** · ⏳ **لم يُرفع بعد** — بانتظار «ارفع»):** كشف الفحص تضاربًا: **الشريط الجانبي** (`navCatalog.js`) يقصد النسخة الحيّة `/dashboard/stock-operations` (المُحسّنة)، لكن **بطاقة الصفحة الرئيسية** (`index.astro`) كانت تفتح النسخة القديمة المستقلّة `/عمليات-مخزنية.html` (تبويب جديد · دخول أدمن وهمي · CDN) — فالموظّف الذي ينقر البطاقة يصل نسخةً **بلا** تحسينات الهاتف/الجماعي/الماستر الحيّ. **ما نُفّذ:** (1) بطاقة الرئيسية أُعيد توجيهها إلى `/dashboard/stock-operations` وأُزيل `newtab` (تفتح داخل البوابة كالشريط الجانبي، ولها زرّ «← البوابة»). (2) **حُذف `public/عمليات-مخزنية.html`** (كان الإشارة الوظيفية الوحيدة له بطاقة الرئيسية — تقرير النظام يشير للمسار الحيّ لا للملف). (3) حُدّث تعليق `stock-operations.astro` (النسخة الوحيدة المعتمدة). **✅ تحقّق:** 490/490 اختبارًا · بناء 47 صفحة (sw.js أُعيد توليده بلا الملف) · تدقيق نظيف · رابط البطاقة في البناء = `/warehouse-system/dashboard/stock-operations`. **بهذا اكتملت مراجعة العمليات المخزنية كاملةً** (الهاتف · الجماعي · الماستر الحيّ · التوحيد).

- **📊 صدق مؤشرات لوحة القيادة — نافذة زمنية بدل «أحدث 400» (2026-07-26 · ✅ **مُصلَح محليًّا والحواجز خضراء** · ⏳ لم يُرفع بعد · بلا بنية مدفوعة):** التقييم كشف أن اللوحة تحسب المؤشرات على أحدث 400 مستند (`listenAllDocuments(…,400)`)، فتنحرف **صامتةً** متى تجاوز الإجمالي ذلك. الإصلاح الشحنيّ (اختاره المالك على التجميع الخادميّ المدفوع): **(1)** `computeKpis(docs, {nowMs, windowDays})` صار يقصر الحساب على **آخر 90 يومًا** (دالّة `withinWindow` النقيّة — مستندٌ بلا تاريخ يُحتسب فلا نُسقط ما نجهله)، فالمؤشر صحيحٌ ومستقرّ مهما نما الحجم. **(2)** رُفع الحدّ 400→**800** في `OperationsCommand.jsx`. **(3) شريط صدق البتر:** يظهر فقط عند بلوغ الحدّ — أحمر إن كانت نافذة الـ90 يومًا نفسها ناقصة (`kpiWindowIncomplete`)، وكهرمانيّ إن كانت اللقطة وحدها مبتورة والمؤشرات مكتملة. لا صمت بعد اليوم. **✅ تحقّق:** **470/470 اختبارًا (+3 للنافذة)** · بناء 47 صفحة · تدقيق نظيف · لينت 122 = خطّ الأساس · لينت الملفّات الجديدة نظيف. **⚠️ لم يُجرَّب حيًّا** (اللوحة تحجب بلا دخول). **الحلّ الجذريّ المؤجَّل:** تجميع خادميّ (Cloud Function تكتب وثيقة KPIs) — يحتاج خطة Blaze، قرار المالك.

- **🛡️ تقييم شامل + إصلاح الطبقة الحرجة (2026-07-26 · ✅ **مُصلَح محليًّا والحواجز خضراء** · ⏳ **لم يُرفع بعد** — بانتظار «ارفع» · ⚠️ **يلزم المالك: إعادة نشر `firestore.rules` + تفعيل نسخ احتياطي**):** تقييمٌ مبنيّ على فحصٍ فعليّ للكود وتشغيل الأدوات وأربعة تدقيقات متوازية موثّقة بالأسطر. **التقييم العام ≈ 6.1/10** (قويّ معماريًّا وأمنيًّا كعملٍ فرديّ إقليميّ، تنقضه عيوبٌ حرجة في سلامة البيانات والأمن). **أُصلحت أربع نقاط:** **(1) 🔴 XSS تخزينيّ** في جدول الجرد — حقول DB (`name/shade/category/notes/barcode`) كانت تصل `innerHTML` بلا تهريب، فموظّفٌ ميدانيّ يحقن سكربتًا يُنفَّذ في جلسة المدير عند فتحه الجدول. أُضيف `escAttr` (تهريب سلسلة JS داخل سمة) و`escapeHtml` على كل موقع في `stock-operations.astro` **والنسخة الحيّة `public/عمليات-مخزنية.html`**، و`showToast` صار يستعمل `textContent`. **(2) 🔴 رصيد سالب** — `ledgerService.js` كان يُنقص الرصيد بـ`increment(delta)` بلا فحص؛ سحبُ 100 من رفٍّ فيه 50 يُنتج `-50` صامتًا. أُضيفت الدالّة النقيّة `findNegativeBalance` (movements.js) وتُفحص **داخل معاملة القيد** (قراءة الأرصدة الطازجة قبل الكتابة) + **5 اختبارات**. **(3) 🟠 سباق الحجز** — `salesService.js` كان يقرأ الأرصدة خارج معاملة ثم يحجز (أمرا بيع متزامنان يحجزان ضعف الموجود)، والفكّ المزدوج يجعل المحجوز سالبًا. أُعيد `allocateAndReserve`/`releaseReservation` إلى `runTransaction` ذرّيّ وإيديمبوتنت (اكتشاف المرشّحين خارجها ثم قراءتهم طازجين داخلها). **(4) 🟠 تجميد بنود القواعد** — `firestore.rules` كانت تسمح للمعتمِد بتبديل `unitPrice` لحظة الاعتماد بلا أثر؛ أُضيف حارس `contentUnchanged()` على فرعَي الاعتماد والإنجاز (تجميد `lines`+`header`). **✅ تحقّق:** **467/467 اختبارًا (+5)** · بناء 47 صفحة · تدقيق نظيف · لينت **122 = خطّ الأساس** · القواعد متوازنة (89/89) · **إثبات سلوكيّ لتهريب XSS بحمولة هجوم فعلية**. **🚨 على المالك عند «ارفع»:** (أ) إعادة نشر `firestore.rules` (حارس تجميد البنود)؛ (ب) تفعيل تصدير Firestore المجدول — **لا نسخ احتياطي إطلاقًا اليوم** (فجوة تعافٍ حرجة). **المتبقّي من التقييم (لم يُنفَّذ):** تجميع خادميّ للوحة القيادة (اليوم تحسب على أحدث 400 مستند فقط ⇒ مؤشّرات خاطئة صامتة عند التوسّع) · تصفّح حقيقيّ للقوائم غير المحدودة · مصادقة توكن على وكيل أودو (`worker.js` يحمي بـCORS فقط) · اختبارات لمعاملات Firestore عبر المحاكي.

- **🎛️ لوحة القيادة التشغيلية — الدورة العالمية تكتمل (2026-07-27 · ✅ **مدفوع لـmain `9b95dae`** والنشر نجح والصفحة 200 والمؤشرات ولوحة الاستثناءات في حزمة `OperationsCommand` الحيّة · **جلسة ٥ من ٥ — الدورة End-to-End اكتملت** · لا تغيير في قواعد Firestore):** الشاشة الواحدة التي تُتوّج كل ما بُني — **تجميعٌ لا اختراع**، كل مصدرٍ جاهزٌ ومُختبَر. **عقلٌ خالص** (`operationsDashboard.js`، الوقت يُمرَّر `nowMs` لا يُقرأ) بثلاث نتائج: **(1) المؤشرات الأربعة** (`computeKpis`): **نسبة التنفيذ** (المخصَّص÷المطلوب) · **زمن دورة الطلب** (متوسط أيام الأمر→التسليم من الطوابع) · **دقّة المخزون** (1−فروق الجرد المطلقة÷الدفتري من محاضر CC المصادَقة) · **دقّة التسليم** (المستلَم÷المشحون من استلامات النقل) — وكلٌّ يُعيد **`null` لا صفرًا مزيّفًا** إن غابت بياناته. **(2) اللقطة** (`operationsSnapshot`): عدّادات لحظية موزَّعة على المبيعات · المخازن · النقل · المخزون · المالية — بما فيها **تحت الحدّ الأدنى/المطلوب شراؤه** (من `Items_Master.minStock` مقابل المتاح). **(3) الاستثناءات** (`operationExceptions`): ما يحتاج تدخّلًا فقط، مرتّبًا بالخطورة برابطٍ لموضع المعالجة — اعتمادٌ متأخّر (`isStale`) · شحنةٌ في الطريق · فرقُ نقلٍ غير مسوّى · أمرٌ بلا رصيد · رصيدٌ عالق في مواقع النظام · **منتهي/قريب الانتهاء** (`expiryStatus`) · تحت الحدّ الأدنى. **الصفحة:** `/dashboard/command-center` + `OperationsCommand.jsx` — المؤشرات ثم الاستثناءات (أوّلًا لأنها الأهمّ) ثم اللقطة، تُحدَّث لحظيًّا (`listenAllDocuments`+`listenBalances`+`subscribeItems`). **ربط:** navCatalog (رأس مجموعة المستودعات). **✅ الإصلاحات الجانبية الثلاثة (المهمّة #٦) أُنجزت عبر الجلسات ٢-٣:** `chainOf` يمشي في سلسلة المستند (`chainFor||PURCHASE`) · أزرار إطلاق المرتجعات/الجرد/التالف في الصندوق · نصّ الأنواع التسعة عشر. **✅ تحقّق:** **462/462 اختبارًا (+9)** · تدقيق نظيف (47 صفحة) · لينت الجديد نظيف · بناء 47 صفحة. **⚠️ لم يُجرَّب حيًّا** (المحرّك يحجب بلا دخول). **🎉 الدورة المستندية العالمية End-to-End اكتملت (٥/٥): دفتر ← مبيعات ← نقل ← تقارير رقابية ← لوحة قيادة.** المتبقّي الطبيعيّ: تجربة المالك الحيّة للدورة كاملةً، وربّما «وثّق» لتقارير الجلسات ٣-٥.

- **📋 التقارير الرقابية للمبيعات (2026-07-27 · ✅ **مدفوع لـmain `5779241`** والنشر نجح والصفحة 200 والتقريران في حزمة `OrderControlBoard` الحيّة · جلسة ٤ من ٥ · **لا تغيير في قواعد Firestore**):** ثمرة الحجز الذي بُني في الجلسة ٢ — أمر البيع صار يحجز الرصيد ويثبّت `soShortfall`/`soAllocation`، فحوّلنا ذلك إلى **تقريرين يقودان القرار** (`salesReports.js` خالص). **(1) تقرير الطلبات المعلّقة** (`pendingOrders` + `classifyOrder`): كل أمرٍ لم يُسلَّم مصنَّفًا بالرصيد وسبب التعثّر — **لم يُعتمد** (من الحالة) · **لا يوجد رصيد** (عجزٌ كامل، يُحوَّل للتقرير الثاني) · **يوجد رصيد/لم تُعبَّأ** (معتمَد بلا PICK) · **قيد التجهيز** (PICK مشتقّ) · **موقوف** (سببٌ يدويّ من `holdReason`) · **سُلّم/مقفل** (DN منجَز، يخرج من المعلّق). التصنيف يُشتقّ من الحالة والسلسلة (`links.SO` المتراكمة تصل DN) لا من ظنّ. **(2) تقرير الأصناف غير المتوفّرة** (`outOfStockItems`): العجز مجمَّعًا صنفًا صنفًا — عدد مرّات الطلب · الكمية العاجزة · **القيمة المفقودة** (العجز × سعر بند الأمر) · **متوسط الطلب الشهري** (إجمالي الطلب ÷ الأشهر المشمولة، لا العجز — فهو حجم الطلب لِضبط حدّ المخزون) · **تاريخ آخر نفاد** — يُغذّي التنبؤ بالطلب والتخطيط الشرائي. **الحقل الجديد:** `holdReason` في رأس أمر البيع (select: مشكلة لوجستية · إيقاف من العميل · مشكلة بالنظام) — يُحرَّر في المسودّة فلا يحتاج تغيير قواعد. **الصفحة:** `/dashboard/order-control` + `OrderControlBoard.jsx` — تبويبان + شريط مؤشّرات + توزيع الأسباب برقائق ملوّنة. **ربط:** navCatalog (مجموعة المستودعات، بعد النقل). **✅ تحقّق:** **453/453 اختبارًا (+9)** · تدقيق نظيف · لينت الجديد نظيف · بناء 46 صفحة. **⚠️ حدٌّ موثَّق:** أسباب الإيقاف اليدوية (لوجستية/عميل/نظام) تُضبط في مسودّة الأمر فقط حاليًّا؛ ضبطها على أمرٍ معتمَدٍ لاحقًا يحتاج مفتاحًا في `isSideEffectStamp` (تحسينٌ مؤجَّل يتجنّب إعادة نشر القواعد). **⚠️ لم يُجرَّب حيًّا** (المحرّك يحجب بلا دخول). **التالي: الجلسة ٥** = لوحة التحكّم التشغيلية والاستثناءات و KPIs (Fill Rate · Order Cycle Time · دقّة المخزون · دقّة التسليم).

- **🚚 دورة النقل ومخزن النقل — Transit (2026-07-27 · ✅ **مدفوع لـmain `a00a965`** والنشر نجح والصفحات 200 و«النقل بين المستودعات» في حزمة `DocumentsInbox` الحيّة · جلسة ٣ من ٥ للدورة End-to-End · ⚠️ القواعد الجديدة تحتاج نشرًا إن لم يفعل المالك بعد):** الجلسة التي تُثمر ثمرة دفتر الحركات مباشرةً — المبدأ «مخزن النقل يجب أن يعود صفرًا» صار **خاصّيةً حسابية مُثبَتة** لا شعارًا. **ثلاثة مخطّطات جديدة:** `TR` (طلب نقل — طلبٌ بلا حركة كطلب الشراء) · `TRN` (مستند النقل/الشحن — **يُخرج من المصدر إلى مخزن النقل TRANSIT**) · `TRC` (استلام الفرع — **يُفرغ مخزن النقل إلى الوجهة**). **التوسعة المعمارية الأهمّ:** قواعد القيد كانت تعرف مستودعًا واحدًا (`@warehouse`)، فأضفتُ رمزَي **`@warehouseFrom` و`@warehouseTo`** (خريطة `WAREHOUSE_TOKENS`) — لأن النقل وحده يحتاج مستودعَين؛ و`resolveLocation` في `movements.js` صار يقرأ الحقل الموافق. **الخاصّية المحورية المُختبَرة:** شحنٌ (TRN: E5→TRANSIT +10) ثم استلامٌ مطابق (TRC: TRANSIT→BR2، TRANSIT −10) ⇒ **صافي مخزن النقل صفر**؛ وإن نقص المستلَم، **يبقى الفرق في مخزن النقل** ظاهرًا — وهو تقرير الفروقات نفسه لا يُخفى. **تقريران رقابيان** (`transferReports.js` خالص): **الشحنات المعلّقة** (في الطريق · استُلمت · بفرق كميات، من TRN المنجَز مقابل TRC المرتبط) و**فروقات النقل** (المشحون↔المستلَم صنفًا صنفًا · السبب · حالة التسوية). **الصفحة:** `/dashboard/transfers` + `TransferBoard.jsx` — ثلاثة تبويبات (الشحنات المعلّقة · الفروقات · **رصيد مخزن النقل الحيّ = إثبات التصفير**) وشريط مؤشّرات. **السلسلة:** `TRANSFER_CHAIN=['TR','TRN','TRC']` باشتقاق خطّي (المطلوب→المشحون→مرجع الاستلام) والمستودعان يُورَّثان. **Firestore:** أدوار TR/TRN/TRC في `approveRoles`/`completeRoles` (اعتماد للمدير · إنهاء لأمين المخزن+المدير) — **⚠️ تلزم إعادة نشر `firestore.rules`**. **ربط:** صندوق الاعتماد (مجموعة «🚚 النقل بين المستودعات») · ChainBar (اسم السلسلة+الحارس) · `listenDocumentsByTypes` (استعلام `in`) · navCatalog (مجموعة المستودعات) · نصّ الأنواع التسعة عشر. **✅ تحقّق:** **444/444 اختبارًا (+10 للنقل)** · تدقيق نظيف (43 صفحة) · بناء 45 صفحة · لينت الملفّات الجديدة نظيف. **⚠️ لم يُجرَّب حيًّا** (المحرّك يحجب بلا دخول). **التالي: الجلسة ٤** = التقارير الرقابية (الطلبات المعلّقة · الأصناف غير المتوفّرة). ثم الجلسة ٥ = لوحة التحكّم و KPIs.

- **🛒 دورة المبيعات — أمر البيع والفاتورة (2026-07-26 · ✅ **مدفوع لـmain `2bcdfb4`** والنشر نجح والصفحات 200 و«أمر البيع/فاتورة العميل» في حزمة `DocumentsInbox` الحيّة · **والمالك نشر القواعد الجديدة** · جلسة ٢ من ٥ للدورة End-to-End):** تُحيي دفتر الحجز الذي بنته الجلسة ١. **ما بُني:** (1) **مخطّط `so.js` (أمر البيع)** — رأس سلسلة الصرف، بعميلٍ ومستودعٍ مصدر **إلزاميّ** وبنودٍ مسعّرة؛ **عند اعتماده يُحجز الرصيد فعلًا** فيهبط المتاح ولا يُباع الصنف مرّتين. (2) **مخطّط `inv.js` (فاتورة العميل)** — قيمةٌ بلا قيدٍ مزدوج (قرار المالك)؛ فرعيّ + ضريبة + خصمان + إجمالي محسوبة؛ **لا تحرّك مخزونًا** (البضاعة خرجت بإذن التسليم). (3) **السلسلة:** الصادر صار `SO→PICK→PACK→DN→GP`، و**إذن التسليم يتفرّع**: منه تصريح البوابة (خروج) ومنه الفاتورة (مالية) — عبر `derivationTargets` (DN→['GP','INV']) و`deriveDocument(source, toType)` متعدّد الوجهات، وشريط السلسلة صار يعرض زرًّا لكل وجهة. سلسلة `BILLING_CHAIN=['DN','INV']` للعرض. (4) **السعر يركب** من الأمر حتى الفاتورة: أضيف `unitPrice` لخرائط `PICK>PACK`/`PACK>DN`، فالفاتورة تأخذ **الكمية المسلَّمة بسعرها المتَّفق** لا يُعاد إدخاله؛ ومرجعا التسليم وأمر البيع يُشتقّان من الأرقام. (5) **الحجز:** `salesService.js` — `allocateAndReserve` (FEFO من `fetchBalancesOnce`، يحجز، يثبّت الخطّة والعجز على SO) عند الاعتماد، و`releaseReservation` عند الإنجاز؛ **أفضلُ جهدٍ** لا شرطُ اعتماد (فشلُ الحجز يُثبَّت `reserve-failed` ولا يُبطل قرار المدير)؛ حدٌّ موثَّق: المتاح متحفّظ بين الاعتماد والإنجاز (آمن). **🚨 إصلاح عطب كامن من جلسة ١:** قاعدة تحديث المستندات كانت تسمح بنقلات الحالة فقط، فختمُ `posted` الذي يضعه الدفتر على «منجَز» كان **يرتدّ من الخادم لغير الأدمن** (لم يظهر لعدم اختبارٍ حيّ بحساب أمين مخزن) — أُضيفت `isSideEffectStamp` (فاعل مخزنيّ · الحالة ثابتة · `diff().affectedKeys().hasOnly(posted/soAllocation/…)`). القواعد: أُضيف SO/INV لـ`approveRoles`/`completeRoles`. **✅ تحقّق:** **434/434 اختبارًا (+14)** · `audit` نظيف (42 صفحة · 32 قاعدة) · لينت الملفّات الجديدة نظيف · بناء 44 صفحة · معاينة: جزيرة `type=SO` تُرطّب بلا أخطاء والبوّابة تُحوّل بنظافة. **🚨 على المالك عند «ارفع»: إعادة نشر `firestore.rules`** (إصلاح `posted` حرج + أدوار SO/INV + `isSideEffectStamp`). **التالي: الجلسة ٣** = دورة النقل ومخزن النقل (TR·TRN·TRC · تصفير TRANSIT · تقريرا الشحنات المعلّقة والفروقات).

- **📇 دفتر حركات المخزون — الأساس الذي يجعل المستند يُحرّك الرصيد (2026-07-26 · ✅ **مدفوع لـmain `ff9b419`** — الحواجز خضراء والنشر انطلق · جلسة ١ من ٥ للدورة المستندية العالمية End-to-End):** رؤية المالك = منظومة تربط المبيعات والمخازن والمالية، **لا حركة بدون مستند ولا مستند بدون أثر**. **التحقّق كشف حقيقتين:** (أ) المبدأ كان **غير محقَّق إطلاقًا** — المستندات تُعتمد وتُنجَز بلا أثر مخزني؛ (ب) `inventoryService.js` (الوحيد الذي يعرف تحديث الأرصدة) كان **ميّتًا بلا مستدعٍ**، والأرصدة تأتي من إكسل فقط. **قرارات المالك الحاكمة:** الدفتر أولًا · **مستندات بقيمة بلا قيود محاسبية مزدوجة** (القيود لأودو) · **حجز فعلي على الرصيد**. **ما بُني — طبقة `src/services/ledger` كاملة:** (1) **`locations.js`** مواقع النظام الستّة (استلام·حجر·تجهيز·**نقل**·إتلاف·تسوية) التي **يجب أن تعود للصفر** — رصيدٌ عالق فيها = استثناء يُلاحَق، لا خطأ في النظام بل التقرير نفسه؛ `EXTERNAL=null` للمورّد/العميل. (2) **`postingRules.js`** جدول القيد — يُقيَّد عند **`done` (منجَز) لا `approved`** (الفرق: أذِنتُ بأن يحدث ≠ حدث)؛ الأنواع المحرّكة GRN·QC·PUTAWAY·PICK·DN·RET·DMG·ADJ، والغائبة عمدًا (PR·PO التزام · PACK إعادة تغليف · GP رقابة · CC عدّ · CN مالي بحت). (3) **`movements.js`** الكمية موجبة أبدًا (الاتجاه من from/to لا إشارة الرقم) · معرّف حتميّ `docId__lineIndex` يمنع الازدواج · يرفض البند بلا هوية صنف أو مستودع بسببٍ مكتوب. (4) **`reservations.js`** الحجز الفعلي: **المتاح = qty − qtyReserved** (فلا يَعِد بائعان بنفس الكمية) · تخصيص FEFO (الأقرب انتهاءً أولًا) · `fillRate`. (5) **`ledgerService.js`** القيد الذرّي في `runTransaction`: يكتب `stock_moves` ويزيد `balances.qty` معًا أو لا شيء، والختم `posted` يُفحص **داخل** المعاملة فلا قيد مزدوج. (6) **`ledgerCard.js`** كشف حركة الصنف برصيد جارٍ. **الربط الحاسم:** `transitionDocument` **يحرس قبل «منجَز»** (لا يُنجَز مستندٌ يستحيل قيده — منجَزٌ بلا أثر أسوأ من عالق: الأوّل يكذب بصمت) ويُقيّد بعده مع أثرٍ في سجلّ التدقيق (`post`/`post-failed`). **الواجهة:** `/dashboard/stock-ledger` + `StockLedger.jsx` (رصيد متاح حيّ · كشف حركة كل صنف بمستنداته · لوحة استثناءات العالق). **Firestore:** مجموعة `stock_moves` ملحقة-فقط + دالة `isStockActor()` + توسيع كتابة `balances` للفاعل المخزني (أمين المخزن يُنجز السحب فيُنقص الرصيد) — **✅ نشرها المالك من الكونسول**. **حقل `warehouse`** أُضيف لـ PICK·PUTAWAY·DMG·ADJ. **إصلاحات جانبية:** `chainOf` يمشي في سلسلة المستند لا الشراء دائمًا (شريط «ما قبل» كان يخلو للصادر/المرتجعات/الجرد) · أزرار إطلاق للمرتجعات·الجرد·التالف في صندوق الاعتماد · نصّ الأنواع الأربعة عشر المحدَّث · تسميات تدقيق `derive`/`post`. **✅ تحقّق:** **420/420 اختبارًا (+26)** · `audit` أخضر (42 صفحة · 32 قاعدة · `stock_moves` التُقطت) · لينت الملفات الجديدة نظيف · بناء 44 صفحة · **معاينة على بناء الإنتاج:** الصفحة تُبنى وتُخدَم، وبوّابة الدخول تعيد التوجيه بنظافة، وصفر أخطاء console/خادم. **التالي: الجلسة ٢** = دورة المبيعات (أمر بيع SO + التخصيص/الحجز + فاتورة INV؛ سلسلة SO→PICK→…→INV؛ أسباب عدم التنفيذ). ثم النقل ← التقارير الرقابية ← لوحة التحكم و KPIs. **قد يلزم «وثّق» للتقارير الثلاثة.**

- **✍️ بنود الاجتماعات تُضاف وتُحذف وتُعدَّل + زرّ الرجوع يُغلق الطبقة في البوابة كلّها (2026-07-25 · ✅ **منشور حيًّا `967a8a2`** — بوابة الجودة والبناء والنشر نجحت، والصفحات ترد 200، **وتُحقِّق أنّ الوحدة المشتركة `overlayHistory` تُستورَد فعلًا في حزم الصفحات التسع على الموقع الحيّ**):** طلبان من المالك. **(أ) تحرير البنود:** الأجندة كانت بذرةً مغلقة (36 بندًا من الخطاب لا تُضاف ولا تُحذف)، والطاولة تُنبت أسئلة لم تخطر في الخطاب وتُسقط أخرى لا تخصّ الإدارة الجالسة. الآن **إضافة · حذف · تعديل** بنافذة تحرير واحدة (بدل ثلاث نوافذ `prompt` متتابعة) وكلٌّ منها يُحفظ سحابيًّا فورًا (`saveNow` لا انتظار 700ms). **والتحرير كلّه في طبقة المحفوظ لا في البذرة:** بند بذرةٍ حُذف يُسجَّل له **شاهد حذف** `{deleted:true}` فيخرج إلى `removedItems` ويبقى **قابلًا للاسترجاع** بموضعه من الخطاب لا في ذيل القائمة؛ والبند المُضاف (`M01-c1`) يُحفظ كاملًا؛ و**العنوان لا يُجمَّد في السحابة إلّا إذا حرّره المالك** — فتصحيح صياغةٍ في البذرة يظلّ يصل لِما لم يُحرَّر. **حدٌّ واحد:** `agendaVerdict` يمنع المسّ ببنود **محضرٍ موقّع** ويُنبّه على محضرٍ صادر برقم. **(ب) 🔙 زرّ الرجوع:** كان الدخول لوضع العرض لا رجعة منه — والضغط على «رجوع» يغادر الصفحة كلّها بدل أن يُنهي العرض، **والمسح كشف أن العلّة في البوابة بأسرها: 39 طبقة تملأ الشاشة، ولا واحدة منها مربوطة بتاريخ المتصفّح**، فزرّ الرجوع — وهو الزرّ الوحيد على الهاتف — كان يقذف المستخدم خارج الصفحة من كلٍّ منها. **الحلّ `src/services/ui/overlayHistory.js`:** فتح الطبقة يُسجّل مدخلًا في التاريخ بالعنوان نفسه (`pushState` بلا رابط — لا تنقّل ولا إعادة تحميل) فيصير الرجوع إغلاقًا، والإغلاق بالزرّ يستهلك المدخل فلا يتراكم ميّت. و**`trackOverlay`** يُراقب الوسم بـ`MutationObserver` فيربط الطبقات القديمة **بسطرٍ واحد لكلٍّ منها بلا لمس دوالّ فتحها وإغلاقها** (و`useOverlayBack` للطبقات المكتوبة بـReact). **رُبطت:** الشريط الجانبي والدليل في `DashboardLayout` (فتسري على كل صفحات البوابة) · وضعا العرض في الاجتماعات والهيكل · 10 نوافذ الجرد · المركبات 4 · الصيانة 3 · العُهد 2 · الأسطول 2 · الأصول 1 · متجر التجزئة 2 · وخمس طبقات React. **ولمن له دالّة إغلاق خاصّة تُستدعى دالّته** لا مجرّد إزالة الصنف — فنافذة الكاميرا مثلًا **توقف بثّ الكاميرا**. **(ج) الحفظ الحيّ لم يعد يقطع الكتابة:** كل لقطة سحابية كانت تُعيد بناء الوسم فتسحب المؤشّر من تحت يد الكاتب في منتصف الجملة — الآن يُحفظ الحقل (`data-f`) وموضع المؤشّر ويُعادان بعد الرسم، ولا يُكتب في حقلٍ تحت التركيز. **(د) تحسينات العرض:** أسهم/مسافة/PageUp/Home/End · **سحب بالإصبع** · تعطيل طرفَي الشريط عند الحدّ · تلميح «الأسهم للتنقّل · زرّ الرجوع أو Esc للخروج». **✅ تحقّق:** `audit` أخضر · **إثبات سلوكي بمتصفّح حقيقي على بناء الإنتاج:** فتح الطبقة يزيد التاريخ مدخلًا · «رجوع» يُغلقها **والرابط لا يتغيّر** · الإغلاق بالزرّ لا يُراكم مدخلًا ميّتًا · و`trackOverlay` يلتقط ظهور الصنف واختفاءه. **التالي:** «ارفع» بعد موافقة المالك، ثم «وثّق» للتقارير الثلاثة.

- **📚 مركز الدراسات + الدراسة المقارنة العالمية (2026-07-25 · ✅ **منشور حيًّا `d900405`** — بوابة الجودة والبناء والنشر نجحت، والصفحات الثلاث ترد 200 على الموقع الحيّ، والروابط الأربعة مُتحقَّق منها):** بطلب المالك: إدخال مجلد «الدورة المستندية» (٨ تقارير DOCX عن شركات عالمية) إلى البوابة **محتوى أصيلًا لا مرفقات**، وإنشاء مسار للتقارير والدراسات، وبناء دراسة مقارنة بمستوى عالمي. **(1) `src/data/global-doc-cycles.json`** — الملفات الثمانية استُخرجت (بسكربت Python يقرأ `word/document.xml` مباشرةً — لا pandoc على الجهاز) وهُيكلت في مصدر واحد بسبعة محاور موحّدة لكل شركة: الفلسفة · المراحل · جدول المستندات · تجديد الفروع · دور ERP · **إسقاط Odoo** · الإدارات و**مثلث المالية-المشتريات-المخازن** · الدروس المنقولة · أرقام دالة. **(2) `/dashboard/global-doc-cycles`** مكتبة تفاعلية: **مصفوفة النماذج الثمانية** (من يملك المخزون؟ ما إشارة الطلب؟ ما المستند المحوري؟ ما شكل الشبكة؟ — المقارنة البنيوية في نظرة) + تبويب لكل شركة بلوحتها الكاملة. **(3) `/dashboard/comparative-study`** الدراسة الرائدة `BFP-SCM-STD-2026-001` بأحد عشر قسمًا: ملخص تنفيذي · نظامنا في سطور · **بطاقة نضج عشرة أبعاد** · **15 فجوة** بتصفية بالخطورة · **20 ممارسة عالمية بحكم على حالنا** · **30 مؤشر أداء** بصيغها ومعاييرها ومصادر بياناتها · **15 مخاطرة** بمصفوفة احتمال×أثر · **29 سؤالًا** موزَّعة على **9 إدارات** بتبويب · **14 توصية** بثلاثة آفاق · المنهجية والحدود والمراجع · اعتماد بالتوقيعين. **(4) `/dashboard/studies`** مركز الدراسات بثلاث فئات (المكتبة المعرفية · الدراسات المقارنة · المراجع الحاكمة). **(5) 🔒 حارسان في `studiesModel.js`:** `companiesVerdict` (لا شركة بلا هوية كاملة ولا أقسام فارغة) و**`studyVerdict`** (لا فجوة بلا دليل من نظامنا وشركة مرجعية · لا توصية تربط فجوة غير موجودة · لا مؤشر بلا صيغة ومصدر · درجات النضج داخل [0..5]) — **والحارس أثبت قيمته فورًا: وكلاء التحليل المتوازين اخترعوا معرّفات فجوات وهمية (`gap-asn` بدل `asn-engine`) فالتقطها الحارس وأُعيد الربط.** **(6) الربط:** navCatalog (٣ عناصر في مجموعة التقارير) · ReportsHub (٣ بطاقات في «التحليل والمقارنات») · قسم **«هذا الهيكل مُقاس على العالم»** في `workflows` · صندوق **«مراجع داعمة للقرار»** في `meetings` (بطاقة ثابتة `no-present` لا تمسّ محرّك الاجتماعات). **📊 أبرز ما كشفته الدراسة:** متوسط النضج **2.9/5** — قويّ حيث نتحكّم بأنفسنا (التنظيم 4.5 · الجودة 4.0 · الحوكمة والسلاسل ودقة المخزون واللوجستيات 3.5) وضعيف حيث نحتاج طرفًا آخر (تكامل المورّدين **1.0** · التجديد والتنبؤ **0.5** · القياس 2.0)؛ وأخطر فجوة مالية: **لا Landed Costs ونحن شركة مستوردة** (نسعّر على فاتورة المورّد لا الكلفة الحقيقية)؛ و**24 من 30 مؤشرًا قابلة للحساب اليوم** من بيانات قائمة — أرخص مكسب في الخارطة. **✅ تحقّق:** `audit` أخضر (41 صفحة كلها مربوطة · 39 رابطًا داخليًّا · لا تسريب صلاحية) · **376/376 اختبارًا (+19)** · بناء **43 صفحة** · لينت **122 = خطّ الأساس بالضبط** · **إثبات سلوكي على بناء الإنتاج:** الصفحات الثلاث 200 · 15 بطاقة فجوة و30 صف مؤشر و29 بطاقة سؤال في 9 تبويبات · التوقيعان محمَّلان · **ومنطق الصفحات المشحون شُغّل فعليًّا على DOM المبنيّ: تبديل الشركات يعمل (كارفور وحدها تظهر) وتصفية الفجوات تعمل (15 ← 3 حرجة ← 15) وتبويب الإدارات يعمل** · ومحرّك الاجتماعات سليم بعد الإضافة (`mGrid`/`itemsBox`/`stage`). **⚠️ ملاحظة:** الورشة متعددة الوكلاء انقطعت عند حدّ الجلسة بعد إنجاز وكلاء التحليل الستة، فأُنقذت حصيلتهم من `journal.jsonl` وتوليتُ التحقق وبطاقة النضج يدويًّا. **التالي:** «وثّق» للتقارير الثلاثة.

- **🏁 S12 — الإغلاق المالي · الباك اند يكتمل 12/12 (2026-07-24 بُني · 2026-07-25 ✅ **مدفوع لـmain `15d1da5`** fast-forward بعد حرّاس محليّين خضر: 357/357 اختبارًا · audit نظيف · بناء 40 صفحة):** آخر مرحلة في العقل المدبّر (موديول أودو `brandzo_warehouse`). **(1) نموذج `bz.period.close`** (فترة شهرية open→closed) بترقيم `PC-%(year)s-####`، تواريخه تُضبط تلقائيًّا على حدود الشهر الجاري، وقيد `ValidationError` يمنع نهايةً قبل بداية. **(2) قائمة تحقّق مشتقّة من ٦ بنود** — لا حقول يدوية، تُقرأ كلّها بـ`sudo` من الحالة الفعلية للفترة لكل شركة: فواتير موردين مرحّلة (لا مسودّة `in_invoice`) · دفعات مسجّلة (لا فاتورة مرحّلة `not_paid/partial`) · إشعارات دائنة مرحّلة (لا مسودّة `in_refund`) · جلسات جرد مُصادَقة (لا `bz.cycle.count` في draft/in_progress) · تسويات مطبَّقة (لا جلسة عالقة في validated) · مرتجعات مقفلة (لا إذن إرجاع `stock.picking` غير منجَز). كل بند يعرض أوّل مانعٍ باسمه، والقائمة HTML حيّة تُبنى بـ`Markup` (تهرّب أسماء المستندات). **مصدر الحقيقة:** `closeChecklist` في محاكي أودو (`simReducer.js`) — نُقل منطقه وعُمِّم من مشيةٍ واحدة إلى فترةٍ حقيقية (دلالة «لا موانع مفتوحة» بدل «مستند واحد موجود»، فالشهر الفارغ يُغلق). **(3) 🔐 الحارس السابع:** `action_close` حكرٌ على المدير المالي، يحجب الإغلاق ما لم تُرفع كل الموانع (نفس رسالة المحاكي «🔒 لا تُغلق الفترة وبنودها ناقصة»)، ثم يُطبّق قفل فترة على `res.company` **دفاعيًّا** (اكتشاف أوّل حقل قفل متوفّر `fiscalyear_lock_date`/`period_lock_date`/… لأن أسماءها تغيّرت بين إصدارات أودو، وأي فشل قياسي يُثبَّت في المحادثة ولا يُسقط الإغلاق) — و`account.move._post` يُمنع من ترحيل أي قيد بتاريخ داخل فترة مقفلة (صنف `account.move` ثانٍ يتسلسل مع حارس المطابقة الثلاثية عبر `super()`)؛ و`action_reopen` يستعيد القفل السابق. **(4) الوصول:** صفّا أمان (قراءة للجميع · إنشاء/إغلاق للمدير المالي وحده) + قائمة «الإغلاق المالي» تحت المالية + عرض بنموذج وقائمة وشارة جاهزية وقائمة تحقّق ملوّنة. **(5) 10 اختبارات قبول** بـ`AccountTestInvoicingCommon` (الحزمة 42 → **52 في 11 ملفاً**): محجوب ببند ناقص → مسموح بعد رفعه · لا ترحيل في فترة مقفلة · الإغلاق للمدير المالي حصراً · اشتقاق البنود الستّة · فاتورة مسودّة/غير مدفوعة/مدفوعة تقلب البنود · إعادة الفتح · قيد الحدود. **✅ تحقّق:** أعدتُ بناء **`verify_module.py`** أشمل (نحو Python `compile` + سلامة كل XML + صحّة CSV وربطها بالنماذج + وجود كل ملفات data + **ترابط المراجع**: كل إجراء قائمة معرَّف وكل مجموعة عرض معرَّفة + 19 فحصًا خاصًّا بـS12) — **107 عناصر، صفر أخطاء** على Python 3.14. **⚠️ لم تُشغَّل الاختبارات حيًّا** (لا بيئة أودو بعد — تُشغَّل في M2). **🧨 درس محفور:** جلسة S12 السابقة **ضاعت بالكامل** لأنها لم تُلتزَم قبل انهيارها؛ هذه المرة التزمتُ محليًّا فور التحقّق. **الوثائق الثلاث محدَّثة** (README الموديول · ROADMAP §4 · هذا الملف): الباك اند **12/12** والحرّاس **سبعة**. **⚠️ ملاحظة نشر 25.07:** تشغيل Pages رقم #87 لالتزام S12 علِق بمُشغّل GitHub معطوب في «تثبيت الحزم» (Quality Gate نجحت في 41ث — العطب في بنية GitHub لا في الكود)؛ **ولا أثر له على الموقع الحيّ**: S12 لم يغيّر ملفًّا واحدًا في `src/`/`public/` فمخرَج البناء مطابق للمنشور أصلًا. علاجه إن بقي عالقًا: فتح التشغيل → Cancel → Re-run (نقرة المالك)، أو تركه — أي دفعة تالية تصطفّ وتُنشر فور زواله. **التالي:** M2 لتشغيل الـ52 اختبارًا حيًّا.

- **♻️ F4 — المرتجعات والجرد والمالية = خطة النماذج اكتملت (2026-07-24 · ✅ منشور حيًّا `9d3dcde` — النشر نجح والأنواع الخمسة 200 والمجلد المحلي مُزامَن):** الحلقة الأخيرة. **(1) خمسة مخطّطات** منقولة من الورق: `ret` (إشعار الإرجاع — قيم محسوبة، تحذير مرتجعٍ بلا سبب/إجراء/إبلاغ مورّد) · `dmg` (سند التالف — **مستقلّ لأنه قد يُكتشف بلا إرجاع**، تحذير إتلافٍ بلا محضر) · `cc` (محضر جرد دوري — **العدّ المزدوج والفرق من الثاني المؤكَّد**، ودقّة المخزون تُقاس من المعدود لا من كل الأسطر) · `adj` (سند تسوية — الأثر الصافي يفصل الزيادة عن النقصان) · `cn` (إشعار دائن). **⚠️ المجموعة نمت من «12» إلى 14** حين فصّلت F4 عنقود المرتجعات/المالية إلى خمسة مستندات كما نصّ الورق — **والصدق مع الواقع أولى من التمسّك برقمٍ قديم** (تحقّق قبل الثقة)؛ حدّثتُ تعليق السجلّ. **(2) سلسلتان قصيرتان جديدتان:** `RET→CN` و`CC→ADJ` (والتالف مفردٌ بلا سلسلة) — فصارت السلاسل أربعًا، والاشتقاق ينقل **العدّ الثاني المؤكَّد** إلى الفعلي و**الكمية المُرجعة وسعرها** للخصم. **(3) 🔒 حارس التسوية** (`adjustmentVerdict`): لا تسوية بلا محضر جردٍ **مصادَق**، ولا بندٍ يُسوّى بلا فرقٍ حقيقيّ وسببٍ موثَّق. **(4) ⚖️ حارس الإشعار الدائن** (`creditNoteVerdict`): لا خصم بلا مرتجعٍ معتمَد، ولا بكميةٍ تتجاوز المُرجَع، ولا لصنفٍ خارج المرتجع. كلاهما معروض في `ChainBar`. **🚨 قواعد الأمان حُدّثت:** الأنواع الخمسة أُضيفت، و**اعتماد ADJ وCN للمالية والمدير — لا لمن أدخلهما** (الفصل بين مَن يُدخل ومَن يعتمد)؛ وحارس الانحراف اصطاد القواعد الناقصة تلقائيًّا وأُثبت عدائيًّا (حذف `finance_manager` من ADJ أسقطه). **⚠️ خلل التُقط:** `sumColumn` غير مستعملة في `cc.js` و`cn.js` — حُذفت. **✅ تحقّق:** `audit` أخضر · **357/357 اختبارًا (+17)** · بناء 40 صفحة · لينت **122 = خطّ الأساس** (قِيس بـstash) · الأنواع الخمسة تُخدَم بلا «نوع مجهول» بصفر أخطاء، والحارسان في الحزم. **🎉 خطة النماذج الـ21 اكتملت: 14/14 نموذجًا محكومًا حيًّا** (F1→F5). **🚨 على المالك: إعادة نشر `firestore.rules`.**

- **📬 F5 — صندوق «بانتظار اعتمادي» (2026-07-24 · ✅ منشور حيًّا `c7c2adf` — بوابة الجودة والبناء والنشر نجحت، والصفحة 200، والمجلد المحلي مُزامَن):** الصندوق كان مبنيًّا وتصفيتُه بالدور صحيحة أصلًا؛ ما أُضيف هو ما يجعله **أداة متابعة لا قائمة**. **(1) `inbox.js` منطق خالص:** `ageInState` يقيس العمر من **آخر نقلة** (`updatedAt`) لا من الإنشاء — فهو عمر **الحالة** لا عمر المستند · `isStale` بمهلة لكل حالة (مُرسَل يومان · معتمَد ٥ · مسودّة أسبوعان) و**المنجَز لا يتأخّر أبدًا** · وبلا تاريخ **لا حكم** (لا نتّهم مستندًا بما لا نعرفه) · `sortByUrgency` يقدّم المتأخّر ثم **الأقدم انتظارًا لا الأحدث** · `filterDocs` ببحث عربي مطبَّع يشمل **عنوان النوع العربي** (الموظّف يكتب «استلام» لا `GRN`) · `toCsv` بعناوين عربية. **(2) الشاشة:** لقطة ستّ بطاقات · بحث وتصفية بالنوع والحالة · **تصدير CSV** · عمود «الانتظار» بشارة ⏰ وصفٍّ محمّر للمتأخّر · وأزرار البدء التسعة **مجمّعة بسلسلتها** (وارد/صادر) بدل صفٍّ مسطّح يُربك. **(3) التبويب الافتراضي صار «بانتظار اعتمادي»** — أول ما يراه الموظّف هو ما يعطّل غيره. **⚠️ خللان التُقطا:** (أ) الـBOM في مقدّمة CSV (بدونها تصل العربية مشوّهة في إكسل) **حُذفت سهوًا مرّتين** أثناء إصلاح تحذير لينت — والاختبار الذي كتبتُه لها أسقط البناء فورًا في المرّتين؛ الحلّ: كُتبت `﻿` بكود الهروب لا بالمحرف غير المرئي. (ب) `now` مثبَّت مرّة عند التحميل — لو قُرئت الساعة في كل رسم لتغيّر الترتيب تحت مؤشّر المستخدم. **✅ تحقّق:** `audit` أخضر · **340/340 اختبارًا (+18)** · بناء 40 صفحة · لينت **122 = خطّ الأساس** · كود الصندوق مُتحقَّق منه في الحزمة المبنيّة والصفحة 200. **بهذا اكتملت F1→F3+F5؛ المتبقّي F4** (مرتجعات · جرد دوري · تسويات).

- **🚚 F3 — الصرف والخروج + حارسا FEFO والبوابة (2026-07-24 · ✅ منشور حيًّا `d9b3fba` — بوابة الجودة والبناء والنشر نجحت، والأنواع الخمسة 200، والمجلد المحلي مُزامَن):** تكتمل بها دورة اليوم التشغيلي: استلام ← تخزين ← سحب ← تعبئة ← تسليم ← خروج. **(1) خمسة مخطّطات جديدة** منقولة من الورق حرفيًّا: `putaway` (الموقع والتشغيلة والصلاحية — وهي ما يعتمد عليه FEFO لاحقًا) · `pick` (الفرق والقيمة محسوبان) · `pack` (عدد الطرود يعدّ **المميّز** لا الأسطر) · `dn` (السائق واللوحة شرط التصريح) · `gp`. **السجلّ صار 9/12 نموذجًا جاهزًا.** **(2) سلسلتان لا واحدة:** الوارد `PR→PO→GRN→QC→PUTAWAY` والصادر `PICK→PACK→DN→GP` — لأن السحب يبدأ رحلةً جديدة قد تقع بعد شهور، وربطهما قسرًا كان سيجعل كل سحبٍ يدّعي أصلًا في أمر شراءٍ بعينه وهو غير صحيح. والاشتقاق ينقل **المقبول جودةً وحده** إلى التخزين (لا المستلَم كلّه)، ويورّث بيانات النقل من الإذن إلى التصريح. **(3) 🥇 حارس FEFO** (`fefoViolations`): يقارن تشغيلة كل بندٍ مسحوب بأقرب تشغيلةٍ **متاحة فعلًا** (كمية>0) — التشغيلة التي نفدت لا تُحسب معيارًا، والبند بلا تاريخ يُعدّ الأبعد فيُكشف. **(4) 🏅 حارس البوابة** (`gateVerdict`): يمنع الخروج بلا إذن · على إذنٍ لم يُعتمد · بكميةٍ تتجاوز المأذون · **وبصنفٍ لا وجود له في الإذن أصلًا** (أخطر تسريب)؛ ويُنبّه دون منع على الخروج الجزئي (مشروع — قد تُشحن على دفعتين). كلاهما معروض في `ChainBar`. **🚨 قواعد الأمان حُدّثت:** الأنواع الخمسة أُضيفت لـ`approveRoles`/`completeRoles`، **واعتماد GP لضابط البوابة والمدير — لا لأمين المخزن** الذي جهّز الشحنة (الفصل بين مَن يُعِدّ ومَن يُجيز هو الحارس نفسه). **وحارس الانحراف الذي بنيتُه في F2 أثبت قيمته: أسقط الاختبارات تلقائيًّا حين كانت القواعد تجهل الأنواع الخمسة.** **✅ تحقّق:** `audit` أخضر · **322/322 اختبارًا (+20)** · بناء 40 صفحة · لينت **122 = خطّ الأساس** · **إثبات سلوكي:** الأنواع الخمسة تُخدَم بلا «نوع مجهول» وبصفر أخطاء console، وكود السلسلة والحارسين مُتحقَّق منه في الحزم المبنيّة. **⚠️ لم يُجرَّب حيًّا** (المحرّك يحجب بلا دخول). **🚨 على المالك: إعادة نشر `firestore.rules`.**

- **📦 حلقة الأصناف اكتملت — I-د + I-هـ (2026-07-24 · ✅ منشور حيًّا `3e358f6` — بوابة الجودة والبناء والنشر نجحت، والمجلد المحلي مُزامَن):** آخر بندين في خطة ماستر الأصناف (ROADMAP §12.5). **(1) I-د الأصناف المعلّقة:** الباركود المجهول كان يُنشئ صنفًا محليًّا في `localStorage` **فيضيع مع الجلسة** — لا المدير يعلم أنه مرّ ولا الماستر يتعلّم منه. الآن يُرفع إلى `Items_Pending/{باركود مطبَّع}` **بمعرّف حتميّ** (مسحُه عشرًا = سجلّ واحد بعدّاده لا عشرة سجلّات)، والعدّاد يزيد **بالخادم** (`increment`) فلا يدهس جهازان عدّ بعضهما. `pendingModel.js` خالص: لا يدخل الماستر صنفٌ **بلا كود واسم عربي** («لا صنف اسمُه رقمُه») · الرفض بلا سبب مرفوض · المبتوت فيه لا يُعاد البتّ فيه · وتكرار المشاهدة **لا يمسّ أثر المشاهدة الأولى**. و`PendingItems.jsx` في شاشة الأصناف (للمديرَين): جدول بعدد مرّات المسح وأول من رآه، واعتمادٌ يكتب في الماستر **قبل** إقفال السجلّ (لو فشلت الكتابة بقي معلّقًا فيُعاد لا يضيع الأثران). **(2) I-هـ فطام الجرد:** `autoHydrateFromMaster()` تجلب الأصناف من الماستر تلقائيًّا عند بدء جلسة فارغة — **لا استيراد شيت في كل جلسة**؛ وما يبقى في localStorage عمدًا هو **تقدّم المسح** وحده (حالة جلسة لا كتالوج، ولها نسختها السحابية في `operations/{opId}/scans`). **🚨 قواعد الأمان:** أُضيفت `Items_Pending` — الكتابة الأولى لأي مصادق (الماسح موظّف ميداني) لكن **البتّ للمديرَين وحدهما**، ولا حذف. **⚠️ خلل التُقط أثناء اللينت:** استعملتُ `currentWarehouse` وهو **غير موجود في الشاشة أصلًا** — كان سيرمي عند أول باركود مجهول؛ حُذف الحقل. **✅ تحقّق:** `audit` أخضر · **302/302 اختبارًا (+11)** · بناء 40 صفحة · لينت **122 = خطّ الأساس بالضبط** (قِيس بـ`git stash` قبل/بعد) · **إثبات سلوكي على بناء الإنتاج:** شاشة الجرد تُحمَّل بصفر أخطاء والجسر كامل (`registerUnknown` · `lookup` · `fetchAll` · `autoHydrateFromMaster`)، ولوحة المراجعة مُتحقَّق منها في الحزمة المبنيّة (شاشة الأصناف يحجبها حارسها الذاتي بلا حساب). **🚨 على المالك: إعادة نشر `firestore.rules`** (قواعد `Items_Pending` + قواعد F2). **بهذا اكتملت حلقة الأصناف كاملةً: I-أ ← I-و.**

- **🔗 F2 — سلسلة الشراء والمطابقة الثلاثية (2026-07-23 · ✅ منشور حيًّا `df86cdc` — بوابة الجودة والبناء والنشر نجحت، والأنواع الأربعة تُخدَم 200، وكود السلسلة والمطابقة والمخطّطات الثلاثة مُتحقَّق منه داخل الحزم على الموقع الحيّ):** أكبر فجوة متبقّية في محرّك المستندات سُدّت: «لا ترابط بين المستندات ⇒ المطابقة الثلاثية يدوية» (ROADMAP §11.1). **(1) ثلاثة مخطّطات جديدة** منقولة من الورق حرفيًّا: `pr.js` (طلب شراء — الإجمالي التقديري محسوب وتحذير تجاوز الميزانية) · `po.js` (أمر شراء — الإجمالي والخصم والصافي محسوبة، والصافي لا يصير سالبًا، وتحذير بند بكمية بلا سعر) · `qc.js` (تقرير جودة مستقلّ — نسبة الرفض محسوبة، وتحذيرات: مرفوض بلا سبب · المقبول+المرفوض ≠ المفحوص · قرار رفض بلا رقم NCR). صار السجلّ **4/12 نموذجًا جاهزًا**. **(2) `chain.js` منطق خالص:** `deriveDocument` يشتقّ المستند التالي من سابقه بالبنود والمراجع — و**لا يشتقّ إلا من معتمَد** (لا يُبنى التزام على ما لم يُعتمد)، و`links` تتراكم فتصل السلسلة كاملة إلى QC؛ و**`threeWayMatch`** يقارن المطلوب↔المستلَم↔المقبول صنفًا صنفًا بست حالات (مطابق · نقص · زيادة · مرفوض جودةً · **خارج أمر الشراء** · بانتظار الفحص) بتسامح 2% أو وحدة، **ولا تُغلق مطابقة بلا تقرير جودة**. **(3) `ChainBar.jsx`:** شريط يعرض مسار السلسلة بأرقامها وحالاتها + زر «أنشئ التالي» (يظهر لأصحاب دور الإنشاء فقط وبعد الاعتماد) + لوحة مطابقة قابلة للطيّ. **(4) `createNextInChain` + `fetchChainDocuments`** في الخدمة، بقيد تدقيق في المستندين معًا (المولود وأصله). **🚨 قواعد الأمان حُدّثت — تلزم إعادة نشرها:** `approveRoles`/`completeRoles` كانتا تعرفان GRN وحدها، فكان اعتماد الأنواع الثلاثة سيرتدّ بـ`permission-denied`؛ **واختبار الانحراف القديم لم يكن ليلتقط ذلك** (كان يكتفي بوجود اسم الدور في أي موضع) فأُعيدت كتابته ليقرأ ما تُعيده دالتا القواعد **لكل نوع جاهز** ويطابقه بمخطّطه دورًا بدور — **وأُثبت عدائيًّا: حذف `finance_manager` من اعتماد PO أسقط الاختبار فورًا، والاستعادة أعادته أخضر**. **✅ تحقّق:** `audit` أخضر · **291/291 اختبارًا (+20)** · بناء 40 صفحة · لينت 122 = خطّ الأساس · الصفحات الأربع تُخدَم 200 وكود السلسلة والمطابقة حاضر في الحزم المبنيّة. **⚠️ لم يُجرَّب حيًّا:** المحرّك يحجب بلا تسجيل دخول (سلوكه الصحيح) فلا سبيل لفحص الواجهة بلا حساب — يحتاج تجربة المالك: PR ← اعتماد ← «أنشئ أمر الشراء» ← … ← QC، ومراقبة لوحة المطابقة. **🚨 على المالك: إعادة نشر `firestore.rules`.**

- **📘 دفعة ٣ الاجتماعات — «النظام الإداري التعاوني الموحّد» (2026-07-23 · ✅ منشور حيًّا `b24e609` — بوابة الجودة والبناء والنشر نجحت والصفحة 200 والزرّ واللوحة وخانة الاعتماد حيّة):** المخرج الذي نصّ عليه خطاب المالك حرفيًّا («تُعدَّ النسخة النهائية وتُرفع إلى الإدارة العامة لاعتمادها والعمل بموجبها») — **وثيقة نهائية لا ملخّص**. **(1) منطق خالص** في `meetingsModel.js`: `commitments` (الالتزامات مرتّبةً بالموعد وما لا موعد له في الآخر) · `escalations` · `deferrals` · `sourceRegister` (لكل اجتماع رقم محضره وحصيلته — **فكل بند في التقرير مردودٌ إلى محضر مرقَّم**) · `systemReportVerdict` **يفرّق بين مانع وتنبيه**: يمنع (اجتماع بلا محضر · بند غير محسوم · قرار بلا نصّ · لا قرار إطلاقًا) ويُنبّه ولا يمنع (قرار بلا مسؤول أو موعد · محضر صادر غير موقّع) · `consolidate` يجمعها في بنية واحدة. **(2) مخرج رسمي** `systemReportHtml` بخمسة أقسام: سجلّ المحاضر المرجعية · القرارات موزَّعةً على الإدارات · خطة التنفيذ بالمواعيد · بنود مرفوعة للإدارة العامة · بنود مؤجَّلة — مع **خانة اعتماد الإدارة العامة** و**التوقيعين المعتمدين** (أُضيف `reportSignatories` للبذرة: البرشي + الباش — لأن هذه وثيقة تصدرها إدارتنا لا محضرًا ثنائيًّا، واختبارٌ يحرس وجودهما). **(3) الترقيم** `SYS-2026-####` من نفس عدّاد `counters`، والسجلّ في `preparatory_meetings/__system_report__` — **قواعد الأمان المنشورة تغطّيه أصلًا فلا معلّق على المالك**؛ الرقم يُحجز مرّة ولا يُحرق، وتُثبَّت لقطة المصادر وقت الإصدار. **(4) الصفحة:** زر «📘 التقرير المجمّع» + لوحة جاهزية (أخضر/أحمر/أصفر) + معاينة + طباعة + إصدار، وتتحدّث مع كل تغيّر ما دامت مفتوحة. **✅ تحقّق:** 271/271 اختبارًا (**+19**: 11 للمنطق و8 للمخرج) · بناء 40 صفحة · لينت 122 = خطّ الأساس · `audit` أخضر · **إثبات سلوكي على بناء الإنتاج** (بردّ حزمة AuthGate بوحدة فارغة — الحزمة منفصلة عن حزمة الصفحة): الزرّ واللوحة والمعاينة بأقسامها الخمسة وسجلّ المصادر السبعة والبطاقات الستّ و**التوقيعان محمَّلان فعليًّا** والطباعة تملأ `.doc.report`، واللوحة **تمنع الإصدار بحقّ** اليوم («7 اجتماعات لم تصدر محاضرها») ولا تنهار عند تعذّر قراءة Firestore. **الاستخدام:** بعد عقد كل اجتماع وإصدار محضره، افتح «التقرير المجمّع» ← ستنقلب اللوحة خضراء ← «إصدار رسمي» ← اطبع وارفع للإدارة العامة. **بهذا اكتملت خطة الاجتماعات الثلاثية (هيكل ← اجتماعات ← تقرير مجمّع).**

- **🔍 التدقيق الشامل وإحكام الصلاحيات (2026-07-23 · ✅ منشور حيًّا `7407c0b` — بوابة الجودة نجحت في أول تشغيل والصفحات الـ21 ترد 200 · **وأكّد المالك اجتياز التدفّقات السبعة كلّها «100% تعمل»**):** طلب المالك يقينًا أن البوابة تعمل بالكامل وأن لا شيء سقط عبر عشرات الجلسات، وأن تُوثَّق النتيجة في ROADMAP وREADME وملف المطوّر. **الفحص الفعلي للكود كشف خمس فجوات حقيقية وسُدّت كلّها:** (1) **خط النشر لم يكن يشغّل اختبارًا ولا لينت** — يبني وينشر فقط، فانحدارٌ يصل للموقع الحيّ ⇒ وظيفة `Quality Gate` تسبق البناء بـ`needs` (اختبارات + تدقيق **حاجزان صلبان**، لينت تقريريّ لخطّ أساسه 122). (2) **ثغرة صلاحيات:** `pageAccess.js` كان يقيّد `department_user` و`fleet` فقط و«غياب الدور = بلا تقييد إطلاقًا»، فكان أمين المخزن أو مفتّش الجودة **يفتح `/dashboard/users` أو `recruitment` أو `wh-manager-eval` بكتابة الرابط** (القائمة تُخفي الرابط لا أكثر) ⇒ **`src/services/auth/navCatalog.js` مصدرًا واحدًا** نُقلت إليه بنية القائمة من `DashboardLayout.astro` ويقرأه الحارس والقائمة والتدقيق؛ و`pageAccess` يشتقّ `canSeeGroup ∧ canSeeItem` مع **منع افتراضي** لما لا يعرفه الكتالوج و**الدور المجهول يسقط على `viewer`**؛ و`AuthGate` سرى على **كل** الأدوار بـ`landingPathFor` (يهبط بالدور المركّز على أوّل صفحة في **مجموعته** لا في الكتالوج كلّه). (3) **ادّعاء توثيقيّ كاذب** «16/16 اختبارًا لتقييد القائمة» بلا ملف اختبار أصلًا ⇒ `navAccess.test.js` مؤلَّف (14 حالة) + `pageAccess.test.js` أُعيدت كتابته (16 حالة) **واختبارا انحراف** يفشلان عند أي صفحة خارج الكتالوج أو رابط بلا صفحة. (4) **صفحة يتيمة** `تقرير-الدورة-المستندية-الكامل` ⇒ رُبطت في مركز التقارير (+3 صفحات تقارير كانت في البطاقات لا القائمة ⇒ 48 عنصرًا). (5) **الوثائق متأخّرة 7–15 يومًا** ⇒ الجذر صار **أربعة ملفات**: README (أُعيد كتابته) · ROADMAP (§1 و§5.2 و§7 و§9 الجدول المعتمد و**§14 واجهة البوابة** و**§15 نتيجة التدقيق**، وحُذفت كل «معلّقات المالك») · **`DEVELOPER_GUIDE.md` جديد** (تشغيل في ساعة · المعمارية وقاعدتها الذهبية · نظام الوصول · محرّك المستندات · الحرّاس الستة · مصادر الحقيقة الواحدة · **سبعة فخاخ موثّقة** · خريطة الاختبارات) · SESSION_HANDOFF؛ والمراجع في `docs/` والمهملات في `docs/archive/` بملف يشرح خلَفَها (**نقل لا حذف**). **🛡️ حارس دائم:** `npm run audit` (`scripts/audit-portal.mjs`) يفحص: يتيمة · روابط مكسورة · ملفات public مفقودة · **مجموعات Firestore بلا قاعدة** · تسريب صلاحيات — **واختُبر بزرع عطبين متعمّدين فالتقطهما** ثم عاد نظيفًا. **🐞 خلل استخدام اصطيد أثناء التحقّق:** زرّ ☰ كان **يقفز 175px هربًا من المؤشّر** لحظة التمرير (لأن التمدّد ينقله لطرف الرأس الآخر) فيطارده المستخدم ولا يصيبه — عولج بتقديمه في ترتيب الرأس (فيثبت على الحافة اليمنى التي لا تتحرّك) + توحيد الحشو الأيمن + `self-start` (فلا يتأثر بتغيّر ارتفاع الرأس) ⇒ **dx=0 و dy=0 والنقرة الأولى تعمل**. **✅ تحقّق:** `npm run audit` أخضر · **252/252 اختبارًا** · بناء 40 صفحة · لينت **122 = خطّ الأساس بالضبط** (صفر جديد) · **إثبات سلوكي بمتصفّح headless على بناء الإنتاج:** المصغّر 76px ↔ المثبّت 256px والهوامش تتبعه · الزرّ يقلب الحالة ويحفظها ويتذكّرها بعد التحميل · البحث العربي المطبَّع (7 استعلامات) بالإبراز والعدّاد و«لا نتائج» والمسح · 9 حالات صلاحية دورًا-بدور (الحركة وأمين المخزن ومفتّش الجودة والمشاهد يُمنعون، والأدمن يمرّ) · الموبايل بلا انحدار. **🚨 على المالك:** **قائمة الفحص `BFP-SCM-PR-2026-003`** (HTML+PDF بالهوية والتوقيعين، 7 تدفّقات و34 خطوة) — سابعها يختبر الإصلاح الأمني بكتابة `/dashboard/users` بدور غير إداري.

- **✅ P0 صُفّيت بالكامل (المالك، 2026-07-23) + إصلاح خلل الاستيراد الصامت:** بعد تقرير الحالة والأولويات (`BFP-SCM-PR-2026-002` في مجلد المراسلات، HTML+PDF) أكّد المالك إنجاز المعلّقات السبعة كلها: **نشر `firestore.rules` الأحدث** (سلاسل الإمداد + مركبات + هيكل + اجتماعات + توظيف + أرصدة) · **تجربة GRN الحيّة نجحت** ويريد إكمال باقي خطة المستندات · **استيراد كتالوج الـ1017 صنفًا نجح** · **رُفعت بذرة المركبات** · **رُوجعت مسودّات البنود الـ36 والعقد التسع** · **قرار التوظيف صدر** · DNS النطاق **مؤجَّل**. ⇒ لم يعد شيء معلّقًا على المالك؛ الخطة المعتمدة: دفعة ٣ اجتماعات ← F2 ← حلقة الأصناف ← F3+F5 ← M2/S12 (التقرير 002). **وأُصلح الخلل المعلَّم:** `upsertItems` كان يُسقط الصفوف بلا كود صامتًا بينما المعاينة تعدّها «جديدة» — الآن `analyzeItemsFile` يفرزها في `plan.skipped` وتُعرض ببطاقة برتقالية في `ItemsImport.jsx` (الاسم + الباركود)، و`upsertItems` يُعيد `skipped` (استيراد الأرصدة كان سليمًا أصلًا بنفس النمط). 228/228 · بناء 40 صفحة · لينت نظيف (`b9c31ed`). **✅ وثّق (23.07):** الثلاثة محدَّثة ومتحقَّق منها بمتصفّح فعلي (صفر أخطاء console · لا تجاوز أفقي · التوقيعان يظهران في الثلاثة): (1) **الموجز اليومي 23.07** — بندان جديدان (واجهة البوابة الذكية · تقرير الحالة وتشغيل الأنظمة الستة) في قسم «إنجازات اليوم الخميس» + غلاف محدَّث + PDF معاد التوليد (2.9MB — سيلتقطه إرسال الواتساب 16:00). (2) **الشامل W29** — محور سابع «تشغيل المنظومة بالكامل وواجهة البوابة الذكية» + بطاقة الخميس في الخط الزمني (7 أيام) + الغلاف 17—23 يوليو. (3) **الموجز W29** — بطاقة 07 + الغلاف سبعة محاور/سبعة أيام + أثر الأسبوع. **وصدر تقرير رسمي جديد:** `تقرير-حالة-المشروع-والأولويات-BFP-SCM-PR-2026-002` (HTML+PDF في جذر مجلد المراسلات) — الحالة والمتبقّي ومصفوفة الأولويات والجدول الزمني حتى سبتمبر.

- **🎛️ واجهة البوابة التفاعلية — الشريط الجانبي الذكي + صدارة اللوحة (2026-07-23 · ✅ منشور حيًّا `70ad109` — deploy success والصفحات والتوقيعان 200):** بطلب المالك (مرحلة تحسين تجربة المستخدم). **(1) الشريط الجانبي على سطح المكتب** (`DashboardLayout.astro`): وضع افتراضي **مصغّر 76px (أيقونات فقط)** يتمدّد بسلاسة إلى 256px عند التمرير فوقه (يطفو فوق المحتوى) أو **بزر ☰ أعلى الشريط** الذي يثبّت التمدد ويدفع المحتوى — الحالة تُحفظ (`bz-sb-pinned-v1` في localStorage) وتُقرأ **قبل الرسم** فلا وميض؛ الموبايل لم يُمسّ (كل القواعد خلف `min-width:768px`). **(2) بحث القائمة:** تطبيع عربي (أ/إ/آ→ا · ة→ه · ى→ي · حذف التشكيل — «الاصناف» يجد «الأصناف») + بحث بأسماء المجموعات («سلاسل» يُظهر عناصر مجموعتها الخمسة) + إبراز المطابَق `<mark>` + عدّاد نتائج + زر مسح ✕ + Escape يمسح قبل أن يغلق. **(3) لوحة التحكم** (`index.astro`): قسم صدارة «🚀 الأنظمة الرئيسية» بالبطاقات الثماني (توظيف · مستندات · أصناف · متابعة عمليات · سلاسل إمداد · اجتماعات · مهام · هيكل workflows) — بُنيت `li[data-item-roles]` **فيرثها إخفاء RoleNav حسب الدور تلقائيًّا** (4 مقيّدة بالمديرَين)، وكل الأقسام القديمة باقية أسفلها (صفر حذف) وتخضع لزر التخصيص. **(4)** أزرار دخول (أسطول · صيانة · عُهد · مركبات) داخل `supply-chain.astro`. **(5) التوقيعان المعتمدان PNG رُفعا** إلى `public/identity/signatures/` (قرار المالك 23.07) و`sigBlock` في `meetingsView.js` يُدرج صورة التوقيع تلقائيًّا حين يطابق اسم الموقّع «البرشي»→signature-2 أو «الباش»→signature-1 (المحضر والخطاب، عبر `opts.assetBase`). **✅ تحقّق:** 228/228 · بناء 40 صفحة · لينت الملفات المعدّلة نظيف · **تحقّق سلوكي على بناء الإنتاج** (iframe بلا سكربتات الحارس): مصغّر 76px/هوامش 76px/نصوص مخفية ↔ مثبّت 256px/نصوص ظاهرة · زر ☰ يقلب الحالة والتخزين وaria-pressed · البحث المطبَّع والإبراز والعدّاد والمسح كلها مُقاسة · بطاقات الصدارة 8 (4 بأدوار) · أزرار sc-nav في dist والخادم (88KB) · اختبار Node للتوقيعات: صورتان صحيحتان والطرف الآخر بلا صورة. (⚠️ أثناء التحقق: كاش «العامل الخفي» لجلسة قديمة قدّم صفحة supply-chain قديمة داخل المتصفح — ليس خللًا؛ التحديث الذاتي يعالجها.) **التالي:** «ارفع» بعد موافقة المالك، ثم بقية مراحل تحسين الواجهة.

- **🤝 نظام الاجتماعات التحضيرية (2026-07-22 · دفعة ٢ من ٣ · ✅ منشور حيًّا `5cea70b` — deploy success والصفحات 200):** أداة تُشغَّل **داخل غرفة الاجتماع** مع الإدارات السبع. **قرارات المالك الأربعة:** (أ) أصوغ أنا مسودّة «ما نطلبه» للبنود الـ36 كلها · (ب) الموقّعون **قائمة قابلة للتعديل** لكل اجتماع (لا عدد ثابت) · (ج) **نعم** لتوليد خطاب الدعوة لكل إدارة · (د) التفريغ الصوتي لاحقًا. **أُنجز:** (1) **`src/data/meetings-seed.json`** — 7 اجتماعات و**36 بندًا بعناوين منقولة حرفيًّا من خطاب المالك** (مالية 10 · موارد بشرية 6 · تقنية 5 · بلاي تايم 3 · تسويق 4 · جودة 4 · حوكمة 4) + نصوص الخطاب الرسمي حرفيًّا؛ ولكل بند مسودّة ثلاثية **ما نطلبه · لماذا · ما يخصّ الطرف الآخر** مستمدّة من المرجع التشغيلي وما بناه النظام فعلًا، معلَّمة `draft:true`. (2) **`services/meetings/meetingsModel.js`** — **الأجندة بذرة ثابتة والمحضر طبقة فوقها**: `mergeMeeting` يدمجهما فتصحيحُ صياغةٍ في البذرة لا يمحو ما سُجّل، والبند اليتيم (حُذف عنوانه) يبقى؛ آلة حالات (مجدول→منعقد→صادر→موقّع، ولا خروج من «موقّع») + **`minutesVerdict`**: لا محضر بلا تاريخ وحاضرين وموقّعَين **من الطرفين** وكل بند محسوم وكل «متفق عليه» له نصّ قرار. (3) **`meetingsView.js`** خالص — الشرائح والمحضر والخطاب. (4) **`meetingsService.js`** — `preparatory_meetings/{M01..M07}` بمعرّف حتميّ، حفظ لحظي مؤجَّل 700ms، ترقيم **`MOM-2026-####`** للمحضر و**`LTR-2026-####`** للخطاب من عدّاد counters (الرقم يُحجز مرّة ولا يُحرق ثانيةً)، وقواعد Firestore **تمنع تغيير الرقم بعد حجزه**. (5) **`/dashboard/meetings`** — 7 بطاقات بتقدّمها · وضع عرض ملء الشاشة (أسهم لوحة المفاتيح) · محضر حيّ (5 حالات للبند · نقاش · قرار · مسؤولان · موعد) · حاضرون وموقّعون بعدد حرّ · إصدار واعتماد وطباعة المحضر · خطاب الدعوة. **✅ تحقّق:** **228/228 اختبارًا** (+45 جديدًا) · بناء 40 صفحة · لينت الملفات الجديدة نظيف · **تحقّق حيّ على بناء الإنتاج:** الصفحة تُحمَّل بصفر أخطاء console · تبديل الاجتماعات يعمل · **وضع العرض يعرض الحقول الثلاثة باسم الإدارة الصحيح والتنقّل يعمل** · بنية البند كاملة. **🚨 معلّق على المالك:** (أ) **نشر `firestore.rules`** (قواعد `preparatory_meetings` + `org_structure`). (ب) مراجعة مسودّات الـ36 بندًا وتعديلها. **التالي: دفعة ٣** = التقرير النهائي المجمّع («النظام الإداري التعاوني الموحّد») — `allAgreements()` جاهزة كبذرة له.

- **🏛️ الهيكل التنظيمي — المصدر الواحد + الصفحة التفاعلية (2026-07-22 · دفعة ١ من ٣ · ✅ منشورة حيًّا — PR #8 دمجه المالك والتقريران المولَّدان 200):** بطلب المالك (اجتماعات قادمة مع سبع إدارات) أُعيد تشكيل الهيكل: **إدارتان عُليا** — «إدارة السلاسل والإمداد والمخازن» (محمد البرشي) وتحتها مباشرة «إدارة الخدمات اللوجستية والخدمية» (رمزي الباش) — **والباقي أقسام ووحدات**. **الاكتشاف الذي غيّر الخطة:** الهيكل كان مكتوبًا يدويًّا في **أربعة أماكن وقد انحرفت فعلًا** (التقريران كانا يعرضان «قسم العمليات اللوجستية» بخمس وحدات مقابل «مدير العمليات اللوجستية» بأربع). **القرار (المالك):** مصدر واحد يولّدهما. **أُنجز:** (1) **`src/data/org-structure.json`** = المصدر الواحد (38 عقدة · 36 بطاقة وصف · 11 صف كادر · RACI · أدوار أودو) — كل عقدة تحمل `source`: `report` (من التقرير المعتمد) · `owner` (نصّ المالك) · **`proposed` (9 عقد اقترحتُها أنا — تظهر بشارة «مقترح» وإطار متقطّع ليقرّرها المالك)**. (2) **`services/org/orgModel.js`** منطق خالص (تسطيح · تحقق · إضافة/حذف/نقل/ترتيب لا تُفسد الشجرة · **`diffTrees` قبل/بعد**) + **`orgView.js`** بناء HTML خالص — **20 + 17 اختبارًا**. (3) **`/dashboard/org-structure`**: شجرة مسنّنة + مخطط هرمي · تعديل كامل · **حفظ سحابي بإصدارات append-only** (`org_structure/current` + `versions/{n}`) · بطاقة «ما تغيّر عن الأساس» · **وضع عرض ملء الشاشة للاجتماع** · طباعة A4 أفقي بالتوقيعين · تصدير/استيراد JSON · **مرشّح «إظهار المقترحات»** (يعرض المعتمد ثم يكشف المقترح للنقاش). (4) **`scripts/build-org.mjs`** يولّد من المصدر: الشجرة والمساندة وjobs وorgTable في **تقرير هيكل الوظائف**، والشجرة والمساندة وجدول الكادر في **المرجع التشغيلي**، و**jobsCatalog.js** — بعلامات `<!-- ORG:*:START/END -->` و**توليد حتميّ** (تشغيلان = بصمة واحدة). **✅ تحقّق:** 183/183 اختبارًا (**كان 144/145 — الاختبار الأحمر المزمن كان يتوقّع 18 وظيفة والكتالوج 36؛ صار يُقاس من المصدر**) · بناء 39 صفحة · لينت الملفات الجديدة نظيف (0 من 11 ملفًّا بها مشاكل) · **مقارنة حقلًا بحقل: 32 بطاقة من 36 مطابقة بايتًا-ببايت و4 فقط تغيّرت (وهي المعاد تسميتها عمدًا)** · **تحقّق حيّ على بناء الإنتاج:** التقريران يعرضان الشجرة الجديدة (38 عقدة · المديران بأسمائهما) بصفر أخطاء console، والصفحة التفاعلية تعمل (العدّادات 2/7/18/11/9 · تبديل قائمة↔مخطط · مرشّح المقترحات يُخفي التسعة). **🚨 معلّق على المالك:** (أ) **نشر `firestore.rules`** (قواعد `org_structure`) — بدونها لا حفظ. (ب) **مراجعة العقد التسع المقترحة** واعتمادها أو حذفها في الصفحة. (ج) قرار: التواقيع PNG **لم تُضف للمستودع** (عام) — الطباعة تُخرج سطر توقيع بالاسم والمسمّى. **التالي: دفعة ٢** = نظام الاجتماعات السبعة (أجندة → وضع عرض → محضر حيّ → **محضر رسمي موقّع PDF**) ثم **دفعة ٣** = التقرير النهائي المجمّع.

- **🔗 نظام سلاسل الإمداد المتكامل (2026-07-22 · ✅ منشور حيًّا `fafb5ec` — deploy success والصفحات الأربع 200):** بطلب المالك («حان الوقت لنكون نظام سلاسل إمداد كامل») بُنيت ملاحق المرجع التشغيلي **م-٢ + م-٣ + ٤-٣ + ٥-١** كاملة فوق البنية القائمة — **4 صفحات جديدة** في مجموعة القائمة المعاد تسميتها «سلاسل الإمداد والحركة»: (1) **`/dashboard/fleet-operations` عمليات الأسطول:** دورة الرحلة Dispatch-to-POD الرسمية (قيد التحضير → تصريح بوابة برقم **`TRIP-2026-####`** → في الطريق → POD **بتوقيع إلكتروني على كانفس + صورة بضاعة مضغوطة** → إقفال بمطابقة الوقود) + **بروتوكول الاستلام والتسليم اليومي** (معرّف حتمي `تاريخ__وردية` على `vehicles/{id}/handovers`، يمنع: وقود دون الربع · حرارة تبريد خارج 2–4°م · بندًا غير مفحوص — **وهو شرط إصدار تصريح البوابة**، الحارس يقرأ `lastHandover` من بطاقة المركبة) + سجل وقود append-only بكشف انحراف >15% تلقائيًّا + مناطق التوزيع A/B/C حرفيًّا من ٢-٤. (2) **`/dashboard/maintenance-center` مركز الصيانة:** أصول بفئات م-٣ الخمس (`assets/{كود}` حتمي) + أوامر شغل **`WO-2026-####`** بدورة مسودة→اعتماد→جارٍ→تم→عودة للخدمة/تكهين (الأولوية العالية توقف الأصل فورًا؛ الوقائية المنجزة تعيد جدولة استحقاقها تلقائيًّا) + لوحة استحقاق وقائي بالتاريخ والكيلومترات (تحذير 7 أيام/500كم) + MTTR. (3) **`/dashboard/custody` العُهد العينية (للمديرَين):** سند **`CUST-2026-####`** يُطبع بإقرار قانوني وتوقيعين + إغلاق بإرجاع/فقد/إتلاف مع قيمة الخصم الموثقة + تجميع لكل موظف. (4) **`/dashboard/supply-chain` اللوحة التنفيذية (للمديرَين):** 8 مؤشرات لحظية (أسطول فوري · POD% هدف 100 · OTIF هدف ≥95 · وقود شاذ · WO مفتوحة · MTTR · عُهد · **صلاحيات ≤7/30 يومًا من مخزن balances القائم**) + أداء المناطق + «يتطلب تدخلًا الآن». **المعمارية:** منطق خالص مُختبَر في `services/fleet/fleetModel.js` + `maintenance/workOrderModel.js` + `custody/custodyModel.js` (آلات حالات لا تقبل القفز) وخدمات سحابية بنمط vehiclesService (لا حذف · ترقيم counters الرسمي · سجلات تدقيق events). **الوصول:** دور fleet توسّع لثلاث صفحات تشغيلية (pageAccess + اختبار جديد)؛ العُهد واللوحة للمديرَين. **تحقّق:** 144/145 اختبارًا (الفاشل قائم قبلُ بالتوظيف · +34 اختبارًا جديدًا) · بناء 38 صفحة · لينت الملفات الجديدة نظيف · **تحقّق حيّ على بناء الإنتاج: الصفحات الأربع تُحمَّل والحارس يحوّل غير المسجّل وصفر أخطاء console**. **🚨 معلّق على المالك:** (أ) **نشر `firestore.rules` المحدَّثة** (trips + fuel/events + handovers + assets + work_orders + custodies) — بدونها لا كتابة. (ب) تجربة الدورة كاملة حيًّا: تسليم يومي → رحلة → تصريح → POD → إقفال. (⚠️ تذكير كاش Pages: تحديث قسري `Ctrl+Shift+R` إن لم تظهر الصفحات الجديدة.) **التالي المقترح:** ربط الرحلة ببنود مستند صرف حقيقي (F2) + Gate Pass بمسح QR للحارس + تقرير شهري لتكلفة الكيلومتر.

- **🔑 زرّ «نسيت كلمة المرور؟» + وثّق W29 (2026-07-21/22 · ✅ منشور `1181bfa` وموثّق):** المالك نصّب البرنامج وجرّب الدخول فرُفض — التشخيص (بسؤالين): نفس الفشل في متصفح خاص ⇒ **خطأ كتابة كلمة المرور** (لا ملء تلقائي في WebView2 النظيف)، وتأكد بعدها أنها كانت خطأ إدخال والدخول نجح. أثناء التشخيص أُضيفت **استعادة كلمة المرور الذاتية**: `sendPasswordReset` في authService + زرّ أسفل نموذج الدخول (رسالة نجاح خضراء + تنبيه «غير المرغوب» + أخطاء عربية) — بوابات خضراء (110/111 · لينت نظيف) ونُشر `1181bfa` (deploy success). **وثّق (بتاريخ الغد بطلب المالك):** إنجازات PWA + setup.exe + الاستعادة الذاتية وُثّقت في الثلاثة: (1) موجز يومي جديد **«الإنجاز اليومي — الأربعاء 22.07.2026»** (3 بنود · WLR-W29/D6) + PDF بهوية Brandzo (979KB) — سيلتقطه إرسال الواتساب 16:00؛ (2) **الشامل W29**: الغلاف 17—22 يوليو · 6 أيام · 3 بنود جديدة في محور المنظومة الرقمية + بند إطلاق المركبات (الثلاثاء) + بطاقتا خط زمني (الثلاثاء/الأربعاء) + مقاييس «3 منصّات تشغيل»؛ (3) **الموجز W29**: الفترة والأيام وبطاقة 03 وأثر الأسبوع («ثلاث منصّات لنظام واحد تعمل حتى دون إنترنت»).

- **💿 برنامج تنصيب ويندوز (setup.exe) — قشرة Tauri حول النظام الحيّ (2026-07-21 · ✅ مبنيّ ومنشور في Releases، أمر `68f2185` · **جرّبه المالك: نُصّب ودخل بنجاح**):** بطلب المالك («برنامج ينصّب من ملف») أُنشئ `src-tauri/` (Cargo.toml · main.rs بأدنى كود · tauri.conf.json يفتح `frontendDist` = رابط الموقع الحيّ مباشرة · `icons/icon.ico` مولَّد بـ`scripts/generate-ico.mjs` من نفس هوية الـPWA) + workflow **`desktop-installer.yml`**: يبني على `windows-latest` عبر `tauri-action` وينشر **Brandzo Hub_1.0.0_x64-setup.exe** في صفحة **Releases** بوسم `desktop-v1.0.0` — **صفر أدوات على جهاز المالك**. المعمارية: WebView2 يشغّل «العامل الخفي» نفسه ⇒ البرنامج المنصَّب يرث الأوفلاين والتحديث الذاتي من الويب (لا يُعاد بناؤه إلا عند تغيير القشرة — الـworkflow مقيّد بمسار `src-tauri/**`). **ملاحظات صريحة:** أول تشغيل يحتاج نت · تحذير SmartScreen لأن الملف غير موقّع (More info → Run anyway مرة واحدة) · واجهة المثبِّت إنجليزية (تقليل مخاطر NSIS/Arabic في أول إصدار) · روابط `target=_blank` (التقارير) داخل القشرة سلوكها غير مُجرَّب — تُختبر بعد أول تنصيب. **✅ تحقّق:** أول بناء CI نجح والإصدار منشور — `Brandzo.Hub_1.0.0_x64-setup.exe` (1.8MB) في https://github.com/albarshi996/warehouse-system/releases — نُزّل وفُحص (رأس MZ + توقيع Nullsoft/NSIS سليمان). **المتبقّي على المالك:** تحميل الملف وتنصيبه وتجربة الدخول ثم الأوفلاين، والإفادة عن سلوك روابط التقارير (`_blank`) داخل القشرة.

- **📲 PWA — النظام صار يُثبَّت ويعمل دون اتصال (2026-07-21 · ✅ منشور حيًّا `c8c7f9f` والملفات 200):** بطلب المالك («سحابي + يتنصّب على الأجهزة + يعمل بلا نت») أُضيفت طبقة PWA كاملة بلا مساس بأي ميزة: (1) **`public/manifest.webmanifest`** (عربي RTL · `standalone` · هوية Navy `#1A1A2E`) + **أيقونات Brandzo** مولَّدة بـ`scripts/generate-icons.mjs` (puppeteer، تُشغَّل يدويًّا عند تغيير الهوية — البناء لا يعتمد عليها). (2) **تكامل `src/integrations/pwa-sw.mjs`** يولّد «العامل الخفي» `sw.js` بعد كل بناء بمسح dist الفعلي: تخزين مسبق لكل الصفحات + حزم `_astro` + الأيقونات (139 ملفًّا) · **بصمة بناء من محتوى الملفات** (حتميّة — بناءان متطابقان = نفس البصمة، أي تعديل = بصمة جديدة ⇒ تحديث ذاتي بلا إشعارات كاذبة) · استراتيجيات: صفحات network-first (يتفادى كاش Pages العشر دقائق) · أصول cache-first · خطوط Google stale-while-revalidate · **Firestore لا يُعترَض إطلاقًا** (كاشه `persistentLocalCache` قائم). (3) **`src/components/pwa/PwaHead.astro`** يُدرج الروابط ويسجّل العامل مع زرّ «🔄 نسخة جديدة متاحة → تحديث» — محقون في **الرؤوس الأربعة كلها**: `DashboardLayout` + `index` + `login` + **`stock-operations` (رأس مستقلّ — اكتشفه فحص التغطية 34/34)**. **خللان اصطيدا أثناء التحقّق الحيّ:** BASE_URL بلا شرطة ختامية في Astro 6 ⇒ روابط ملتصقة (عولج بالتطبيع + اشتقاق مسار SW من رابط الـmanifest نفسه). **تحقّق حيّ ببناء الإنتاج:** SW «activated» يتحكّم بالنطاق · كاش 139 · **الخادم أُطفئ فعليًّا وصفحات لم تُزَر رجعت 200 من الكاش والصفحة رُسمت كاملة** · manifest وأيقوناته الأربع 200 · بصمة حتميّة مثبتة ببناءين · 110/111 (الفاشل قائم قبلُ بالتوظيف) · 34 صفحة · لينت 84=84 صفر جديد. **القيد الوحيد:** كل جهاز يحتاج فتح التطبيق مرّة واحدة بالنت (دخول + أول مزامنة) ثم يعمل أوفلاين للأبد. **بعد النشر:** التثبيت من شريط العنوان (كمبيوتر) أو «أضف إلى الشاشة الرئيسية» (جوال).

- **📦 كتالوج الأصناف الأول جاهز للاستيراد (2026-07-21 · لا تغيير كود — ملف بيانات فقط):** ملفات المالك الخمسة في `Desktop/العمليات اليومية/BrandZo HUB/الاصناف` (جيس 25 · ميا سكين كير 45 · ميا ميك اب 674 · ميبسون 35 · ويت ان وايلد 238 = **1017 صنفًا**، عمودان: الاسم/الرقم) حُوِّلت إلى ملف موحّد بالقالب المفهوم للنظامين: **`الاصناف/Brandzo-Items-الاصناف-الموحد.xlsx`** (كود الصنف · اسم الصنف · الباركود · التصنيف=العلامة). **قاعدة توليد الكود (تُحفظ — إعادة التحويل بغيرها تُنشئ تكرارًا):** `بادئة-الرقم` (GUESS/MIASC/MIAMU/MAUB/WNW)، والرقم يدخل أيضًا في `barcodes[]` (27 كودًا غير رقمي مثل IP34927/MB161 و19 رقمًا قصيرًا — كلها معرّفات تُكتب/تُمسح). **تحقّق آليًّا:** مُرّر على `importSheet` الحقيقي: ok · 1017 صفًّا · صفر أخطاء · الأعمدة sku/nameAr/barcode/family · ومحاكاة حرفية لكاشف أعمدة `عمليات-مخزنية.html` القديمة نجحت. **🚨 على المالك:** دخول ← الأصناف ← «استيراد شيت» ← اختيار الملف ← تأكيد (المعاينة ستُظهر ~1017 جديدًا)، ثم في `/dashboard/stock-operations` زرّ «جلب الأصناف من الماستر السحابي». **⚠️ خلل مكتشف غير مُصلح:** `upsertItems` يُسقط بصمت أي صفّ بلا كود بينما المعاينة تعدّه «جديدًا» (لا يمسّ ملفنا — كل صفوفه بأكواد) — مُعلَّم كمهمة مستقلة.

- **🔢 تسامح الأصفار البادئة + تشخيص «صنف غير موجود» (2026-07-21 · مبنيّ ومُختبَر · غير مرفوع):** المالك جرّب مسح `8059692041626` في `/dashboard/stock-operations` فظهر «صنف غير موجود» وطلب أن يتطابق `00251` مع `251`. **(أ) القاعدة الجديدة:** `normalizeBarcode` تُسقط الأصفار البادئة من الأرقام الخالصة (غير الرقمي مثل `IP34927` لا يُمسّ) — تسري تلقائيًّا على الاستيراد والبحث السحابي والأرصدة لأنها نقطة التطبيع الواحدة؛ و`lookupByBarcode` يبحث بالصيغتين (`array-contains-any`) لالتقاط مخزونٍ خُزّن قبل القاعدة؛ و`findInvIdx` في شاشة العمليات + منع تكرار «جلب الأصناف» يقارنان بالصيغة القياسية (`bzCanonCode`). **(ب) الفشل الصامت:** تعذُّر سؤال الماستر (شبكة/دخول) كان يُبتلع فيبدو «صنفًا غير موجود» — الآن Toast يفرّق بينهما. **تحقّق:** 110/111 (الفاشل قائم قبلُ بالتوظيف) · بناء 34 صفحة · صفر لينت جديد (84=84) · **اختبار سلوكي على كود dist المبني: 10/10** (251↔00251 · 777↔0000777 · UPC بصفر/بلا صفر · كود المورد بمسافة). **التشخيص الأرجح لحالة المالك:** الماستر فارغ — خطوة «استيراد شيت» للملف الموحّد لم تتم بعد؛ زر «جلب الأصناف» يقولها صراحةً («الماستر فارغ») إن كانت كذلك. **بعد الرفع:** لو سبق استيراد أصناف قبل هذا الإصلاح، إعادة استيراد الملف الموحّد مرّة توحّد الصيغ (تحديث لا تكرار).

- **🚚 جرد المركبات صار نظامًا حيًّا (2026-07-20 · فرع `claude/vehicle-inventory-system` · مبنيّ ومُختبَر · غير مرفوع):** أداة `vehicle-inspection-brandzo.html` المنفصلة تحوّلت إلى **سجلّ أسطول سحابي** في `/dashboard/vehicles-inventory`: (1) **دور جديد `fleet` («الحركة»)** — مقيّد بهذه الصفحة وحدها (roles/navAccess/pageAccess + مجموعة قائمة «إدارة الحركة»)؛ إسناده لأي بريد من «إدارة المستخدمين» يفتح له النظام. (2) **قاعدة البيانات:** `vehicles/{لوحة}` + فروع `inspections` (النماذج الكاملة بترقيم رسمي `VINSP-2026-####` من عدّاد counters) و`maintenance` (طلبات الصيانة) و`oil_changes` (الزيوت واستحقاقها) و`accidents` (محاضر الحوادث) — كتابة للحركة والمديرَين، قراءة لكل مصادق، **لا حذف**. (3) **بذرة الأرشيف:** الـ23 ملف إكسل فُكّت بمحلّل مُختبَر إلى `src/data/vehicles-seed.json` وزرّ «⬆️ رفع بيانات الأرشيف» يرفعها دفعة (حتميّ المعرّفات — إعادة الرفع تُحدّث لا تكرّر). (4) **الهوية:** الطباعة وPDF من **قالب واحد** بترويسة «إنجاز المشارق» (`public/identity/enjaz-letterhead.jpeg`) — html2pdf يرسم من نفس `#printArea` الذي تطبعه المتصفّح. (5) **الإكسل احتياطي:** تصدير كامل بلا فقد (أُصلح سقوط قسم الملحقات) + استيراد ملفات متعددة (القديمة والجديدة) مباشرةً إلى السحابة. **أخطاء الأداة القديمة أُصلحت:** عدّ «الداخلي» مرتين + محاسبة الإدارية على بنود النقل (النسبة لا تبلغ 100%) · ترقيم عشوائي متصادم · «المستلم» التوقيع يدهس «المستلم من». **تحقّق:** 107/108 اختبارًا (الفاشل قائم قبلُ في كتالوج التوظيف) · بناء 34 صفحة · لينت نظيف · الحارس يحوّل غير المسجّل ✅. **🚨 معلّق على المالك:** (أ) نشر `firestore.rules` المحدَّثة (قواعد vehicles). (ب) إسناد دور «الحركة». (ج) الدخول والضغط على «رفع بيانات الأرشيف» ثم تجربة حفظ فحص وطباعته. (د) ⚠️ البذرة تحوي أسماء سائقين ولوحات وستكون عامة مع الرفع للمستودع العام — قرارك قبل «ارفع».

- **🎛️ تحسين لوحة التحكم — دفعة واجهة (مبنيّة ومُختبَرة · غير مرفوعة):** خمس مهام بطلب المالك «تطوير لا تلخيص». (1) **تقرير هيكل الوظائف والأدوار** (`public/تقرير-هيكل-الوظائف-والادوار.html`) طُوِّر إلى **إصدار 2.0** تفاعلي: بحث شامل (71 عنصرًا مفهرسًا) · توجيه بالـhash · جدول نماذج بفرز/تصفية/عدّاد · مراحل بتنقّل أسهم وشريط تقدّم وشرائح «النماذج المرتبطة» · بطاقات وظائف ببحث وتصفية Admin/Manager/User · RACI بإبراز صف/عمود وتلميح وتركيز دور · شجرة هيكل قابلة للطي · زر عودة للأعلى ورابط بوابة. **تحقّق حيّ في المتصفّح: صفر أخطاء console + كل المصفوفات العشر مطابقة HEAD بايتًا-ببايت (لا فقدان محتوى) + دورة الطباعة توسّع 8 مراحل + 18 وظيفة ثم تستعيد.** (2) **حُذفت صفحتان** من البوابة: `dashboard/grn` (إطار GRN القديم) و`dashboard/development-proposals` — أُزيلت من القائمة الجانبية وبطاقات اللوحة وتقرير النظام؛ نموذج `form_GRN.html` وشاشة «المستندات» لم يُمسّا. (3) **صفحة المهام** (`tasks`): **أُصلح خلل حرج** — تصدير PDF كان يفشل دائمًا لأن `window.jspdf` غير مُحمَّلة إطلاقًا (وخطوطها لا تعرض العربية)، فاستُبدل بنافذة طباعة HTML عربية بهوية Brandzo؛ + إنجاز/إعادة فتح · قائمة تحقق تفاعلية على البطاقة · تعديل المهمة · شارة «متأخرة» + KPI · بحث/فرز · نسخ احتياطي JSON (تصدير/استيراد بالدمج). (4) **مساعد الاجتماعات** (`meeting-assistant`): **أُصلح خللان حرجان** — التعرّف الصوتي لا يستأنف بعد الصمت (closure قديم لـ`isRecording` ⇒ عولج بـrefs + إنشاء الكائن مرة واحدة)، ومحرّر التفريغ `contentEditable` كان يمسح تعديلات المستخدم (⇒ قائمة مقاطع قابلة للتحرير/الحذف + ملاحظات يدوية)؛ + بانر «المتصفح لا يدعم» · مؤقّت تسجيل · ترجمة النص كاملًا مجزّأة · أرشيف جلسات في localStorage. **التحقّق:** build أخضر (32 صفحة) · lint نظيف · 96/96 اختبار · الصفحتان خلف Firebase Auth (لم تُجرَّبا حيًّا — تحتاج دخول المالك). **التالي: «ارفع» بعد موافقة المالك.**

- **📊 مخزن الأرصدة مكتمل (`25381e5`، مثبَّت غير مرفوع) — يُكمل ماستر الأصناف:** ورقة Balances صار لها مخزن سحابي `balances/{صنف__مخزن__تشغيلة}` (مفتاح مركّب ⇒ إعادة الاستيراد تُحدّث لا تُكرّر). `balanceKey.js` خالص مُختبَر: **FEFO** (الأقرب انتهاءً، بلا صلاحية للآخر) + حالة الصلاحية + قيمة المخزون Σ(كمية×تكلفة التشغيلة) لأساس S12. شاشة الأصناف: زرّ **«📊 استيراد الأرصدة»** + ملخّص حيّ (كمية · قيمة د.ل · تشغيلات قاربت الانتهاء) + عمود الرصيد يعرض **الحقيقي من المخزن** (⏳ لقرب الانتهاء · tooltip FEFO). قواعد `balances` أُضيفت (تُكمل I-و). 96/96. **🚨 معلّق على المالك: نشر `firestore.rules` (balances) — بدونه لا يُحفظ رصيد.** التالي: I-د الأصناف المعلّقة · F2 سلسلة الشراء.

- **📱 I-ج مكتمل (`a929f02`، مثبَّت غير مرفوع) — الماسح يعمل على آيفون:** كانت شاشة الجرد تعتمد `BarcodeDetector` الأصلي وحده (غائب في سفاري/iOS) فلا يمسح موظّف بآيفون إطلاقًا. `public/lib/bz-barcode.js` يوفّر كاشفًا موحّدًا: أصلي حيث توفّر، وإلا **احتياطي html5-qrcode** (يلتقط إطار الفيديو ويفكّه صورةً). حلقتا المسح (مفرد/دفعي) تنتظران `createDetector()` بدل الاستسلام. **تحقّق حيّ في متصفّح بلا BarcodeDetector (حالة iOS): المسار الاحتياطي عمل، engine=html5-qrcode، صفر أخطاء.** المسار يُشتقّ من موقع الملف (`document.currentScript`). التالي بالمسار: مخزن الأرصدة · I-د الأصناف المعلّقة · F2.

- **🔗 I-ب/2 = F1.5 مكتمل (`305ed49`، مثبَّت غير مرفوع):** بنود GRN تُملأ بالباركود من الماستر — مسح/كتابة+Enter في عمود الباركود ⇒ `lookupByBarcode` ⇒ يتعبّأ الكود والوصف (الاسم — الظل). **الفارغ فقط يُملأ** (اليدوي لا يُدهس)، والمجهول ينبّه ولا يوقف. عام عبر `lookup:'item'` في المخطّط — أي نموذج قادم يرثه بخاصية واحدة. 83/83. **بهذا اكتملت حلقة المالك الأصلية: الصنف يُعرَّف مرة ويُستدعى بالباركود في كل مستند.** المحوران 2+3 نُشرا (`9506511` deploy success). التالي بالمسار: I-ج (ماسح آيفون) · مخزن الأرصدة · F2.

- **🧑‍💼 المحور 1 — التوظيف الذكي (منشور `d43c4aa`، 16.07):** `/dashboard/recruitment` حيّة (HTTP 200) وشريط الصفحة القديمة حيّ. كتالوج 18 وظيفة مولَّد من ملف الهيكل · خط أنابيب مرشحين + تدقيق + سيرة ≤700KB في مستند فرعي · د.ل · للمديرَين فقط. **🚨 معلّق على المالك: نشر `firestore.rules` (قواعد candidates) — بدونه لا يُحفظ مرشح.** ثم تجربته. **التالي: المحور 2** (مطابقة workflows/erp-workflows بالمرجع التشغيلي) ثم 3 (ضمّ المرجعَين) — الخطة كاملة **ROADMAP §13**.

- **📦 ماستر الأصناف + القالب القياسي (`8474f9c`→`55ee5e5` — ✅ منشور حيًّا):** طلب المالك = ماستر أصناف يكون **مصدر البوابة كلّها**، يُرفع بشيت إكسيل، وتتعرّف عليه المستندات بالباركود. **القرارات السبعة اتُّخذت** (ROADMAP §12.2 + §12.6). **أُنجز:** `barcodes[]` + **`lookupByBarcode()` نقطة الاستدعاء الواحدة** + استيراد بمعاينة + **القالب القياسي** `public/templates/Brandzo-Items-Template.xlsx` (ملف واحد · 3 أوراق: Items 15 عمودًا · Balances 10 · تعليمات) مولَّدٌ **من المخطّط** فلا ينحرف عمّا يقرأه النظام. **تحقّق:** أعمدة شيت المالك الحقيقية **12/12** · القالب **15/15** و**10/10** · **64/64 اختبار**.
- **✅ واجهة الاستيراد (I-أ) أُنجزت (`3cf0077`):** زرّ «استيراد شيت» داخل شاشة الأصناف (مدير/مدير مستودع فقط) ← معاينة كاملة (جديد/تحديث بفروقه/بلا تغيير/أخطاء) ← تأكيد ← كتابة. + عمود الباركود والبحث به + حقل باركودات في النموذج + إزالة الوعد الكاذب عن الرصيد. **✅ تحقّق المالك حيًّا (2026-07-15): «تعمل 100%»** — المسار كاملًا: قالب ← رفع ← معاينة ← كتابة سحابية. (فخّ للمستقبل: Pages يخبّئ الصفحات 10 دقائق بعد النشر — تحديث قسري `Ctrl+Shift+R` يحلّها.) المتبقّي: I-ب/2 (بنود GRN) · I-ج (ماسح يعمل على آيفون) · I-د/هـ/و — **ROADMAP §12.5**.
- **☁️ I-ب/1 — شاشة العمليات المخزنية صارت تستدعي الماستر (`66a19b9`، **منشور 16.07** والزرّ مؤكَّد على الصفحة الحيّة):** بقرار المالك نُفّذ الربط في شاشة المسح أولًا (لا GRN): زرّ **«جلب الأصناف من الماستر السحابي»** بديلًا أوّليًّا عن استيراد الشيت يدويًّا كل جلسة (الإكسل بقي بديلًا يدويًّا) + **مسحُ باركودٍ غير موجود يسأل الماستر** قبل الحكم «صنف غير موجود» (☁️ يتجسّد في الجلسة برصيده الدفتري) + توحيد مواضع البحث التسعة على `findInvIdx` الذي يعرف **الباركودات المتعدّدة** (مسح الباركود الثاني كان يُنشئ سجلًّا مكرّرًا). **ومعه سُدّ خلل صامت:** ترقية المخطّط تركت `upsertItems` يرمي الأعمدة الجديدة (الأسعار · التسلسل الرباعي · المورّد · الوحدة) — عولج بوحدة `itemShape.js` الخالصة + **اختبار انحراف عمودًا-بعمود** (اصطاد أيضًا سقوط `nameEn` سهوًا). 71/71 اختبارًا. **لم يجرّبه المالك حيًّا بعد** — وبعد التجربة: ⚠️ **إعادة استيراد شيت الأصناف مرة واحدة** لتكتمل الحقول التي كانت تُفقد، ثم «جلب الأصناف من الماستر» في شاشة العمليات ومسح باركود. **التالي: I-ب/2** — نفس الاستدعاء في بنود GRN.
- **📦 (أرشيف الفحص) — ماستر الأصناف:** طلب المالك = ماستر أصناف يكون **مصدر البوابة كلّها**، يُرفع بشيت إكسيل، وتتعرّف عليه كل المستندات بالباركود. **الفحص كشف أن المشكلة ازدواج لا نقص:** نظاما أصناف متوازيان (`Items_Master` سحابية بـSKU **بلا حقل باركود** لشاشة واحدة · `inventoryDB` في localStorage بالباركود وهي التي تخدم كل مسح حقيقي)، وثلاث طبقات مبنيّة بلا مستدعٍ (excel · odooMapper · inventoryService). **القرارات الأربعة اتُّخذت** (ROADMAP §12.2). **أُنجز (§12.4):** `barcodes[]` + **`lookupByBarcode()` نقطة الاستدعاء الواحدة** + أعمدة الشيت الحقيقية + اكتشاف صفّ العناوين + `upsertItems` دفعية + خدمة استيراد بمعاينة. **🚨 خللان صامتان أُصلحا:** «كود» مُحتواة في «الباركود» فكان عمود الباركود يُسند لـSKU (كل الاستيرادات كانت ستفسد بصمت)؛ و`createItem` كان يدهس الأصناف ويُصفّر أرصدتها. بناء أخضر · لينت نظيف · **46/46 اختبار**.
- **▶️ التالي مباشرةً: ROADMAP §12.5** — I-أ واجهة الاستيراد · I-ب ربط بنود GRN بالباركود · I-ج ماسح موحّد (**⚠️ الجرد لا يعمل على آيفون اليوم**) · I-د الأصناف المعلّقة · I-هـ ترحيل شاشة الجرد · I-و قواعد الأمان.

- **📑 آخر إنجاز (مبنيّ ومُختبَر — لم يُرفع بعد) — F1: محرّك المستندات + GRN رائدًا:** القرارات الثلاثة اتُّخذت (GRN · الـ12 المحكومة · `GRN-2026-0001`) وبُني المحرّك كاملًا: `services/documents/` (7 ملفات) + 8 مكوّنات في `components/brandzo-erp/documents/` + صفحتان (`/dashboard/documents` صندوق المستندات · `/dashboard/document?type=GRN` شاشة المستند) + `firestore.rules` موسَّعة. **العيوب الخمسة** (صفر حفظ · رقم ثابت · لا هوية · لا ترابط · العزلة) عولجت أربعة منها؛ الترابط `links` جاهز في النموذج ويُفعَّل في F2. **🥇 حارس الجودة صار حقيقيًّا:** لا طريق من «مُرسَل» إلى «منجَز» دون اعتماد مفتّش الجودة — مفروض في ثلاث طبقات آخرها قواعد الخادم. **التحقّق:** بناء أخضر 33 صفحة · لينت نظيف (صفر جديد) · **30/30 اختبار وحدة** (`npm test`) · الحارس يحمي الصفحتين. **التفاصيل: ROADMAP §11.7.**
- **🔒 قواعد `firestore.rules` الموسَّعة (documents/counters): نشرها المالك بتأكيده «تم الصق والتحديث» (15.07)** — وفحص خارجي أكّد ألّا وصول بلا حساب. **المتبقّي للتثبّت النهائي:** تجربة مسار المستندات حيًّا (إنشاء GRN ← إرسال ← رقم) — لم تُجرَّب بعد؛ لو ظهرت `permission-denied` فالنسخة الملصوقة ناقصة.
- **⏳ لم يُتحقَّق حيًّا بعد:** المسار الكامل (ملء → حفظ → إرسال برقم → اعتماد بالدور → طباعة). يحتاج دخولك (لا أُدخل كلمات المرور) **بعد** نشر القواعد.
- **آخر أمر منشور:** `008941c` (خطة §11). عمل F1 على فرع `claude/session-handoff-roadmap-94f1c6` غير مرفوع.
- **سابقًا — صفحة الأدوار والصلاحيات** التفاعلية `/dashboard/access-control` (commit `023908b`).
- **الباك اند:** أُنجِز **10 مراحل من 12** (S1→S6 + S5a + S5b) بـ **6 حرّاس ذهبية**. آخر أمر `ebf5c01`.
- **المسار الباك اند موقوف مؤقتًا** لصالح عمل الواجهة (حسب قرار سابق).
- **📡 آخر إنجاز (منشور ومُتحقَّق حيًّا بلقطة المالك) — شاشة متابعة العمليات للمدير (`c8b6101`):** `/dashboard/operations` + `OperationsMonitor.jsx` + `listenOperations()` في الخدمة. تعرض: لقطة سريعة (إجمالي/مفتوحة/مقفلة) · قائمة العمليات (النوع · من بدأها · متى · مرشّح حالة) · **سجلّ مسح حيّ** (`listenScans`) يتحدّث تلقائيًّا · إجماليات (مسوحات/كمية/أصناف) · **توزيع الإنتاجية على الموظّفين** · زر إقفال (مديرين فقط). محصورة بـ`admin`+`warehouse_manager` (رابط مقيّد + حارس ذاتي + قواعد Firestore تفرضها). **تقنية:** الترتيب بحقل واحد + تصفية في الواجهة ⇒ لا حاجة لفهرس مركّب. **تحقّق المالك:** ظهرت العملية لحظيًّا باسمه.
- **☁️ قبله — «الجرد الذكي» صار نظامًا حيًّا مؤمَّنًا:** فحصٌ هندسي للملف `public/عمليات-مخزنية.html` (2961 سطرًا) كشف 4 مشكلات جوهرية، عولجت على مرحلتين. **م1 (`2f33dfa`، 14.07):** ترحيل أمين إلى `src/pages/dashboard/stock-operations.astro` — إعدام `admin/1234` المكشوفة واعتماد دور Firebase · الحارس يحميها · Tailwind وXLSX محليًّا (نُقل مقياس الألوان لـ`@theme`) · زر «← البوابة». **م2 (`6aca094`، 15.07):** `services/stock/operationsService.js` + سجلّ **append-only** في `operations/{opId}/scans` (مربوط بنقطة اختناق واحدة `addToScanLog` تمرّ منها المسارات الخمسة) · `persistentLocalCache` (يعمل بلا إنترنت) · مؤشّر حالة سحابية أنهى «الفشل الصامت». **إصلاح (`f434247`):** `global.css` كان يسحق أيقونات FontAwesome بـ`!important` (كشفته لقطات التوثيق).
- **🚨 ثغرة حرجة سُدّت:** ثبت بالاختبار أن Firestore كانت **مفتوحة للعالم** (قراءة/كتابة/حذف بلا حساب). أُنشئ [`firestore.rules`](firestore.rules) شاملًا (تمهيد أدمن بالبريد لكسر الحلقة المفرغة · سجلّ المسح غير قابل للتعديل/الحذف · منع افتراضي)، **ونشره المالك**، وأُعيد الاختبار: كل المحاولات مرفوضة ✅.
- **🎉 قبله — نظام الوصول الكامل:** M1 (دخول Firebase + حارس + أدوار، `9c88a5f`+`db2b2ef`) ثم **الإكمال (`d06bb98`)**: **تقييد القائمة حسب الدور** (`navAccess.js` المصدر الوحيد + جزيرة `RoleNav` + البحث يحترم المخفيّ) + **صفحة إدارة المستخدمين** `/dashboard/users` (أدمن فقط: إسناد الأدوار العشرة، تفعيل/تعطيل، إضافة بالـUID، وقواعد أمان Firestore جاهزة للصق داخل الصفحة). تحقّق: بناء CI-like أخضر · 16/16 اختبار وحدة للتقييد · الحارس يحمي الصفحة الجديدة. **Firebase Console مفعّل:** Email/Password + حساب المالك (دخل حيًّا كـ«👑 المدير العام»).
- **⚠️ خطوتان معلّقتان على المالك:** (أ) **نشر قواعد أمان Firestore** (منسوخة داخل صفحة إدارة المستخدمين — بدونها تعديل الأدوار مفتوح لأي مصادق). (ب) فحص بصري حيّ للقائمة المقيّدة كأدمن.
- **🚀 الخطة الاستراتيجية (جلسة #2):** الركائز الثماني في [`ROADMAP.md`](ROADMAP.md) §8 + مسار M1–M5 (§9) + بروتوكول مكافحة النسيان (§10). النطاق الرسمي `warehousehub.com.ly` (Libyan Spider) مسجّل كأصل + خطوات ربطه بـ Pages (§8 ركيزة 4).
- **✅ سابقًا (منشور):** تحديث لوحة التحكم `/dashboard` (`5eb1845`).
- **⏸️ مؤجّل للنقاش:** (أ) بوابة الدخول بالرمز **7777** (منطق الأدمن يرى كل شيء) — قرار قادم. (ب) **البند الخامس المقصوص** من صورة الخطة لم يُقرأ — بانتظار نصّه. (ج) دمج خطة OneDrive في §6 — بانتظار لصق نصّها.

### ▶️ الخطوة التالية المباشرة (اختر مسارًا)
| المسار | الخطوة المقترحة | المرجع |
|---|---|---|
| 🔴 **عليك أنت (أولًا)** | **انشر `firestore.rules`** ثم جرّب المسار حيًّا: افتح «المستندات» ← ＋ مذكرة استلام ← املأ ← حفظ ← إرسال (يظهر الرقم) ← ادخل بدور مفتّش جودة ← اعتمد ← اطبع | `firestore.rules` |
| 📑 **F1.5 (مرشّح — الأقوى)** | **ربط الجرد الذكي ببنود GRN:** تمسح الباركود فتُملأ البنود تلقائيًّا بدل 119 حقلًا يدويًّا. `getScans(opId)` جاهزة | ROADMAP §11.7 |
| 📑 F2 | **سلسلة الشراء:** مخطّطات PR → PO → GRN → QC + تفعيل `links` (أساس المطابقة الثلاثية) | ROADMAP §11.5 |
| 🟢 واجهة (مرشّح) | تعميقات الجرد الحيّ: **تقرير/تصدير عملية مكتملة** · ربط الجرد بـ`Items_Master` بدل الاستيراد اليدوي · **قيد عكسي للتصحيح** (السجلّ append-only فلا حذف) · إشعار عند فروقات كبيرة | `OperationsMonitor.jsx` · `operationsService.js` |
| 🌐 M2 | **بيئة أودو الحيّة** (Oracle Always Free: أودو 19 + Postgres + تثبيت الموديول + تشغيل الـ42 اختبارًا) | ROADMAP §8 ركيزة 3 |
| 🌐 باك اند (تالٍ) | **تشغيل اختبارات S12 والـ52 حيًّا في M2** — S12 أُنجز برمجيًّا (12/12) ومُتحقَّق ساكنًا **ومدفوع لـmain `15d1da5`**؛ يُثبَت في بيئة أودو حيّة | ROADMAP §4.1 |
| 🟢 واجهة | لمسات وصول متبقّية: قواعد أمان Firestore (على المالك) · لاحقًا سجلّ تدقيق + جلب الأدوار من أودو | ROADMAP §8 ركيزة 1 |

---

## 📋 سجلّ الجلسة (ماذا فعلنا — الأحدث أولًا)

> **[Claude] 2026-08-18 — ★★★ خطة قطاع الأغذية اكتملت 39/39 (١٠٠٪) مرفوعةً حتى `307ef8c`.** حسم المالك **ق-O06: ملفٌّ يوميّ**، فأُكملت FNB-704: مجموعة `posSales` في **مخطّط الاستيراد القائم** (لا مسارَ رفعٍ ثانٍ) بأربعة أعمدةٍ لا أكثر — «ملفٌّ بعشرين عمودًا لا يُملأ يوميًّا وملفٌّ بأربعةٍ يُملأ»؛ و**السعر لا يُطلب** لأنّ الإيراد من أودو (حدّ ق‑ت١) بحارسٍ يثبت غيابه. **★★ وأخطر ما في الملفّ اليوميّ سُدّ:** ملفٌّ ناقص صباحًا ثمّ كاملٌ مساءً — لو أُضيف الثاني للأوّل **تضاعفت المبيعات واتُّهم كلّ فرعٍ بأنّه يُخفي**؛ فالدفعة (فرع × يوم) بمعرّفٍ حتميّ، ورفعُها ثانيةً **يحلّ محلّ** الأولى، والاستبدال يُعلَن **قبل** الحفظ. وثلاثةُ حرّاسٍ على المدخل (فرعٌ مجهول · تاريخٌ مستقبليّ · صفٌّ بلا فرع)، و`feedDaysOf` تقرأ أيّام التغذية من الدفعات فتُعرف المدّة الصالحة للقياس من الواقع لا من ظنّ. والمصدران الآخران معلَنان غير مفعَّلين — تبديلهما لاحقًا لا يهدم شيئًا. **البوّابات: 2345/2345 · بناء 102 صفحة · تدقيق نظيف.** **⚠️ ما تبقّى ليس عملًا:** (١) **التجربة الحيّة بالأدوار — لم تُجرَ لشيءٍ ممّا بُني، وهي أكبر خطرٍ متبقٍّ**؛ (٢) بيانات شجرة القطاع (ق-O01)؛ (٣) ستّةُ قراراتٍ لكلٍّ سلوكٌ افتراضيّ مطبَّق. **وواجهات ما بُني منطقًا** (المقترح · التجميع · الإغلاق اليوميّ · إعادة التوازن) تنتظر خطةً لاحقة — المنطق ومختبَراته جاهزةٌ لها.

> **[Claude] 2026-08-18 — FNB-704 بُنيت ثلاثة أرباعها والمتبقّي قرارٌ لا عمل (مرفوع `1ed63eb`):** قرارُ **مصدر** بيانات POS لم يُحسم (ق-O06)، لكنّ ثلاثةً من معايير المهمّة الأربعة لا تتوقّف عليه فبُنيت في `integration/posFeed.js`: **الاتّجاه محسوم** (تُسحب ولا تُدفع — «دفعُها يجعل لرقم البيع مصدرَين») · **POS Accuracy** بتسمية غير المطابق صنفًا صنفًا («دقّةٌ ٪٩٠ بلا معرفة الـ١٠٪ رقمٌ لا يُعالَج») · و**★★ انقطاع المصدر يُعلَن ولا يُسكِت الاستهلاك النظريّ بصمت** — وهو أخطر ما في الطبقة: مصدرٌ توقّف بلا إعلان يجعل الاستهلاك صفرًا **فيبدو كلّ فرعٍ مهدِرًا بينما العطب في التغذية**؛ والرسالة تقول «غير محسوب لا صفر»، و`measurableWindow` ترفض القياس على مدّةٍ ناقصة. والمصادر الثلاثة معلَنةٌ بلا اختيار، و`sourceVerdict` يمنع تفعيل ما لم يُربط. **البوّابات: 2339/2339 · بناء 102 صفحة · تدقيق نظيف.** وأُنتج **تقريرٌ بصريّ للمالك** بروابط الشاشات الحيّة وبيّنة كلّ مهمّة (artifact). **الخلاصة: 38/39 والمتبقّي كلّه على المالك** — ق-O06، والتجربة الحيّة بالأدوار، وبيانات الشجرة.

> **[Claude] 2026-08-18 — ★★★ خطة قطاع الأغذية اكتملت عمليًّا: 38/39 (٩٧٪) مرفوعةً حتى `1d99033`، والمتبقّي محجوبٌ على قرارك لا على عمل.** أُغلقت في هذه الجولة: **FNB-703** (تكلفة الغذاء — و★ **حدّ البوابة محروسٌ بالكود**: `scopeVerdict` يمنع عرض مقياسٍ يملكه أودو إن لم يصل من المرآة، «وحسابُه هنا يفتح للمال دفترًا ثانيًا»؛ والفرق **يُنسب لأسبابه الثلاثة** والباقي يُشتقّ ولا يُخمَّن) · **FNB-801/802** (البرج ينزل خماسيًّا بحارس توازنٍ «الأعلى = مجموع ما تحته»؛ و★★ **حارس الشمول أمسك أربعة أنواع استثناءٍ مبنيّةٍ من خططٍ سابقة كانت ستسقط من اللوحة بصمت**) · **FNB-803** (الإغلاق اليوميّ بعشرة عناصر · «لا يُغلق يومٌ وله فرقُ جردٍ بلا سبب» · وملحق-فقط فالتصحيح سجلٌّ جديد) · **FNB-804** (إعادة التوازن بثلاثة قيودٍ لكلٍّ حارسُه: لا يُفرَّغ فرعٌ تحت حدّه · FEFO يُحترم · وما لا يكفي مدّة الطريق لا يُنقل) · **FNB-805** (العرض يُحدَّث بالمبنيّ، وثلاثةُ حرّاسٍ تمنعه أن يَعِد القطاع بما لم يُبنَ). **وأُصلح عطبٌ كشفه المالك:** فتح `/dashboard/transfers` فرأى صفحة التسويق — لأنّ عامل الخدمة كان يُرجع **الصفحة الجذر لأيّ مسارٍ غير محفوظ** عند فشل الشبكة؛ فصار الجذر احتياطيًّا **لنفسه وحده** وما عداه يرى إشعارًا صريحًا يسمّي المسار. **البوّابات: 2332/2332 اختبارًا (+35 هذه الجولة) · بناء 102 صفحة · تدقيق نظيف.** **⚠️ المتبقّي كلّه عليك:** FNB-704 محجوبةٌ على **ق-O06** (مصدر بيانات نقطة البيع)، وتسعةُ قراراتٍ في `ownerInputs` لكلٍّ سلوكٌ افتراضيّ مطبَّق، و**التجربة الحيّة بحسابٍ بالأدوار لم تُجرَ لشيءٍ ممّا بُني**.

> **[Claude] 2026-08-18 — ★ اكتملت المرحلة الخامسة ق٥ (٣/٣) والتقدّم 32/39 (٨٢٪) مرفوعًا حتى `d2c21aa`:** **FNB-601 طلب الشراء بسياقه** — المستند يرفض «مطلوب ١٠٠٠ كجم دجاج» صراحةً، فالحقول العشرة **محسوبةٌ لا مكتوبة** ولكلٍّ مصدرٌ حيّ (المخزون من الأرصدة · المعدّل من الدفتر · بالطريق من `openDemand` · الفروع والبراندات من الشجرة · أصنافُ المنيو المتعطّلة من الوصفة)؛ وتاريخ الاحتياج **قبل النفاد بمهلة التوريد** فيصل الطلب قبل أن ينفد؛ و**★★ حارس الإرسال يمنع لا ينبّه** والناقص يُسمّى حقلًا حقلًا. **FNB-602 الأولويّة محسوبة** — التصنيف الرباعيّ يُشتقّ فيتغيّر بتغيّر الواقع (درس ت٣)، و**بديلٌ معرَّف يخفّف الدرجة لا يُلغي الإلحاح**، وبلا تاريخٍ كافٍ **لا يُخمَّن إلحاح**؛ والتصعيد اليدويّ بسببٍ مكتوب **والمحسوب يبقى مذكورًا** فيُراجَع التجاوز. **FNB-603 الرؤية والـSLA** — ★ **وصلٌ لا بناء**: `procurementKpisByOrg` تُشغّل **نفس الدوال الأربع** ببُعد الفرع/البراند/القطاع (والجرد العدائيّ كان قد أثبت أنّها حيّةٌ على شاشتين)، و`procurementStatus` تُظهر الحالة من PR حتّى الوصول في سطر، و**★★ التأخّر يُنسب إلى مرحلته** لا إلى الطلب جملةً. **البوّابات: 2297/2297 · بناء 102 صفحة · تدقيق نظيف.** **درسٌ للسجلّ:** أمسك الاختبار عطبًا في نواةٍ قائمة — `impactOfShortage` كانت تقبل كائنًا عاديًّا فقط، ومستدعٍ يمرّر `Map` يتلقّى `[]` **بصمت**؛ فصارت تقبل الاثنين (نفس درس وجه «الصنف» في FNB-304: **الصمت الخاطئ أسوأ من الخطأ الصريح**). **التالي: ق٦ وق٧** — FNB-703 (تكلفة الغذاء المثاليّة مقابل الفعليّة · وحدُّ البوابة عند التكلفة التشغيليّة) ثمّ ق٧ (برج المراقبة والإغلاق اليوميّ). و**FNB-704 محجوبة على ق-O06** (مصدر بيانات نقطة البيع).

> **[Claude] 2026-08-18 — ★★ اكتملت المرحلة الرابعة ق٤ (٥/٥) والتقدّم 29/39 (٧٤٪) مرفوعًا حتى `6eff368`:** بعد نشر المالك للقواعد (1410 سطرًا · `6FE9D0F0…`) أُغلقت ثلاث مهامّ. **FNB-503 الدفعة والـYield** — الدفعة برقمٍ من المولّد القائم، و**لا تاريخَ صلاحيّةٍ يُخترع** (بلا مدّةٍ معرَّفة يُعلَن النقص — وتاريخٌ مخترَع أخطر من غيابه)، والختم **لا يدهس ما مُلئ بيد**؛ و**★★ الـYield مقياسان لا واحد**: `vsPlanned` للخطّة و`vsExpected` لما تكفيه الموادّ المصروفة — وهو **مقياس الشيف**، فلا يُلام التحضير على صرفٍ ناقص (باختبار: ٤٨ من خطّة ١٠٠ لا يفتح استثناءً إن كانت الموادّ تكفي ٥٠). **FNB-504 التخصيص** — **النقص يُقتسم بالتناسب** ولا فرعَ يخرج بلا شيء، وحارسٌ يثبت أنّ المخصَّص = المنتَج حرفيًّا، و`production_delay` يُفتح مرّةً للصنف لا لكلّ فرع، والتخصيص يخرج طلباتِ نقلٍ بالسلسلة القائمة **والدفعة تسافر مع الكمّيّة**. **FNB-505 سلسلة التبريد** — خمس نقاط تحكّم وحدودها **من `CCP1_LIMITS` القائم**، و**★★ القياس الغائب ليس خرقًا** («لم تُقَس» نقصُ إجراء و«قِيست فخُرقت» استثناء)، والخرق يفتح `qc_reject` القائم لا نوعًا ثالثًا، و**دفعةٌ خُرقت سلسلتها لا تُخصَّص قبل قرار جودةٍ مكتوب**. **البوّابات: 2272/2272 · بناء 102 صفحة · تدقيق نظيف.** **درسان:** ① حارس النقاء وقف على حافّة خطّ الأساس (26/26) فقُسِم اختبار التبريد إلى ملفٍّ مجاور — **الخطّ ينزل ولا يعلو**. ② وإعادة أساسٍ ثانية على `main` بعد دفع جلسة «الهوية التشغيليّة» (`9d6ff34`)، وحُلّ تعارض الملفّات المولَّدة **بإعادة التوليد لا باليد**. **التالي: ق٥ المشتريات** — FNB-601 (حقول السياق العشرة) فـ602 (الأولويّة الرباعيّة المحسوبة) فـ603 (وصلُ SLA بالفرع).

> **[Claude] 2026-08-18 — ★ ق٣ اكتملت وبدأت ق٤: التقدّم 26/39 (٦٧٪) مرفوعًا حتى `fe8cc02`:** **ق٣ (٥/٥)** — **FNB-401** وصلُ **الفحص بين السحب والتعبئة** (كان مقطوعًا: من الجودة لا سبيل إلى التعبئة) مع `derivationTargetsFor` تقصّ الوجهات **بسياق المستند** فلا يُخلط فحصُ الوارد بالصادر · و**حكم الإغلاق**: «لا يُغلق طلبٌ وله فرقٌ غير مسوّى» و`releaseOnClose` تحرّر الحجز («لا رصيدَ محجوزٌ إلى الأبد») · **FNB-402** استثناءات الاستلام من **الأنواع القائمة** (`transit_variance` المبنيّ) وأُضيف `transfer_unreceived` وحده · **FNB-403** الوجهة حقلَ طرفٍ من الشجرة **ولا اسمَ حقلٍ سادس في ORG_FIELDS** · **FNB-404** الهدر **سببًا محكومًا** على سند التالف القائم لا مستندًا ثانيًا يقسم الرصيد · **FNB-405** `supplierBatch` و`mfgDate` يُورَّثان GRN←QC←PUTAWAY فتتّصل السلسلة، **ودفعة المورّد صفةٌ لا مفتاحٌ يقسم الرصيد** (باختبار). ثمّ **★★ FNB-502 دورة الإنتاج** — الطبقة التي كانت معدومةً تمامًا: PRO (خطّةٌ لا تقيّد) · MIS (الموادّ تخرج إلى موقع `PRODUCTION` الوسيط) · PRC (المنتَج يخرج منه فيُقفل ما فُتح)، بـ`mustZero` فرصيدٌ باقٍ = **دفعةٌ لم تُغلق**؛ و**الإنتاج تحويلُ قيمةٍ لا خلقُها** (`financial:false`) فلا تتضاعف تكلفة المخزون؛ ودور `executive_chef` بالسلوك الافتراضيّ لق-O05. **البوّابات: 2258/2258 (+78 هذه الجلسة) · بناء 102 صفحة · تدقيق نظيف.** **درسٌ حاكم:** خمسة حرّاسٍ أوقفوا FNB-502 (عدّاد المخطّطات · أدوار القواعد · الحقول الزمنيّة · عبور أودو · الأثر الماليّ) وحارسٌ سادس رفض ثلاثة ملفّاتٍ بلا اختبارٍ مجاور — **فأُضيفت ثلاثة حرّاس ولم يُرفع خطّ الأساس: الخطّ ينزل ولا يعلو.** **⚠️ على المالك: نشر `firestore.rules`** (أدوار PRO/MIS/PRC و`executive_chef`). **التالي:** FNB-503 (الدفعة والصلاحيّة والـYield) فـ504 (التخصيص على الفروع) فـ505 (سلسلة التبريد) فتكتمل ق٤.

> **[Claude] 2026-08-18 — ★ اكتملت المرحلة الثانية ق٢ (٩/٩) والتقدّم 20/39 (٥١٪) مرفوعًا حتى `67ec022`:** بعد نشر المالك للقواعد ثانيةً (1403 سطرًا · بصمة `CC941B3B…`) أُكملت ق٢ بأربع مهامّ. **FNB-302 الفرع يراجع لا يُنشئ** — `intelligence/branchOrder.js`: الطلب يُفتح **مملوءًا** بكمّيّاتٍ محسوبة، وكلّ سطرٍ يحمل **مرجعه** (المقترح · المخزون · بالطريق · المعدّل · الأيّام)، ويتحوّل إلى **طلب نقلٍ قائم** بمركز تكلفة الفرع والمقترح **مختومًا على السطر**. **FNB-303 الانحراف يُسجَّل بسببه** — سياق `order_deviation` في سجلّ الأسباب القائم بثمانية أسباب من واقع مطعم **ولا سببَ يلوم الفرع**؛ وتجاوزٌ فوق ٪٢٥ يفتح استثناءً في السجلّ القائم؛ و**★★ الانحراف تعلُّمٌ لا لومٌ فقط**: `policyReviewSignals` ترصد تكرارًا باتّجاهٍ واحد فتقترح ضبط Par Level، والمتذبذب ليس إشارةَ سياسة. **FNB-304 الطلب المجمَّع** — `consolidatedDemand.js`: التجميع **عرضٌ مشتقّ لا يبتلع مستند الفرع** (باختبارٍ يقارن المستندات قبل وبعد)، بالأوجه الستّة والمدينة **صفةَ فرعٍ تُشتقّ من الشجرة** لا حقلًا سادسًا، وحارس **التوازن** (مجموع أيّ وجهٍ = مجموع الطلبات)، و**الرقم المجمَّع يُفتَح** إلى مستنداته. **FNB-305 قناة الطلب** — القنوات الأربع سجلًّا محكومًا تصبّ في محرّكٍ واحد وتظلّ مميَّزة، وخيارات `so.js` تُوسَّع ولا تُستبدل. **البوّابات: 2218/2218 · بناء 101 صفحة · تدقيق نظيف.** **درسان:** ① `reasonProblem` تُعيد حكمًا `{ok, problem}` لا نصًّا — قرأتُها خطأً فأمسك الاختبار الخطأ وصُحّح المستدعي لا العقد. ② **وجه «الصنف» كان يقرأ حقلًا اسمه `item` والسطر يحمل `sku`** فكان يجمّع كلّ شيءٍ تحت «غير محدَّد» **بصمت** — فصار للوجه `field` صريح. **⚠️ إعادة أساسٍ على `main`:** جلسةٌ أخرى دفعت عرض «الهوية التشغيلية» (`2e72074`)، فأُعيد الأساس فوقه وحُلّ تعارض `arch-wiki.html` **بإعادة التوليد لا باليد**. **التالي: ق٣** — FNB-401 (دورة الطلب التسع: الفحص بعد السحب والإغلاق) فـ402 (فرق استلام الفرع) فـ403 (وجهة المطعم) فـ404 (الهدر) فـ405 (التتبّع المتّصل).

> **[Claude] 2026-08-18 — المرحلة ق٢ نصفها: التقدّم 15/39 (٣٨٪) مرفوع حتى `0bf46c6`:** خمس مهامّ متتابعة بلا انكسارٍ واحد. **FNB-201 ملفّ الفرع** — `org/branchProfile.js`: المفاهيم الستّة قائمةً محكومة و«آخر» بابٌ معلَن · حالتا الفرع (افتتاح ← تشغيل) · و**ما يُشتقّ لا يُخزَّن** (`storageAbilities` تقرأ التبريد من سيّد المواقع) · و**المنعُ لما يكذب لا لما ينقص**. **FNB-202 سياسة المخزون** — `intelligence/stockPolicy.js` بوراثةٍ **حقلًا حقلًا** (فرعٌ حدّد سقفه يرث مهلته من برانده وأمانه من قطاعه، و`sources` تقول لكلّ حقلٍ من أين جاء)، وأيّام التغطية **بندٌ مستقلّ** لا مرادفٌ لمخزون الأمان. **FNB-203 المسارات الخمسة** — ومنها **Supplier → Restaurant Direct** الذي أسقطته القراءة الأولى: `receiptRouteWarnings` يحرس موضع الاستلام **بالاتّجاهين** («استلامٌ في مخزنٍ لم تدخله البضاعة يقلب الرصيد»)؛ والأصناف المعتمَدة قائمةٌ على صفّ الفرع **بلا تسجيله عميلًا**. **FNB-204 شدّة الافتتاح** — تُولَّد من الملفّ بـ**مرجعِ كلّ كمّيّة** (Par ← الحدّ الأدنى ← محسوبةً من الطاقة والوصفة)، والمجهول **صفرٌ معلَن لا يُخمَّن**؛ وهي **طلبُ نقلٍ يُولَّد لا مستندٌ جديد**؛ والانتقال بين الحالات محكومٌ («الماضي لا يُلغى»). **★ FNB-301 المعادلة على مستوى الفرع** — رصيدُه واستهلاكُه (عبر ختم FNB-104) وسياستُه و«بالطريق» مطروحة، وحارسٌ يثبت أنّ **فرعَين مختلفَي الاستهلاك يُقترح لهما رقمان مختلفان** — وهو ما كان مستحيلًا قبل اليوم. **البوّابات: 2180/2180 (+119 هذه الجلسة) · بناء 101 صفحة · تدقيق نظيف.** **درسان للسجلّ:** ① حارس «الاقتراح يحمل سببه» رفض تغييري لصياغته فأُعيدت كما كانت — المهمّة سياسةٌ لا إعادةُ صياغة. ② اختبار FNB-204 كشف أنّ `shapeBranchProfile` كان يُسقط `allowedSkus` التي عرّفها FNB-203 — ثغرةٌ بين مهمّتين أمسكها الاختبار لا القراءة. **⚠️ على المالك: نشر `firestore.rules`** (أُضيف `isFnbManager` في ق١). **التالي:** FNB-302 (الفرع يراجع لا يُنشئ) فFNB-303 (الانحراف بسببه) فFNB-304 (الطلب المجمَّع).

> **[Claude] 2026-08-18 — ★ المرحلة الأولى ق١ اكتملت ٧/٧ · التقدّم 10/39 (٢٦٪):** **FNB-106 المطبخ منشأةً** — `locations/facilityModel.js`: نوعٌ ثانٍ للمنشأة (`production_unit`) بحارسٍ يثبت أنّه **يزيد ولا ينقص** عمّا يقدر عليه المستودع · المناطق الستّ (Freezer·Cold·Dry·Packing·Staging·Dispatch) **بذرةُ مواقعَ تمرّ بسيّد المواقع نفسه** بأنواع تخزينه القائمة لا سيّدًا ثانيًا · `KITCHEN_FLOW` بستّ مراحلَ ومستنداتِها الموجودة فعلًا، والإنتاج معلَّقٌ صراحةً على FNB-502 **وحارسٌ يثبت أنّ كلّ نوعٍ مذكورٍ مبنيّ** فلا يَعِد السجلّ بما لم يُبنَ · وشاشة المستودعات تُوسَّع بحقل النوع وشارةٍ وتنبيه «مطبخٌ واحد يخدم الشبكة». **FNB-107 أدوار القطاع** — `fnb_manager` **صاحبَ المدخل** (سجلّ `FNB_OWNED_INPUTS` بالمدخلات التسعة من القسم «أولًا») و`branch_manager` **مصدرَ بيانات** (سطر 529)، وكلاهما **خارج `isStockActor`** عمدًا كالمندوب باختبارٍ يمنع التسرّب؛ ومطابقة ل‑١٨ باختبارٍ يقرأ الشاشة والقاعدة معًا (`VIEWER_ROLES` ↔ `isFnbManager`)؛ وحارس **«لا دورٌ يُفقد»** يثبت بقاء الستّة عشر. ودرسٌ للسجلّ: **الحارس القائم `navAccess.test` هو من أمسك أنّ الدورين بلا مجموعات قائمة** — الحرّاس تعمل. **البوّابات: 2136/2136 (+14) · بناء 101 صفحة · تدقيق نظيف (١٨ دورًا).** **⚠️ على المالك: نشر `firestore.rules` ثانيةً** (أُضيف `isFnbManager`) وإلّا لم يكتب مدير القطاع شجرته حيًّا. **التالي: المرحلة ق٢** — FNB-201 (ملفّ الفرع بحقوله الخمسة عشر) ثمّ Par Level فالمعادلة على مستوى الفرع.

> **[Claude] 2026-08-18 — القواعد نُشرت فتسارعت الخطة: 8/39 (٢١٪) — المرحلة ق١ شارفت والهيكل يعمل طرفًا إلى طرف (مرفوع حتى `5569507`):** المالك أعلن نشر `firestore.rules` (1392 سطرًا · `88DAB14A…`) فأُغلقت **FNB-101** (غرس الشجرة — والحدّ المعلَن: التجربة الحيّة من حسابه لم تُجرَ). ثمّ خمس مهامّ في الجلسة نفسها: **FNB-702 الاستهلاك النظريّ** — `intelligence/posSales.js` مصدر-محايد (مطبِّع يقبل Foodics/أودو/شيتات بمرادفات · النظريّ لكلّ فرعٍ ويومٍ **بوصفة يوم البيع** لا وصفة اليوم · `consumptionVariance` بالاتّجاهين يفتح استثناء `consumption_variance` — الزائد هدرٌ والناقص وصفةٌ لا تُتّبع) · **FNB-102** مركز التكلفة حقلَ طرفٍ سادسًا في `partyFields` القائم («الاسم (المستوى)» · المعطَّل لا يُعرض · حارس ثبات `ORG_FIELDS`) على DN وTRN · **FNB-103** حكم «لا يُصرف على وعاء» — `dispatchTargetVerdict` يرفض القطاع والبراند على مستند خروجٍ **ويقترح فروع الوعاء نفسِه**، وTRN صار الحقل فيه إلزاميًّا · **FNB-104 ★** الحركة تُختم بأبعادها لحظة القيد (`orgCode`/`orgBranch`/`orgBrand`/`orgSector` في `buildMoves` كسابقة CC-301، و`ledgerService` يقرأ الشجرة عند الإنجاز والعجز ليس منعًا) — **وبها اكتملت حقول توجيه المدير العام الستّة على الحركة** · **FNB-105** الصعود عُمِّم — `rollupBy` محرّكًا واحدًا للمقاييس التسعة و`costByLocation` غلافٌ فوقه بعائده القديم حرفيًّا، و`orgEntriesFromMoves` يحوّل الدفتر المختوم مقاييسَ بسبب قيده، و`rankByMeasure` للترتيب والنواقص. **البوّابات: 2122/2122 (+61 هذه الجلسة) · بناء 101 صفحة · تدقيق نظيف.** **درسٌ للسجلّ:** حارس «لكلّ نوعٍ سلوكٌ مميّز» رفض نوع المنيو لتطابقه مع الخدمة — فوُلد المحور الرابع `explodes` سلوكًا حقيقيًّا لا تسمية. **التالي:** FNB-106 (المطبخ منشأةً — ق-O03) وFNB-107 (أدوار القطاع — ق-O08) فتكتمل ق١، ثمّ ق٢ (ملفّ الفرع والمعادلة على مستوى الفرع).

> **[Claude] 2026-08-18 — تنفيذ خطة القطاع بدأ: FNB-501 وFNB-701 مغلقتان ببيّنة، وFNB-101 كودها مكتمل (3/39 · مرفوع `5ca631c`):** **FNB-501 الوصفة** — الطبقة الثالثة التي كانت معدومة: `items/recipe.js` خالصًا (المكوّن من الماستر بكوده المطبَّع · التحويل بـ`uomModel.toBase` نفسه لا معاملَ ثانٍ · انفجار BOM متعدّد المستويات بحلقاتٍ محروسة · **نسخٌ لا تعديل**: `recipeAsOf` تفسّر بيعَ الأمس بوصفة الأمس · أثر النقص من الوصفة والشجرة معًا حتى البراند) + `recipesService.js` (معرّف `SKU@vN`) + قاعدة `recipes` — ومثال المستند حرفيًّا مثبَّت اختبارًا: ١٠٠٠ برجر × ١٥٠غ = ١٥٠ كجم. **FNB-701 صنف المنيو** — نوعٌ رابع في `ITEM_TYPES` نفسها: حارس «لكلّ نوعٍ سلوكٌ مميّز» كشف تطابقه مع الخدمة فأُضيف المحور الرابع `explodes` سلوكًا حقيقيًّا؛ ومعه `approvedMenuProblems` (منيو الفرع قائمةٌ محكومة) و`itemChain` (السلسلة الستّة: مورّد ← صنف ← وصفة ← صنف بيع ← براند ← فرع، والعميل طرفًا) واستثناء `recipe_unlinked` في السجلّ القائم. **FNB-101 غرس الشجرة** — `org/orgImport.js` بعقد **«تُقبل كاملةً أو تُرفض كاملةً»** + بذرة `DEMO-` موسومة بحارس اختلاط + كتابة ذرّيّة + قسم غرسٍ بالجملة في الشاشة القائمة — **محجوبة على نشر المالك للقواعد** (org_locations + recipes الجديدة) والمطابقة الحرفيّة. **البوّابات: 2100/2100 · تدقيق نظيف · rebase نظيف فوق `66c78f0` (تعارضٌ وحيد في `arch-wiki.html` المولَّد أُعيد توليده).** **التالي:** FNB-702 (الاستهلاك النظريّ) — و`npm run plan:fnb` يقول الباقي.

> **[Claude] 2026-08-18 — FNB-000: خطة قطاع الأغذية والمشروبات مصمَّمةً على جردٍ لا على تقدير (لم تُرفع · تنتظر مراجعة المالك):** المصدر توجيه السيد المدير العام (F&B حسابًا رئيسيًّا تحته البراندات والفروع) + خطة مسؤول قطاع F&B، محفوظًا حرفيًّا في [`docs/plan/fnb/المصدر-توجيه-المدير-العام-وخطة-القطاع.md`](docs/plan/fnb/المصدر-توجيه-المدير-العام-وخطة-القطاع.md) (٧٢٤ سطرًا). **قبل كتابة سطرٍ واحد جرى جردٌ متعدّد الوكلاء على الكود** (١٣ وكيلًا · ٦٠٦ استدعاء أداة): ١٣٣ متطلَّبًا من المستند + ١٠ أعرافٍ للبيت = ١٤٣ فحصًا، بقراءة ملفّاتٍ لا باستنتاجٍ من الأسماء. **النتيجة: ٢٦ منفَّذًا · ٧٧ نواةً جزئيّة · ٣٠ مفقودًا ⇒ ٧٨٪ من الخطة له نواةٌ قائمة.** ثمّ **تحقّقٌ عدائيّ بالاتّجاهين** صحّح ١٣ حكمًا — أخطرها إنقاذُ **متابعة SLA للمشتريات** من دعوى «مفقود» (وهي مبنيّةٌ وحيّةٌ على `/dashboard/kpis` و`/dashboard/vendor-scorecard`) — ثمّ **ناقد اكتمالٍ** قرأ المستند سطرًا سطرًا فاستدرك **٥٨ بندًا** (مسار Supplier→Restaurant Direct · المسؤوليّات السبعة في الخلاصة · «الجودة» طرفًا ثالثًا · «العميل» في سلسلة الربط · ربحيّة الفرع)، وكلٌّ منها مربوطٌ بمهمّة في مصفوفة التغطية. **★ أكبر اكتشاف: هيكل F&B → Brand → Branch مبنيٌّ منذ شهور** في `orgLocations.js:31` (`sector › brand › branch › cost_center` بصعودٍ للتكلفة ودالّة `dimensionsOf()`) **ولا يستدعيه أحد** — الوعاء جاهزٌ وفارغ. ولهذا صارت قاعدة الخطة: **«الفرع بُعدٌ لا نظام»** — لا نظام مطاعمَ موازٍ يُبنى، والطبقة المفقودة الوحيدة حقًّا هي **الوصفة والإنتاج**. **المخرَج:** خطةٌ سادسة بحارسها — [`docs/plan/fnb-sector.json`](docs/plan/fnb-sector.json) (٣٩ مهمّة · ٧ مراحل تطابق مراحل المستند · ٢٤ فجوة كلّها مغطّاة · ٨ قرارات مالك لكلٍّ سلوكٌ افتراضيّ معلَن · ٤ مؤجَّلات بأسبابها) + `npm run plan:fnb` + [الوثيقة الحاكمة](docs/خطة-قطاع-الأغذية-والمشروبات.md) + [مصفوفة التغطية](docs/plan/fnb/مصفوفة-التغطية.md). **وتعارضٌ داخل المستند رُفع للمالك ولم يُطمس:** التقييم النهائيّ يطلب «ربحيّة فرع» بينما «٧. عدم تكرار المحاسبة» تجعل أودو المصدر الماليّ — الحدّ المقترح: البوابة تُنتج التكلفة التشغيليّة وأودو يُنتج الربحيّة (ق-O07). **البوّابات: 2061/2061 · تدقيق نظيف · لا سطر كود إنتاجيّ (وثائق وخطط فقط).** **التالي:** حسم ق-O01…ق-O08 ثمّ FNB-101 (غرس شجرة القطاع).

### جلسة #3 (تكملة) — 2026-07-16
- ☁️ **I-ب/1 (منشور `6323988`):** شاشة العمليات المخزنية تستدعي الماستر — زرّ الجلب + مسحٌ يسأل الماستر + بحث موحّد متعدّد الباركودات. تفاصيله في نقطة الاستئناف أعلاه.
- 🐞 **خلل صامت سُدّ:** `upsertItems` كان يرمي أعمدة القالب الجديدة (أسعار · تصنيفات · مورّد · وحدة) — `itemShape.js` + اختبار انحراف عمودًا-بعمود (أعاد أيضًا `nameEn` الساقط). 71/71.
- ✅ **وثّق (16.07):** البند 18 في التقريرين + موجز يومي جديد 16.07 + تحديث فترة الأسبوع 11–16.07 + تحديث تقرير النظام («7») والـArtifact.

### جلسة #3 — 2026-07-15 (تابع: ماستر الأصناف)
- ✅ **ماستر الأصناف — طلب المالك:** مصدر أصناف واحد للبوابة كلّها، يُرفع بإكسيل، يُستدعى بالباركود. الفحص وجد **ازدواجًا لا نقصًا** (نظاما أصناف متوازيان). أُنجز: `barcodes[]` · `lookupByBarcode()` · استيراد بمعاينة · إصلاح خللين صامتين (خطف عمود الباركود لحقل الكود + دهس `createItem`). `8474f9c`+`5328810`.
- ✅ **القالب القياسي — بُني على أعمدة المالك الحقيقية** (Item Description · Bar Code ×2 · Purchase/Sell Price · UoM ×2 · Department→Section→Family→Sub-Family · المورد). قراره: الكود «حاوية تُملأ من أودو» ⇒ ليس المعرّف ولا إلزاميًّا. `35c133a`.
- ✅ **ورقة الأرصدة** (ملف واحد · ورقتان): رصيد لكل مخزن + تشغيلة + صلاحية (لحارس FEFO). `b6efba0`.
- ✅ **ثلاث إضافات بتفويض المالك:** Min Stock (كان يُحيي ميزة تنبيه مبنيّة وميّتة) · Status · Unit Cost للتشغيلة (أساس S12). `55ee5e5`.
- ✅ **رُفع ونُشر:** `2abe013..55ee5e5` إلى main.
- ⏳ **كان معلّقًا حينها (حُلّ لاحقًا):** نشر `firestore.rules` ← نشرها المالك مساء 15.07 · واجهة الاستيراد ← بُنيت وتحقّق منها المالك «تعمل 100%».

### جلسة #3 — 2026-07-15 (محرّك المستندات F1)
- ✅ **القرارات الثلاثة (§11.6):** النموذج الرائد = **GRN** · النطاق = **الـ12 المحكومة** · الترقيم = **`GRN-2026-0001`**. (كلها موافقة للتوصيات.)
- ✅ **F1 — محرّك المستندات:** 7 ملفات خدمة + 8 مكوّنات + صفحتان + قواعد أمان موسَّعة. **محرّك واحد لا 21 تطبيقًا:** إضافة نموذج = ملف مخطّط لا شاشة. التفاصيل في **ROADMAP §11.7**.
- 🔍 **فحص هندسي للورق قبل النقل** كشف ما صُحِّح: الرقم كان يُكتب بيد الموظّف · حرارات CCP1 نصّ بلا فحص رغم أن الحدّ مكتوب فوقها · الإجماليات تُعاد كتابتها يدويًّا · اسما المستلم والفاحص يُكتبان بالقلم · 8 صفوف جامدة · **وخلل `const fileName` يكسر تصدير إكسيل في النماذج الورقية** (لم يُلمس — النماذج تبقى كما هي بقاعدة صفر حذف).
- 🐞 **خلل التُقِط قبل وصوله:** المحرّك كان يقرأ `dirty` من إغلاق قديم داخل مستمع Firestore ⇒ تحديث سحابي كان سيبتلع تعديلات الموظّف وهو يكتب. عولج بمرجع حيّ (`dirtyRef`).
- ✅ **التحقّق:** بناء أخضر (33 صفحة) · لينت نظيف (صفر مشكلة جديدة) · **30/30 اختبار وحدة** (`npm test` — أُضيف الأمر) · الحارس يحمي الصفحتين الجديدتين (تحقّق حيّ: التحويل إلى `/login`).
- ⏳ **لم يُتحقَّق حيًّا:** المسار الكامل — يحتاج **نشر القواعد** ثم دخول المالك.

### جلسة #1 — 2026-07-12
- ✅ **«وثّق» — بند 08 (مفاتيح أبوهادي/155 + أقفال):** استلام مفاتيح المخازن المُفرَّغة في موقع 155 (أبوهادي) ونقل عهدتها + طلب شراء أقفال جديدة. **المخازن الثلاثة: E5 (~3500م²) · E2 (~1000م²) · E3 (~1000م²) = ~5500م².** وُثّق في التقارير الثلاثة (شامل + موجز 08/07/01 + يومي) **مع صور المخازن الثلاث** (shot-155-e5/e2/e3.jpeg) وPDF معاد التوليد.
- ✅ **«وثّق» — بند 07 (أرفف الرحبة):** التنسيق مع المشتريات لتركيب أرفف الرحبة غدًا الإثنين 13.07 (المورّد: عالم الديكور — اسكفاري أردني Heavy-Duty ‏2.5م: 20+6، مخطط 9×7م، بوكسات 5 وحدات تسليم 4–5 أيام) تمهيدًا لنقل فينيسيا — وُثّق في التقارير الثلاثة (شامل + موجز 07/06/01 + يومي بصدارة العناوين وPDF معاد التوليد) **بلا أسعار** + صورتا المخطط منسوختان shot-decor-*.jpeg.
- ✅ **نظام التقارير — حفظ الإنجازات + الموجز اليومي الدوري:** (أ) إضافة بند 06 (بوابة متعددة الأنظمة) للتقريرين الأسبوعيين الشامل والموجز في `Weekly Logistics & Warehouse Report/لأسبوع W28— 2026/` مع لقطتين جديدتين للبوابة + تحديث إحصاءات الغلاف 06/05/01. (ب) إنشاء نظام «الموجز اليومي الخفيف» — قالب `الموجز اليومي/Daily-Brief-2026-07-12.html + .pdf` (عناوين اليوم + أمس، هوية Brandzo). (ج) مهمة مجدولة دائمة `daily-brief-whatsapp` (سبت→خميس 16:00 بنغازي): توليد + PDF + إرسال واتساب لغرفة «متابعة المهام». **معلّق:** إضافة Claude in Chrome غير متصلة — يلزم تثبيتها ليعمل الإرسال؛ وتأكيد اسم الغرفة الدقيق.
- ✅ إنشاء `SESSION_HANDOFF.md` كذاكرة جلسة حيّة.
- ✅ **خطة تحديث لوحة التحكم (OneNote) — نُفّذت:** (1) العنوان ← «إدارة مستودعات برند زو» · (2) شريط أنظمة (تعلّم أودو + صلاحيات) بدل البانر الأحمر — «محاكي أودو» = نفس تعلّم أودو (قرار المالك) · (3) أقسام مجمّعة (الخيار أ) بدل الشبكة المسطّحة · (4) إخفاء بطاقات قابل للتخصيص يُحفظ في localStorage + قسم «أنظمة قادمة» بأزرار «قريباً» · (5) إزالة إطار تقرير الجودة (متاح في مركز التقارير). كل الملف: [`dashboard/index.astro`](src/pages/dashboard/index.astro). بُني ونُجح، ومُتحقّق بصريًّا/تفاعليًّا محليًّا.
- ⏸️ **مؤجّل:** بوابة الرمز 7777 (منطق الأدوار) — قرار المالك «خطوة قادمة للنقاش». + البند الخامس المقصوص بانتظار نصّه.
- ✅ **رُفع إلى main ونُشر:** `023908b..22d42e1` — Deploy Astro site to Pages ناجح، والعنوان الجديد ظاهر على الصفحة الحيّة.
- ✅ **حُلّ مانع رفع ROADMAP.md:** كان يحوي رابط OneDrive خاصًّا (§6) + أُضيف رابط لوحة النطاق (§0) والمستودع عام. **القرار (المالك فوّض القائد):** تنقيح الرابطين إلى نصّ عام (محفوظان خاصًّا في الذاكرة) ثم الرفع.

### جلسة #2 — 2026-07-13
- ✅ **الخطة الاستراتيجية (الركائز الثماني):** أُدرجت في `ROADMAP.md` §8 (أدوار/صلاحيات · متبقّي الباك اند · قاعدة بيانات حيّة مجانية [Firebase + Oracle Always Free] · الواجهة الكاملة · مكافحة النسيان · وثيقة المطوّر · ملف أودو التعريفي · 10 اقتراحات) + §9 مسار M1–M5 + §10 بروتوكول الاتساق.
- ✅ **النطاق الرسمي:** `warehousehub.com.ly` (Libyan Spider، فعّال) سُجّل كأصل + خطوات ربطه بـ Pages (CNAME + `base:'/'` + سجلّات A). **لا أدخل حساب المسجّل — DNS مهمة المالك.**
- ✅ **تصحيحات اتساق:** أُكّد أن تحديث اللوحة مرفوع (`5eb1845`)، وأن ROADMAP كان موجودًا untracked لا مفقودًا، وأن الباك اند S5b منجز (لا S5a). حُفظت قاعدة «تحقّق قبل الثقة».
- ✅ **رُفع:** ROADMAP.md (منقّح) + هذا الملف إلى main.
- 🎉 **M1 — نظام الدخول والأدوار (منشور ومُتحقَّق حيًّا):** `auth` في firebase · `services/auth/` (roles.js بـ10 أدوار + authService.js: دخول/خروج/قراءة دور Firestore/رسائل عربية/تمهيد أدمن بالبريد) · شاشة `/login` عربية · حارس `AuthGate` بطبقة تغطية قبل الترطيب · `UserMenu` في السايدبار. تحقّق حيّ: التحويل التلقائي، معالجة الأخطاء، ودخول المالك الفعلي كـ«👑 المدير العام». رُفع `9c88a5f`.
- 🐞 **إصلاح فشل نشر CI:** `getAuth()` يتحقّق من المفتاح فورًا؛ في CI (بلا `.env`) تصبح `PUBLIC_FIREBASE_*` فارغة و`??` لا يلتقط الفراغ → `auth/invalid-api-key`. الحل: تبديل `??` بـ `||`. رُفع `db2b2ef` والنشر ناجح.
- 🔥 **Firebase Console (المالك نفّذ):** فعّل Email/Password + أنشأ حساب `albarshi.96@gmail.com` (UID يبدأ `l5jxkGb9…`). مشروع `brandzo-erp-2026` خطة Spark مجانية.
- 📋 **تقرير النظام التفصيلي (دائم + زناد «7»):** تقرير شامل مبني على مسح الكود الفعلي (المعمارية · 5 مجموعات Firestore · الصفحات الـ29 · نظام الأدوار · 16 خدمة · الأدوات · موديول أودو · التكامل · 80 ملف public · الميزات/المتبقّي). محفوظ في `public/تقرير-النظام-التفصيلي.html` (يُنشر على الموقع الحيّ) + Artifact ثابت الرابط. **الأمر «7» في أي جلسة = إعادة المسح والتحديث بنفس الروابط.**
- 🔒 **إكمال نظام الوصول (منشور `d06bb98`):** (أ) تقييد القائمة حسب الدور — `services/auth/navAccess.js` (خريطة الدور→المجموعات، المصدر الوحيد) + جزيرة `RoleNav.jsx` (تُخفي المجموعات/العناصر غير المسموحة بـ`data-role-hidden` والبحث يحترمها) + مفاتيح `key` وقيود `roles:['admin']` في `DashboardLayout` («الصلاحيات» و«إدارة المستخدمين» أدمن فقط). (ب) صفحة `/dashboard/users` + `UsersAdmin.jsx` + `usersService.js`: جدول المستخدمين، إسناد الأدوار الـ10، تفعيل/تعطيل، إضافة بالـUID، رسالة «غير مصرّح» لغير الأدمن، وقواعد أمان Firestore جاهزة داخل الصفحة. **تحقّق:** بناء CI-like أخضر (29 صفحة) · **16/16** اختبار وحدة (`canSeeGroup`/`canSeeItem`) · الحارس يحوّل الصفحة الجديدة لغير المسجّل · النشر success والصفحة الحيّة تخدم الجزيرتين.

---

## ✅ الحالة المختصرة (لقطة سريعة)

**الواجهة (منشورة حيّة):** بوابة العمليات (4 دفعات) · محاكي «تعلم أودو» (12 مرحلة) · **🔒 نظام وصول كامل** (دخول Firebase + حارس + قائمة مقيّدة بالدور + إدارة مستخدمين `/dashboard/users`) · طبقة ربط أودو جاهزة غير مفعّلة.

**الباك اند (`brandzo_warehouse`):** 37 ملف · 11 كائناً · 7 مجموعات أمان · 52 اختبار · **7 حرّاس ذهبية · 12/12 مرحلة — على main (`15d1da5`)** + سكربت تحقّق ساكن `verify_module.py`.

**المتبقّي الأساسي:** M2 بيئة أودو الحيّة لتشغيل الـ52 اختبارًا فعليًّا (S12 على main) · تفعيل تكامل أودو · نشر قواعد أمان Firestore (على المالك) · خطة OneDrive بانتظار الدمج.

---

## 🔗 روابط ومسارات سريعة

- **المستودع:** `albarshi996/warehouse-system` · فرع `main`
- **الموقع الحيّ:** https://albarshi996.github.io/warehouse-system/
- **منهج الرفع:** عند «ارفع»/«خلصنا» → مزامنة main → build+lint → commit → push مباشر → مراقبة النشر.
- **لغة الردود:** العربية حصريًّا (الكود/الأوامر/الواجهة تبقى كما هي).

### وثائق أساسية
| الملف | الغرض |
|---|---|
| [`SESSION_HANDOFF.md`](SESSION_HANDOFF.md) | ★ هذا الملف — الذاكرة الحيّة السريعة |
| [`ROADMAP.md`](ROADMAP.md) | خريطة الطريق والقواعد (الوثيقة المعمارية الكبرى) |
| [`PROJECT_MEMORY.md`](PROJECT_MEMORY.md) | وثيقة التسليم الجامعة |
| [`ODOO_BACKEND_DEVELOPMENT_PLAN.md`](ODOO_BACKEND_DEVELOPMENT_PLAN.md) | خطة الباك اند المعمارية |
| [`ODOO_INTEGRATION_HANDOFF.md`](ODOO_INTEGRATION_HANDOFF.md) | دليل ربط الواجهة بأودو |

---

## 📜 قاعدة التوثيق الثلاثي (دائمة — في كل جلسة)

**كل مهمة تُنهى → تُوثَّق في التقارير الثلاثة** بمجلد الأسبوع الحالي (`Weekly Logistics & Warehouse Report/لأسبوع W##— 2026/` — مجلد واحد لكل أسبوع):
1. **الشامل** `Weekly-Logistics-Warehouse-Report.html` — التفاصيل والصور.
2. **الموجز الأسبوعي** `Key-Accomplishments.html` — بطاقة مختصرة + تحديث إحصاءات الغلاف.
3. **الموجز اليومي** `الموجز اليومي/Daily-Brief-<تاريخ>.html` — عناوين آخر مهمة؛ وهو **المُرسَل واتساب** لغرفة «متابعة المهام» (سبت→خميس 16:00، مهمة `daily-brief-whatsapp`).

توثيقان لا يختلطان: التقارير الرسمية (أعلاه) ↔ توثيق المشروع التقني (هذا الملف + ROADMAP).

---

## 🔁 كيف تُستأنف جلسة جديدة (تعليمات للقائد)

1. افتح جلسة جديدة في هذا المجلد.
2. قل: **«تحديث أولي»** — فحص شامل فوري (هذا الملف + المهمة المجدولة + تقارير الأسبوع) وأردّ: أين توقفنا وماذا ينتظر و«لا يوجد خطأ» أو قائمة الأخطاء.
3. أعطِ المهمة، وسأحدّث هذا الملف في نهاية كل مهمة أو خطة.
4. عند اقتراب الامتلاء، قل: **«أعطني ملفك»** — أُسلّمك آخر نسخة محدّثة جاهزة.

## 📖 قاموس الأوامر الموحّد (معتمد 2026-07-12)

| الأمر | الفعل |
|---|---|
| **«تحديث أولي»** | فحص بداية الجلسة الشامل + تقرير حالة يختم بـ«لا يوجد خطأ» أو الأخطاء |
| **«وثّق»** | توثيق آخر مهمة في التقارير الثلاثة (شامل + موجز أسبوعي + يومي) |
| **«حدّث اليومي»** | توليد الموجز اليومي + PDF فقط — الإرسال 16:00 حصرًا آليًا |
| **«احفظ»** | تحديث هذا الملف (SESSION_HANDOFF) |
| **«أعطني ملفك»** | تسليم هذا الملف لجلسة جديدة |
| **«ارفع» / «خلصنا»** | منهج الرفع الكامل إلى GitHub Pages |
| **«حالة الإرسال»** | فحص مهمة `daily-brief-whatsapp` المجدولة |
| **«7»** | تحديث «تقرير النظام التفصيلي»: إعادة مسح الكود فعليًّا (صفحات · خدمات · قواعد بيانات · أدوات · حالة) ثم تحديث `public/تقرير-النظام-التفصيلي.html` + الـ Artifact بنفس الرابط + رفعه |

> **[Claude] 2026-08-13 — خطة٤: الجرد أُغلق (SAP-0) وختم اتّجاه أودو نُفّذ (SAP-15) ومدفوعان لـmain `13e62b6`:** قُرئت الخطة الحاكمة كاملةً (4203 أسطر) وأُنتج الفهم الحرفيّ في `docs/plan/sap/` بثلاثة ملفّات (§1–§25 · 80 بند مرجع · المصفوفات وجدول التغطية 38×5)، والمتتبّع `docs/plan/sap-alignment.json` بـ19 مهمّة و48 فجوة و8 قرارات مالك — **الاستئناف `npm run plan:sap`**. **قرار المالك المالي المحسوم:** أودو مصدر الحقيقة · سلطة القيد له وحده · الاتّجاه **سحبٌ فقط** والدفع يُختم لا يُحذف. **SAP-15 نُفّذت:** `odoo/directionGuard.js` بقائمة سماحٍ لا منع، مركّبٌ في `callProxy` وفي `odoo` المُصدَّر من `index.js` (فيغطّي المحاكي أيضًا)، و`defaultPolicyFor` قُلب إلى `pull` للجميع، ولوحتا التكامل والمزامنة تُعلنان الختم. **درسٌ مسجَّل:** لقطة المالك للصفحة الحيّة كشفت ثلاثة عيوبٍ لم تكشفها 1304 اختبارًا (المحاكي غير مختوم في وضع التدريب الحيّ · نصّ الصفحة يُعلن الدفع · عطب `[object Object]` على `TRN`) — §23 ‹1023› صحيحٌ حرفيًّا. **البوّابات على الشجرة المدموجة:** build 96 صفحة · 1327/1327 اختبارًا · audit نظيف · lint 126 (53/73) بلا زيادة. **الحدّ الصادق:** لم أفحص الصفحتين بصريًّا حيًّا (خلف تسجيل دخول). **التالي:** SAP-1 جاهزة، أو SAP-4 إن حُسم قرار الحالتين `closed`/`canceled`.

> **[Claude] 2026-08-13 — SAP-16: السحب الموحَّد والمرآة المالية مدفوعان لـmain `0acd79f`:** جواب سؤال المالك «هل الاستيراد من أودو موجود دوريًّا وبزرّ؟» كان **لا**: زرٌّ واحد في تبويب الأصناف وحده، ولا دوريّة، وثلاثة مسارات من أربعة لا تُسحب. **المبنيّ:** `pullRegistry.js` (جدول خمسة نطاقات بمعرّف مرآةٍ حتميّ) · `financeMapper.js` (مترجم لا محرّك قيود — الفاتورة `account.move` بنوعٍ مختلف، والرصيد المفتوح مستوردٌ لا محسوب، والتوازن يُعلَن ولا يُسوّى) · `pullService.js` (سحبٌ عامّ، وما يختفي من أودو يُوسم `missingInOdoo` ولا يُحذف، ونطاقٌ يفشل لا يحجب الباقي) · بذرة مالية في المحاكي (١٠ حسابات · ٤ قيود · ٦ أسطر · دفعتان) · واجهة: زرّ «اسحب من أودو» في **كلّ** التبويبات + سحبٌ تلقائيّ كلّ ٥ دقائق قابل للإيقاف + «آخر سحب» **محسوبًا من أقدم نطاق لا أحدثه** + حالة كلّ نطاق على حدة. **القواعد:** ٥ مجموعات مرآة (`odoo_accounts`/`odoo_moves`/`odoo_move_lines`/`odoo_payments`/`odoo_pull_state`) — قراءةٌ لكلّ مصادَق وكتابةٌ للمديرَين و**لا حذف أبدًا**؛ الملفّ الآن **١١٩٣ سطرًا** على main. **⚠️ حدّ حاكم:** المالك أعلن النشر قبل الدفع، وملفّ main كان حينها ١١٥٥ سطرًا بلا المجموعات الخمس — **يلزم تأكيد أنّ المنشور فعلًا يحوي `odoo_pull_state`، وإلا فُشِل السحب المالي بمنع صلاحية**. **البوّابات:** 1355/1355 · build 96 · audit نظيف · lint 126 بلا زيادة. **الحدّ الصادق:** لم أفحص الصفحة حيًّا (خلف تسجيل دخول) — ولقطتا المالك اليوم أثبتتا أنّ هذا الحدّ يكشف ما لا تكشفه ١٣٠٠ اختبار. **التالي:** SAP-1 جاهزة، أو SAP-4 إن حُسم قرار الحالتين.

> **[Claude] 2026-08-13 — نقطة حفظ: الشقّ الأوّل من خطة٤ مُغلق، والثاني ينتظر:** ثلاث مهامّ أُنجزت ودُفعت لـmain (`2c563b9`): **SAP-0** الجرد الحاكم (الفهم الحرفيّ 4203 أسطر + المصفوفات + جدول التغطية 38×5 · 48 فجوة) · **SAP-15** ختم اتّجاه التكامل (سحبٌ فقط · حارس عند حدّ النقل يغطّي الحقيقيّ والمحاكي · `defaultPolicyFor` قُلب إلى `pull`) · **SAP-16** المرآة المالية والسحب الموحَّد (سجلّ نطاقات + مطابقات + خدمة سحب + بذرة محاكي + زرّ ودوريّة و«آخر سحب» + 5 مجموعات قواعد). **الاستئناف: `npm run plan:sap`** — البطاقة تقول أين توقّفنا حرفيًّا. **السؤال الوحيد المفتوح في SAP-16:** لقطة المالك بعد النشر أظهرت «شجرة الحسابات 10 · الآن» (⇒ الدورة تعمل من طرفها إلى طرفها) والثلاثة الباقية «لم يُسحب» — والحسم بضغطة زرّ ولقطة، فالشارات صارت تُظهر «تعذّر» بالأحمر مع السبب. **الشقّ الثاني حين نعود:** SAP-1 (هويّة الصنف — الكود هو الهويّة) ثمّ SAP-2 (كتالوج الطرف‑الصنف — الفجوة الأكبر) ثمّ SAP-3 (وصل محرّك الوحدات بالمستندات — مبنيٌّ ومعزول اليوم). و**SAP-4 محجوبة على قرار المالك** في الحالتين `closed`/`canceled`، وفتحُها يفتح ثلاث مهامّ خلفها. **درسا اليوم:** (١) البناء والاختبارات لا تُثبت القبول الحيّ — لقطتا المالك كشفتا ٤ عيوب لم تكشفها 1300 اختبار؛ (٢) العطب الظاهر قد يكون عرَضًا لعطبٍ في المنبع (`[object Object]` كان يُكتب في أودو وقت الدفع لا وقت العرض).

> **[Claude] 2026-08-13 — SAP-4: حالتا «مغلق» و«ملغى» مدفوعتان لـmain `3aa158a`:** قرار‑٣ حُسم بـ«نعم». الحالات صارت **سبعًا**: الخمس السابقة + `closed` (صحيحٌ ولن يُنفَّذ المتبقّي · من `approved` أو `done`) + `canceled` (لم يعد صحيحًا ويبقى في السجلّ · من `draft`/`rejected`/`approved` — **لا من `done`**: المنجَز رحّل حركاتٍ في دفترٍ ملحق-فقط فالتصحيح بمستندٍ عكسيّ لا بقلب حالة، §16.9 ‹660›). كلتاهما بسببٍ مُلزِم ولا رجوع منهما، ولا اشتقاق منهما (`canDeriveFrom`). **وثلاث حالاتٍ محسوبة لا مخزَّنة:** `open/partial/fulfilled` عبر `executionStatus(state, {capacity, executed})` — المخزَّن يتقادم لحظةَ يُنشأ مستندٌ لاحق ويتعارض مع الروابط. و`hasOpenWork` تُخرج المنتهيَين من صندوق العمل اليوميّ. **تصحيحٌ ذاتيّ:** أتحتُ الإلغاء من `submitted` فأسقط اختبارًا قائمًا يحرس «لا يرى أمين المخزن أيّ إجراء على مستندٍ ينتظر الجودة» — والقاعدة صحيحة، وكانت إتاحتي **سلطةً جديدة لا مصدر لها** (§3-٩)، فأُزيلت. وأُزيل `emoji` من جدول الحالات وحلّت محلّه نقطةٌ ملوّنة في `StateBar`/`ChainBar`. **القواعد 1212 سطرًا على main وتفرض النقلتين في الطبقة الثالثة — ⚠️ لم تُنشر بعد.** **البوّابات:** 1370/1370 · build 96 · audit نظيف · lint 126 بلا زيادة. **فُتحت خلفها ثلاث:** SAP-5 (جلب من مستند سابق — البوّابة) ثمّ SAP-9 (الأرقام القابلة للنقر) وSAP-12 (صندوق المستندات المفتوحة).

> **[Claude] 2026-08-13 — SAP-5: «جلب من مستند سابق» مكتملة ومدفوعة (`d7f438d`) — الحزمة الرابعة أُغلقت:** كان نصف مسار النسخ مفقودًا («إنشاء مستند لاحق» مبنيّ منذ CC-204). **العقد الخالص** (`af9ed8d`): `derivationSources` في chain.js **معكوسةٌ من الجدول نفسه** لا خريطة ثانية — فمن أضاف تفرّعًا لا يستطيع نسيانه في الاتّجاه الآخر؛ و`copyFrom.js` بأهليّةٍ ثلاثيّة (الحالة عبر `canDeriveFrom` من SAP-4 · الطرف · الكمّيّة المفتوحة) **وغير المؤهّل يُعاد مع سببه** لأنّ الإخفاء الصامت يُنتج الازدواج؛ والدمج من عدّة مصادر بقيد **طرفٍ واحد**؛ ولا تجاوز للمتبقّي والتجاوز يُقال بالرقم. **الشاشة** (`d7f438d`): `CopyFromPanel.jsx` تظهر على المستند الجديد وحده (بعد الحفظ يتولّى `ChainBar` — فلا يختلط المساران §20 ‹972›)، والإنشاء يمرّ بـ`createCombinedInChain` **نفسها** فلا مسار بياناتٍ ثانٍ، والقائمة مُرشِّح بلا شبكة بينما الجدول يجلب العلاقات الحقيقيّة عند الاختيار — فلا كمّيّةَ تُكتب على تقدير. **البوّابات:** 1385/1385 (+15) · build 96 · audit نظيف · lint 126 بلا زيادة. **التقدّم 5/19 (26٪).** **⚠️ لم تُفحص الشاشتان (SAP-4 وSAP-5) حيًّا** — خلف تسجيل دخول؛ المطلوب: منجَزٌ يُظهر «إغلاق» لا «إلغاء»، ومستندٌ جديد يُظهر «جلب من مستند سابق» فيعمل المسار كاملًا. **التالي:** SAP-1 (هويّة الصنف) ثمّ SAP-2 (كتالوج الطرف‑الصنف — الفجوة الأكبر)، أو SAP-9/SAP-12 وقد فُتحتا بإغلاق SAP-5.

> **[Claude] 2026-08-13 — SAP-9: الأرقام تُفتح على مستنداتها، مدفوعة (`cfed7bb`):** §15.1 ‹400› — من أمر الشراء يُفتح «١٠٠» على الاستلامات و«٩٧» على التخزين و«٣» على محضر الرفض. **والبيانات كانت مبنيّةً كلّها:** `documentLineProgress` يحسب الخمسة ويحمل `contributions` بالمستند الهدف؛ الناقص كان **قلبَ التفصيل إلى سؤالٍ معكوس** — أُضيف بـ`lineOutcomes`/`progressOutcomes` في **مكانٍ واحد** لا في كلّ شاشة (نفس قاعدة `derivationSources`). **ولا جدولَ ثانيًا:** الجدول الثلاثيّ يُجمّع بالصنف عبر ثلاثة مستندات وهذا يُفصّل ما صار لكلّ سطر — سؤالان مختلفان، فأُغني القائم بالحفر وأُضيف عمودا «مرفوض» و«مفتوح» (§3-٤ ‹43›). وما لا مصدر له يبقى نصًّا لا زرًّا. **البوّابات:** 1390/1390 (+5) · build 96 · audit نظيف · lint 126 بلا زيادة · **ولا قواعد جديدة فلا انتظار نشر.** **المتبقّي في SAP-9:** حقل RMA في المرتجع (ف‑٢٢)، وقرار ASN (ف‑٢٠ — موجودٌ حقلًا لا مستندًا، والتوصية ألّا يُبنى مستند مستقلّ §4 ‹78›). **التالي بعدها:** SAP-12 صندوق المستندات المفتوحة.

> **[Claude] 2026-08-13 — ★ نقطة استئناف خطة٤: سبع مهامّ مغلقة (37٪) والكلّ مدفوع لـmain `34622bc`:** **الاستئناف بأمرٍ واحد `npm run plan:sap`** — البطاقة تقول أين توقّفنا حرفيًّا. **المغلقة:** SAP-0 الجرد (الفهم الحرفيّ 4203 أسطر · المصفوفات · جدول التغطية 38×5 · **48 فجوة**) · SAP-15 ختم اتّجاه أودو (سحبٌ فقط · حارس عند حدّ النقل يغطّي الحقيقيّ والمحاكي) · SAP-16 المرآة المالية والسحب الموحَّد (زرّ + دوريّة ٥ دقائق + «آخر سحب» + ٥ مجموعات قواعد) · SAP-4 حالتا **مغلق/ملغى** (+ثلاثٌ محسوبة) · SAP-5 **جلب من مستند سابق** (عكس خريطة الاشتقاق + شاشة) · SAP-9 **الأرقام القابلة للنقر** ١٠٠/٩٧/٣ + RMA · SAP-12 **صندوق العمل المفتوح**. **قرارات المالك المحسومة (٦):** أودو مصدر الحقيقة المالي · سلطة القيد له وحده · الاتّجاه سحبٌ فقط · الحالتان الجديدتان · ASN يبقى حقلًا لا مستندًا · تأجيل Supabase لما بعد الجرد. **البوّابات:** 1401/1401 · build 96 · audit نظيف · lint 126 بلا زيادة. **⚠️ الحدّ الحاكم: خمس شاشات مبنيّةٌ ولم تُفحص حيًّا** (خلف تسجيل دخول): الحالتان · جلب من مستند سابق · الأرقام القابلة للنقر · صندوق العمل المفتوح · مرآة أودو المالية (نطاقٌ واحد من أربعة تأكّد: «شجرة الحسابات 10 · الآن»). **درسا اليوم:** (١) البناء والاختبارات لا تُثبتان القبول الحيّ — لقطتا المالك كشفتا أربعة عيوب لم تكشفها 1300 اختبار؛ (٢) العطب الظاهر قد يكون عرَضًا لعطبٍ في المنبع (`[object Object]` كان يُكتب في أودو وقت الدفع لا وقت العرض). **التالي:** SAP-1 (هويّة الصنف — الكود هو الهويّة) ← SAP-2 (**كتالوج الطرف‑الصنف · الفجوة الأكبر**، ويحتاج مجموعة Firestore جديدة ⇒ نشر قواعد) ← SAP-3 (وصل محرّك الوحدات بالمستندات — مبنيٌّ ومعزول). وSAP-8 محجوبة على قرار تمثيل الرقم التسلسليّ.

> **[Claude] 2026-08-13 — SAP-1: هويّة الصنف وبطاقته مغلقة (`250288a`) — التقدّم 8/19 (42٪):** §9.1 ‹182› «الكود هو الهويّة، والباركود وسيلة بحث» صار عقدًا مكتوبًا: `itemIdentity.js` خالصةٌ بأربعةٍ وعشرين اختبارًا حارسًا **قبل أيّ واجهة** (§22 ‹995›) — حارس `assertNewItemIdentity` يرفض صنفًا جديدًا بلا كود (ف‑١) وهو مربوطٌ في `createItem`، والهويّة بالباركود موسومةٌ `barcode-legacy` توافقًا رجعيًّا فلا ينكسر رصيدٌ قديم، وتغيير الاسم مُثبَتٌ ألّا يمسّ مفتاحًا (§9 ‹220›). **بطاقة صنفٍ جديدة** تُفتح من كود الصنف في القائمة: الكمّيّات الأربع (§9.2 ‹191› — «المطلوب» من أوامر الشراء المفتوحة **تقريبًا معلَنًا** بعقد صندوق SAP-12 نفسه، ودخوله معادلة المتاح متروكٌ عمدًا لدفتر SAP-7/ف‑١٧) + **البدائل** (ف‑٣: حقل `substitutes` مطبَّع والمفقود من الماستر يُعلَن بالأحمر) + تبويب «الموردون والعملاء» (ف‑٥ جزئيًّا حتى كتالوج SAP-2) + أرصدة FEFO تفصيليّة. **وعمود الكود في بنود المستندات كلّها صار مرجعيًّا** (ف‑٤٣): كتابة كودٍ تسأل الماستر بالهويّة أولًا ثمّ الباركود وتثبّته بصيغة الماستر القانونيّة بلا دهس المختلف — في `LineItemsTable` الواحد لا في ٣٨ مخطّطًا. **درس جانبيّ:** أمر PowerShell لاستبدال نصٍّ أفسد ترميز ملفٍّ عربيّ (قراءة ANSI) — أُعيدت كتابته بأداة Write؛ لا round-trip لملفٍّ عربيّ عبر PowerShell بلا `-Encoding utf8` صريحة في القراءة. **البوّابات:** 1425/1425 (+24) · build 96 · audit نظيف · lint 120 دون خطّ الأساس 126 وملفّات المهمّة صفر. **⚠️ لم تُفحص الشاشة حيًّا** (خلف تسجيل دخول): المطلوب — ضغط كودٍ يفتح البطاقة بكمّيّاتها الأربع، وكتابة كودٍ بحروفٍ صغيرة في بند مستندٍ تُثبّته وتملأ الوصف، ومحاولة صنفٍ بلا كود تُرفض. **التالي:** SAP-2 (كتالوج الطرف‑الصنف — الفجوة الأكبر، يحتاج مجموعة `item_partner_catalog` ⇒ قرار‑٥ نشر القواعد) أو SAP-3 بعدها.

> **[Claude] 2026-08-13 — ملاحظتا المالك أُصلحتا (`ca7f40a`) ثمّ SAP-2 أُغلقت (`4df8832`) — التقدّم 9/19 (47٪):** **أولًا الإصلاح:** لقطتا المالك من الموقع الحيّ كشفتا 404 عند «صندوق المستندات» ومنظرًا فوضويًّا في شاشة المستند — والجذران: رابطٌ **نسبيّ** (`href=\"documents\"`) يتحوّل على Pages إلى `document/documents` لأنّ الخادم يضيف الشرطة الختاميّة، وصفحة المستند **لا تستورد** `tokens.css`/`odoo.css` بينما شريط التنقّل (CC-202) وخريطة العلاقات (CC-203) مبنيّان على أصناف `.o_theme` ومتغيّرات `--o-*` — فسقطت الحدود والألوان وتكدّست الأزرار نصًّا عاريًا. أُصلح الاثنان وثُبّت من `dist`: الصفحة تضمّ `odoo.css` والرابط مطلق. **درسٌ يتكرّر:** الشاشة الحيّة تكشف ما لا يكشفه الاختبار — عطبان عاشا منذ CC-202/203 عبر بوّاباتٍ خضراء متتابعة لأنّ فحص المتصفّح جرى على dev لا على Pages. **ثمّ SAP-2 (الفجوة الأكبر ف‑٦):** `item_partner_catalog` بمعرّفٍ حتميّ والإلزاميّ ثلاثة فقط (§10.2 ‹251›) · **ترتيب البحث الخماسيّ حرفيًّا** (SR-49: كود ← باركود ← كود الطرف لهذا الطرف وحده ← كتالوج المصنع ← الاسم) والمجهول null لا صنفَ جديدًا (SR-50) · بنود المستندات تسأل كتالوج طرف المستند عمّا جهله الماستر، والصنف الداخليّ يُخزَّن وكود الطرف يبقى على السطر عرضًا (§10 ‹257›) · الربط المتبادل (ف‑٧) بخطّة مرآتين نقيّة وحقلٍ في نموذج الشريك · تبويب «الموردون والعملاء وأكوادهم» حيّ في بطاقة الصنف مع إدخالٍ للمديرَين. **19 اختبارًا حارسًا. البوّابات: 1444/1444 · build 96 · audit نظيف · lint 120.** **⚠️ حدّان:** الشاشات خلف تسجيل الدخول لم تُفحص حيًّا، **وكلّ كتابةٍ على الكتالوج مرهونة بنشر القواعد — قرار‑٥ صار على المسار الحرج**. **التالي:** SAP-3 (وصل محرّك الوحدات بالمستندات — المحرّك مبنيٌّ ومعزول وحقل uom نصٌّ حرّ في كلّ المخطّطات).

> **[Claude] 2026-08-13 — SAP-3 مغلقة (`0bc0bcd`) والنشر معلَن — تجاوزنا النصف: 10/19 (53٪):** **الاكتشاف تأكّد حرفيًّا: لا كاتبَ واحدًا لـ`uomFactors` في النظام كلّه** — محرّك الوحدات كان يعمل في الفراغ. الوصل في `uomWiring.js` (16 اختبارًا · `uomModel` لم يُمسّ — سجلّ التكيّف: يُعاد استخدامه لا يُستبدل): **(١)** أوّل محرّر تعريفاتٍ في نموذج الصنف (أساس · معاملات «carton=24» · شراء/بيع · باركودات وحدات) يحرسه `factorProblems`؛ **(٢)** عمود الوحدة في البنود اختيارٌ من وحدات الصنف — مركزيًّا في `LineItemsTable` الواحد، والنصّ القديم يبقى خيارًا («CTN = كرتون») فلا يُكسر مستند (ف‑٤٢)؛ **(٣)** كلّ تعديل بندٍ يختم المعامل والكمّيّة الأساسيّة على السطر (§10.1 ‹234› · ف‑١١)؛ **(٤)** الدفتر يقدّم معامل الطرف المختوم على معامل الصنف — موردان بتعبئة 24 و20 لا يختلطان (§10 ‹256›) والختم البائت يسقط، باختبارَين على `movements` نفسه؛ **(٥)** باركود الوحدة يحدّد الصنف والوحدة والمعامل معًا (ف‑١٠)؛ **(٦)** وحدتا شراء/بيع افتراضيّتان بعائلة المستند (ف‑٩). `3 × carton(24) = 72` مثبتة في السطر وفي الدفتر. **وقرار‑٥ سُجّل**: المالك أعلن نشر القواعد (المرجع 1232 سطرًا `75A3A867…`) — والمطابقة الحيّة (تسجيل كودٍ ينجح) لم تُجرَ بعد. **البوّابات: 1462/1462 (+18) · build 96 · audit نظيف.** **⚠️ الحدّ:** الشاشات لم تُفحص حيًّا؛ السيناريو المطلوب: تعريف كرتون=24 لصنف ⇒ GRN بثلاثة كراتين ⇒ الدفتر 72 قطعة، وكتالوج مورّدٍ بمعامل 20 ⇒ 60. **التالي:** SAP-6 (الخريطة — مفتاح «إظهار المراجع») أو SAP-7 (دفتر المخزون: Ordered والموقع) وقد فُتحت بإغلاق SAP-3.

> **[Claude] 2026-08-13 — SAP-19 (طلب المالك): شاشة العمليات المخزنية على الهاتف — امسح فتُعبَّأ (`b0c7411`) — 11/20 (55٪):** شكوى المالك الحرفيّة عن /dashboard/stock-operations: خارج الهويّة، منطقٌ متشعّب، تجمّد عند مسح الباركود بالهاتف، والمطلوب «أختار وضعًا، أمسح، تظهر خانة فيها الاسم أو أسمّيه والكمّيّة». **أُضيفت للمتتبّع معلمًا جديدًا «حط» (طلبات المالك) ونُفّذت فورًا:** `scanFlow.js` خالصة (10 اختبارات — خانة التعبئة للمعروف والمجهول · حارس الكسر بوحدة الصنف · قيم opType القديمة حرفيًّا) + `ScanFlow.jsx` بهويّة bz-light: ثلاث خطوات، كاميرا **BarcodeDetector عتاديّة** بفحص كلّ ٣٠٠م.ث (القديم كان يفكّ على المعالج إطارًا إطارًا — هذا هو سبب التجمّد)، والمجهول يُسمّى فيدخل Items_Pending القائمة. **ولا حذف (§3-٣):** الرابط الأصليّ صار الشاشة السريعة والأداة الكاملة القديمة (3532 سطرًا) بقيت حرفيًّا على `/stock-operations-classic` برابطٍ متبادل، ومسار البيانات واحد (نفس stock_operations وappendScan ومفتاح الاستئناف). **البوّابات: 1472/1472 (+10) · build 97 · audit نظيف.** **⚠️ الحكم الحقيقيّ فحص المالك من هاتفه** — الشكوى جاءت من الهاتف والقبول منه. **التالي:** العودة لمسار الخطة — SAP-6 أو SAP-7.

> **[Claude] 2026-08-13 — ★ تصحيحٌ بأمر المالك: شاشةٌ واحدة لا صفحة فوق صفحة (`b5da018`):** نسختي الأولى من SAP-19 أبقت الأداة القديمة على مسارٍ ثانٍ `/stock-operations-classic` برابطٍ متبادل — **فرفضها المالك نصًّا: «أنت بنيت صفحة فوق صفحة وهذا خطأ كارثي، نريد منطقًا في التنفيذ»**. الدمج النهائيّ: شاشةٌ واحدة على الرابط الأصليّ — المسح ثلاث خطوات + جدول الجلسة **يُشتقّ من دفتر العملية الملحق-فقط مباشرةً** (فالعمل الجماعيّ حيٌّ بطبيعته: جهازان على العملية نفسها يريان جدولًا واحدًا بلا مئات أسطر التوفيق التي احتاجتها القديمة) + **الدفتريّة من ماستر الأصناف** لا من استيراد شيتٍ ثانٍ + التصحيح والحذف **قيودُ فرقٍ** تبقى في السجلّ + مرشّحات وتصدير إكسل + انضمامٌ برمز العملية. صفحة classic حُذفت (أُنشئت وحُذفت في اليوم نفسه — تاريخ git يحفظ الأداة القديمة كاملة 3532 سطرًا). **درسٌ للسجلّ: «لا حذف» لا تعني «انسخ القديم جانبًا»** — حين يكون الازدواج نفسه هو العطب، الدمج بمنطقٍ واحد هو الوفاء الحقيقيّ، والمالك يحكم على الشكل النهائيّ لا على النيّة. **البوّابات: 1477/1477 (+15 حارس التدفّق) · build 96 · audit نظيف.** **التالي:** فحص المالك من هاتفه، ثمّ العودة للمسار — SAP-6 أو SAP-7.

> **[Claude] 2026-08-14 — SAP-6 مغلقة (`9bb1691`) — 12/20 (60٪):** الخريطة تميّز المرجع وتطويه: **§13 ‹348›** البطاقة المرجعيّة لا توحي بترحيل كمّيّة — `carriesQuantity` و«مرجع — لا ترحيل كمّيّة» **ولو حمل الرابط رقمًا مسجَّلًا** (باختبار)، وإطارٌ متقطّع بلون info وشارة «مرجع» · **المرجع ‹1735›** مفتاح «إظهار المراجع» يطوي عقد REFERENCE ويُحصي المطويّ (`graphView` خالصة) ولا يظهر لمستندٍ بلا مراجع · **ف‑٤٦** أُغلقت: VAN_CHAIN (من CC-301) + سجلّ `STANDALONE_TYPES` بتسعة أنواعٍ مستقلّة **بسببٍ مكتوبٍ لكلٍّ** (الأمانة بتدفّق التسوية القائم لا اشتقاقٍ موازٍ §4 ‹78› · المالية قيمتها لأودو) + **اختبارٌ حارس يمنع نوعًا خارج الخريطة بلا سببٍ بعد اليوم**. الأسئلة الستّة §19.3 كلّها نعم — موثَّقة في المتتبّع. **البوّابات: 1487/1487 (+4) · build 96 · audit نظيف.** **التالي:** SAP-7 (دفتر المخزون: Ordered والموقع والتكلفة) — فُتحت بإغلاق SAP-3.

> **[Claude] 2026-08-14 — SAP-7 مغلقة (`085f540`) — 13/20 (65٪):** معادلة §14 ‹356› اكتملت بمصدرها الحقيقيّ: `ledger/openDemand.js` يقرأ «المطلوب» من الرصيد المفتوح لأوامر الشراء عبر `measureDocument` **نفسه** (لا حساب رصيدٍ ثانٍ يفترق)، و**المتاح = الموجود − المحجوز + المطلوب** في `itemQuantities` ودفتر المخزون (عمود «المطلوب» الجديد) وبطاقة الصنف — والسالب يُعلَن بالأحمر لا يُقصّ. **§21-٧ باختبار:** نقلٌ مفتوح يحجز في مصدره ويطلب في وجهته **ولا يُخرج دلتا موجودٍ بنيويًّا**، وعلى مستوى الصنف يتوازن. **§14 ‹377›:** كلّ حركةٍ في الكشف رابطٌ يفتح مستندها برقم بندها. **ف‑١٨ عرضًا لا مفتاحًا:** بند التخزين ينقل الموقع (bin) للحركة ولوجهة الرصيد ويظهر في الكشف — **وقرارٌ معماريّ موثَّق:** إدخال bin في مفتاح الرصيد الآن يكسر السحب (التخزين يقيّد على مفتاحٍ بموقع والسحب يخصم بلا موقع فتتباعد الأرصدة ويرفض حارس السالب سحبًا صحيحًا)؛ الترقية آمنة حين تحمل أسطر PICK مواقعها — مرشّح لاحق. **البوّابات: 1496/1496 (+9) · build 96 · audit نظيف.** **التالي:** SAP-10 (المرتجعات — الشروط الستّة ومسار الفحص) أو SAP-11 (المرفقات) — وSAP-8 محجوبة على قرار‑٤ (التسلسليّ).

> **[Claude] 2026-08-14 — SAP-10 مغلقة (`9ae2f68`) — 14/20 (70٪):** **ف‑٤٨ سُدّت:** `GRN>VRT` و`DN>RET` دخلا خريطة الاشتقاق فصارت «الكمّيّة المؤهلة للإرجاع» معرَّفةً = المنفَّذ − ما أُرجع سابقًا (ف‑٢٥). **★ وإصلاحٌ جوهريّ اكتُشف أثناء الوصل:** الاشتقاق كان يكتب `BASE` دائمًا — فإضافة مسار الإرجاع كانت ستجعل استلامَ ١٠٠ وإرجاعَ ١٠ «تنفيذًا لـ١١٠» وتقلب المطابقة الثلاثية؛ فأُضيف `derivationLinkType` والإرجاع صار علاقةَ `RETURN` (مُثبَتٌ باختبارٍ على `documentLineProgress` نفسه). **الشروط الستّة** حكمٌ يُسمّي كلّ ناقص. **ف‑٢٤:** موقعا `RETURNS` (mustZero — رصيدٌ باقٍ = مرتجعٌ لم يُفرز) و`MAINTENANCE`، و`RET` يهبط في **الفحص لا في ساحة استلام المورّد** (§15 ‹430›)، والفرز ثلاثيّ الوجهة من حقلَي البند القائمين بلا حقلٍ مخترع. **SR-63:** عقد العكس بتسعة حقول يقلب المستودعين ولا يمسّ الأصل؛ **SR-62:** لا إرجاع من نقلٍ لم يُنفَّذ. **ف‑٢٣:** الأربعة الماليّة موصوفةٌ فجوةً ولا يُلفَّق لها أثر. **البوّابات: 1515/1515 (+19) · build 96 · audit نظيف.** **⚠️ حدّ:** واجهة الفرز لم تُبنَ — `inspectionPlan` جاهزةٌ بلا شاشة، وشاشة «تنفيذ الإرجاع» بعمود المتاح (SR-60) مرشَّحة تالية. **التالي:** SAP-11 (المرفقات) — والمتبقّي بعده SAP-13/14 ثمّ SAP-17 و18.

> **[Claude] 2026-08-14 — SAP-11 مغلقة (`7faa7ba`) — 15/20 (75٪):** المرفق صار كائنًا موثَّقًا بعقد SR-57 كاملًا: **ف‑٢٧** بصمة SHA-256 تُحسب في `attachmentBody` الواحد لكلّ المواضع (المحتوى المتطابق بصمته متطابقة — تنكشف النسخة المكرّرة ويُثبَت أنّ الدليل لم يُبدَّل) + **الحجم صار يُخزَّن** (كان يُفحص ويُرمى) · **ف‑٢٦ (§17 ‹882›)** «إصدار جديد» مستندٌ يشير لسابقه (`supersedes`+`version`) والسابق لا يُمحى، والعرض **سلاسل** (`versionChains`) الأحدث يتصدّرها والتاريخ تحتها · **ف‑٢٨ (SR-55)** مجموعة `entity_attachments` لمرفقات البطاقات (صنف/مورّد/عميل) بنفس الإلحاق-فقط + `EntityAttachments` في بطاقة الصنف ونموذج الشريك — المواضع التسعة مغطّاة. الضغط انتُزع لوحدة `attachmentCapture` المشتركة. **البوّابات: 1519/1519 (+4) · build 96 · audit نظيف.** **⚠️ القواعد 1248 سطرًا (`3E7FD66E…`) تنتظر نشر المالك** — قبلها كتابة مرفقات البطاقات تُرفض. **التالي:** SAP-13 (الحفر التحليليّ) ثمّ SAP-14 — وبهما تكتمل حزمة التقارير قبل SAP-17 المالية وSAP-18 الختامية.

> **[Claude] 2026-08-14 — نشر القواعد ثمّ SAP-20 (طلب المالك): حقول الطرف من قوائم النظام (`226d3ea`) — 16/21 (76٪):** المالك أعلن نشر القواعد (1248 سطرًا) وسُجّل. ثمّ طلبان: **(١)** «الطرف يُختار من القائمة التي في النظام و`***` تعرضها» — بُني عقد `partyFields.js` مركزيًّا (11 مفتاح حقلٍ: مورّد/عميل رمزًا واسمًا · 4 مفاتيح مخزن/فرع · مندوب · مركبة) + منبثق `PartyField` يفتح بالتركيز و`***` تعرض الكلّ والبحث احتواءٌ بالرمز والاسم **والاختيار يملأ الرمزَ والاسمَ معًا** (الشقّ الحيّ من ف‑٤٤) — في `FieldInput` الواحد لا في ٣٨ مخطّطًا؛ المؤرشف لا يُعرض، والحرّ باقٍ توافقًا، وقارئ المندوبين المحصور يتدهور بهدوء. **(٢)** «لا يوجد مستند يعبّئ البضاعة للمندوب» — **تشخيص لا بناء**: المستند موجود (VLD أمر تحميل المركبة، مخزن ← عهدة `VAN:‹لوحة›`) والعطب أنّ مشغّل «بدء مستند جديد» لم يعرض عائلة المركبة الثمانية إطلاقًا؛ أُضيفت مجموعة «البيع من المركبة» — لا اختراعَ لموجود (§4 ‹78›). **البوّابات: 1526/1526 (+7) · build 96 · audit نظيف.** **التالي:** SAP-13 (الحفر التحليليّ).

> **[Claude] 2026-08-14 — SAP-21 (طلب المالك): ماستر المندوبين (`36f457a`) — 17/22 (77٪):** المالك أكّد أنّ منبثق الطرف «ظهر» وطلب «إدارة المندوبين وإضافتهم وإدارة الفروع وإضافتهم». **المندوبون — ماسترٌ كان معدومًا:** `sales_reps` (الاسم هويّةٌ لا تتكرّر · هاتف يُفحص شكلًا · مركبة معتادة من الجرد · أرشفة لا حذف · `uid` اختياريّ لربط حساب دخولٍ لاحقًا بلا ازدواج) + شاشة `/dashboard/sales-reps` في «الميدان» + **قوائم SAP-20 صار مصدرها الماستر المقروء لكلّ مصادَق** بدل دليل المستخدمين المحصور. **الفروع — تشخيص:** شاشة `/dashboard/warehouses` قائمةٌ أصلًا بإضافةٍ وتعديل — لا ازدواج، وقرار‑٦ (فروع أم كيانات) يبقى للشقّ الماليّ. **البوّابات: 1531/1531 (+5) · build 97 · audit نظيف.** **⚠️ القواعد 1261 سطرًا تنتظر النشر** (sales_reps). **التالي:** SAP-13.

> **[Claude] 2026-08-14 — نشر القواعد (1261) ثمّ SAP-13: الحفر التحليليّ (`ebb124d`) — 18/22 (82٪):** **§18 ‹922› مفروضٌ فرضًا لا موعودًا:** كلّ دالّة تفكيكٍ في `ledger/drill.js` تُعيد القائمة **ومجموعها**، والاختبار الحارس يقارنه برقم البطاقة المحسوب من `itemQuantities`/`itemOpenDemand` — فقائمةٌ لا تفسّر رقمها تُسقط البناء. **§18 ‹921›:** الأرقام الثلاثة في بطاقة الصنف أزرارٌ تفتح قائمتها تحتها لا صفحةً عامّة (الموجود ⇒ أرصدةٌ بمخزنها وتشغيلتها وموقعها · المحجوز ⇒ وعودات بيعٍ ونقلٌ صادر · المطلوب ⇒ مستنداتٌ مفتوحة بأرقامها وسببها)، و«المتاح» بلا قائمةٍ عمدًا فهو معادلةٌ منها. **المسار الكامل:** كلّ صفٍّ رابط — المستند يفتح شاشته، والرصيد يفتح دفتر الحركات ومنه البند والدفعة. **البوّابات: 1536/1536 (+5) · build 97 · audit نظيف.** **⚠️ حدّ:** الحفر من لوحات المؤشّرات العامّة (kpis وتقارير الشراء) لم يُوصَل — البطاقة والدفتر أوّلًا لأنّهما مصدر الأرقام. **التالي:** SAP-14 (تقارير 360° وتتبّع الدفعة) — ثمّ SAP-17 و18.

> **[Claude] 2026-08-14 — SAP-14 مغلقة + قاعدة المالك صارت حارسًا (`fb0e1f7`→`21c863b`) — 19/22 (86٪):** طلب المالك: «أيّ شيءٍ له أثرٌ في البوابة يجب أن يكون له تقرير» — **وهي قاعدةٌ حاكمة لا قائمة، فصارت حارسًا آليًّا:** `reportCoverage.test.js` يفرض أنّ كلّ نوعٍ له قاعدة ترحيلٍ مخزنيّة يظهر في تقريرٍ واحدٍ على الأقلّ، ومن أضاف نوعًا ذا أثرٍ بلا تقرير يسقط بناؤه — **وجُرّب بحذف تقريرٍ عمدًا فأسقط البناء فعلًا ثمّ أُعيد** (حارسٌ صادق لا زينة). **ف‑٤٧ سُدّت** (كانت التقارير تذكر ١٤ نوعًا من ٣٨): دفعةٌ ثالثة بسبعة تعريفات على المحرّك القائم **لا شاشات جديدة** — المبيعات · الفروع والمستودعات · النقل · المندوبون (المحمَّل − المُباع − المُرجَع = العهدة) · **سجلّ المستندات الجامع** · تتبّع الدفعة · المورّدون 360°. **ف‑٣٣:** الأسئلة الثمانية مُجابةٌ من الدفتر — التتبّع استعلامٌ لا بناءُ بيانات، والاتّجاهان رحلةٌ واحدة بقراءتين. **ف‑٣١/٣٢:** `item360` و`supplier360` بمؤشّراته الثلاثة محسوبةً من المستندات (والمجهول `null` لا صفرٌ كاذب). السجلّ **١٩ تقريرًا**. **البوّابات: 1548/1548 (+12) · build 97 · audit نظيف.** **⚠️ حدّان:** `item360` بلا تقريرٍ مجمَّع بعد (الصنف يُعرض في بطاقته)، ووحدة العرض القابلة للتبديل (‹3922›) لم تُبنَ — الأرقام بوحدة الأساس وهي الصادقة دفتريًّا. **التالي:** SAP-17 (الأثر الماليّ) ثمّ SAP-18 الختاميّة — وSAP-8 محجوبة على قرار‑٤.

> **[Claude] 2026-08-14 — SAP-17 مغلقة: الحزمة المالية اكتملت (`ea6792c`) — 20/22 (91٪):** شقّ «المنطق يبقى في البوابة» من قرار المالك صار عقدًا: `financialImpact.js` جدولٌ واحد لكلّ نوعٍ جاهز (حكمٌ ماليّ · حكمٌ مخزنيّ · مستند أودو · **سببٌ مكتوب**) — **ولا حسابٌ ولا مبلغٌ مخترع**، والبوابة لا تُولّد قيدًا. **★ سجلّ الفروقات صار آليًّا:** `stockEffectDiscrepancies` يقارن العقد بـ`postingRules` — **وأمسك خمسة أنواعٍ منسيّة حقًّا** (سلسلة المشتريات الداخليّة) فأُكملت؛ لا تعارضَ يمرّ بصمت. **ف‑٤١:** لوحة الأثر المالي تعرض الثمانية (§16.18) من حقول المرآة **الحقيقيّة** لا مفترضة، و«لا يُدّعى توازنٌ بلا أسطر»، وخطأ المزامنة بلا كشف منفذٍ أو اعتماد (باختبار)، وزرّ CC-202 الذي كان بلا وجهة صار له مرساة. **البوّابات: 1561/1561 (+13) · build 97 · audit نظيف.** **⚠️ حدّان:** ربط القيد بالمستند بالمرجع (`invoice_origin`) — فما لم يُكتب مرجعُه في أودو لا يُربط؛ وإظهار القيد في خريطة الدورة (§16.7-١١) مرشَّح لـSAP-18. **التالي:** SAP-18 — التحقّق الشامل الختاميّ (الخمسة عشر سيناريو وبوابة §23).
