# 🗺️ خطة التطوير الشاملة — الكاشير · أجهزة البيع · تكامل أودو · القاعدة المحلّية

> **ما هذه الوثيقة؟** خطّة عملٍ حاكمة، مبنيّة على **مراجعة فعليّة للكود** (لا تخمين)، تجيب على طلب المالك:
> «راجعوا المشروع، تأكّدوا أن كل شيء سليم، اكتشفوا الثغرات والأعطال، وأضيفوا خاصّية كاشير وأجهزة بيع مباشر، وفصّلوا الربط مع أودو والمزامنة الدوريّة واللحظيّة، وخطّة قاعدة بيانات محلّية قابلة للتثبيت مع آليّة تبديل القاعدة.»
>
> **آليّة الإعداد:** مسحٌ متوازٍ للكود على أربعة محاور (تكامل أودو · قاعدة البيانات والأوفلاين · الأمن وصحّة الكود · جرد الخدمات وفجوة الكاشير) — كلّ رقم أو مسار هنا مؤكَّد من المصدر.
>
> **الحالة:** مسوّدة للمراجعة (المالك + الشريك ChatGPT). لا يُنفَّذ منها سطرٌ قبل اعتماد الأولويّات في القسم ٩.
>
> **آخر تحديث:** 2026-08-03 · **يُحدَّث مع كل بندٍ يُنفَّذ (نظام تجديد الخطة).**

---

## ⏱️ سجلّ التنفيذ — نظام تجديد الخطة

> **قاعدة العمل:** لا يُعلَن بندٌ «✅ منفّذ» إلا بعد تنفيذه والتحقّق منه فعليًّا (بناء/اختبار/لينت أو تحقّق حيّ). كل تغيير يُحدِّث هذا الجدول والوثيقة معًا حتى لا نرتبك — كلّ شيء منطقيّ وحقيقيّ ومنفَّذ.
>
> **الدفعة الجارية:** القسم ٢ — الثغرات والأعطال (بالترتيب). **مُنجَز ومُتحقَّق:** البنود ١ (xlsx) · ٣ (المُهرِّب) · ١٠ (التعليق). **مُحرَّرة بانتظار نشرك:** دفعة القواعد ٢ · ٤–٧ في `firestore.rules`.
>
> **✅ مدفوع لـmain (2026-08-03):** كل تغييرات الكود (xlsx · المُهرِّب · التعليق · ملفّ القواعد) + هذه الخطة — حاجز أخضر (بناء ٥٩ صفحة · اختبار · لينت 124=124). **يبقى عليك خطوة واحدة:** نشر `firestore.rules` (٦٢٥ سطرًا، ينتهي بـ`}`) في Firebase Console → Firestore → Rules → **Publish** — دفع الكود لا يَنشر القواعد.

| البند | الأولويّة | الحالة | ملاحظة |
|---|---|---|---|
| ١ · ترقية `xlsx` (CVE) | 🔴 عالٍ | ✅ منفّذ ومُتحقَّق (2026-08-03) | رُقِّيت إلى **0.20.3** في التوزيعين (npm tarball رسميّ في `package.json` + `public/lib/xlsx.full.min.js`) · بناء ٥٩ صفحة ✓ · اختبارات ✓ · لينت 124=124 |
| ٢ · سلامة المخزون (قواعد) | 🟠 متوسط-عالٍ | 🟡 مُحرَّرة — بانتظار نشر المالك | حارس `qty≥0` على الأرصدة + قصْر ختم الأثر (`isSideEffectStamp`) على حالة «معتمَد/منجَز». ⚠️ التحقّق الكامل (رصيد = مجموع الحركات) يحتاج Cloud Function على Blaze — موثّق (القسم ٦-احتياطي) |
| ٣ · توحيد المُهرِّب | 🟠 متوسط | ✅ منفّذ ومُتحقَّق (2026-08-03) | مُهرِّب JS (`escJs`/`escJsAttr`) في ٨ صفحات؛ كل مُعالِج `on*` مضمَّن ببيانات صار مؤمَّنًا؛ `meetings` توحّد على `escape.js`؛ `supply-chain`/`retail-hub` رُقِّيا لمستوى `escAttr` · بناء ٥٩ صفحة ✓ · لينت 124=124 |
| ٤–٧ · بنود قواعد سلامة | 🟡 متوسط-منخفض | 🟡 مُحرَّرة — بانتظار نشر المالك | `Items_Pending` للفاعل المخزنيّ · `stock_moves` ثُبِّتت حقول الهويّة (ملحق-فقط محكم) · سجلّ تدقيق المستندات لفاعلي المستندات · `Inbound/Outbound_Log` مُنع تحديثها/حذفها. توازن أقواس ✓ · مُتحقَّق بالتفتيش (المُحاكي يحتاج Java عندك) |
| ٨ · حرق أرقام | 🟡 منخفض | ⬜ مقبول حاليًّا | القاعدة تمنع التكرار/التصفير |
| ٩ · ازدواج الصفحات/المُهرِّب | 🟡 بنيويّ | ✅ مغلق | المُهرِّبات صارت متّسقة (البند ٣)؛ والنسختان الميتتان `AssetsInventory.jsx`/`VehiclesInventory.jsx` **حُذفتا 2026-08-21 (دفعة «إخلاء الميّت»)** بعد تحقّقٍ عدائيّ أثبت أنّ الصفحتين الحيّتين مجموعةٌ فوقيّة صارمة منهما |
| ١٠ · تعليق متقادم | ⚪ توثيق | ✅ منفّذ (2026-08-03) | تعليق `firebase.js` صار يصف الأمن الفعليّ (Auth + قواعد مشدّدة) بدل «لوحة مفتوحة» |

**رموز الحالة:** ⬜ مخطّط · 🟡 قيد التنفيذ · ✅ منفّذ ومُتحقَّق · ⏸️ بانتظار المالك.

---

## ٠) الملخّص التنفيذي — هل «كل شيء سليم»؟

**الحكم الصادق:** النظام **سليمٌ بنيويًّا ومتقدّم** — لا انهيار ولا خطر وشيك. البوابة خضراء: **٦٥٧ اختبار وحدة + اختبار قواعد بالمحاكي**، حاجزا نشرٍ صلبان (`npm test` + `npm run audit`)، قواعد أمان **مشدّدة ومُختبَرة** (لا مجموعة مفتوحة للعموم، لا تصعيد أدوار)، وأوفلاين حقيقيّ عبر PWA مثبَّت. الفجوات ليست أعطالًا حرجة بل من ثلاثة أصناف:

1. **ثغرات سلامة بيانات** (integrity) لا سرّية — أخطرها ثغرة مكتبة `xlsx` معروفة يصل إليها المستخدم.
2. **ميزة الكاشير/POS غير موجودة إطلاقًا** — دورة البيع الحاليّة مستنديّة (SO→PICK→DN→INV) لا لحظيّة.
3. **تكامل أودو جاهزٌ لكن غير موصول** — طبقة كاملة معزولة، والمزامنة كلّها تصميمٌ بلا تنفيذ.

| المحور | الحالة الفعليّة | الخلاصة |
|---|---|---|
| **صحّة الكود** | ٦٥٧ اختبارًا · حاجزان صلبان · لينت ١٢٤ (٥٣ خطأ/٧١ إنذار، غالبها ضجيج CDN) | ✅ سليم |
| **الأمن** | قواعد مشدّدة ومُختبَرة · فصل واجبات خادميّ · حارس عدّادات حقيقيّ | 🟡 سليم مع ثغرات سلامة |
| **الأوفلاين** | PWA مثبَّت + كاش Firestore دائم + طابور كتابة | ✅ قويّ (قيد: أوّل دخول أونلاين) |
| **الكاشير/POS** | لا شيء (`retail-hub` خريطة جغرافيّة لا كاشير) | 🔴 غير موجود — بناءٌ جديد |
| **تكامل أودو** | عميل + وسيط + موديول جاهزون · **غير موصولين** · لا مزامنة | 🔴 بناءٌ جديد فوق أساسٍ جاهز |
| **تبديل القاعدة** | Firestore خام في ١٥١ موضعًا · لا طبقة عزل | 🟠 مكلف — يحتاج طبقة Repository أوّلًا |

---

## ١) تأكيد سلامة الكود ومراجعة الخدمات

### ١-أ) صحّة الكود (مؤكَّدة)
- **الاختبارات:** ٤٢ ملفّ اختبار وحدة / **٦٥٧ حالة** (`node --test`) + اختبار قواعد Firestore بالمحاكي (`test/firestore-rules.test.mjs`) — تغطية منطق قويّة ونادرة لهذا النوع من المشاريع.
- **حواجز النشر:** `npm run audit` (`scripts/audit-portal.mjs`) يفحص خمسة أشياء (صفحات يتيمة · روابط مكسورة · مجموعة Firestore بلا قاعدة أمان · تسريب صفحة لدور غير صاحبها · لقطة عامّة) + `npm test` — حاجزان صلبان قبل الرفع.
- **اللينت:** خطّ أساس ١٢٤ مشكلة (٥٣ خطأ / ٧١ إنذار). التصنيف: `no-unused-vars` ٧٠، `no-undef` ٣٨ (غالبها منصّات CDN مثل `XLSX` في سكربتات Astro الداخليّة — ضجيج لا أعطال)، والباقي طفيف. **لا يوجد خطأ يمنع البناء.**

### ١-ب) جرد الخدمات (٢٥ مجموعة تحت `src/services/` + ٥٧ صفحة لوحة)
| المجموعة | ماذا تفعل | الجاهزيّة |
|---|---|---|
| `documents/` | محرّك المستندات المحكوم: ٢٤ نموذجًا/٧ سلاسل، ترقيم، صندوق وارد، حرّاس | ✅ ناضج |
| `ledger/` | دفتر حركات المخزون الملحق-فقط + قواعد القيد + **خدمة المبيعات** + تقاريرها | ✅ ناضج |
| `balances/` | الأرصدة الذرّية (المتاح = الموجود − المحجوز) | ✅ ناضج |
| `items/` + `itemService.js` | ماستر الأصناف: استدعاء بالباركود، استيراد، حالة، معلّقة | ✅ ناضج |
| `stock/` | المسح الحيّ متعدّد الأجهزة (`scanMerge.js`) + العمليّات المخزنيّة | ✅ ناضج |
| `partners/` `custody/` `fleet/` `vehicles/` `maintenance/` | موردون/عملاء · عُهد · أسطول · صيانة | ✅ |
| `inventory/` `kpi/` `dashboard/` | تحليل المخزون · مؤشّرات · لوحة اللوجستيات | ✅ |
| `auth/` | مصادقة · مستخدمون · **كتالوج التنقّل مصدرًا واحدًا** · مصفوفة الصلاحيات | ✅ |
| `odoo/` | عميل تكامل (حقيقيّ+وهميّ) · إعداد · مخطِّط | 🟡 جاهز غير موصول |
| `meetings/` `org/` `recruitment/` `hiring/` `studies/` `reference/` | خدمات مساندة | ✅ |

**الحكم:** الخدمات التشغيليّة (المستندات · الدفتر · الأرصدة · الأصناف · الباركود) **متينة ومختبَرة وجاهزة للبناء عليها**. الفجوات في «الكاشير» و«أودو» لا في القلب التشغيليّ.

---

## ٢) الثغرات والأعطال المكتشفة — مرتّبة بالأولويّة

> جميعها مؤكَّدة بمسار وسطر. البنية الأمنيّة **جيّدة** أصلًا؛ هذه بنود تشديدٍ لا إنقاذ.

| # | الخطورة | الثغرة/العطل | الموضع | الأثر | الإصلاح المقترح |
|---|---|---|---|---|---|
| ١ | 🔴 **عالٍ** | تبعيّة `xlsx@0.18.5` بثغرة معروفة (تلويث النموذج الأوّليّ CVE-2023-30533 + ReDoS) تصل إليها ميزة الاستيراد | `excelImport.js:42` · `AssetsInventory.jsx:841` · `VehiclesInventory.jsx:1778` | ملفّ `.xlsx` خبيث يُلوِّث `Object.prototype` أثناء التحليل | ترقية SheetJS لإصدار مُصحَّح من مستودع البائع الرسميّ (نمط الاستضافة الذاتيّة الحاليّ في `public/lib`) |
| ٢ | 🟠 **متوسط-عالٍ** | سلامة المخزون بلا تحقّق خادميّ: `balances` كتابة لأي فاعل مخزني بأي قيمة + `isSideEffectStamp` يختم `posted/soAllocation` على أي مستند | `firestore.rules:380-384` · `256-264` | تضخيم/تخفيض رصيد أو ختم «مقيَّد» زورًا دون مستند مقابل | تشديد القواعد (تحقّق ملكيّة/حالة) + نقل القيد لمسار موثوق (Cloud Function عند Blaze) — مرحليًّا |
| ٣ | 🟠 **متوسط** | صنف XSS كامن: مُهرِّبات محليّة تُهمِل `'` مع حقنٍ في مُعالِجات مفردة الاقتباس | `assets-inventory.astro:463,561` · `supply-chain.astro:163` وأخواتهما | كسر سلسلة JS وحقن كود (منخفض الاستغلال اليوم لأن المُدخل مولَّد آليًّا) | توحيد الاستيراد على `src/services/ui/escape.js` (`escAttr`/`escJsAttr`) في كل معالِج مضمَّن |
| ٤ | 🟠 متوسط | كتابة واسعة على `Items_Pending`: أي مصادَق يعدّل أي صنف معلّق ما دام `pending` | `firestore.rules:345-349` | تعديل بيانات مشتركة بلا صلاحيّة | تقييد التحديث بالمُنشئ أو دور محدّد |
| ٥ | 🟠 متوسط | «الملحق-فقط» غير محكم في `stock_moves`: إعادة الكتابة تسمح بتغيير المواقع/الصلاحية | `firestore.rules:397-400` | إضعاف وعد عدم التعديل | منع تحديث الحقول الحسّاسة بعد الإنشاء |
| ٦ | 🟡 متوسط-منخفض | تلويث سجلّ التدقيق: أي مصادَق يُلحق قيودًا بسجلّ `documents/*/audit` | `firestore.rules:290-291` | تلويث لا تزوير | ربط الإلحاق بفاعل المستند |
| ٧ | 🟡 منخفض | محو سجلّات الوارد/الصادر مسموح لـ`isAdmin` خلافًا لمبدأ عدم المحو | `firestore.rules:409,415` | فقد أثر تاريخيّ | إزالة تحديث/حذف السجلّات |
| ٨ | 🟡 منخفض | حرق أرقام: دورٌ مصرَّح يقدر يزيد العدّاد دون إنشاء المستند (فجوات ترقيم لا تزوير) | `numbering.js` + `rules:303` | فجوات في التسلسل | مقبول حاليًّا (القاعدة تمنع التكرار/التصفير) |
| ٩ | 🟡 بنيويّ | ازدواج صفحات كامل (`assets-inventory.astro`⟷`AssetsInventory.jsx`) + ~٨ نسخ مُهرِّب محلّيّ | متعدّد | يضاعف سطح الإصلاح ويخاطر بنسخة غير مُصحَّحة | حذف النسخ الميتة + توحيد المُهرِّب |
| ١٠ | ⚪ توثيق | تعليق متقادم في `firebase.js:20-21` يصف «اللوحة مفتوحة الوصول» — لم يعد صحيحًا | `firebase.js:20-21` | تضليل قارئ | تصحيح التعليق |

**دفعة الإصلاح المقترحة (سريعة، منخفضة المخاطر):** البنود ١ + ٣ + ١٠ أوّلًا (ترقية xlsx · توحيد المُهرِّب · تصحيح التعليق) — لا تعتمد على المالك ولا على استضافة. ثم البنود ٢/٤/٥/٦/٧ في **دفعة قواعد موحّدة** تُنشر مرّة واحدة.

---

## ٣) خاصّية الكاشير (POS) — التصميم والمراحل

### ٣-أ) لماذا بناءٌ جديد؟
دورة البيع الحاليّة **مستنديّة**: أمر بيع (SO) → تجهيز (PICK) → إذن تسليم (DN، **هنا يُخصم المخزون فعلًا**) → فاتورة (INV). هذه دورةٌ رقابيّة بطيئة لا تصلح لنقطة بيعٍ تخصم لحظيًّا. الكاشير يحتاج مسارًا مضغوطًا: **امسح → أضِف للسلّة → استلم الدفع → اخصم المخزون الآن → اطبع الإيصال**.

### ٣-ب) ما هو موجود للبناء عليه (لا نبدأ من الصفر)
| المكوّن الجاهز | الموضع | الدور في الكاشير |
|---|---|---|
| `lookupByBarcode` | `itemService.js` | قلب الكاشير: باركود → صنف بسعره ووحدته |
| المعاملة الذرّية للبيع | `ledger/salesService.js` (`allocateAndReserve` داخل `runTransaction`) | نمطٌ جاهز يُنسخ إلى «بيع فوريّ يخصم لحظيًّا» |
| الأرصدة والحجوزات | `balancesService.js` + `ledger/reservations.js` | المتاح = qty − reserved (حارس عدم البيع تحت الصفر) |
| قاعدة خصم DN | `ledger/postingRules.js` | الكاشير يعيد استخدامها مضغوطةً في خطوة واحدة (قاعدة قيد `POS`) |
| حقول السعر/الخصم/الضريبة | `schemas/so.js` + `schemas/inv.js` | أساس بنود السلّة (سعر · خصم بند/فاتورة · نسبة/قيمة ضريبة · إجماليّات) |
| نمط السجلّ الملحق-فقط | `stock/scanMerge.js` | **أقرب نمط قائم لـ«جلسة/درج كاشير»** (سجل أحداث لا يُعدَّل، مجموع الأحداث = الحالة) |
| مكتبات الباركود | `public/lib/` (html5-qrcode · JsBarcode) | طبقة المسح جاهزة |

### ٣-ج) الإصلاح المُمكِّن الأوّل (صغير وحرج)
**تفعيل سعر البيع:** حقل `sellPrice` يُستورَد اليوم لكن **لا يُقرأ في أيّ منطق** — كل الحسابات تستعمل التكلفة (`unitPrice`/`costPrice`). أوّل خطوة: قراءة `sellPrice` في `lookupByBarcode` (مع سقوطٍ آمن للتكلفة عند غيابه). بلا هذا لا يوجد سعر بيع فاعل.

### ٣-د) نموذج البيانات المقترح (مجموعتان جديدتان)
```
pos_sessions/{sessionId}          ← جلسة/وردية كاشير (ملحق-فقط بنمط scanMerge)
  cashierUid · terminalId · openedAt · openingFloat
  events[]: {sale|cash_in|cash_out|no_sale}   ← لا يُعدَّل حدثٌ بعد قيده
  closedAt · countedCash · expectedCash · variance

pos_orders/{orderId}              ← أمر بيع لحظيّ
  sessionId · lines[{sku, qty, sellPrice, lineDiscount, lineTotal}]
  invoiceDiscount · taxRate · taxValue · grandTotal
  payments[{method: cash|card|transfer, amount}]   ← دفع مُجزّأ
  changeDue · status: paid|refunded
  idempotencyKey   ← معرّف محلّيّ يمنع الازدواج عند المزامنة الأوفلاين
  stockPosted: bool   ← ختم الخصم الذرّي
```
- **الخصم الذرّي:** عند «استلام الدفع» تُنفَّذ معاملة `runTransaction` واحدة: تتحقّق المتاح (FEFO)، تخصم الرصيد، تقيّد حركة `stock_move` من نوع `POS-OUT`، وتختم `stockPosted`. مفتاح `idempotencyKey` يمنع الخصم المزدوج.
- **قواعد Firestore:** مجموعتان جديدتان بقاعدتَي أمان (كتابة للكاشير النشط فقط · `pos_sessions` ملحق-فقط · لا تعديل بعد الإغلاق) — يفرضها حاجز `audit-portal.mjs` (يرفض مجموعة بلا قاعدة).

### ٣-هـ) مراحل التنفيذ
| مرحلة | المخرج | الاعتماد |
|---|---|---|
| **٣-١** | تفعيل `sellPrice` + قاعدة قيد `POS` (خصم فوريّ ذرّيّ) + اختبارات | — (داخليّ) |
| **٣-٢** | واجهة كاشير لمسيّة: بحث/مسح فوريّ · سلّة · خصم/ضريبة على السطر والأمر · شاشة دفع (نقد/بطاقة/تحويل + باقٍ + دفع مُجزّأ) | ٣-١ |
| **٣-٣** | جلسة/وردية + درج نقديّة (فتح برصيد افتتاحيّ · حركات نقد · تقفيل بمطابقة `expected/counted/variance`) | ٣-٢ |
| **٣-٤** | إيصال حراريّ ٨٠مم (قالب مستقلّ عن A4) + تقارير المبيعات اليوميّة للوردية | ٣-٢ |
| **٣-٥** | وضع بيع أوفلاين: طابور مبيعات محلّيّ + مزامنة لاحقة بمفتاح idempotency | ٣-٣ + طبقة الأجهزة (القسم ٤) |

---

## ٤) أجهزة البيع المباشر — طبقة الأجهزة الطرفيّة

> «الكاشير» برمجيّة، و«أجهزة البيع المباشر» هي العتاد الطرفيّ: الماسح · الطابعة الحراريّة · الدرج النقديّ · شاشة العميل · قارئ البطاقة. الويب يفرض قيودًا يجب تصميم الحلّ حولها.

| الجهاز | الحالة | الخيار المقترح |
|---|---|---|
| **ماسح الباركود** | ✅ جاهز | يعمل كـ«لوحة مفاتيح» (keyboard-wedge) أو كاميرا عبر `html5-qrcode` الموجود |
| **الطابعة الحراريّة ٨٠مم** | 🔴 غير موجود | **المرحلة أ:** طباعة المتصفّح بقالب CSS ٨٠مم (`window.print`) — تعمل على أي نظام/طابعة، بلا تبعيّات. **المرحلة ب:** WebSerial/WebUSB بأوامر ESC/POS للطباعة المباشرة (كروم/سطح مكتب فقط) |
| **الدرج النقديّ** | 🔴 غير موجود | يُفتح بنبضة من الطابعة (ESC/POS pulse) → يتطلّب WebSerial/WebUSB أو **جسرًا محلّيًّا (Tauri/عميل صغير)** |
| **شاشة العميل** | 🔴 غير موجود | نافذة/تبويب ثانٍ يعرض الإجمالي (بسيط) |
| **قارئ البطاقة** | 🔴 غير موجود | خارج نطاق الويب المباشر — يُسجَّل الدفع يدويًّا أو عبر مزوّد خارجيّ لاحقًا |

### التوصية المرحليّة لطبقة الأجهزة
1. **المرحلة أ (فوريّة، تعمل في كل مكان):** إيصال ٨٠مم بطباعة المتصفّح. لا درج، لا تبعيّات — تكفي لأغلب نقاط البيع.
2. **المرحلة ب (كروم/سطح مكتب):** WebSerial/WebUSB → طباعة ESC/POS مباشرة + فتح الدرج بنبضة.
3. **المرحلة ج (المحطّة المتكاملة):** تغليف التطبيق بـ **Tauri** (أو Electron) → مثبِّت `.exe`/`.apk` حقيقيّ + وصولٌ كامل للأجهزة + أوفلاين متين. **هذه المرحلة تلتقي مباشرةً مع القسم ٧ (النسخة المحلّية القابلة للتثبيت).**

---

## ٥) تكامل أودو — الحالة والبناء (ولو اختلف الموديل)

### ٥-أ) الحالة الفعليّة (مسحٌ للكود — صادم لكنه صحيح)
التكامل **غير مُفعَّل**: الوضع الافتراضي `training` (وهميّ)، و**طبقة `src/services/odoo/**` لا يستوردها أيّ مكوّن أو صفحة إطلاقًا** — كودٌ جاهز ومعزول ومَيْت وظيفيًّا اليوم.

| المكوّن | الحالة | الموضع |
|---|---|---|
| عميل أودو (`authenticate/searchRead/create/write/unlink` + وهميّ) | ✅ مكتوب · **غير موصول** | `src/services/odoo/` |
| المُطابِق `odooMapper` | 🟡 يغطّي **نموذجين فقط**: `product.product↔Items_Master` (ثنائيّ) و`stock.move→log` (اتجاه واحد) | `odooMapper.js` |
| الوسيط (Cloudflare Worker + بديل Firebase Function) | ✅ مكتوب (allowlist ٦ موديلات) · **غير منشور** | `odoo-proxy/` |
| الموديول `brandzo_warehouse` (١٢ موديلًا، ٥٢ اختبارًا) | ✅ يعمل على أودو ١٩ · **بلا `controllers/`** (سطحه `execute_kw` فقط) | `brandzo-addons/` |
| جسر المزامنة (syncService/outbox/mirrors/webhooks) | 🔴 **لا يوجد إطلاقًا** — تصميمٌ في وثيقة العقد فقط | — |
| إرسال مستند (GRN/PO/DN) إلى أودو | 🔴 **صفر كود** (لا مُطابِق مستند→أودو، لا استدعاء) | — |

**⚠️ تصحيح واجب:** تعليق `index.js:14-16` يزعم توصيلًا غير قائم — مضلِّل ويجب تصحيحه.

### ٥-ب) معالجة اختلاف الموديل (جوهر سؤالك «ولو اختلف الموديل»)
المبدأ الحاكم (من `docs/عقد-التكامل-مع-أودو.md`): **المعرفة الأودوّية محصورة في طبقتين فقط** — `odooMapper.js` (أسماء الحقول) والموديول `brandzo_warehouse` (المنطق). أي مكوّن آخر لا يعرف شيئًا عن أودو. أثر ذلك على اختلاف الموديل:
- **اختلاف أسماء الحقول/الإصدار:** يُحلّ بتعديل `odooMapper.js` وحده (سابقة موثّقة: ترقية ١٩ كشفت `product_uom→product_uom_id`).
- **اختلاف بنية المستند:** نُطابق مستند البوابة لأقرب موديل أودو قياسيّ:
  | مستند البوابة | موديل أودو |
  |---|---|
  | PO (أمر شراء) | `purchase.order` |
  | PR (طلب شراء) | `purchase.requisition` |
  | GRN/الاستلام | `stock.picking` (وارد) |
  | DN (إذن تسليم) | `stock.picking` (صادر) |
  | SO/POS (بيع) | `sale.order` / `pos.order` |
  | INV (فاتورة) | `account.move` |
- **حيث لا مطابقة نظيفة:** نستعمل حقولًا مخصّصة `x_*` (Studio) أو موديلات الموديول الخاصّة `bz.*` (`bz.cycle.count` · `bz.gate.pass` · `bz.period.close`). المُطابِق يتدهور بأمان عند غياب أي حقل مخصّص.
- **الوسيط يحرس القائمة:** `worker.js` يفرض allowlist صريحة (نموذج+دالّة) فلا يمرّر استدعاءً عشوائيًّا — أي توسعة موديل تُضاف صراحةً.

### ٥-ج) خطوات البناء
1. **توصيل العميل** (إزالة العزل): استيراده في قمرة `RealOdooCockpit`/شاشة مزامنة + **اختبار تكامل** يحرس الطبقة من التعفّن + تصحيح التعليق المضلِّل.
2. **توسعة `odooMapper`** للموديلات الحاكمة (purchase.order · stock.picking · res.partner · account.move) — الأساس لإرسال المستندات.
3. **نشر الوسيط** (`odoo-proxy/cloudflare-worker`) — **يتطلّب حساب Cloudflare مجانيًّا (المالك)** + مستخدم تكامل `bz_integration` في أودو بمفتاح API مقيَّد + CORS محصور بأصل Pages.
4. **اختبار القبول الأوّل:** `searchRead('product.product')` من البوابة يعرض أصناف المختبر حيًّا.

---

## ٦) المزامنة — الدوريّة (periodic) واللحظيّة (real-time)

> ثلاث مراحل من وثيقة العقد، **لا شيء منها منفَّذ بعد**. أرتّبها من الممكن اليوم إلى المشروط باستضافة أودو.

### المرحلة ١ — سحبٌ دوريّ (Periodic Polling) ✅ تعمل على Spark
- `syncService` (يُكتَب) يسأل أودو كل فترة عن `write_date > lastSync`.
- المؤشّر `lastSync` في وثيقة `sync_state` (Firestore) + مرايا قراءة `odoo_mirror_*` + **قواعد أمان جديدة لها**.
- **المُشغِّل (نقطة القرار):** خطّة Spark بلا Cloud Functions → خياران: (أ) **Cloudflare Cron Trigger** على الوسيط (الأنظف)، أو (ب) مؤقّت في المتصفّح عند فتح تبويب مديرٍ (بديل بلا بنية).
- **هذه هي «التحديث الدوريّ» المطلوب** — بسيطة وكافية للبداية.

### المرحلة ٢ — دفعٌ من البوابة (Outbox) — إرسال المستندات
- عند بلوغ مستندٍ حاكم حالة **«منجَز»** يُقيَّد في طابور `sync_outbox` (Firestore).
- `syncService` يدفعه عبر الوسيط: `odoo.create('purchase.order' | 'stock.picking' | 'account.move', mapper(doc))`.
- **مفتاح منع التكرار (idempotency) = معرّف مستند Firestore** → لا ازدواج عند إعادة المحاولة.
- **هذا هو مسار «إرسال المستندات إلى أودو»** — ويحتاج توسعة المُطابِق (٥-ج/٢) أوّلًا.

### المرحلة ٣ — لحظيّة حقيقيّة (Real-time Webhooks) 🔴 مشروطة باستضافة أودو عامّة
- Webhooks من `base_automation` في أودو ١٩ → الوسيط يستقبل ويكتب في Firestore عبر REST بحساب خدمة → **يُلغي الـpolling**.
- **الشرط الحاكم:** أودو على عنوان عامّ (المختبر اليوم `localhost:8069` فقط — الوسيط السحابيّ لا يراه). **قرار مالك: توقيت استضافة أودو حقيقيّة.**
- حتى ذلك الحين: «شبه لحظيّ» عبر polling قصير + دفع Outbox فور «منجَز».

### التعارضات والنسخ الاحتياطيّ
- **سياسة التعارض:** «الأحدث يكسب + سجلّ تعارض» يُعرض في شاشة «مراقبة المزامنة» (تُبنى بنمط أودو `ListView`).
- **قاعدة أودو (PostgreSQL):** `pg_dump` يوميّ مجدول + لقطة قبل أي ترقية + هجرة لا مسح + **تمرين استعادة شهريّ** (النسخة التي لم تُجرَّب استعادتها ليست نسخة).

---

## ٧) قاعدة البيانات المحلّية القابلة للتثبيت (نسخة setup على الأجهزة)

### ٧-أ) ما هو قائمٌ فعلًا (٨٠٪ من الهدف مُنجَز)
- التطبيق **PWA قابل للتثبيت** بالفعل: `manifest.webmanifest` (`display:standalone`، أيقونات 192/512/maskable، RTL) + Service Worker مولَّد بعد كل بناء (precache للهيكل · `networkFirst` للصفحات · `cacheFirst` للأصول · صفحة «غير متّصل»).
- **كاش Firestore دائم** (`persistentLocalCache` + دعم تبويبات) → قراءة أوفلاين + **طابور كتابة يُدفع تلقائيًّا** عند عودة الشبكة.
- **القيد الوحيد:** أوّل تسجيل دخول على كلّ جهاز يجب أن يكون **متّصلًا** (`signInWithEmailAndPassword`)؛ بعده تبقى الجلسة محليًّا ويعمل أوفلاين.

### ٧-ب) تقوية منخفضة الكلفة (تُنجَز الآن)
1. `cacheSizeBytes: CACHE_SIZE_UNLIMITED` في `firebase.js` (اليوم سقف ~٤٠م.ب مع طرد الأقدم).
2. خطوة **«تسخين مُسبق»** بعد الدخول تسحب مجموعات الدور (فيتوفّر ما يلزم أوفلاين لا ما جُلب صدفةً).
3. **معالجة رفض الكتابة عند العودة** برسالة واضحة (كتابةٌ أوفلاين تخالف القواعد تُرفَض وتُرتجَع صمتًا اليوم).
4. توثيق «أوّل دخول أونلاين» في دليل التشغيل.

### ٧-ج) الاستقلال الكامل عن السحابة (للمحطّة المنعزلة — كلفة عالية)
لجهازٍ يعمل **بلا إنترنت إطلاقًا** (مثل محطّة كاشير في موقعٍ بلا شبكة):

| الخيار | الجدوى | الكلفة |
|---|---|---|
| **PWA مثبَّت (الحاليّ)** | ✅ قائم | ~صفر (لكنه سحابيّ-أوّلًا) |
| **Tauri/Electron + SQLite محلّيّة مصدرًا للحقيقة + طبقة مزامنة** | ✅ الاستقلال الحقيقيّ + مثبِّت `.exe` + وصول الأجهزة (يخدم القسم ٤) | **عالية** — يشترط طبقة التجريد (القسم ٨) أوّلًا |
| **IndexedDB محلّيّ خلف Repository** | ✅ استقلال ضمن المتصفّح | عالية |

**التوصية:** للهدف القريب (كاشير مثبَّت يعمل أوفلاين) تكفي **المرحلة أ+ب** (PWA مقوّى، ثم Tauri عند الحاجة لدرجٍ وطابعة). الاستقلال الكامل يُجدوَل بعد بناء طبقة الـRepository (القسم ٨).

---

## ٨) آليّة تبديل قاعدة البيانات

### ٨-أ) العائق الحقيقيّ
لا توجد **أيّ طبقة عزل**: Firestore خام في **١٥١ موضعًا عبر ٢٥ ملفًّا**، و**٥٩ مستمع `onSnapshot`** حيّ، والأخطر أن **منطق العمل نفسه يعيش في `firestore.rules` (٦٠٧ أسطر)** لا في JS — بوّابات الاعتماد، اللاتعديل، «الرقم مرّة واحدة». تبديل المزوّد اليوم يعني إعادة كتابة ١٥١ موضعًا + إعادة إنتاج الدلالات الذرّية (`runTransaction`/`increment`/`serverTimestamp`) + إعادة بناء ٥٩ اشتراكًا + **إعادة بناء منطق القواعد بالكامل** في طبقة إلزام مكافئة.

### ٨-ب) القالب الجاهز داخل المشروع
النمط الصحيح **موجود بالفعل** لأودو (`odoo/index.js`): واجهة واحدة تُختار بمتغيّر بيئة.
```js
export const odoo = isProduction ? realOdooClient : mockOdooClient;   // odoo/index.js:24
```
Firestore لا تملك نظيرًا — إنها خام ومنتشرة. نطبّق النمط ذاته على مرحلتين:

### ٨-ج) الخطة على مرحلتين
**المرحلة ١ — إدخال طبقة Repository (شرط مسبق):**
أنشئ `src/services/db/` بمستودعات لكل مجموعة (`itemsRepo` · `ledgerRepo` · `documentsRepo` · `balancesRepo` …) تُخفي بدائيّات Firestore، فتنتقل الـ١٥١ موضعًا خلف ~١٥ وحدة. **يُنجَز تدريجيًّا** — نبدأ بالمجموعات التي يلمسها الكاشير (الأصناف · الأرصدة · الدفتر) فيخدم القسمين ٣ و٧ معًا.

**المرحلة ٢ — مُبدِّل المزوّد بنمط أودو:**
```js
// src/services/db/index.js  (على غرار odoo/index.js:24)
export const store = PROVIDER === 'firestore' ? firestoreStore : localStore;   // PUBLIC_DB_PROVIDER
```

**العوائق التي يجب أن يحلّها أي مزوّد بديل:** الاشتراك الحيّ (وإلّا استقصاء)، الذرّية (`runTransaction`/`increment` شرط صحّة لا رفاهية)، وطبقة إلزامٍ تكافئ `firestore.rules` (وإلّا هبطت الضمانات إلى عميلٍ يُتجاوَز). أما المصادقة فيمكن إبقاء Firebase Auth مع تبديل القاعدة، أو هويّة محلّية للمزوّد المنعزل.

---

## ٩) خارطة التنفيذ الموحّدة — الأولويّات والتسلسل

> مبدأ الترتيب: **قيمةٌ عالية × كلفةٌ منخفضة × لا اعتماد خارجيّ** أوّلًا؛ ثم ما يشترط قرار/حساب المالك.

| الموجة | البنود | الاعتماد الخارجيّ | الأثر |
|---|---|---|---|
| **الموجة ١ — تحصين فوريّ** (أيّام) | ترقية `xlsx` (ثغرة ١) · توحيد المُهرِّب (٣) · تصحيح تعليق `firebase.js` (١٠) · دفعة قواعد سلامة موحّدة (٢/٤/٥/٦/٧) · تقوية الكاش (٧-ب) | لا شيء | يغلق الثغرات + يقوّي الأوفلاين بلا مخاطرة |
| **الموجة ٢ — نواة الكاشير** (أسبوع) | تفعيل `sellPrice` (٣-ج) · قاعدة قيد `POS` بخصمٍ فوريّ ذرّيّ (٣-١) · واجهة كاشير + سلّة + دفع (٣-٢) · إيصال ٨٠مم بطباعة المتصفّح (٣-٤ + ٤-أ) | لا شيء | ميزة بيعٍ عاملة كاملة على أي جهاز |
| **الموجة ٣ — إتمام الكاشير** | جلسة/درج (٣-٣) · تقارير الوردية · بيع أوفلاين بطابور idempotency (٣-٥) | لا شيء | تشغيل نقطة بيع حقيقيّ |
| **الموجة ٤ — طبقة الأجهزة المتقدّمة** | WebSerial/WebUSB ESC/POS + فتح الدرج (٤-ب) · تغليف Tauri للمحطّة المثبَّتة (٤-ج + ٧-ج) | لا شيء (كروم/سطح مكتب) | محطّة بيع متكاملة مثبَّتة |
| **الموجة ٥ — توصيل أودو والمزامنة الدوريّة** | توصيل العميل + اختبار تكامل (٥-ج/١) · توسعة المُطابِق (٥-ج/٢) · نشر الوسيط · مزامنة ١ polling (٦-١) · مزامنة ٢ outbox لإرسال المستندات (٦-٢) | **حساب Cloudflare (المالك)** | تحديث دوريّ + إرسال المستندات لأودو |
| **الموجة ٦ — المزامنة اللحظيّة** | Webhooks (٦-٣) + شاشة مراقبة المزامنة + `pg_dump` وتمرين استعادة | **استضافة أودو عامّة (قرار المالك)** | مزامنة لحظيّة حقيقيّة |
| **الموجة ٧ — طبقة القاعدة الاستراتيجيّة** | طبقة Repository تدريجيًّا (٨-ج/١) بدءًا بمجموعات الكاشير · مُبدِّل `PUBLIC_DB_PROVIDER` (٨-ج/٢) | لا شيء | يفتح تبديل القاعدة والمزوّد المحلّي المنعزل |

**الملاحظة الحاكمة:** الموجات ١→٤ **لا تعتمد على أحد** ويمكن إنجازها فورًا وتسلّم أثمن ما طلبته (الكاشير العامل). الموجتان ٥→٦ (أودو اللحظيّ) مشروطتان بقرارَي المالك. الموجة ٧ استثمارٌ استراتيجيّ يُجدوَل بعد استقرار الكاشير.

---

## ١٠) ما يلزم المالك — قرارات وحسابات

### قرارات تحتاج حسمك (مع توصيتي)
1. **نطاق الكاشير أوّلًا:** أوصي بـ**الموجة ٢ كاملة** (كاشير عامل بإيصال طباعة متصفّح) قبل الاستثمار في الدرج/ESC/POS — قيمةٌ فوريّة بلا عتاد خاصّ.
2. **أولويّة الكاشير مقابل أودو:** أوصي بتقديم **الكاشير (الموجات ١→٤)** لأنه بلا اعتماد خارجيّ ويسلّم قيمة تشغيليّة مباشرة؛ وأودو (٥→٦) بالتوازي متى توفّر حساب Cloudflare.
3. **عمق القاعدة المحلّية:** هل الهدف كاشير مثبَّت يعمل أوفلاين (تكفيه الموجة ١+٤) أم استقلالٌ تامّ عن السحابة (يستلزم الموجة ٧)؟
4. **توقيت استضافة أودو حقيقيّة** — يفتح المزامنة اللحظيّة (الموجة ٦).

### حسابات/تفعيلات مطلوبة منك (لا أستطيع فعلها)
- **حساب Cloudflare مجانيّ** لنشر الوسيط (يفتح المزامنة الدوريّة وإرسال المستندات).
- **مستخدم تكامل `bz_integration`** في أودو بمفتاح API مقيَّد.
- لاحقًا: **استضافة أودو على عنوان عامّ** (المزامنة اللحظيّة) + **Blaze** (نسخ مجدول أقوى وبديل Cloud Function — معلّق على تجديد البطاقة).

---

## المراجع (كلّها مؤكَّدة من المصدر)
- طبقة أودو: `src/services/odoo/{index,odooConfig,odooClient,mockOdooClient,odooMapper}.js` · الوسيط `odoo-proxy/` · الموديول `brandzo-addons/brandzo_warehouse/` · وثيقة العقد `docs/عقد-التكامل-مع-أودو.md`
- القاعدة والأوفلاين: `src/config/firebase.js` · `src/integrations/pwa-sw.mjs` · `public/manifest.webmanifest` · `src/services/auth/authService.js`
- الأمن: `firestore.rules` (٦٠٧ أسطر) · `src/services/documents/numbering.js` · `src/services/ui/escape.js` · `scripts/audit-portal.mjs`
- البيع الحاليّ: `src/services/ledger/salesService.js` · `src/services/documents/schemas/{so,inv}.js` · `src/services/items/itemService.js` (الباركود)
- الخطّة السابقة المرجعيّة: `docs/خطة-العملية-الجراحية.md`
