# خطة بوابة الأمن والطبليات العائدة ‹GATE›

> **الحالة:** ✅ **منفَّذةٌ ١٥/١٦ (٢٠٢٦-٠٨-٢٨)** — الكوميتات `a55333f` → `b4e98de` · ٣٠٣٤ اختبارًا خضراء · دليلُ الاستخدام ١٠٥ شاشة · تدقيقٌ نظيف.
> **والمتبقّي واحدةٌ لا يملكها وكيل:** GATE-502 — تجربةٌ حيّةٌ بالأدوار الحقيقيّة، وتسبقها **GATE-O01: نشرُ `firestore.rules` من Firebase Console** (وبدونه ترتدّ كتابةُ دفتر الطبليات `permission-denied`).
> **المصدر:** ورقة المالك «Gate Control System» (2026-08-28).
> **الحاكم:** [`AGENTS.md`](../AGENTS.md) · والمتتبّع التاسع المقترح `npm run plan:gate`.
> **القاعدة المُلزِمة:** لا شاشةَ تُضاف إلى [`navCatalog.js`](../src/services/auth/navCatalog.js) بلا شرحٍ في [`usage-guide.json`](../src/data/usage-guide.json) — يفرضها [`usageGuide.test.js`](../src/services/auth/usageGuide.test.js) و`prebuild`.

---

## ٠ · الحكم على الورقة في سطرين

الفكرة **صحيحةٌ وفي محلّها**، ونصفُها **مبنيٌّ في البوّابة منذ ‹EXE-601/602›** ولا يعرف به أحد: دورةُ ساحةٍ من عشر مراحل، وأبوابٌ ومؤقّتاتٌ وحارسُ «لا خروج بلا تصريح»، وقواعدُ Firestore لها، ودورُ `gate_officer` قائمٌ منذ البداية. والنصف الآخر — **سببُ الحركة، وحالةُ الحمولة، ومستنداتُها، والطبلياتُ العائدة، والمطابقة** — غائبٌ كلُّه.

فهذه الخطة **توسعةٌ لما يعمل لا نظامٌ ثانٍ بجانبه**. وأخطرُ ما فيها ليس تقنيًّا: **كلمة «طبلية» تعني في هذه البوّابة شيئين مختلفين**، وخلطُهما يُنتج رقمين متناقضين لا يعرف أحدٌ أيّهما الصحيح (ق-١ أدناه).

---

## ١ · جردُ القائم — ما لا يُبنى ثانيةً

| القدرة | أين هي اليوم | الحالة |
|---|---|---|
| دورةُ زيارةٍ من عشر مراحل بأختامها | [`yardModel.js`](../src/services/fleet/yardModel.js) — `YARD_CYCLE`: حجز · وصول · **تسجيل بوابة** · تحقّق · موقف · باب · تنزيل/تحميل · إخلاء · **تصريح** · **خروج** | ✅ مبنيّةٌ ومختبَرة |
| منعُ القفز بين المراحل والرجوع | `canTransitionVisit` | ✅ |
| ★ «لا تخرج مركبةٌ بلا تصريح» — ثلاثةُ أقفال | `exitVerdict` (المرحلة · رقم GP · إخلاء الباب) | ✅ |
| الأبوابُ **بياناتٌ يضيفها المدير** لا كودًا | `shapeDoor` · `doorAccepts` · `assignDoorVerdict` (قرار المالك ت-O04) | ✅ |
| مؤقّتاتُ الانتظار والمناولة والبقاء وتجاوزاتُها | `visitTimers` · `visitAlerts` · `YARD_LIMITS` | ✅ |
| التخزين والحرّاس في السحابة | [`yardService.js`](../src/services/fleet/yardService.js) — `doors/{CODE}` · `yard_visits/{id}/events` | ✅ |
| قواعدُ الأمن | [`firestore.rules:914`](../firestore.rules) (الأبواب) و[`:930`](../firestore.rules) (الزيارات) — الكتابة لـ`gate_officer` وغيره، **ولا حذف** | ✅ |
| شاشةٌ تعرضها | [`fleet-operations.astro:160`](../src/pages/dashboard/fleet-operations.astro) — تبويب «الساحة والأبواب» داخل لوحة الأسطول | ⚠️ قائمةٌ لكن في غير بيتها |
| تصريحُ الخروج مستندًا رسميًّا | [`schemas/gp.js`](../src/services/documents/schemas/gp.js) — `GP-2026-####` يُشتقّ من إذن تسليمٍ معتمَد، ويعتمده ضابط البوابة لا مَن جهّز الشحنة | ✅ |
| دورُ ضابط البوابة وصلاحياتُه | [`roles.js:13`](../src/services/auth/roles.js) · [`navAccess.js:49`](../src/services/auth/navAccess.js) — يرى `daily · odoo · fleet · putaway` | ✅ |
| ثلاثُ لغاتٍ للعامل الميدانيّ | [`fieldLexicon.js`](../src/services/lpn/fieldLexicon.js) — عربيّة (أصلٌ واحتياط) · إنجليزيّة · فرنسيّة | ✅ يُعاد استعماله |
| محرّكُ المسح لخمس شاشات | [`src/services/scan/`](../src/services/scan/) — كاميرا وجهاز باركود معًا | ✅ يُعاد استعماله |
| نمطُ **حسمِ الفرق** لا إغلاقِه | [`transferPallets.js:211`](../src/services/lpn/transferPallets.js) — لا يُغلق فرقٌ بلا قرارٍ **وصاحبٍ يتحمّله** وفاعلٍ باسمه | ✅ يُحتذى |
| المرفقاتُ صورًا | [`attachmentFile.js`](../src/services/documents/attachmentFile.js) — **٧٠٠ ك.ب بعد الضغط** (Firestore لا Storage) | ✅ بقيدِه |

**والخلاصة:** ما تطلبه الورقةُ من «تسجيل دخولٍ وخروجٍ وباب وتصريح» **موجودٌ**. المطلوب إضافتُه هو ما يجعل البوابة **نقطةَ تحقّقٍ** لا سجلَّ مرور.

---

## ٢ · الفجوات — بندًا بندًا من ورقة المالك

| # | ما طلبته الورقة | القائم | الفجوة |
|---|---|---|---|
| ج‑١ | **سببُ الدخول** بتسع قيم (زيارة · مورد · مركبة عائدة · مرتجع · نقل داخليّ · صيانة · تحميل · موظّف · أخرى) | `VISIT_PURPOSE` قيمتان فقط: `inbound`/`outbound` | ⛔ غائب — وتوسيعُه يمسّ `doorAccepts` (ق-٣) |
| ج‑٢ | **حالةُ الحمولة**: محمّلة · فارغة · جزئيًّا | لا حقل | ⛔ غائب |
| ج‑٣ | حقولُ الحمولة الأحد عشر (المورد · PO · فاتورة · DN · حاوية · ختم · طرود · طبليات · نوعها · صورة · ملاحظات) | `docRef {type, number, id}` وحده | ⛔ غائب |
| ج‑٤ | **إظهارُ ما يحتاجه الحارس فقط** حسب السبب | النموذج ثابتٌ لكلّ زيارة | ⛔ غائب |
| ج‑٥ | **حالةُ الخروج** بخمس قيم، و«الخروجُ حركةٌ ثانية» | `permitRef` + `exitVerdict` — ولا وصفَ لحمولةِ الخروج | ⛔ غائب |
| ج‑٦ | **حمولةُ الدخول ≠ حمولةُ الخروج** | الزيارة لها غرضٌ واحد | ⛔ غائب (ق-٤) |
| ج‑٧ | **الطبلياتُ العائدة**: عددٌ ونوعٌ وملكيّةٌ وحالة | لا شيء — و«الطبالي» في البوّابة تعني LPN وهي **شيءٌ آخر** | ⛔ غائب (ق-١) |
| ج‑٨ | **رصيدُ الطبليات لكلّ مورد** (دخل ٨٠ · خرج ٦٥ · الرصيد ١٥) | لا شيء — و`VISITS_CAP = 200` في `yardService.js` يمنع اشتقاقَ رصيدٍ شهريّ من الزيارات | ⛔ غائب (ق-٥) |
| ج‑٩ | **شاشةُ مراقبة الطبليات** بأرصدتها الستّة | لا شاشة | ⛔ غائبة |
| ج‑١٠ | **المطابقة**: بوابة ٢٠ / استلام ١٨ | `receivedByLine` في [`grnBridge.js`](../src/services/lpn/grnBridge.js) يعرف المستلَم — ولا أحدَ يقابله ببيان البوابة | ⛔ غائبة (وهي أثمنُ ما في الورقة) |
| ج‑١١ | «قراءةُ رقم اللوحة» | لا OCR ولا ANPR — ولا CDN ولا خدمةً خارجيّة (قاعدة الأوفلاين) | ⚠️ يُقرأ باليد أو بباركود بطاقة (ق-٦) |
| ج‑١٢ | شاشةٌ للحارس على البوابة | التبويب داخل لوحةِ أسطولٍ مكتبيّةٍ بأربعة تبويبات | ⛔ لا تصلح للهاتف عند الحاجز |

---

## ٣ · القرارات المطلوبة من المالك — قبل أيّ كود

> هذه ليست تفاصيلَ تنفيذ؛ كلُّ واحدٍ منها يغيّر شكل النظام.
>
> ## ✅ حُسمت السبعةُ كلُّها — المالك · 2026-08-28
>
> | القرار | ما أقرّه المالك |
> |---|---|
> | ق-١ | الاسم: **«الطبليات العائدة»** — و«الطبالي» تبقى لـLPN |
> | ق-٢ | **تطبيقٌ مستقلّ** `gate` بثلاث شاشات — والتبويب القائم يبقى منظارَ المدير |
> | ق-٣ | حاجةُ الباب **تُستنتَج من السبب** لا يختارها الحارس |
> | ق-٤ | **زيارةٌ واحدةٌ بحمولتين** لا زيارتان |
> | ق-٥ | **دفترٌ دائمٌ ملحقٌ-فقط** `pallet_moves` لا اشتقاقٌ من الزيارات |
> | ق-٦ | اللوحةُ **يدويّةٌ الآن** — وبطاقةُ الباركود تُؤجَّل ولا تُلغى |
> | ق-٧ | الزائر: **اسمٌ وجهةٌ وهاتف** — بلا رقم هويّةٍ ولا صورة |
>
> **فالطريق مفتوحٌ لـGATE-000 بأمر «نفّذ».**

**ق-١ · اسمُ «الطبلية» — وهي أخطر التباسٍ في الورقة. ✅ محسومة (المالك · 2026-08-28)**
في البوّابة اليوم **«الطبالي» = LPN**: هويّةٌ منطقيّةٌ لوحدةِ مناولةٍ تحمل بضاعةً وتُخزَّن وتُسحَب ([`lpnCode.js`](../src/services/lpn/lpnCode.js) وتسعٌ وعشرون ملفًّا معها). وما تصفه الورقةُ **أصلٌ ماديٌّ عائد**: خشبةٌ ملكُها لمورّدٍ أو لنا، تُعدّ وتُردّ ويُحسب رصيدُها. لو حملا الاسمَ نفسه ظهر في البوّابة رقمان اسمُهما «عدد الطبليات» لا يتّفقان أبدًا — أحدُهما وحداتُ مناولةٍ نشطة والآخر خشبٌ في الساحة.

> **القرار:** يبقى «الطبالي» لـLPN، والجديدُ اسمُه **«الطبليات العائدة»** (Returnable Pallets) — في كلّ شاشةٍ ونصٍّ ومدخلِ دليل. **ولا يُكتب «طبليات» مجرّدةً في شاشةٍ جديدة**، ويُفحص ذلك في قبول GATE-303.

**ق-٢ · موضعُ الميزة: تطبيقٌ مستقلّ أم تبويبٌ قائم؟ ✅ محسومة (المالك · 2026-08-28)**

> **القرار: تطبيقٌ مستقلّ** — التفصيلُ وحجّتُه في §٤.

**ق-٣ · السببُ التسعُ مقابل غرضِ الباب. ✅ محسومة (المالك · 2026-08-28)**
`doorAccepts` يقابل غرضَ الزيارة بتدفّق الباب (استلام/تحميل/كلاهما)، فلو حلّت الأسبابُ التسعةُ محلَّ الغرض انكسر الحارس.

> **القرار: الغرضُ يُشتقّ من السبب** بـ`purposeOf(reason)` ولا يُسأل عنه الحارس — خطوةٌ أقلُّ على الحاجز وموضعُ خطأٍ أقلّ:
> · مورّدٌ لتسليم بضاعة · استلامُ مرتجعٍ · مركبةُ شركةٍ عائدةٌ من مهمّة ⇐ **استلام** (`inbound`)
> · تحميلُ بضاعةٍ ⇐ **تحميل** (`outbound`)
> · نقلٌ داخليٌّ بين المواقع ⇐ **حسب حالة الحمولة**: محمّلةٌ داخلةً ⇐ استلام · فارغةٌ لتُحمَّل ⇐ تحميل
> · زيارةٌ · موظّف/إدارة · صيانةٌ أو خدمة ⇐ **بلا باب** — فلا تدخل طابورَ الأبواب أصلًا
> · أخرى ⇐ **بلا باب**، ويُعلَن نقصُه ليُستكمَل (نمط `visitGaps`).
>
> **والثلاثةُ الملتبسةُ أقرّها المالك نصًّا (2026-08-28):** المركبةُ العائدةُ من مهمّةٍ **تحتاج بابًا** (قد تعود ببضاعةٍ لم تُسلَّم أو بمرتجع) · والنقلُ الداخليُّ **حسب حالة الحمولة** · والصيانةُ **بلا باب** (تذهب إلى الورشة لا إلى بابِ المستودع).
>
> ★ **وهذا اشتقاقٌ لا حذف:** إن اقتضت حالةٌ خلافَه أسندَ مشرفُ المناولة البابَ يدويًّا كما هو اليوم — `assignDoorVerdict` يُنذر ولا يمنع.

**ق-٤ · الخروجُ حركةٌ ثانية — في زيارةٍ واحدة أم زيارتين؟ ✅ محسومة (المالك · 2026-08-28)**
مؤقّتُ البقاء `turnaround` يُقاس من الوصول إلى الخروج، وزيارتان تكسرانه وتُنتجان مركبةً «داخل الموقع» للأبد.

> **القرار: زيارةٌ واحدةٌ بحمولتين** (`load.in` و`load.out`) لا زيارتان. والمطلوبُ في الورقة — «١٥ طبليةً دخلت و٦ مرتجعاتٍ خرجت» — يتحقّق في البطاقة الواحدة، والفرقُ يبقى مقروءًا فيها.

**ق-٥ · رصيدُ الطبليات: يُشتقّ أم يُقيَّد؟ ✅ محسومة (المالك · 2026-08-28)**
عرفُ البوّابة أن يُشتقّ لا يُخزَّن (`doorOccupancy` · `buildCard`). لكنّ `VISITS_CAP = 200` — «الساحة تُدار باليوم لا بالتاريخ كلّه» — ورصيدُ مورّدٍ شهريٌّ لا يُشتقّ من مئتي زيارة، **ويَنقُص صامتًا** بلا أن يُعلن نقصَه.

> **القرار: دفترٌ دائمٌ ملحقٌ-فقط `pallet_moves`** — كلُّ حركةٍ سطرٌ لا يُعدَّل ولا يُحذف، والرصيدُ حاصلُ جمعه. والبيّنةُ على أنّ لا نواةَ قائمةً تصلح: `stock_moves` للأصناف بوحداتها، وإقحامُ الخشب فيه يُفسد دفترَ المخزون — وهي البيّنةُ نفسُها التي بُرّرت بها `yard_visits` حين رُفض إقحامُها في `trips`. **والمجموعةُ تُلحق بالمجموعات الملحقة-فقط المقدّسة** في `firestore.rules`: `allow update, delete: if false`.

**ق-٦ · رقمُ اللوحة. ✅ محسومة (المالك · 2026-08-28)**
لا قراءةَ آليّة (ANPR/OCR): لا CDN ولا خدمةَ خارجيّة بقاعدة الأوفلاين ١٠٠٪.

> **القرار: إدخالٌ يدويٌّ الآن** — لوحةُ مفاتيحَ رقميّةٌ كبيرةٌ للإبهام، بلا ترقيمٍ ثانٍ ولا ماستر مركباتٍ جديد. **وبطاقةُ الباركود للمركبات المتكرّرة مؤجَّلةٌ لا ملغاة**: تُضاف حين يشكو الحرّاسُ من بطء الكتابة، ومحرّكُ المسح `src/services/scan/` جاهزٌ لها بلا تغييرٍ في النموذج (`plate` هو المفتاح في الحالين).

**ق-٧ · بياناتُ الزائر. ✅ محسومة (المالك · 2026-08-28)**
«زيارة» تعني أشخاصًا ليسوا موظّفين، وما يُكتب يبقى في سجلٍّ لا يُحذف.

> **القرار: الحدُّ الأدنى — اسمٌ · جهةُ المقابلة · رقمُ هاتف.** **لا رقمَ هويّةٍ ولا صورةَ هويّة** — فالسجلّ يجيب «من دخل وإلى من؟» بلا حفظِ وثائقَ شخصيّة. وقراءةُ بيانات الزائر محصورةٌ بالمديرَين وضابط البوابة، وتُفرض في `firestore.rules` لا في الشاشة وحدها (GATE-501).

---

## ٤ · موضعُ الميزة في البوّابة — وهو شرطُ المراجعة

### المقترَح: تطبيقٌ مستقلّ رابعَ عشرَ — `key: 'gate'` · «بوّابة الأمن»

**ولماذا لا تبويبٌ خامسٌ في «عمليات الأسطول»؟** لثلاثةِ أسبابٍ كلُّها من دروس البوّابة نفسِها:

1. **هذه دورةُ تنفيذٍ كاملة لا شاشتان متجاورتان** — وهي حرفيًّا الحجّةُ التي أُنشئ بها تطبيقُ ‹LPN›: تبدأ من وصولِ شاحنةٍ وتمرّ بالسبب والحمولة والباب والتفريغ والتصريح وتنتهي بالمطابقة مع الاستلام.
2. **الحارسُ على الحاجز لا يفتح لوحةَ أسطولٍ بأربعة تبويبات.** وهذا عينُ العطب الذي أصلحه ‹LOC›: «شاشةُ العامل مدفونةً داخل لوحةٍ إداريّة». شاشةُ البوابة **للهاتف أوّلًا** وبثلاث لغات.
3. **الصلاحيّة تتّسع بلا داعٍ.** `gate_officer` يرى اليوم مجموعةَ `fleet` كاملةً — جردَ المركبات ومركزَ الصيانة والعُهدَ العينيّة — ولا شأن له بها. التطبيقُ المستقلّ يسمح **بتضييقها** لاحقًا بلا كسرِ أحد.

**ولا يُحذف شيء:** تبويبُ «الساحة والأبواب» في `fleet-operations.astro` **يبقى كما هو** — هو منظارُ المدير. والشاشاتُ الجديدة تقرأ **المجموعتين نفسَيهما** (`doors` · `yard_visits`) وتستدعي **النموذجَ الخالص نفسَه** (`yardModel.js`). فلا مصدرَ ثانٍ ولا نموذجَ موازٍ — وهي القاعدةُ التي أنهت انحرافَ `pageAccess`.

### الشاشاتُ الثلاث

| المسار | الاسم | لمن | ماذا |
|---|---|---|---|
| `/dashboard/gate-post` | **مركز البوابة** | `gate_officer` · المديران | للهاتف أوّلًا وبثلاث لغات: تسجيلُ دخولٍ بسببٍ وحمولةٍ وطبليات، وخروجٍ بحمولتِه، وقائمةُ ما في الموقع الآن |
| `/dashboard/gate-log` | **سجلّ البوابة والمطابقة** | المديران · `gate_officer` · `inventory_auditor` | مكتبيّة: الزياراتُ وأزمنتُها وتجاوزاتُها، **وفروقُ البوابة مقابل الاستلام** وحسمُها |
| `/dashboard/pallet-control` | **الطبليات العائدة** (ق-١) | المديران · `gate_officer` · الماليّ | الأرصدةُ الستّة، ورصيدُ كلّ مورّدٍ (دخل · خرج · الرصيد)، وسجلُّ الحركات |

**الأيقونة:** `shield` **موجودةٌ** في [`icon-data.js`](../src/components/ui/icon-data.js) (ومعها `shieldCheck`) — فلا رسمَ جديدًا: التطبيقُ `shield`، وبنودُه `truck` و`clipboardList` و`layers`. **لا إيموجي في الواجهة** (أيقونات SVG خطّية).

**الأدوار في [`navAccess.js`](../src/services/auth/navAccess.js):** تُضاف `'gate'` إلى `gate_officer` و`admin` و`warehouse_manager` و`inventory_auditor`، والبنودُ الحسّاسةُ محصورةٌ بـ`roles` كنمط `gate_officer` القائم.

---

## ٥ · نموذج البيانات — توسعةٌ لا استبدال

كلُّ ما يلي يُضاف إلى **`shapeVisit` في [`yardModel.js`](../src/services/fleet/yardModel.js)** — منطقٌ خالصٌ بلا Firestore وبلا DOM، ولكلّ حقلٍ اختبارُه:

```
visit {
  plate, carrier, driverName, driverId,         // قائم
  purpose,                                      // قائم — يُشتقّ من reason (ق-٣)
  reason,                                       // ✚ ج‑١: تسعُ قيمٍ معلَنةٌ في GATE_REASONS
  doorCode, spot, stage, stamps, permitRef,     // قائم
  load: {
    in:  { state, cargoType, party, poRef, invoiceRef, dnRef,
           containerNo, sealNo, packages, pallets[], photoIds[], notes },
    out: { state, reason, cargoType, destination, doRef, transferRef,
           invoiceRef, soRef, issueRef, pallets[], receivedBy, handedBy, notes },
  },
  visitor: { name, phone, host },               // ✚ ق-٧ — للسبب «زيارة» وحده
}
pallets[] = { count, type, ownership, condition }  // ✚ ج‑٧
```

ودوالُّ الحكم الخالصة الجديدة:

- `GATE_REASONS` — التسعةُ **معلَنةٌ في مصدرٍ واحد** لا مكتوبةً في شاشة (نمط `YARD_LIMITS`).
- `purposeOf(reason)` — اشتقاقُ ق-٣، فلا يُكسَر `doorAccepts`.
- `fieldsFor(reason, loadState)` — **ج‑٤ كلُّها**: ما الحقولُ التي تظهر؟ دالّةٌ نقيّةٌ مختبَرة، **والشاشةُ تعرض ولا تقرّر**.
- `loadGaps(load)` — ما ينقص **ويُعلَن ولا يمنع** (نمط `visitGaps` القائم: شاحنةٌ على الرصيف لا تُردّ لأنّ رقم الأمر لم يُكتب).
- `exitVerdict` — **يُوسَّع لا يُستبدَل**: يبقى «لا خروج بلا تصريح»، ويُضاف «لا خروجَ محمّلًا بلا وصفِ حمولةٍ» — وهو حارسُ الورقة الحقيقيّ.
- `palletBalance(moves)` — رصيدُ الطبليات (ج‑٨) من السجلّ الملحق-فقط.

والمرفقاتُ (الصورةُ في ج‑٣) تمرّ بـ`attachmentFile.js` كما هي: **٧٠٠ ك.ب بعد الضغط**، فلا صورةَ تكسر سقفَ المستند.

---

## ٦ · المطابقة — أثمنُ ما في الورقة

بوابةُ الأمن تصير **نقطةَ تحقّقٍ مستقلّة** حين يُقابَل ما رآه الحارسُ بما سجّله المخزن:

| الطرف | المصدر |
|---|---|
| ما رآه الحارس | `visit.load.in.pallets` · `packages` |
| ما استلمه المخزن | `receivedByLine(session)` في [`grnBridge.js`](../src/services/lpn/grnBridge.js) وعددُ الطبالي المعتمَدة |

والربطُ بينهما بمفتاحٍ مشترك: **رقمُ أمر الشراء PO أو رقمُ الإذن**، واللوحةُ سندًا احتياطيًّا.

**والفرقُ لا يُغلق بزرّ** — يُحتذى نمطُ [`transferPallets.js:211`](../src/services/lpn/transferPallets.js) حرفيًّا: قرارٌ منصوصٌ (ماذا تقرّر ولماذا) · **وطرفٌ يتحمّله** (البوابة أم الناقل أم الاستلام) · وفاعلٌ باسمه. والفرقُ المفتوح يظهر في `gate-log` وفي `lpn-governance` معًا.

---

## ٧ · المراحل والمهامّ

> **م٠ إلزاميّة قبل أيّ شيء.** ومن م١ فصاعدًا: **لا مهمّة تُغلق بلا `evidence.commit`** — يرفضها `plan-check.mjs`.

### م٠ — الحوكمة (جلسةٌ واحدة · **بلا سطر كودٍ إنتاجيّ**)

| # | المهمّة | القبول |
|---|---|---|
| GATE-000 | تثبيتُ هذه الوثيقة + `docs/plan/gate-control.json` + بطاقةُ استئنافٍ + `npm run plan:gate` (المتتبّع التاسع) | `npm run plan` يقبله ويولّد البطاقة · القراراتُ السبعة مثبَّتةٌ بنصّ المالك |

### م١ — السببُ والحمولةُ في النموذج الخالص (٢–٣ جلسات)

| # | المهمّة | القبول |
|---|---|---|
| GATE-101 | `GATE_REASONS` + `purposeOf` + توسعةُ `shapeVisit` بـ`load.in` | اختبارٌ يُثبت أنّ كلّ سببٍ يشتقّ غرضًا صالحًا · **و`doorAccepts` لم يتغيّر سلوكُه لزيارةٍ قديمة** |
| GATE-102 | `fieldsFor(reason, loadState)` — الإظهارُ المشروط | «زيارة» تُظهر حقولَ الزائر وحدها · «تسليم بضاعة» تُظهر PO والفاتورة والطبليات · اختبارٌ لكلّ سببٍ من التسعة |
| GATE-103 | `loadGaps` + توسعةُ `exitVerdict` بحارس «لا خروجَ محمّلًا بلا وصف» | نقصُ الحقول **يُعلَن ولا يمنع** التسجيل · والخروجُ المحمّل بلا وصفٍ **يُمنع** — مُثبَتًا بالنقض |

### م٢ — مركزُ البوابة: الشاشةُ التي يفتحها الحارس (٣–٤ جلسات)

| # | المهمّة | القبول |
|---|---|---|
| GATE-201 | `/dashboard/gate-post` — للهاتف أوّلًا · تُدرَج في `navCatalog` ضمن تطبيق `gate` · **وشرحُها في `usage-guide.json` شرطُ قبولٍ لا تابعٌ لاحق** | `npm test` أخضر (حارسُ الدليل) · `npm run audit` بلا شاشةٍ يتيمة · `npm run guide` يُولّد الدليل |
| GATE-202 | ثلاثُ لغاتٍ بـ`fieldLexicon.js`، و**العربيّةُ أصلٌ واحتياط** | مفتاحٌ ناقصٌ يعود عربيًّا لا فارغًا — مُثبَتًا بالاختبار |
| GATE-203 | وصلُ محرّك المسح `src/services/scan/` للباركود، ولوحةُ إدخال اللوحة (ق-٦) | يعمل بالكاميرا **وبجهاز الباركود** كنمط الشاشات الخمس |
| GATE-204 | الخروجُ حركةً ثانية `load.out` (ق-٤) | «١٥ دخلت · ٦ خرجت» تُقرأ في ملفّ الزيارة صراحةً — وهو **سيناريو قبولٍ منصوص** |

### م٣ — الطبلياتُ العائدة ورصيدُها (٢–٣ جلسات)

| # | المهمّة | القبول |
|---|---|---|
| GATE-301 | `pallet_moves` ملحقٌ-فقط + قواعدُ Firestore (لا تعديل ولا حذف) | تُكتب في اللحظة نفسِها للختم — **بكتابةٍ واحدةٍ من `yardService`** لا بيدِ الشاشة |
| GATE-302 | `palletBalance` — رصيدُ كلّ مورّدٍ وكلّ نوعٍ وحالة | مثالُ الورقة: ٨٠ − ٦٥ = ١٥ · اختبارٌ للتالف والمفقود |
| GATE-303 | `/dashboard/pallet-control` — الأرصدةُ الستّة وجدولُ الموردين · **+ شرحُها في الدليل** | الأرقامُ **مشتقّةٌ من السجلّ** لا مخزَّنةٌ حقلًا |

### م٤ — المطابقة: البوابةُ نقطةُ تحقّق (٢–٣ جلسات)

| # | المهمّة | القبول |
|---|---|---|
| GATE-401 | مقابلةُ بيان البوابة بـ`receivedByLine` بمفتاح PO/الإذن | «بوابة ٢٠ / استلام ١٨» يظهر — **وقياسُ نجاحٍ أن يُطلق فعلًا على بياناتٍ حقيقيّة** |
| GATE-402 | حسمُ الفرق بنمط `transferPallets`: قرارٌ وطرفٌ وفاعل | لا إغلاقَ بلا الثلاثة — مُثبَتًا بالنقض |
| GATE-403 | `/dashboard/gate-log` — الزياراتُ والفروقُ المفتوحة · **+ شرحُها في الدليل** | الفرقُ المفتوح يظهر في `lpn-governance` أيضًا |

### م٥ — الحوكمةُ والإقفال (جلسة)

| # | المهمّة | القبول |
|---|---|---|
| GATE-501 | مصفوفةُ الأدوار في `navAccess` و`pageAccess` و`firestore.rules` | اختبارُ الانحراف أخضر · **ولا صلاحيّةَ تتّسع صامتةً** |
| GATE-502 | تجربةٌ حيّةٌ بالأدوار الحقيقيّة: حارسٌ · أمينُ مخزن · مدير | ★ **سيناريو من الوصول إلى الخروج والمطابقة، موثَّقٌ بلقطات** — ودَينُ ‹LPN› أنّها لم تُجرَّب حيًّا بالأدوار قطّ، فلا يُكرَّر |

---

## ٨ · دليلُ الاستخدام — القاعدةُ المُلزِمة

ثلاثةُ مدخلاتٍ **تُكتب مع شاشاتها لا بعدها**، كلٌّ بـ`what` و`steps` و`tips`:

| المسار | «لمن؟» | جوهرُ الشرح |
|---|---|---|
| `/dashboard/gate-post` | حارسُ البوابة | اقرأ اللوحة ← اختر السبب ← **يظهر لك ما يخصّ سببَك وحده** ← الحمولةُ والطبليات ← تسجيلُ دخول. وعند الخروج: هل خرجت فارغة؟ فإن لا — بماذا؟ |
| `/dashboard/gate-log` | مديرُ المستودع · مدقّقُ الجرد | اقرأ زياراتِ اليوم وأزمنتَها، وافتح **الفروقَ المفتوحة** بين البوابة والاستلام واحسِمها بقرارٍ وطرفٍ يتحمّله |
| `/dashboard/pallet-control` | مديرُ المستودع · الماليّ · الحارس | من له طبلياتٌ عندنا، ومن عنده طبلياتُنا، وما التالف والمفقود |

**والفحص:** `npm test` (حارسُ الدليل) ثمّ `npm run guide` — والدليلُ المنشور يُعاد توليدُه إلى `public/دليل-استخدام-البوابة.html`، وهو المرجعُ العامّ. **ولا شرحَ يتيمًا** لشاشةٍ لم تُنشأ.

---

## ٩ · البوّابةُ الخضراء قبل كلّ كومِت نهائيّ

```
npm run build   ·  npm run guide  ·  npm test  ·  npm run audit  ·  npm run plan  ·  npm run lint
```

---

## ١٠ · مزالقُ معلومةٌ — من دروسِ الطبقات السابقة

1. **★★★ «مبنيٌّ ومنشورٌ وبلا مستدعٍ».** نصفُ هذه الخطة موجودٌ منذ ‹EXE-602› ولا يعرفه أحد. فكلُّ مهمّةٍ هنا **تسمّي الشاشةَ التي تستدعي** ما تبنيه، ولا تُغلق قبل أن يُستدعى.
2. **★★★ حارسٌ يقرأ حقلًا لا يُكتب.** `exitVerdict` الموسَّعُ سيقرأ `load.out.state` — فإن لم تكتبه الشاشةُ **لم يُطلق الحارسُ ولا مرّة**. اختبارُ كلّ حارسٍ **بالنقض**: أدخِل ما يجب أن يمنعه وتأكّد أنّه منع.
3. **★★ الشاشةُ تعمل وتُعرض عاريةً.** غلافُ `o_theme` على جذر المكوّن.
4. **الأرقامُ المتناقضة.** ق-١ ليس تسميةً: هو الفرقُ بين بوّابةٍ تُصدّق وبوّابةٍ تُشكّك في نفسها.
5. **`VISITS_CAP = 200`.** أيُّ تقريرٍ تاريخيٍّ يُبنى على الزيارات وحدها **سيكذب صامتًا** بعد مئتي زيارة.
6. **قواعدُ Firestore يعدّلها الوكلاء وينشرها المالكُ وحده** من Firebase Console — والنشرُ خطوةٌ في م٣ و م٥ لا تُنسى.

---

## ★★★ ١٠أ · التقاطعُ مع ‹LPN-722› — كُشف بعد التنفيذ (2026-08-28)

> **قرار المالك:** «أبقِ الاثنتين واربطهما».

جلسةٌ متوازية (`claude/task-a-plus-plus-bd1eb6`) تبني **م٧ من طبقة الطبالي**،
وفيها شاشةُ «الأبواب والبوّابة» (`/dashboard/dock-operations`) بوضعٍ اسمُه
`EXIT` — **بوّابةُ خروجٍ أيضًا**، ولضابط البوابة نفسِه.

### ولماذا لا تُحذف إحداهما؟ لأنّهما تجيبان سؤالين مختلفين

| | مركز البوابة ‹GATE-201› | الأبواب والبوّابة ‹LPN-722› |
|---|---|---|
| الفاعل | حارسُ الأمن على الحاجز | عاملُ التحميل عند الباب |
| الأداة | كتابةٌ (لوحة · سبب · أعداد) | **مسحُ باركود** (باب ← مركبة ← رحلة ← طبليّة) |
| السؤال | **لماذا** جاءت وماذا تحمل؟ | **أيُّ طبليّةٍ بعينها** رَكِبت هذه السيارة؟ |
| الأثر | `yard_visits` — زيارةٌ بأختامٍ ومؤقّتات | `dock_sessions` — بيّنةُ مسحٍ عند عنوانٍ ماديّ |
| المخرَج | فرقُ البوابة/الاستلام · رصيدُ الطبليات | إثباتُ أنّ العامل كان عند الباب فعلًا |

**فالأولى تتابع المركبة، والثانية تتابع البضاعة.** وحذفُ إحداهما يُفقد سؤالَها.

### والتقاطعُ نقطةٌ واحدة: **لحظةُ الخروج**

وقِيس لا خُمِّن: `DockOperations.jsx` فيه **صفرُ ذكرٍ** لـ`yard_visits` أو
`permitRef`، و`gateService.js` فيه صفرُ ذكرٍ لـ`dock_sessions`. **فالمنظومتان
لا تعرف إحداهما الأخرى** — والسؤال «أما خرج من البوّابة هو ما حُمِّل عند
الباب؟» بلا جواب.

### تصميمُ الربط (‹GATE-601› — بعد أن يُودَع عملُها)

١. جلسةُ `EXIT` تُغلق على لوحةٍ ما ⇐ تُختم الزيارةُ المفتوحة لتلك اللوحة
   بـ`dockExitRef` (معرّفُ الجلسة). **ختمُ أثرٍ لا مرحلةٌ جديدة** — نمط
   `isSideEffectStamp` في المستندات.
٢. و`checkOut` يقرأ: أثمّة جلسةُ مسحٍ لهذه اللوحة؟ فإن لم تكن، **يُعلَن ولا
   يُمنع** (نمط `loadGaps`): «خرجت بلا مسحٍ عند الباب».
٣. والمصدرُ يبقى واحدًا لكلّ سؤال: عددُ الطبليات من البوّابة، وهويّاتُها من
   المسح — **ولا يُطرح رقمٌ من رقم**.

### ⚠️ وتصادمٌ في الملفّات يجب أن يُحلّ بإبقاء الإضافتين

`navAccess.js` (سطرُ `gate_officer` نفسُه: هي تضيف `lpn` وأنا أضفتُ `gate`) ·
`navCatalog.js` · `firestore.rules` · `SESSION_HANDOFF.md` · `architecture.json`

---

## ١١ · ما ليس في هذه الخطة

قراءةُ اللوحات آليًّا (ANPR/OCR) · الحواجزُ والبوّاباتُ الكهربائيّة · كاميراتُ المراقبة · بطاقاتُ الدخول RFID · مواعيدُ الشاحنات للموردين عبر بوّابةٍ خارجيّة · وزنُ المركبات (جسر الوزن). **ولا تُذكر منجزةً ولا مؤجَّلةً ضمنًا — بل خارجَ النطاق صراحةً.**

---

## ١٢ · الخطوةُ التالية

القراراتُ السبعةُ **محسومةٌ كلُّها** (§٣ · 2026-08-28). فالمتبقّي أمرُ المالك **«نفّذ»** لبدء **GATE-000** — وهي وثائقُ وخططٌ لا كود: المتتبّعُ التاسع `docs/plan/gate-control.json` وبطاقةُ استئنافه وأمرُ `npm run plan:gate`. وأوّلُ كودٍ إنتاجيٍّ يأتي في GATE-101.
