# بطاقة الاستئناف — بوابة الأمن والطبليات العائدة ‹GATE›

> **ملفّ مولَّد آليًّا — لا يُحرَّر بيد.** مصدره [`gate-control.json`](gate-control.json)؛ يُعاد توليده بـ`npm run plan:gate`.
> **اقرأني أوّلًا في كلّ جلسة جديدة.** لا تقرأ `SESSION_HANDOFF.md` كلّه — هذه الصفحة تكفي للاستئناف.

**الخطة:** [طبقة بوابة الأمن والطبليات العائدة ‹GATE›](../خطة-بوابة-الأمن-والطبليات-العائدة.md) · **المسار:** المتتبّع التاسع — البوابة نقطةُ تحقّقٍ لا سجلَّ مرور
**آخر تحديث:** 2026-08-28 بواسطة Claude
**التقدّم:** 15 من 17 مهمّة (88٪)

## أين توقّفنا
_لا مهمّة قيد التنفيذ — ابدأ من «الجاهز للبدء» أدناه._

## الجاهز للبدء الآن (بلا انتظار أحد)
_لا شيء جاهز — كلّ ما تبقّى محجوب. راجع «المحجوب» و«على المالك»._

## المحجوب
- **GATE-502** — التجربةُ الحيّة بالأدوار الحقيقيّة — ودَينُ LPN لا يُكرَّر — _محجوب بـ:_ GATE-O02 — تجربةٌ حيّةٌ بالأدوار الحقيقيّة على البوّابة المنشورة، ولا يملكها وكيل: تحتاج حسابَ حارسٍ حقيقيًّا وهاتفًا عند الحاجز وشاحنةً تدخل. وتسبقها GATE-O01 (نشرُ القواعد).
- **GATE-601** — ربطُ لحظة الخروج بمسح الباب — سلسلةٌ واحدةٌ لا منظومتان — _محجوب بـ:_ عملُ الجلسة المتوازية claude/task-a-plus-plus-bd1eb6 غيرُ مُودَعٍ بعد (dock_sessions و exitGate.js و DockOperations.jsx). ودستورُ AGENTS بندٌ ٤ يمنع لمسَ نطاق وكيلٍ آخر عن عملٍ غير مرفوع. يُستأنف حين تُودِع ويُعاد أساسُها على main.

## على المالك
- **GATE-502** — التجربةُ الحيّة بالأدوار الحقيقيّة — ودَينُ LPN لا يُكرَّر
- **GATE-O01** — نشرُ firestore.rules من Firebase Console بعد إضافة pallet_moves — الوكلاء يعدّلونها في الريبو والمالك وحده ينشرها _(إن لم يتوفّر: الكتابة في دفتر الطبليات ترتدّ permission-denied والرصيد يبقى صفرًا)_
- **GATE-O02** — تجربةٌ حيّةٌ بالأدوار الحقيقيّة: حارسُ بوابةٍ وأمينُ مخزنٍ ومدير — من الوصول إلى الخروج والمطابقة _(إن لم يتوفّر: تتكرّر دَينُ ‹LPN›: طبقةٌ كاملةٌ لم تُجرَّب حيًّا بالأدوار قطّ)_

## أين أجد ما بنيناه؟ (روابط البوابة)
| الميزة | مكانها |
|---|---|
| مركز البوابة — تسجيل الدخول والخروج | `/dashboard/gate-post` — بوّابة الأمن |
| سجلّ البوابة والمطابقة | `/dashboard/gate-log` — بوّابة الأمن |
| الطبليات العائدة وأرصدتها | `/dashboard/pallet-control` — بوّابة الأمن |

## القاعدة عند كلّ إغلاق مهمّة
١. حدّث حالة المهمّة و`evidence.commit` في `gate-control.json`.
٢. `npm run plan:gate` — يُعيد توليد هذه البطاقة ويرفض الإغلاق بلا بيّنة.
٣. سطر في `SESSION_HANDOFF.md` بمعرّف المهمّة.

---
_مولَّد بـ`scripts/plan-check.mjs`_
