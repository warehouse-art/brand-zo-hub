# بطاقة الاستئناف — الرحلة المتّصلة ‹JR› · اكتب «رحلة» لتُستأنف

> **ملفّ مولَّد آليًّا — لا يُحرَّر بيد.** مصدره [`connected-journey.json`](connected-journey.json)؛ يُعاد توليده بـ`npm run plan:journey`.
> **اقرأني أوّلًا في كلّ جلسة جديدة.** لا تقرأ `SESSION_HANDOFF.md` كلّه — هذه الصفحة تكفي للاستئناف.

**الخطة:** [الرحلة المتّصلة — من المستند إلى الرفّ وبالعكس](../خطة-الرحلة-المتصلة.md) · **المسار:** المتتبّع الرابع عشر — والقياسُ قلب ترتيبَه: أغلبُ ما طُلب مبنيٌّ بالكامل ولا يستدعيه أحد. لا ينقص منطقٌ، ينقص سلك.
**آخر تحديث:** 2026-09-04 بواسطة Claude
**التقدّم:** 15 من 15 مهمّة (100٪)

## أين توقّفنا
_لا مهمّة قيد التنفيذ — ابدأ من «الجاهز للبدء» أدناه._

## الجاهز للبدء الآن (بلا انتظار أحد)
_لا شيء جاهز — كلّ ما تبقّى محجوب. راجع «المحجوب» و«على المالك»._

## المحجوب
_لا شيء محجوب._

## على المالك
- **JR-O01** — ضغطةُ «ولّد الناقص (٣٬٦٠٠)» في /dashboard/warehouses — الكتابةُ في bin_locations لـisManager() وحدَه _(إن لم يتوفّر: كلُّ توجيهٍ معطَّل: بلا مواقعَ مسجَّلةٍ يُعيد suggestLocations «سيّد المواقع فارغ»، ولا شبكةَ مشيٍ لـpickPlan. والدفعاتُ تُبنى وتُختبر وحدويًّا ولا تُعرَض حيّةً)_
- **JR-O02** — حساباتُ الأدوار الحقيقيّة (storekeeper · picking_unit · putaway_unit) — LOC-O07 قائمٌ منذ 2026-08-16 _(إن لم يتوفّر: يبقى كلُّ ما يُسلَّم مبنيًّا مختبَرًا **غيرَ مجرَّبٍ حيًّا** ويُوسم كذلك — كدرس المراحل التي بُنيت بلا تجربة واحدة)_
- **JR-O03** — ١٠٤٠ صنفًا بلا وحدةِ أساس في Items_Master — والكتابةُ فيه للمديرين _(إن لم يتوفّر: المسارُ (أ) في د٣ لا يعمل لها. والمسارُ (ب) — خانتا «صناديق × قطع» بمعاملٍ مختومٍ على القيد — يحملها ريثما تُملأ، وuomDebtReport قائمةُ العمل)_
- **JR-O05** — إعادةُ فتح مهمّة تحضيرٍ على أمرٍ حُضِّر سلفًا: معرّفُ المهمّة صار حتميًّا `PICK__{docId}` والمعاملةُ ترفض إن وُجد المستند — فأمرٌ أُقفلت مهمّتُه (DONE) أو أُلغيت **لا تُفتح له مهمّةٌ ثانيةٌ من هذا الباب أبدًا**. أتريد بابًا لإعادة الفتح (استئنافُ القائمة إلى IN_PROGRESS · أم معرّفٌ بمحاولةٍ ثانية `PICK__{docId}__2`)؟ _(إن لم يتوفّر: يبقى المنعُ كما هو — وهو **مقصودٌ لا سهو**: قاعدةُ `picking_tasks` تُجيز التحديث، فكتابةٌ ثانيةٌ على المعرّف نفسِه كانت **تمحو `steps`** أي سجلَّ سحبٍ بُنيت عليه طبليّةُ صرفٍ وشحنة. والحاجةُ تُقضى اليوم بفتح المهمّة القائمة، أو بأمرِ بيعٍ جديدٍ إن تغيّر الطلب.)_
- **JR-O06** — قراءتان في طبليّةٍ واحدةٍ بدفعتنا نفسِها ومن لوطَين مختلفين للمصنع تندمجان في بندٍ واحدٍ يحمل **أوّلَ** لوط، والثاني يُبتلع بلا إعلان — لأنّ مفتاح البند `صنف__دفعة__صلاحية__وحدة` لا يشمل `supplierBatch`. أتريد إدخالَه في المفتاح فيُشقّ البند؟ _(إن لم يتوفّر: يبقى الاندماجُ — **وهو مُعلَنٌ باختبارٍ يثبّته** لا منسيّ. وإدخالُ الحقل في المفتاح **يشقّ بندًا كان واحدًا** فتتغيّر تجميعةُ الكمّيّات في مسارٍ ماليّ، ولا يُدسّ في دفعةِ تتبّع. والاختلافُ **بين طبليّتين** يُعلَن اليوم صحيحًا.)_

## القاعدة عند كلّ إغلاق مهمّة
١. حدّث حالة المهمّة و`evidence.commit` في `connected-journey.json`.
٢. `npm run plan:journey` — يُعيد توليد هذه البطاقة ويرفض الإغلاق بلا بيّنة.
٣. سطر في `SESSION_HANDOFF.md` بمعرّف المهمّة.

---
_مولَّد بـ`scripts/plan-check.mjs`_
