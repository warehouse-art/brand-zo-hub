# بطاقة الاستئناف — التنفيذ الميدانيّ

> **ملفّ مولَّد آليًّا — لا يُحرَّر بيد.** مصدره [`warehouse-execution.json`](warehouse-execution.json)؛ يُعاد توليده بـ`npm run plan:exec`.
> **اقرأني أوّلًا في كلّ جلسة جديدة.** لا تقرأ `SESSION_HANDOFF.md` كلّه — هذه الصفحة تكفي للاستئناف.

**الخطة:** [التنفيذ الميدانيّ — من «أين البضاعة» إلى «ما العمل الآن»](../خطة-التنفيذ-الميداني.md) · **المسار:** جراحيّ · **النواة تكبر ولا يُبنى بجانبها** · لا مجلّد خدمةٍ جديد ولا مجموعة Firestore جديدة إلّا ببيّنة أنّ لا نواة تصلح · ولا شاشة ثانية لمنطقٍ قائم
**آخر تحديث:** 2026-08-17 (EXE-803 — مرحلة ت٨ مكتملة) بواسطة Claude
**التقدّم:** 26 من 29 مهمّة (90٪)

## أين توقّفنا
_لا مهمّة قيد التنفيذ — ابدأ من «الجاهز للبدء» أدناه._

## الجاهز للبدء الآن (بلا انتظار أحد)
- **EXE-901** — تصنيف الحركة ABC — من دفتر الحركات لا من التخمين `ت٩`
- **EXE-903** — تعبئة موقع الالتقاط — نقلٌ داخليّ لا أمرُ شراء `ت٩`

## المحجوب
_لا شيء محجوب._

## على المالك
- **ت-O01** — من يُسنِد المهمّة الميدانيّة؟ النظام تلقائيًّا بحسب الأولويّة، أم المشرف يدويًّا، أم النظام يقترح والمشرف يُطلق؟ _(إن لم يتوفّر: يُبنى «النظام يقترح والمشرف يُطلق» — لا يُلزم ثقةً بمحرّكٍ لم يُجرَّب ولا يُبقي التوزيع كلّه يدويًّا)_
- **ت-O02** — من يملك إغلاق الاستثناء لكلّ نوع (رفض جودة · نقص سحب · تعذّر تخزين · تأخّر شحن)؟ وهل يُغلق بلا دليلٍ مرفق؟ _(إن لم يتوفّر: المُنشئ لا يُغلق · والإغلاق يحتاج قرارًا مكتوبًا · والدليل يُعلَن نقصه ولا يمنع — كي لا تتوقّف الدورة على مرفق)_
- **ت-O03** — أوزان عوامل الأولويّة (قرب الموعد · أهمّيّة العميل · توفّر الرصيد · عمر المهمّة) — أم تُترك مبدئيّةً تُضبط بالتجربة؟ _(إن لم يتوفّر: أوزانٌ مبدئيّة معلَنة في مصدرٍ واحد قابلٍ للتعديل، ويُعرض للمشرف **سببُ الدرجة** لا الرقم وحده)_
- **ت-O06** — هل ستدخل الشركة خدمة التخزين للغير (3PL)؟ الجواب وحده يفتح Owner والفوترة أو يبقيهما مؤجَّلين _(إن لم يتوفّر: يبقى مؤجَّلًا ولا يُبنى نصفُه)_
- **ت-O08** — حسابات الأدوار للتجربة الحيّة (`storekeeper` · `labor_supervisor` · `gate_officer` · `inventory_auditor`) — نفس عائق CC-602 وLOC-O07 _(إن لم يتوفّر: يبقى كلّ المبنيّ not tested حيًّا ويُصرَّح بذلك في كلّ تسليم — ولا يُعلَن قبولٌ تشغيليّ)_

## القاعدة عند كلّ إغلاق مهمّة
١. حدّث حالة المهمّة و`evidence.commit` في `warehouse-execution.json`.
٢. `npm run plan:exec` — يُعيد توليد هذه البطاقة ويرفض الإغلاق بلا بيّنة.
٣. سطر في `SESSION_HANDOFF.md` بمعرّف المهمّة.

---
_مولَّد بـ`scripts/plan-check.mjs`_
