# 🩺 خطة «العملية الجراحية» — الأسبوع 2026-08-01 → 2026-08-08

> **المصدر:** طلب المالك في ملف `عملية جراحية.md` (2026-08-01).
> **المنهج:** بُنيت هذه الخطة على **مسح ثلاثي موازٍ للكود الفعلي** (الهيكل والأدوار · المستندات والترقيم · الأمن والأوفلاين وأودو) — كل خطوة فيها تشير إلى ملف وسطر حقيقيين، لا تخمين.
> **الحالة:** جاهزة للتنفيذ خطوة بخطوة. رمز الاستئناف «1236» (شاشتا tasks+recruitment + المرحلة ٥) يبقى قائمًا ويُستأنف **بعد** هذا الأسبوع ما لم يأمر المالك بغير ذلك.

---

## ٠) القواعد الحاكمة للأسبوع كله

1. **المنطق مقدّس:** أي إعادة تصميم أو تحسين لا يمسّ منطق CRUD/الاشتراكات/الحرّاس إلا حيث تنصّ الخطة صراحة، وبمراجعة عدائية.
2. **بوابة خضراء بعد كل جلسة:** بناء 49 صفحة · 571+ اختبارًا · `npm run audit` نظيف · لينت 124=124 (صفر جديد).
3. **دفعة قواعد واحدة:** كل تعديلات `firestore.rules` عبر المحاور (٢ + ٤ + ٦) تُجمَع في **دفعة نشر موحّدة** تُسلَّم للمالك جاهزة للّصق مرة واحدة — مع ضمان أن الكود يعمل مع القواعد القديمة حتى لحظة النشر (توافق رجعي).
4. **معيار الواجهة الحاكم:** بلا إيموجي (أيقونات `Icon` SVG) · الأحمر للتحذير فقط · أرقام لاتينية · مكوّنات أودو داخل `.o_theme` · الربط قبل الإضافة · لا حذف/تغيير URL.
5. **تحقّق حيّ لا grep وحده:** أي تغيير بصري/سلوكي يُثبَت بقيم محسوبة أو معاينة مبنية.
6. **«ارفع» على دفعات:** دفعة أولى بعد المحورين ١+٢، ودفعة ختامية نهاية الأسبوع، حتى لا يتراكم عمل غير مدفوع.

---

## المحور ١ — إعادة التسمية الحاكمة: «قسم التوثيق والاعتماد» ← «قسم الحوكمة المستندية»

### الهدف
الاسم الجديد يظهر في **كل** المواضع: التقريرين المنشورَين (تقرير هيكل الوظائف والأدوار + المرجع التشغيلي الرسمي)، والمصدر الواحد، وكتالوج الوظائف، وكل صفحة تذكره — مع أثر تاريخي (`formerTitle`) لا يضيع.

### الوضع الحالي المكتشف (وقائع)
- المصدر الواحد `src/data/org-structure.json` يحمل الاسم في **14 موضعًا**: عقدة القسم `sec-doc` (~سطر 176-185)، الوظائف J01/J15/J20/J21 (426-907)، صف التوظيف (1257)، RACI (1323)، `odooRoles` (1447).
- المولّد `scripts/build-org.mjs` يستخدم الاسم **كمرساة نصية** في السطر 194 — إن لم تُحدَّث انكسر الحقن.
- التقريران **مولّدان جزئيًّا فقط**: مناطق `ORG:TREE/SUPPORT/STAFFING` آلية، لكن توجد مواضع **يدوية** لن يلمسها المولّد: في المرجع (أسطر 1211، 1222، 1370، 2048، 2162-2167، 2196، 2224، 2408) وفي تقرير الهيكل (مواضع يدوية مماثلة).
- **انجراف تسمية قائم أصلًا:** الجدول اليدوي في المرجع يسميه «**إدارة** التوثيق والاعتماد» (2167) بينما الشجرة المولّدة تقول «**قسم**» (2142).
- مواضع إضافية: `src/services/recruitment/jobsCatalog.js` (مولّد بالكامل — يكفي إعادة التوليد)، `src/pages/dashboard/workflows.astro:24` (يدوي)، `ROADMAP.md:507`.
- ⚠️ **`build-org.mjs` لا يعمل تلقائيًّا أبدًا**: خطاف `prebuild` يشغّل `build-arch.mjs` فقط، وCI يستدعي `npx astro build` مباشرة **فيتجاوز خطاف prebuild كليًّا**.

### الخطوات
1. تعديل مرساة `scripts/build-org.mjs:194` أولًا (قبل أي توليد).
2. تعديل `src/data/org-structure.json`: العقدة `sec-doc` → `title: "قسم الحوكمة المستندية"` · `titleEn: "Document Governance"` · إضافة `formerTitle: "قسم التوثيق والاعتماد"` (مع إبقاء سلسلة `formerTitle` القديمة في سجل الوظيفة J01) · تحديث الوظائف الأربع (J01: «رئيس قسم الحوكمة المستندية»، J15: «موظف الحوكمة المستندية»، J20، J21) · صف التوظيف · RACI · `odooRoles[2]`.
3. **حسم التوحيد:** نصّ المالك هو «**قسم** الحوكمة المستندية» — تُوحَّد كل المواضع اليدوية التي تقول «إدارة» على «قسم» (ويُحتفظ بالمسمى القديم في سياق تاريخي صريح فقط).
4. تشغيل `node scripts/build-org.mjs` → يجدّد المناطق الآلية في التقريرين + `jobsCatalog.js` (36 وظيفة).
5. تحرير يدوي لكل المواضع غير المولّدة في `public/المرجع-التشغيلي-الرسمي.html` و`public/تقرير-هيكل-الوظائف-والادوار.html` (القائمة أعلاه).
6. تحديث `workflows.astro:24` و`ROADMAP.md:507`.
7. **أتمتة دائمة (سدّ الثغرة الجذرية):** إضافة سكربت `"org": "node scripts/build-org.mjs"` لـ`package.json` + ضمّه إلى `prebuild` + تصحيح CI (`.github/workflows/astro.yml`) ليشغّل الخطاطيف (استدعاء السكربتين صراحة قبل البناء) — فلا يتكرر أبدًا سيناريو «عدّلنا المصدر ولم تتحدث التقارير».
8. `grep` نهائي: صفر بقايا للاسم القديم عدا `formerTitle` والسياق التاريخي.
9. بوابة خضراء + فحص بصري للتقريرين المبنيين + فحص شاشة التوظيف (لأن `jobsCatalog.js` سيتغيّر).

### معايير القبول
- التقريران يعرضان الاسم الجديد في كل المواضع بلا استثناء، والمراسي/الأنكورات سليمة.
- إعادة تشغيل المولّد مرتين لا تغيّر شيئًا (خمول idempotent).
- التوليد صار آليًّا في كل بناء محلي وفي CI.

### المخاطر
- كسر مناطق الحقن إن تغيّر ترتيب الخطوات → لذلك المرساة أولًا ثم المصدر ثم التوليد.
- `jobsCatalog.js` يغذّي شاشة التوظيف → فحصها ضمن القبول.

**التقدير:** جلسة واحدة قصيرة (½ يوم).

---

## المحور ٢ — مطابقة المرجع الرسمي مع البوابة + ترقية آلية الأدوار والصلاحيات

### ٢-أ: المطابقة وسدّ النواقص (طلب المالك: «التأكد أنه مطابق لنا ووضع خطة للنواقص»)

#### الوضع الحالي المكتشف (الفجوات بالدليل)
1. **المرجع يتكلم بلغة أودو فقط**: فصل الصلاحيات (#s-odoo-perms) يوثّق 16 دور أودو و9 جداول صلاحيات — **ولا يذكر أيًّا من أدوار البوابة الفعلية الاثني عشر** (`admin`, `warehouse_manager`, `storekeeper`, `qc_inspector`, `gate_officer`, `purchase_officer`, `finance_manager`, `return_manager`, `inventory_auditor`, `viewer`, `department_user`, `fleet` — المصدر `src/services/auth/roles.js:8-21`).
2. **أدوار بلا مقابل تنظيمي موثّق:** `department_user` · `fleet` · `viewer` · `storekeeper` — و**قسم الحوكمة المستندية نفسه بلا دور بوابة إطلاقًا**.
3. **فصل الوظائف #s-jobs قديم:** 12 بطاقة يدوية بينما المصدر فيه **36 وظيفة**.
4. **`workflows.astro:24` يدوي وقديم:** يقول «8 إدارات و18 وظيفة» والحقيقة 36.
5. **رابطان ميتان في المصدر:** `unit-storage → /dashboard/warehouse-maps` و`sub-cold → /dashboard/cold-storage-plan` (صفحات محذوفة) — و`pages[]` غير مشمولة بفحص `audit-portal.mjs`.
6. **معرّف مكرر** `id="s-raci"` في المرجع (سطرا 2000 و3556).
7. **47 صفحة بوابة** موجودة فعليًّا — ولا يوجد في المرجع فهرس يذكرها (طلب المالك: «لو يوجد شيء في البوابة غير مذكور نذكره»).

#### الخطوات
1. **توسيع `build-org.mjs` بثلاث مناطق توليد جديدة في المرجع:**
   - `ORG:JOBS` — توليد فصل الوظائف #s-jobs من المصدر (36 بطاقة بدل 12 اليدوية).
   - `ORG:PORTAL-ROLES` — **جدول أدوار البوابة الفعلية**: الأدوار الـ12 من `roles.js` × الصفحات المسموحة (تُحسَب فعليًّا من `pageAccess.allowedPathsFor`) × المقابل التنظيمي × مقابل أودو. هذا يجعل المرجع «مرجعًا رئيسيًّا للأدوار والصلاحيات» بحق — يوثّق ما يُنفَّذ لا ما يُتمنّى.
   - `ORG:PORTAL-PAGES` — **فهرس شاشات البوابة** (47 صفحة من `navCatalog.js`: الاسم، المسار، المجموعة، الأدوار) — فلا يبقى في البوابة شيء غير مذكور في المرجع، ويتجدد آليًّا مع كل صفحة جديدة.
2. **سدّ فجوات التعيين** (توصيات تُعرض على المالك ضمن الجدول المولّد):
   - استحداث دور `doc_controller` («موظف الحوكمة المستندية») مقابلًا للقسم الذي يحمل الآن اسمه الجديد — صلاحياته: `/dashboard/documents` + مجموعة `daily` + أدوار الاعتماد المستندية المناسبة. *(قرار مالك — التوصية: نعم)*.
   - تعيين `fleet` ↔ قسم النقل والحركة، `department_user` ↔ «مستخدم إدارة مساندة»، `viewer` ↔ «اطّلاع رقابي»، `storekeeper` ↔ «أمين مخزن / Barcode User».
3. إصلاح الرابطين الميتين في `org-structure.json` + **إضافة فحص `pages[]` إلى `audit-portal.mjs`** (كل صفحة يذكرها المصدر يجب أن توجد).
4. إصلاح تكرار `id="s-raci"`.
5. ربط `workflows.astro` بالمصدر (استيراد العدّ من `org-structure.json` بدل الأرقام اليدوية).
6. بوابة خضراء + فحص بصري للمرجع.

### ٢-ب: ترقية آلية الأدوار — «تحكّم بتفاصيل التفاصيل، قابل للتنفيذ»

#### الوضع الحالي المكتشف
- صفحة `access-control` الحالية (`AccessControlHub.jsx`) **عرضٌ توثيقي بحت**: تعرّف أدوارها ومناطقها محليًّا (8 أدوار منها اثنان **لا وجود لهما** في النظام: `warehouse_user`, `purchase_manager`)، لا تقرأ ولا تكتب Firestore، ولافتتها تقول «البوابة مفتوحة» وهو كلام صار قديمًا. **لا شيء مما تعرضه مُنفَّذ**.
- التنفيذ الحقيقي طبقات أربع من مصدر واحد: `navCatalog.js` → (`navAccess.js` + `pageAccess.js` + `AuthGate.jsx` + `audit-portal.mjs`). أي ترقية يجب أن تركب فوق هذا لا أن تبني موازيًا له.
- `MANAGER_ROLES` **مكرر يدويًّا 4 مرات** في مكوّنات متفرقة بدل تصديره من `roles.js`.
- علم `active:false` (إيقاف مستخدم) **يُكتب ويُعرض لكنه غير مُنفَّذ** — لا في `AuthGate` ولا في القواعد: الموقوف يدخل عاديًّا.
- شاشة المستخدمين لا تنشئ حساب Auth (تتطلب إنشاءه يدويًّا في كونسول Firebase ولصق UID).

#### التصميم المقترح (قابل للتنفيذ فعلًا، لا عرضًا)
**مصفوفة صلاحيات سحابية تركب فوق الكتالوج:**
- مجموعة جديدة `access_control/matrix`: `{ overrides: { [role]: { allow: [مسارات], deny: [مسارات] } }, version, updatedBy, updatedAt }` + سجل نسخ append-only (كنمط `org_structure/versions`).
- **الدلالة:** الافتراضي يبقى الكتالوج (fail-safe: تعذّر الجلب = الكتالوج وحده) · `allow` يضيف صفحة لدور · `deny` يحجبها · `admin` فوق المصفوفة دائمًا.
- **نقاط الدمج:** `pageAccess.js` (طبقة overrides في `canOpenPath`/`allowedPathsFor`) · `navAccess.js`/`RoleNav` (الإخفاء يتبع نفس المصدر) · `AuthGate.jsx` (اشتراك + كاش محلي).
- **حدود صادقة تُقال للمالك بوضوح:** هذه المصفوفة تتحكم في **المشاهدة والتنقل** (طبقة الشاشات). حماية **البيانات** تبقى بقواعد Firestore الخشنة بالأدوار — أي توسيع عبر المصفوفة لا يمنح كتابة بيانات لم تُجزها القواعد؛ الطبقتان متكاملتان («هذه تمنع الشاشة، وتلك تمنع البيانات»).
- **مستوى أدق لاحق (اختياري):** بعد استقرار مستوى الصفحات، مفتاح `canSee(sectionKey)` لأقسام داخل الصفحة الواحدة — يُجدول كمرحلة تالية لا هذا الأسبوع.

#### الخطوات
1. توحيد المصدر: تصدير `MANAGER_ROLES` من `roles.js` وإزالة النسخ الأربع (LogisticsDashboard · DocumentsInbox · OperationsMonitor · org-structure.astro).
2. نموذج خالص `accessMatrixModel.js` + اختبارات (دمج overrides، أسبقية deny، سقوط آمن).
3. خدمة `accessMatrixService.js` (قراءة حية + كتابة admin + نسخ إصدارات).
4. دمج `pageAccess.js` + `navAccess.js` + `AuthGate.jsx` (مع كاش يمنع وميض إعادة التوجيه).
5. **تفعيل `active:false`:** في `AuthGate` (خروج فوري برسالة «الحساب موقوف») وفي القواعد (`isActive()` ضمن شروط الكتابة الحساسة) — ضمن دفعة القواعد الموحدة.
6. **إعادة بناء `AccessControlHub` محررًا فعليًّا:** شبكة الأدوار الحقيقية الـ12 (من `roles.js`) × الصفحات (من `navCatalog`) بمفتاح ثلاثي (افتراضي/سماح/حجب) + معاينة حية «ماذا سيرى هذا الدور» عبر `allowedPathsFor` + حفظ بنسخة — وحذف الأدوار الوهمية واللافتة القديمة. النمط البصري: مكوّنات أودو (`ListView`/`Badge`) بلا إيموجي.
7. ترقية شاشة المستخدمين: إنشاء حساب Auth كامل من الشاشة عبر **تطبيق Firebase ثانوي** (`initializeApp` باسم ثانٍ + `createUserWithEmailAndPassword` عليه — لا يُسقط جلسة المدير، ويعمل على Spark بلا خوادم) + تحقق من صيغة UID/البريد. *(اختياري إن ضاق الوقت — الجوهر هو المصفوفة)*.
8. قاعدة `access_control` في دفعة القواعد الموحدة (قراءة لكل مصادَق، كتابة admin).
9. اختبارات + بوابة خضراء + تجربة حية بحساب مدير (على المالك).

### معايير القبول (للمحور كله)
- المرجع المنشور يحوي: أدوار البوابة الفعلية مولّدة + فهرس 47 شاشة مولّدًا + 36 وظيفة — صفر انجراف مستقبلي (كل شيء من المصدر آليًّا).
- تعديل خانة في محرر الصلاحيات يغيّر فعليًّا ما يراه الدور المستهدف (تجربة حية).
- مستخدم موقوف لا يستطيع الدخول ولا الكتابة.

**التقدير:** جلستان (يوم ونصف).

---

## المحور ٣ — خطة الربط مع أودو (Integration)

### الوضع الحالي المكتشف
- موديول `brandzo_warehouse` حقيقي مكتمل (12/12 مرحلة، 52 اختبارًا خضراء على أودو 19 في مختبر Docker المحلي `C:\brandzo-odoo`) — **لكنه بلا أي وحدة تحكم HTTP** (لا REST ولا مسارات مخصصة): سطحه الخارجي هو `execute_kw` القياسي عبر `/jsonrpc`.
- **طبقة عميل جاهزة وغير موصولة إطلاقًا:** `src/services/odoo/` (odooClient بـ`authenticate/searchRead/create/write/unlink` + mock للوضع التدريبي + `odooMapper`) — **لا يستوردها أي مكوّن**.
- **وسيط جاهز غير منشور:** `odoo-proxy/` (Cloudflare Worker + بديل Firebase Function) — `PUBLIC_ODOO_PROXY_URL` فارغ.
- `RealOdooCockpit.jsx` = روابط عميقة فقط، صفر استدعاءات.
- **لا يوجد أي جسر مزامنة Firestore↔أودو** — النظامان منفصلان تمامًا اليوم.
- قيد التشغيل: خطة Spark = لا Cloud Functions → الوسيط الوحيد المتاح مجانًا هو **Cloudflare Worker**.

### القرار المعماري الحاكم (مبنيّ على قرار §19-5 المحسوم)
**Firestore يبقى مصدر الحقيقة التشغيلي. أودو مرآة محاسبية/رقابية.** التكامل «Integration» لا استبدال — البوابة تعمل كاملة لو سقط أودو، وأودو يتغذى منها.

### ٣-ب: قناة الاتصال (الأسبوع الحالي — تأسيس)
1. نشر `odoo-proxy/cloudflare-worker` على حساب Cloudflare مجاني: يحمل أسرار أودو (`ODOO_URL/DB/USER/API_KEY`) في secrets الوسيط حصريًّا (المتصفح لا يرى سرًّا أبدًا — القاعدة موثقة أصلًا في `odooConfig.js`) + CORS مقيّد بأصل Pages + **قائمة سماح صريحة** (نموذج+دالة: `product.product:search_read` … إلخ) فلا يمرّر الوسيط أي استدعاء عشوائي.
2. إنشاء مستخدم تكامل في أودو `bz_integration` بمفتاح API ودور مقيّد (قراءة/كتابة النماذج المسموحة فقط).
3. ضبط `PUBLIC_ODOO_PROXY_URL` والتجربة على المختبر المحلي عبر `wrangler dev` (الوسيط السحابي لا يرى localhost — التطوير محلي، الإنتاج عند استضافة أودو).
4. أول اختبار قبول: `searchRead('product.product')` من صفحة القمرة يعرض أصناف المختبر حيًّا.

### ٣-ج: «جلب التحديث من أودو أول بأول» — المزامنة على ثلاث مراحل
- **مرحلة ١ — سحب تزايدي (polling):** خدمة `syncService` تسأل أودو كل فترة عن السجلات التي `write_date > lastSync` (المؤشر محفوظ في وثيقة `sync_state` بـFirestore) وتحدّث المرايا. بسيطة، تعمل على Spark، تكفي للبداية.
- **مرحلة ٢ — دفع من البوابة:** عند بلوغ مستند حاكم حالة «منجَز» يُقيَّد في طابور `sync_outbox` (Firestore) يدفعه `syncService` عبر الوسيط إلى أودو (`purchase.order`، `stock.picking`…) بمفتاح تكرار idempotency = معرّف المستند، فلا ازدواج عند إعادة المحاولة.
- **مرحلة ٣ — فوري حقيقي (عند استضافة أودو):** Webhooks من `base_automation` (مدعومة في أودو 19) → Worker يستقبل ويكتب في Firestore عبر REST بحساب خدمة → يلغي الـpolling نهائيًّا.
- **التعارضات:** «الأحدث يكسب + سجل تعارض» يُعرض في شاشة «مراقبة المزامنة» (تُبنى لاحقًا بنمط أودو ListView).

### ٣-د: «لو تغيّر الإصدار — حاليًّا 19»
سياسة مكتوبة تصبح فصلًا في وثيقة العقد:
1. **المعرفة الأودووية محصورة في طبقتين فقط:** `odooMapper.js` (أسماء الحقول) والموديول نفسه — لا يعرف أي مكوّن آخر شيئًا عن أودو. تغيّر الإصدار = تحديث الطبقتين فقط.
2. سطح الاتصال `execute_kw` **مستقر عبر الإصدارات** منذ سنوات — القناة لا تتأثر.
3. **مسار الترقية الإلزامي:** تثبيت الإصدار في صورة Docker → فرع ترقية → مختبر بالإصدار الجديد → `odoo -u brandzo_warehouse` → تشغيل الاختبارات الـ52 → جدول توافق الحقول (السابقة الموثقة: ترقية 19 كشفت `product_uom → product_uom_id` و`category_id` المحذوف) → لا إنتاج قبل خضرة المختبر.

### ٣-هـ: «المزامنة مع قاعدة البيانات — التغيير دون فقد، مثل PostgreSQL»
1. بيانات أودو تعيش في PostgreSQL (حاوية `pg15` في المختبر): **`pg_dump` يومي مجدول** + **لقطة إلزامية قبل أي ترقية أو تهجير** (سكربت جاهز في المختبر).
2. **التغيير بالهجرة لا بالمسح:** أي تعديل مخطط يمرّ عبر آليات هجرة أودو/OpenUpgrade التي تحوّل البيانات في مكانها — ممنوع إعادة إنشاء قاعدة من الصفر على بيانات حية.
3. **تمرين استعادة شهري** (restore drill): استعادة آخر نسخة على قاعدة جانبية والتحقق — النسخة التي لم تُجرَّب استعادتها ليست نسخة.
4. التحقق من فصل volume قاعدة البيانات عن حاوية أودو في `docker-compose` (بقاء البيانات عند إعادة إنشاء الحاوية).

### مخرجات هذا الأسبوع للمحور
وثيقة «عقد التكامل مع أودو» (`docs/`) بالتفاصيل أعلاه + نشر Worker تجريبي إن توفر حساب Cloudflare + هيكل `syncService` — **التنفيذ الكامل للمزامنة يُجدول أسبوعًا لاحقًا** لأنه مشروط باستضافة أودو حقيقية (قرار مالك).

**التقدير:** الوثيقة ساعة ضمن جلسة؛ القناة التجريبية ½ يوم عند توفر الحساب.

---

## المحور ٤ — الوضع الأمني وسدّ خوارق النظام

### الخوارق المكتشفة (بالدليل — هذه إجابة سؤال «ما هي خوارق النظام»)
| # | الخرق | الموضع | الخطورة |
|---|---|---|---|
| 1 | **حرق أرقام المستندات الرسمية**: أي مصادَق (حتى `viewer`) يستطيع ترقية العدّادات | `firestore.rules:249-254` (`signedIn` فقط) | عالية |
| 2 | إنشاء عمليات جرد وسجلات دخول/خروج لأي مصادَق | `rules:83, 354, 360` | عالية |
| 3 | **دليل المستخدمين كله مقروء لأي مصادَق** (أسماء/أدوار/بريد) | `rules:76` | متوسطة |
| 4 | **علم الإيقاف `active:false` غير مُنفَّذ** — الموقوف يدخل ويكتب | `AuthGate.jsx` + القواعد | عالية |
| 5 | **XSS عالي القيمة في مساعد الاجتماعات**: نص التفريغ + الملاحظات + **ردّ API ترجمة خارجي** يُحقن خامًا في `innerHTML` ثم `html2pdf` | `MeetingAssistant.jsx:701-716` (والجلب `:635`) | عالية |
| 6 | XSS بلا تهريب في جدول المركبات + self-XSS في retail-hub من localStorage | `VehiclesInventory.jsx:1619,1630` · `retail-hub.astro:1931-1943` | متوسطة |
| 7 | إنشاء `documents` و`Items_Pending` لأي مصادَق (مقيّد بحالة draft/pending لكنه يفتح بابا للإغراق) | `rules:188-191, 291` | متوسطة |
| 8 | مُهرِّب HTML مكرر **10 نسخ** في ملفات متفرقة — خطأ مستقبلي شبه مؤكد | متعددة | متوسطة |
| 9 | `xlsx@0.18.5` عليها تحذيرات معروفة (prototype pollution/ReDoS) بلا إصلاح رسمي | `package.json` | متوسطة |
| 10 | لا `npm audit` في CI ولا **اختبارات لقواعد Firestore** إطلاقًا | CI | متوسطة |
| 11 | بيانات أعمال في localStorage فقط (المهام · retail-hub · خطة التبريد · أرشيف المساعد · التقييم) — فقدٌ بجهاز واحد وخارج الحوكمة | `taskService.js` وأخوات | دين فني موثّق |

*(للعلم: الحماية الحالية سليمة في جوهرها — رفض افتراضي شامل، 34 مجموعة كلها مقنّنة، append-only للسجلات، صفر أسرار مسربة، التهريب مطبق في stock-operations الضخمة. الخوارق أعلاه هي الفجوات المتبقية.)*

### خطوات العلاج
1. **دفعة القواعد الموحدة** (مع بنود المحورين ٢ و٦): `counters` و`operations` و`Inbound_Log`/`Outbound_Log` و`documents create` و`Items_Pending create` → `isStockActor()` · قراءة `users` → الذات أو admin (بعد التحقق أثناء التنفيذ من كل مستهلكي `listUsers`) · إضافة `isActive()` لشروط الكتابة الحساسة · قاعدة `access_control` الجديدة. تُسلَّم للمالك نصًّا كاملًا جاهز اللصق **مرة واحدة**.
2. **توحيد التهريب:** ملف واحد `src/services/ui/escape.js` (يرث `esc` الحالي من `meetingsView.js`) + إحلال تدريجي محل النسخ العشر.
3. **إصلاح الثغرات الثلاث:** مساعد الاجتماعات (تهريب كل حقن الـPDF: التفريغ، الملاحظات، النقاط، **وردّ الترجمة الخارجي**) · `VehiclesInventory.jsx` · retail-hub.
4. **اختبارات قواعد Firestore** (سدّ النقص الأخطر منهجيًّا): تبعية dev‏ `@firebase/rules-unit-testing` + الإيميولاتور، وسيناريوهات: viewer لا يكتب · حرق عداد مرفوض · موقوف مرفوض · تصعيد ذاتي للدور مرفوض · append-only لا يُحدَّث — تعمل محليًّا وتُتخطى بأمان حيث لا إيميولاتور.
5. **CI:** خطوة `npm audit --omit=dev` استشارية (لا تُفشل البناء أولًا؛ نراقب ثم نشدّد).
6. **قرار xlsx:** هذا الأسبوع تخفيف (استيرادها من الحزمة لا CDN + تعقيم المدخلات)، والترحيل إلى `exceljs` يُجدول جلسة مستقلة لاحقة *(توصية: نعم لاحقًا)*.
7. تحديث لوحة الأمان في `users.astro` + فصل «نموذج التهديد» مختصر في المرجع الرسمي.

### معايير القبول
- اختبارات القواعد خضراء على السيناريوهات كلها، والقواعد منشورة (فعل المالك).
- صفر حقن غير مهرَّب في الملفات الثلاثة المعالجة (مراجعة عدائية).
- الموقوف يُطرد فعليًّا (تجربة حية).

**التقدير:** جلسة كاملة (يوم).

---

## المحور ٥ — العمل دون إنترنت: من «جيد جزئيًّا» إلى 100% حقيقي

### ✅ جولة القبول الأوفلاين (2026-08-02، مع المالك حيًّا)
اختُبرت الدورة كاملةً على الموقع المنشور: **قطع الشبكة → تحديث وتنقّل بين الشاشات (يعمل) → إنشاء عملية جرد وإدخال بيانات بلا نت (قَبِل وسجّل) → إعادة الشبكة (التزامن يتكفّل به `persistentLocalCache`)**. أكّد المالك: «يعمل بشكل سليم». الحدّ المعروف الوحيد: أول دخول على جهاز جديد يحتاج شبكة (قيد Firebase Auth).

### ✅ حالة التنفيذ (2026-08-02) — المحور ٥ شبه مكتمل
- ✅ **الخطوط**: بديل `@font-face` في `fonts.css` يجعل `Cairo`/`Tajawal` تشيران للخط الرسمي المستضاف ذاتيًّا → حُذفت **١٤ رابط Google Fonts** من كل صفحات اللوحة + `stock-operations` + `index` + نوافذ الطباعة (`TaskManager`, `erp-workflows`).
- ✅ **مكتبات CDN لصفحات اللوحة استُؤصلت كلها** (`e0cb429`): vendored في `public/lib` (نمط html5-qrcode، **لا تبعيات npm** — ملفات ثابتة مدمجة):
  - `xlsx` · `xlsx-js-style` · `jspdf` · `html2canvas` · `mermaid` · `leaflet/` (js+css+images) · `fontawesome/` (css/+webfonts/ بالبنية القياسية).
  - `assets-inventory` (xlsx) · `stock-operations` (FontAwesome) · `retail-hub` (leaflet+xlsx-js-style) · `erp-workflows` (mermaid بمسار مطلق للطباعة). **تحقّق حيّ:** كل الأصول تُخدَم من الموقع المنشور (200، أحجام صحيحة).
  - **اكتشاف مهم:** `AssetsInventory.jsx`/`VehiclesInventory.jsx` **نسخ ميتة** (غير مستوردة، ليست في البناء)، و`vehicles-inventory.astro` تستورد xlsx من الحزمة أصلًا — فالمواضع الحقيقية أربع صفحات فقط لا ثمانٍ. **وقد حُذفتا 2026-08-21** — ومعهما آخرُ مواضع cdnjs في `src/` (xlsx 0.18.5 · jspdf · html2canvas).
  - **بلاطات الخريطة (Leaflet tiles) أونلاين بطبيعتها** — موثّق كتدهور رشيق (الشاشة تعمل، البلاطات تحتاج شبكة).
- 🟡 **المتبقّي (أثر أقل):** ~٥٤ مرجع CDN/خطوط في **`public/*.html`** (النماذج والتقارير الثابتة المستقلّة) + `arch-wiki.html`/`warehouse-manager-eval.html` المولّدتان + بديل FontAwesome بـSVG في stock-operations (تحسين لا ضرورة). **أُنجز منها 2026-08-21:** `public/formUtils.js` كان يجلب SheetJS من cdnjs فصار يشتقّ `lib/` من موضع الوسم نفسه (تحقّق حيّ: 200 · 951928 بايتًا من `/lib/xlsx.full.min.js` من داخل نموذجٍ في `forms/`) — فتصدير الإكسل في النماذج الأربعة والعشرين صار يعمل بلا إنترنت. **يبقى** `html5-qrcode` من jsdelivr في ٢٤ نموذجًا، وFontAwesome من cdnjs في ٤ وثائق أرشيف، وTailwind من CDN في `تقرير-التوظيف-والجدول-الزمني.html`. **التالي:** تمريرة على `public/` + جولة قبول أوفلاين موثّقة (قطع الشبكة → دخول → جرد → مستند → تزامن).

### الوضع الحالي المكتشف
**الأساس ممتاز وموجود فعلًا:** عامل خدمة مخصص (`src/integrations/pwa-sw.mjs`) يسبق-يكاش كل HTML وحزم `_astro` مع صفحة أوفلاين عربية وتحديث بإذن المستخدم · `manifest.webmanifest` قائم · **كاش Firestore الدائم مفعّل** (`persistentLocalCache` متعدد التبويبات في `firebase.js:61-70`) فالقراءة والكتابة تعملان أوفلاين وتتزامنان عند العودة · الخط الرسمي مستضاف ذاتيًّا.

**ما يكسر الأوفلاين اليوم (بالحصر):**
1. **8 مكتبات CDN لا يكاشها العامل أبدًا** (عابرة النطاق تمرّ بلا كاش): FontAwesome في stock-operations (كل أيقونات الشاشة تختفي!) · `xlsx` في 4 مواضع · `jspdf`+`html2canvas` في المركبات · Leaflet في retail-hub · Mermaid في erp-workflows · Tailwind CDN في تقرير عام واحد.
2. **14 موضع Google Fonts في `src/`** (Cairo/Tajawal/JetBrains Mono) + ~54 في `public/`.
3. الصور/التواقيع خارج الـprecache (تعمل بعد أول زيارة فقط).
4. حدّان طبيعيان: أول تسجيل دخول يحتاج شبكة (قيد Firebase Auth) · ترجمة مساعد الاجتماعات وبلاطات الخرائط أونلاين بطبيعتهما.

### الخطوات
1. **استئصال الـCDN الثماني:**
   - `xlsx`: مثبتة أصلًا في التبعيات — استيرادها من الحزمة في `assets-inventory.astro` و`AssetsInventory.jsx` و`VehiclesInventory.jsx` و`retail-hub.astro` (و`xlsx-js-style` يُقيَّم: استبدال أو تضمين محلي).
   - `jspdf`+`html2canvas`: الانتقال إلى `html2pdf.js` المثبتة (تجمعهما) أو إضافتهما تبعيتين مجمّعتين.
   - `mermaid`: مثبتة أصلًا — استيراد الحزمة بدل jsdelivr في `erp-workflows.astro`.
   - FontAwesome في stock-operations: استبدال بأيقونات `Icon` SVG (الدرس المعتمد في المشروع) — تمريرة يدوية حذرة لأن الصفحة 3653 سطرًا.
   - Leaflet: تبعية npm بأصول محلية، مع توثيق أن **بلاطات الخريطة أونلاين بطبيعتها** (تدهور رشيق: الشاشة تعمل والخريطة تعرض رسالة).
   - Tailwind CDN في `تقرير-التوظيف-والجدول-الزمني.html`: تضمين CSS مصرّف ثابت.
2. **تمريرة الخطوط:** إزالة المواضع الـ14 في `src/` (توحيد على IBM Plex Sans Arabic الذاتية — وهي القاعدة الرسمية أصلًا) + تمريرة نصف آلية على `public/` (54 موضعًا) مع مراجعة بصرية للتقارير الرسمية الموقعة.
3. **توسيع الـprecache بحكمة:** التواقيع PNG (تدخل في كل تقرير رسمي) + الصور الحرجة + `public/lib` — مع إبقاء استثناء الفيديوهات (ميزانية الحجم).
4. **تدهور رشيق للخدمات الخارجية:** الترجمة في مساعد الاجتماعات تتخطى بهدوء برسالة عند الانقطاع.
5. **جولة قبول أوفلاين رسمية وموثقة:** قطع الشبكة من DevTools ثم: دخول بجلسة محفوظة → تنقل عبر الشاشات → عملية جرد كاملة بالماسح → إنشاء مستند (يُقيَّد في طابور الكاش) → عودة الشبكة → التحقق من التزامن. النتيجة تُوثَّق في المرجع كفصل «العمل دون اتصال: المضمون والحدود».

### معايير القبول
- صفر طلبات عابرة النطاق في الشاشات التشغيلية كلها (عدا الخرائط والترجمة الموسومتين «أونلاين بطبيعتهما»).
- الجولة الاختبارية تمر كاملة بلا كسر وظيفي ولا بصري.
- **صياغة صادقة للمالك:** «100%» تعني: كل شيء يعمل دون اتصال **بعد أول زيارة وأول دخول** — أول تسجيل دخول على جهاز جديد يحتاج شبكة (قيد منصة Firebase نفسها، لا حيلة له على Spark، ويوثَّق).

**التقدير:** جلسة كاملة (يوم).

---

## المحور ٦ — الربط التسلسلي للمستندات (Document Chaining) + الإنشاء المباشر

### تصحيح فرضية أولًا
المنظومة أكبر مما في الطلب: **19 نوعًا محكومًا في 6 سلاسل** (وارد PR→PO→GRN→QC→PUTAWAY · صادر SO→PICK→PACK→DN→GP · مرتجعات RET→CN · جرد CC→ADJ · فوترة DN→INV · تحويلات TR→TRN→TRC) — كلها في مجموعة `documents` واحدة بمحرك واحد.

### الوضع الحالي المكتشف
- **الربط البنيوي موجود ومتين — لكن عبر «الاشتقاق» فقط:** خريطة `links` تراكمية (`chain.js:197`) تجعل QC يحمل روابط GRN وPO وPR معًا، والمطابقة الثلاثية (`threeWayMatch`) والحرّاس الخمسة يقرأون منها.
- **الفجوة بالضبط كما شخّصها المالك:** حقول المرجع (`poRef`، `grnRef`…) **نص حر بلا أي تحقق أو بحث أو جلب** — كتابة `PO-2026-0007` يدويًّا تنتج نصًّا ميتًا: `links` تبقى فارغة، وشريط السلسلة لا يظهر، والمطابقة لا تعمل. **لا يوجد استعلام واحد بالرقم في المستودع كله**، ولا أي «إنشاء أصل مفقود».
- الترقيم: `reserveNumber` معاملة ذرّية على `counters` تمنح الرقم **عند التقديم حصريًّا** (`GRN-2026-0001`) — أي حل يجب ألا يلفّق أرقامًا.

### التصميم
**١. التعرف التلقائي والسحب (Auto-Recognition):**
- ترقية تعريف حقول المرجع في المخططات من نص حر إلى نوع `docref` يعلن نوع الأب: `{ kind: 'docref', docType: 'PO' }`.
- خدمة `lookupByNumber(type, number)` في `documentsService` (استعلام `type`+`number` — يتطلب **فهرسًا مركبًا** يُجهَّز في `firestore.indexes.json` ويُسلَّم للمالك رابط إنشائه بنقرة).
- فرع جديد في `FieldInput.jsx`: تحقق صيغة (`parseNumber`) ثم بحث عند الإكمال → **وُجد**: شريحة تأكيد (الرقم · النوع · الحالة · زر فتح) + تثبيت الربط `links` **تراكميًّا** (روابط الأب + الأب نفسه، بنفس منطق الاشتقاق الحرفي) + ختم `audit` بفعل `link` على الطرفين → شريط السلسلة والمطابقة الثلاثية يعملان فورًا كأنه اشتقاق.
- حراسة السلامة: الربط بأب غير معتمد بعدُ **مسموح مع تحذير أصفر** («سلسلة غير معتمدة»)، ويُضاف حارس يمنع «منجَز» للابن قبل اعتماد الأب — انسجامًا مع فلسفة حارس الاشتقاق القائم (`chain.js:160`).

**٢. الإنشاء المباشر عند عدم التوفر (Inline Creation):**
- **لم يوجد** → نافذة `Modal` دون مغادرة الصفحة: «المستند PO-2026-0007 غير موجود».
- **الحقيقة الحاكمة التي يُبنى عليها الحل:** الأرقام الرسمية تُحجز حصريًّا عبر عدّاد المعاملة عند التقديم — **لا نلفّق رقمًا كتبه المستخدم**. لذلك الخياران داخل النافذة:
  - **«إنشاء الأصل الآن»** — معالج مصغّر بالحقول الإلزامية للنوع الأب (مع نسخ معكوس لما يصح نسخه من بيانات الابن الحاضرة)، يُنشئ المسودة ثم — لمن يملك صلاحية التقديم — يقدّمها فتأخذ **رقمها الحقيقي من العدّاد**، ثم يُثبَّت الربط آليًّا ويعود المستخدم لنموذجه المفتوح والربط حاضر. سلسلة غير منقطعة، بلا خروج من الصفحة — حرفيًّا ما طلبه المالك.
  - **«رقم ورقي/تاريخي»** — إن كان الرقم المكتوب يعود لمستند ورقي قديم خارج النظام: يُحفظ في حقل جديد `header.externalRef` ويُنشأ الأصل بترقيم النظام الطبيعي، فيُحفظ الأثران معًا (الرقم الرسمي الجديد + المرجع الورقي).
- كل إنشاء مباشر يُختم في `audit` بفعل `create-inline` مع من ومن أين.

**٣. النشر التدريجي:** الجولة الأولى على الأزواج الحرجة الستة: GRN→PO · QC→GRN · PUTAWAY→GRN · GP→DN · CN→RET · ADJ→CC، ثم البقية (INV→DN، TRN/TRC، PO→PR…).

### الخطوات
1. نموذج: توسيع `schemaUtils` + المخططات الستة الأولى بنوع `docref` (+ اختبارات).
2. `lookupByNumber` + فهرس مركب + اختبارات.
3. فرع `docref` في `FieldInput.jsx` + شريحة التأكيد + منطق الربط التراكمي (+ ختما audit).
4. حارس «لا منجَز قبل اعتماد الأب» في `chain.js` (+ اختبارات).
5. نافذة الإنشاء المباشر (معالج مصغّر + مساري الرقم) في `DocumentEngine.jsx`.
6. مراجعة قواعد `documents` (السماح بتحديث `links` في الحالات المحررة + ختم `link`) — ضمن دفعة القواعد الموحدة.
7. `ChainBar`: تأكد أن الآباء المربوطين يدويًّا يظهرون ويُطابقون (المتوقع: تلقائيًّا لأن البنية نفسها).
8. مراجعة عدائية مقابل `git HEAD` (قاعدة «المنطق مقدّس») + بوابة خضراء.
9. تعميم بقية الأزواج + فصل توثيقي في المرجع.

### معايير القبول
- كتابة رقم PO موجود في مذكرة استلام → ربط فوري + شريط سلسلة + مطابقة ثلاثية تعمل.
- رقم غير موجود → إنشاء مباشر يمشي حتى النهاية دون مغادرة الصفحة، وبرقم شرعي من العدّاد.
- كل اختبارات السلاسل القديمة خضراء (صفر انحدار في الاشتقاق).

**التقدير:** جلسة كاملة إلى جلسة ونصف (الأثقل في الأسبوع).

---

## المحور ٧ — الترقيم Pagination

### ✅ حالة التنفيذ (2026-08-02)
- ✅ **الطبقة النقيّة**: `src/services/ui/pagination.js` (`pageSlice`/`pageInfo`/`clampPage`/`pageCount`) + **٥ اختبارات** — تسمية «١–٥٠ / ٢٤٣» بأرقام لاتينية.
- ✅ **مكوّن `Pager`** بنمط أودو (`src/components/odoo/Pager.jsx`) — مكتفٍ ذاتيًّا بأنماط سطريّة (لا يعتمد `.o_theme`) فيعمل في أيّ شاشة؛ يختفي تلقائيًّا لو صفحة واحدة.
- ✅ **طُبِّق على ٣ شاشات** (ترقيم من طرف العميل فوق المصفوفة المحمّلة — **يكشف الذيل بدل قصّه صامتًا، بلا لمس تدفّق البيانات**): `PartnerMaster` · `ItemMaster` (كان يعرض الكلّ) · `StockLedger` (كان يقصّ عند 60 صامتًا). حجم الصفحة 50 (60 لدفتر الحركات).
- 🟡 **المتبقّي (يحتاج فهرسًا/تجربة حيّة):** ترقيم **خادميّ بمؤشّرات** (`startAfter` + `getCountFromServer`) لصندوق المستندات (سقف 100 خادميّ) والاشتراكات الكاملة (Items_Master/balances) — يُجدول عند الحاجة (البيانات ليست ضخمة بعد). قاعدة DEVELOPER_GUIDE: «لا اشتراك غير محدود جديد في شاشة قائمة؛ رقّمها أو افصل مسار التصدير».

### التصميم الأصليّ (للمرجع)

### الوضع الحالي المكتشف
- **صفر ترقيم خادمي في المستودع كله:** لا يوجد `startAfter`/مؤشر واحد. كل `limit()` القائمة (14 موضعًا) سقوف ثابتة تقصّ الذيل بصمت.
- **اشتراكات غير محدودة بالكامل:** `Items_Master` (تشترك فيه 4 شاشات كاملًا!) · `balances` (4 شاشات) · الشركاء · سجلات المسح (`scans`) · سجل التدقيق · المستخدمون — وشاشة الأصناف تفتح الاشتراكين الكاملين معًا.
- `ListView` الموحد يرسم **كل** الصفوف بلا صفحات، والترقيم الوحيد الموجود (stock-operations) ترقيم عرضٍ فوق مصفوفة محمّلة كاملة أصلًا.
- لا توجد أي مكتبة windowing ولا زر «عرض المزيد» في المشروع.

### التصميم
- **مكوّن `Pager` بأسلوب أودو** (‏«1-50 / 243» بسهمين — النمط الذي اعتمدناه تصميميًّا) يُضاف لـ`ListView` كخاصية اختيارية `pager` فلا تتأثر الشاشات غير المرقّمة.
- **خطاف `usePagedCollection(queryDef)`**: مؤشرات `startAfter` بمكدس (السابق/التالي) · **الصفحة الأولى حيّة** (`onSnapshot` بحدّ) والصفحات الأعمق لقطات بزر تحديث — توازن الحيوية مع فاتورة القراءات · العدّ الكلي عبر `getCountFromServer` (تجميع مدعوم على Spark، يكلف قراءة واحدة).
- **البحث خادميًّا** حيث يلزم: بادئة `orderBy(sku) + startAt(term) + endAt(term + '\uf8ff')`.
- **قاعدة معمارية جديدة تُدوَّن في DEVELOPER_GUIDE:** «لا اشتراك غير محدود جديد في شاشة قائمة» + مسار `fetchAllForExport` صريح ومقصود للتصدير والحسابات فقط.

### الخطوات (بالأثقل أولًا)
1. بناء `Pager` + `usePagedCollection` + اختبارات نموذج الترقيم الخالص.
2. **صندوق المستندات** `DocumentsInbox` (سقف 100 صامت اليوم) → صفحات 50 بعدّ حقيقي.
3. **ماستر الأصناف** `ItemMaster` (اشتراكان كاملان اليوم) → صفحات 50 + بحث بادئي خادمي بالـSKU/الباركود، مع إبقاء مسار كامل للتصدير والأرصدة الحيّة (حساباتها تحتاج الكل — يُفصل مسار العرض عن مسار الحساب).
4. **الشركاء** `PartnerMaster` (موردون/عملاء).
5. **دفتر الحركات** `StockLedger`: قصّ الـ60 الصامت → صفحات؛ وحركات الصنف بزر «المزيد» بمؤشر.
6. **سجلات المسح** في مراقبة العمليات (غير محدودة اليوم) + **سجل التدقيق** في محرك المستندات → آخر 50 + «المزيد».
7. **الشاشات التحليلية** (القمرة 800 · KPIs 800 · التحليلات 2500): تبقى بسقوفها لأنها تجميع لا قوائم، لكن مؤشر «النتائج مقتطعة» (المبني أصلًا في `truncated`) يُعرض بوضوح ويُوثَّق السقف.
8. فهارس مركبة مطلوبة تُجمع في `firestore.indexes.json` مع روابط إنشاء بنقرة للمالك.
9. بوابة خضراء + قياس قبل/بعد (عدد القراءات عند فتح شاشة الأصناف).

### معايير القبول
- شاشة الأصناف تفتح بقراءة ≤ صفحة واحدة بدل المجموعة كاملة، والتنقل والبحث يعملان.
- صفر انحدار في الحسابات المعتمدة على البيانات الكاملة (FEFO، الأرصدة، التحليلات).

**التقدير:** جلسة كاملة (يوم) للشاشات الثلاث الأولى، والبقية تعميم لاحق.

---

## ٨) الجدول الأسبوعي المقترح (سبت → جمعة)

| اليوم | الجلسة | المخرج |
|---|---|---|
| **السبت 02** | المحور ١ كاملًا + بدء ٢-أ | الاسم الجديد في كل مكان + أتمتة التوليد |
| **الأحد 03** | إتمام ٢-أ + وثيقة عقد أودو (٣) | مرجع مولّد مطابق للبوابة + عقد التكامل |
| **الاثنين 04** | ٢-ب: المصفوفة والمحرر وactive | صلاحيات دقيقة قابلة للتنفيذ |
| **الثلاثاء 05** | المحور ٤: الأمن كاملًا | دفعة القواعد الموحدة تُسلَّم للمالك + XSS مغلقة + اختبارات قواعد |
| **الأربعاء 06** | المحور ٥: الأوفلاين كاملًا | صفر CDN + جولة قبول أوفلاين موثقة |
| **الخميس 07** | المحور ٦: الربط التسلسلي + الإنشاء المباشر | الأزواج الستة تعمل |
| **الجمعة 08** | المحور ٧: الترقيم (الشاشات الثلاث الأولى) + إتمامات + «ارفع» ختامية | بوابة أخف وأسرع + تقرير الأسبوع |

نقطتا رفع: بعد الاثنين (١+٢) وبعد الجمعة (الكل) — وتقديم دفعة القواعد للمالك مساء الثلاثاء.

---

## ٩) ما يلزم من المالك (مجمّع — لا يعطل بدء التنفيذ)

1. **نشر دفعة `firestore.rules` الموحدة** عند تسليمها (لصق واحد في الكونسول) — الثلاثاء تقريبًا.
2. **إنشاء الفهارس المركبة** بروابط بنقرة واحدة نجهّزها (محورا ٦ و٧).
3. قرار: استحداث دور «موظف الحوكمة المستندية» `doc_controller` *(التوصية: نعم)*.
4. قرار: توحيد «قسم» في كل المواضع اليدوية *(التوصية: نعم — وفق نص طلبك)*.
5. **حساب Cloudflare مجاني** للوسيط (محور ٣) + قرار توقيت استضافة أودو الحقيقية.
6. **تجربة حية بحساب مدير** بعد كل دفعة (CRUD الشاشات المرقّاة لم يُجرَّب حيًّا منذ 13 شاشة).
7. لاحقًا وليس الآن: تفعيل Blaze يفتح Cloud Functions (مزامنة أقوى + نسخ مجدول) — معلّق على تجديد البطاقة كما هو معلوم.

---

## ١٠) سجلّ القرارات المفتوحة (تُحسم أثناء الأسبوع ولا توقف البدء)

| القرار | التوصية | المحور |
|---|---|---|
| دور `doc_controller` جديد | نعم | ٢ |
| «قسم» أم «إدارة» للحوكمة المستندية | قسم (نص المالك) | ١ |
| إنشاء حساب Auth من شاشة المستخدمين (تطبيق ثانوي) | نعم إن اتسع الوقت | ٢ |
| ترحيل `xlsx` إلى `exceljs` | نعم — جلسة مستقلة لاحقة | ٤ |
| ترحيل بيانات localStorage (المهام وأخواتها) إلى Firestore | يُدرج في خطة الشهر لا الأسبوع | ٤ |
| مستوى الصلاحيات داخل الصفحة الواحدة (أقسام) | مرحلة تالية بعد استقرار مستوى الصفحات | ٢ |
